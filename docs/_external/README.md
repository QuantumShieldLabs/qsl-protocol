# External bundles

Place large archival bundles here (e.g., Phase2/Phase3 zip packs) for local reference.
This directory is ignored by default to avoid bloating the git repo.
Publish archival bundles via release packets / project files instead.
