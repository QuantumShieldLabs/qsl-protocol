# QSL PREDICTION LEDGER (SR-16) — seeded 2026-08-05
**Rule:** the Director appends predicted-vs-measured rows at every lane close. Predictions
are quoted from the directive/block as written BEFORE execution; measurements from the
stop-file of record. Purpose: after enough rows, ceremony tiering (SR-17) gets decided
with data instead of feel. Scoring: HIT (as written), MISS (direction wrong), PARTIAL.

| lane | prediction (as written) | measured | score | lesson row |
|---|---|---|---|---|
| NA-0695 | "expected calm" (D-shape, one stop at PR) | six stops, one gate surprise disposed pre-push | PARTIAL | calm ≠ stop-count; the surprise was the directive's own contradiction |
| NA-0695 | CodeQL WF-0047 class "EXPECT IT TO FIRE" on new salt consumers | ZERO alerts fired; dismissals carried | MISS (safe direction) | taint model narrower than assumed; salt→account-name not a crypto sink |
| NA-0695 | suite 596/0/2 across 129, by-name +1 binary | exact | HIT | — |
| NA-0695 | goal-lint green (§7.5) with TRACEABILITY absent from §6 | RED first run; directive internally inconsistent | MISS | became SR-07 |
| NA-0695 promo | fmt/clippy/structural base rows | all exact | HIT | — |
| NA-0696 probe | banked D1 mechanism assumptions (drop-order, panic-safety, path-key) | all held; zero contradictions | HIT | probe-before-draft pays |
| NA-0696 probe | "7 call sites" (banked) | 8 textual = 7 calls + 1 fn-value | PARTIAL | became SR-09/SR-10 |
| D630 draft | preflight GREEN at promotion (count 0 at base) | count 1 at base (:71 prose); promotion → 2 → RED mask | MISS | inference from exit code, not from the count itself; A1.1 corrected |
| D630 draft | "NO test asserts on stderr" | vault.rs:165 not-contains predicate exists (harmless to the emit) | MISS | census grep shape too narrow; A1.2 corrected |
| D630 draft | banked-block reconstruction faithful | 3 substantive divergences (D5a shape, D4 boundary, D4 comment) | MISS | became SR-14 (R-BANK) |
| NA-0696 promo | preflight RED (mask, unanchored 2 / anchored 1) | exact | HIT | corrected prediction held |
| NA-0696 promo | §7.1 base rows incl. two-needle split | all exact (12 + 1) | HIT | SR-10 applied |

<!-- Append below at each lane close. NA-0696 execution rows DISCHARGED — entered
directly below at that lane's close. NA-0697 + NA-0698 close rows appended at NA-0699
(D-1339), byte-verbatim from the Director's ruling banked as RBANK_NA0699_003. -->
| NA-0696 exec | §2d.5: "NO test pins the old collapse" | FALSIFIED — na0694:155 pinned it; 1-red suite | MISS | became SR-18; third census-class instance |
| NA-0696 exec | "expects its R16 stops" (the lane framing) | exactly one R16 (STOP 006), fully diagnosed | HIT | the framing prediction held |
| NA-0696 exec | suite 606/0/2 across 130, by name | exact, on the amended commit (mandatory re-run) | HIT | identity-carry correctly refused across a test edit |
| NA-0696 exec | six E-C control red sets, committed exact | all EXACT incl. two pre-named sketch deviations | HIT | E-C discipline at full scale |
| NA-0696 exec | sweep: zero unclassified; 7 retired sites in held locks | exact, bidirectional | HIT | — |
| NA-0696 exec | CodeQL "may re-anchor" (hedged) | 0 open alerts on the ref | HIT (hedged) | second consecutive non-fire; WF-0047 set still 4 |
| NA-0696 exec | preflight A1.1 corrected trajectory (RED window → green post-flip) | exact | HIT | the corrected prediction, not the drafted one |
| NA-0696 exec | ProVerif pass (no formal/ touch) | pass | HIT | — |
<!-- NA-0697 close rows entered 2026-08-06 by the incoming Director (SR-16; STOP 006
ruled ACCEPTED, RBANK 006 sha 611a6085…0a36; desktop #24 merged bcb1dc1a, spine #1706
merged 5fafce42 — merge-commit shape verified, second parents = the ruled heads). -->
| NA-0697 draft | Control 1 as ruled: assert-red at base, inline edit shape (D631 §7/§8) | FALSIFIED pre-edit — Probe F: E0425 compile error at base, not assert-red; the §7/§8 triangle proven JOINTLY unsatisfiable (static lemma + Probes L2/F) | MISS | became SR-19 (delta-symbol reachability, verified at drafting); A2 replaced the pair with Controls 1′/2′ |
| NA-0697 draft | base residue listing ["qsc","settings.json"] | exact — Probe L2 and both controls each measured the predicted listing verbatim | HIT | the prediction survived the instrument redesign intact |
| NA-0697 exec | suite 104+N by name (N=1 → the 105 pinned names) | exact — 104/0/1 across 12, exit 0; by-name delta exactly destroy_residue_set_enumerated_by_name | HIT | figures re-attached to the amended commit per the NA-0695 identity rule |
| NA-0697 exec | Controls 1′/2′: EXACTLY the one-test red, same listing, all others green | exact both — 103/1/1, same test, same listing; restores cmp-identical (2′ also byte-identical to committed HEAD) | HIT | mechanism-identical replacement pair held at full suite scale |
| NA-0697 promo | SR-11 mask: unanchored 2 / anchored 1; a count of 3 = STOP; post-flip green | exact at promotion AND re-observed at the executor seat; post-flip exit 0, clean_tree=yes | HIT | pre-named, never chased — third consecutive validation of the mask model |
| NA-0697 | operator-paste fidelity (transport) | RBANK 002 arrived TRUNCATED; caught by the block's one-anchored-READY invariant BEFORE consumption; RBANK 003 superseded it. At STOP 006 the chat paste was mechanically diffed against the sealed artifact: content-identical | MISS (safe direction) | invariants as transport tripwires validated; Director now diffs paste-vs-artifact as routine |
<!-- NA-0698 close rows entered 2026-08-07 by the Director (SR-16; lane COMPLETE per
STOP 008 / R59; class QSL_CI_PHASE1_HERMETIC_SHARD_PASS; impl PR #1708 merged 3fcda47d). -->
| NA-0698 draft | Control-1-analog: shard args as drafted would run (I3/E8: emit non-empty) | FALSIFIED — cargo REJECTS shard 6's args outright (`can't mix --doc`, exit 101, pre-build); found by SR-15, corroborated upstream | MISS | became SR-20 (consumer-validated emission); the F1 blocker; 11/12 shards WERE accepted first attempt |
| NA-0698 draft | `--no-run` = universal acceptance probe (the first SR-20 control's own design) | FALSIFIED — `can't skip running doc tests with --no-run`, exit 101 on the doc shard | MISS | SR-20 caught its own control; probe now matches the workflow's real invocation per shard |
| NA-0698 draft | the `--assert-workflow` gate reads the matrix it claims to read | FALSIFIED run 1 — bare `shard:` needle matched the JOB named shard (:96); 6 controls died; fixed to inside-`matrix:` scope; full series re-run | MISS (caught by its own controls) | needle-wider-than-claim INSIDE the gate built to catch that class; red run preserved |
| NA-0698 exec | drafting fixture represents the production log (E24 vs the real workflow) | FALSIFIED — Control G attempt 1 red 12/12: `CARGO_TERM_COLOR=always` from dtolnay; fixture came from the ONE uncoloured ci.yml job | MISS | fixture-from-the-wrong-producer → SR-20 extension (environment is part of artifact identity); FIRST defect this lane's own CI caught that drafting controls could not (R52) |
| NA-0698 exec | re-running the sealed series is side-effect-free | FALSIFIED — output root not re-pointed; run 2 `.out` files overwritten (bounded ×7 arithmetic; red run + verdict log intact) | MISS | a re-run narrower than its side effects → output-root-first practice (R43c), applied to the very next run |
| NA-0698 close | repo-wide cache total = evidence of our workflow's write behaviour (R27-specified, R53-elevated) | FALSIFIED — total moved on an UNRELATED workflow's write; valid instrument (keyed entries) shows 0 across 4 PR runs, then exactly 1 from main | MISS — caught by LUCK, not an honest instrument; DUAL AUTHORSHIP, Director first (R27 spec → seat offered → R53 elevated) | → SR-21 (instrument scope = claim scope) + the pre-offer check; property itself STRONGER on the valid instrument |
| NA-0698 drafting-evidence | expectations-before-checks kept for all controls | E9/E10 expectations existed only inside the run log carrying their results; SR-15 did not catch it | MISS ×2 (half-kept rule + adversarial-read coverage gap) | cured in v2 forward; SR-15 coverage data recorded per R28 |
| NA-0698 exec | worst shard ~36.5 CI-min (derived 1.373× ratio + build); 60-min STOP threshold | measured 30.9 / 31.1 / 30.3 / 30.4 min across four runs — conservative ~15%, threshold never approached; shard-level variance real (shard 4: 14.7↔30.4 for identical targets) | HIT | derivation method TRANSFERRED from the monolith; tune against measured worst case, never the threshold (R54) |
| NA-0698 exec | Control P red set EXACTLY {qsc-shard-4, qsc-sharded-suite}; 13 shard-mates reconcile clean under --no-fail-fast | EXACT — 2 contexts red, nothing else; `expected 14 / observed 14 / missing 0 / extra 0` WITH the mid-shard failure | HIT | first gate in this program proven red-capable end-to-end on real infrastructure before being trusted (R48) |
| NA-0698 close | cache: PRs restore, never write; one writer, main-only | PROVEN both sides on the valid instrument: 0 keyed entries across 4 PR runs (48 shard-jobs); exactly 1 from refs/heads/main (shard 0 post-step, 301 MB) | HIT | ~3.3 GB saved vs per-shard keys; writer-move pre-authorization correctly NOT exercised |
| NA-0698 promo | SR-11 mask: unanchored 2 / anchored 1; 3 = STOP; post-flip 1 | EXACT at promotion AND at the flip | HIT | fourth consecutive validation |
| NA-0698 outage | (operational) `rerun` throttled ⇒ Actions down | FALSE — `workflow_dispatch` unthrottled the whole time; recovered on the OPERATOR's try-anyway; status page lagged capacity in both directions; 4 runs wedged self-contradictory | MISS (Director/seat shared) | measure more than one door before concluding the building is locked (→ recorded practice, SR-23 candidate) |
| NA-0698 Director | D1 finding-of-fact from a chat rendering (R15); "three names" adopted without remeasurement (R16); R27 instrument spec (R56b) | three Director misses, owned in the Director's name | MISS ×3 | → SR-01 extension (findings require the sealed file); census-as-wide-as-claim; SR-21 |
| NA-0698 ceremony-payoff (SR-17 substrate) | Director's successive sums: "7 pre-commit / execution caught 1" (R61e), then "7 honest / 1 fortune; 5 pre / 3 after" (first packet correction) | BOTH SUPERSEDED by the SR-15 D633 read (findings sha 570e546e…d3d3, B2 — canonical): "the five instrument defects" names TWO unreconciled sets sharing 2 members; on Set A at most 2 pre-commit defects in the deliverable; EXECUTION caught ≥5 (X1 ANSI via the lane's own CI, X2–X4 the watcher trio, X5 the cache confound by fortune) plus 2 more via execution-side pre-write instruments; "one required-check hazard" unnamed anywhere and NO required check was ever degraded; the catching instrument was the 37-control series (E32 postdates the commits) | MISS ×2, DIRECTOR'S, and the direction matters: both wrong sums FAVORED the Director's own tier-down preference — the exact conflict the operator's mandatory red-team existed to test, and it fired | ratio ≈ parity, not 7:1; the per-stop ritual produced the post-commit catches; the "keep drafting rigor, tier the ritual" inference RAN BACKWARDS from the evidence (B2's words) — SR-17-as-drafted falsified by its own mandated read |
| NA-0717 draft | A7: each failing assert prints (-1,35) vs (-1,11); :393 shows probe_rc=-1 probe_errno=35 | byte-exact in the fresh pull AND identical in all four historical failure logs | HIT | seat-blind seal (68 s mtime margin); predicted bytes pre-observed in-program via NA-0715's banked copy of the same job log; corroborative, not discovery-grade. C-2 rests on the five-run logs + source + one-sha two-platform pair |
| NA-0717 draft | id sweep: max NA anywhere across main+3 PRs = 0716 (pr1727); nothing ≥ 0717 | exact; only NA-9999 fixture placeholders above, classified | HIT | WF-0068 discipline, 4 refs × 6 spaces |
| NA-0717 draft | pr1725 max NA heading = 0714 | measured 0712 — NA-0714 closed with no block ever born | MISS (benign) | blocks are born at promotion; prose/STATE carry the max |
| NA-0717 draft | Linux lib baseline at 5b43eefe: 122 passed / 0 failed, four tests ok by name | exact | HIT | both platforms measured at one sha |
| NA-0717 draft | 653 Phase 3 sealed "goal-lint PASS" for the single-PR shape; the R269/R270 packet sealed it again for PR-B | goal_lint.py:90-93 fails BOTH shapes — core path, no tests/-dir path; unsatisfiable as sealed | MISS (three chairs: 653's author, SR-15 T4, the Director's ruling packet) | caught by the amending seat re-verifying every consumer against the new shape; a sealed expectation is not sealed until its consumer has been executed or read against the exact shape it seals |
| NA-0717 draft | 653 Phase 1 sealed "cargo fmt --check clean on the file" jointly with §4(b)'s exact one-line site forms | rustfmt rewraps 4 of 6 sites (symbol +13 chars; only one rewrapped site crosses max_width=100 — the operative trigger is an inner call-width heuristic, unmeasured; governing evidence = measured rc chain + the four-hunk diff); the seal pair is unsatisfiable as written — base file clean, instrument valid | MISS (653's author; SR-15 and the ruling packet did not execute the post-edit fmt consumer either) | row 5's lesson, second instance in one lane: a sealed expectation is not sealed until its consumer has been executed against the exact shape it seals |
<!-- LEDGER DEBT AUDIT 2026-08-13 (NA-0718, D-1354): close rows exist through NA-0698 and
drafting rows for NA-0717 ONLY. For NA-0699..NA-0716 (18 lanes) NO close rows exist here
AND no on-box source contains them (instrument and per-file classification in
docs/ops/DIRECTOR_OWED_REAUDIT_2026-08-13.md): they are STILL-OWED at each lane's
retrospective close — recorded as debt, deliberately NOT reconstructed from memory.
NA-0717's execution rows enter at its close per the standing deferral. -->
| NA-0699 draft | SR-17 tiering rule as drafted (§6.3) is adoptable | FALSIFIED by the operator-mandated SR-15 read (findings 570e546e…d3d3): 2 BLOCKER / 11 MAJOR — three tiers with the content of none defined; "blast radius" announced, domain-keyed enumeration delivered; all six hard cases ambiguous or unclassifiable; the old text's any-surprise-auto-upgrades clause silently dropped | MISS, DIRECTOR'S — the design was written on the Director's recommendation | REMANDED; 26 findings become binding design constraints; the predecessor entry survives BYTE-IDENTICAL (verified independently: base entry present verbatim inside the amended entry) |
| NA-0699 draft | ceremony-payoff substrate: "drafting caught 7 pre-commit, execution caught 1" | FALSIFIED (B2): "the five instrument defects" named TWO unreconciled sets sharing 2 members; execution caught ≥5; "one required-check hazard" unnamed anywhere and no required check ever degraded | MISS ×2, DIRECTOR'S — **both wrong sums favored the Director's own tier-down preference** | the conflict of interest the operator named when ordering the read; the read fired on exactly it. Ratio ≈ parity, not 7:1 |
| NA-0699 draft | "SR-17 restraint" is a real citation (used as operative ground in D-1338 twice) | FALSIFIED (M7): SR-17's text contains zero occurrences of restraint/mint/one-per; the practice was real, the citation was not; the convention was breached at its own birth (WF-0048 activated SR-14/15/16 in one merge) | MISS (program-wide, inherited) | → the unnumbered RESTRAINT convention at §B head; D-1339 records every prior citation's referent |
| NA-0699 exec | R97's premise: the unsealed draft contains the remanded SR-17 tiering text | FALSIFIED by the executor seat BEFORE writing: ten tiering needles = 0 in the draft and 1 each in the canonical directive (red-capability proven); draft mtime 14:46:38Z predates STOP 001's 15:12:54Z by 26 min — the text could not have been there | MISS, DIRECTOR'S — contents inferred from the file's ROLE rather than read from its bytes; the SR-01 extension violated by the Director who ruled it three stops earlier | the false clause would have been written PERMANENTLY into the artifact whose purpose is preventing a future seat from believing a false thing. Retirement stands on the real defect: the draft carries the SR-17-restraint MIS-CITATION verbatim → **an authorized act whose premise measures false is an R16 STOP, not a judgment call** |
| NA-0699 exec | seat needle discipline (STOP 005 §7.2/§7.3, STOP 002 §4.1) | four expectations wrong: a line-wrapped clause missed by a single-line literal; SR-20 occurrence needle counting cross-references; `minimum gate set` counting the annotation's own quote; a port needle matching `DECISIONS.md:37370` as host.tld:port | MISS ×4, SEAT'S — all free (expectation written first), all self-reported | SR-21 caught its own author within hours of landing; cure adopted: **measure the target's bytes (cat -A) first, then build the needle**; and when a needle keeps mismatching, ENUMERATE AND CLASSIFY rather than refine |
| NA-0699 exec | Director's own needle while verifying R66/R70 | reproduced the seat's wrap defect exactly — single-line literal returned 0 against line-wrapped text; corrected instrument returned 1 | MISS, DIRECTOR'S | the wrap-safe needle is now house instrument for any sentence-presence claim; the family has now claimed every chair at the table |
| NA-0699 exec | G6 preflight at base: exit 0, ready_count=1 | measured exit 1, READY_COUNT=2 — the figure was copied from STOP 002's PRE-PROMOTION base where the NA-0699 block did not yet exist | MISS, SEAT'S — a figure carried across a base change | → **an expectation is measured at the base it will be checked at; a carried figure is re-derived, not copied.** The measured result was SR-11's pre-named mask behaving exactly as pre-named |
| NA-0699 exec | R86 packet-mirror inheritance provable | PROVEN both directions on an independent target: pre-merge cmp identical (m8's trap), post-merge cmp differs at byte 935/2256, post-copy sha matches the Director's independently-measured targets EXACTLY (6a22b172… / a4fd2b83…), cmp exit 0, mode re-asserted 444 | HIT | the m8 gap (two ruled appends untaken for two days) is closed and its own reassurance-instrument became the evidence |
| NA-0699 exec | method/ directory fully sealed | `find method/ -type f ! -perm 444` = 0 — true for the first time; the retired draft carries its defects named on its face, content otherwise unaltered (diff of body: empty) | HIT | a mutable draft beside canonical mirrors was a future seat's wrong source; the SR-17-restraint mis-citation would have been re-inherited verbatim |
| NA-0699 ceremony-payoff (SR-17 substrate, second datapoint) | the mandated adversarial read on a Director-recommended rule is worth its cost | the read killed the rule and corrected the Director's arithmetic in the Director's own name; every one of the lane's other catches came from expectations-written-first at seat level | — (data row) | **the two mechanisms that produced every catch this lane: the adversarial read on the Director, and expectations-before-checks on the seat.** Any successor tiering design must not tier either away |
| NA-0700 draft | R119(a): `receipt_policy_mvp_na0177` is the byte-identity gate; "any red is a STOP" | FALSIFIED BY EXECUTION — the SR-15 seat ran two deliberate regressions (probe A: TUI emit fd-swapped to stderr; probe B: gross `QSC_MARK/1` prefix) and the gate stayed GREEN BOTH TIMES: it asserts substrings over stdout+stderr MERGED, so it can see neither which stream bytes land on nor what precedes them | MISS, DIRECTOR'S, BLOCKER-class | the Director named a gate without requiring it be proven red-capable — violating the gate-liveness principle the Director himself ruled at NA-0698 R7/R48. The lane could have shipped non-identical output with every gate green and recorded "byte-identical: measured green" — a false record |
| NA-0700 draft | R131: the emitted-line surface is "7 TUI labels" | FALSIFIED — measured 6 TUI labels across 7 SITES; the findings carried a site count into a label count and the Director repeated it verbatim | MISS, DIRECTOR'S | figure adopted from a findings document rather than measured; same class as R8's "three names" and R118's comma. The seat refused to write it and re-measured |
| NA-0700 draft | R144's criterion (`should_redact_value`) spans the Director's stated concern (contact aliases in a shareable debug artifact) | FALSIFIED — the criterion is SHAPE-based (length, digits, URL-form, timestamp-form); `should_redact_value("peer","mom")` = false on BOTH paths. It catches a subset; the semantic residue survives | MISS, DIRECTOR'S | an instrument narrower than the claim it was offered for → filed as the shape-vs-semantics entry (R151): a contact alias is semantically sensitive in this threat model and shape-innocuous; the two do not meet |
| NA-0700 draft | R123's manual build-root needle rows: "expected 0" over added lines | FALSIFIED-BY-CONSTRUCTION — R124 separately ordered a filing that must NAME the exposure class's tokens; the two orders cannot both be satisfied literally. Measured 3/2/2, every hit enumerated: exactly three lines, all the WF-0049 filing quoting its own needles; zero in source/tests/executable text | MISS, DIRECTOR'S (instrument design) | → the rows re-specified as MEASURE · ENUMERATE · CLASSIFY, zero-unclassified as the gate (R159(b)) — R90's escalation applied to the Director's own rows. Obfuscating the filing was rejected: adjusting content to satisfy a needle is how instruments stop measuring what they claim |
| NA-0700 draft | STOP 001 §5.5's desktop required-context census: {advisories, public-safety, rust} | FALSIFIED at execution — measured live at the server: **{rust, advisories, infra-literal-scan}**. The census recorded ci.yml's own COMMENT — a FILE claim — as if it were a server measurement | MISS (census, Director-approved) | safe direction (infra-literal-scan required where prose said advisory); `public-safety` no longer required is UNCONFIRMED and filed as WF-0050 for the operator's audit-log check — only that instrument can date it |
| NA-0700 exec | the census of 23 print statements | EXACT — confirmed by FOUR independent seats with independent needles (macro variants, cfg proximity, fd-level routes, the linked refimpl path at zero) | HIT | the lane's foundation was sound; what failed was the instruments around it |
| NA-0700 exec | golden control: probe A reds separate-capture and is INVISIBLE merged; probe B reds stdout and merged, stderr identical | EXACT on every stream — and the base-vs-edited comparison measured IDENTICAL across all 78 separate files and all 52 merged files | HIT | **byte-identity PROVEN by an instrument itself proven able to fail**, replacing a gate that could not. The three-stream design's reason demonstrated: merged capture is structurally blind to an fd swap, separate capture structurally blind to interleaving |
| NA-0700 exec | the two probe binaries are distinct artifacts | FALSIFIED — they were the SAME FILE (identical sha): a shared cargo target dir marked treeB "fresh" on an older mtime, so the second copy took treeA's artifact. Visible ONLY because probe B's stderr was PREDICTED identical and measured different | MISS, SEAT'S — self-caught, free | SR-20's family one level down: not the wrong producer's fixture, but ONE producer's output mistaken for two → practice adopted: verify the distinct shas of every artifact under comparison BEFORE trusting any capture |
| NA-0700 exec | control C4's red set = {cell-4, I-1} | first run reded the committed pair PLUS a pre-existing test — a LATENT PARALLEL-TOGGLE RACE between tests mutating process-global routing | MISS (pre-existing defect, exposed) | **a control that finds a bug outside the code it was aimed at is the control working.** Cured by design (mod-wide ROUTING_LOCK incl. the pre-existing test), not by retry; re-run hit exactly the committed pair. By-name count unchanged — nothing added or removed, only serialized |
| NA-0700 exec | worst shard ~30–36 CI-min against the 60-min STOP threshold | measured 1852s ≈ **30m52s** on run 31234531796, and ~19–31 min on the second round — HALF the threshold, twice, on different trees | HIT | union across shard logs reconciled BY NAME to the local suite: 610 passed identical, 2 ignored identical, 130 result lines. **Cloud and box agree name for name.** Promotion count 0 → 1 of 3 |
| NA-0700 exec | R162 uniformity: `emit_raw_payload_line(&format!(` = 21 | EXACT — contacts 14 + lib 7, no other file; Director re-measured independently on the PR head | HIT | closes the gap between the byte-identity CLAIM and its evidence: the driven surface is measured, the 4 undriven sites carry by PROVEN uniformity stated as a structural argument — not an implied measurement |
| NA-0700 close | the on-box method/ PREDICTION_LEDGER mirror differs from repo truth (the Director's close rows) | FALSIFIED — measured IDENTICAL. The Director conflated THREE artifacts sharing a name: repo truth (40 data rows) · the on-box MIRROR (copied from repo truth at NA-0699's close, never touched since) · the Director's PACKET copy (which alone carries the composed close rows) | MISS, DIRECTOR'S | contents predicted from a model of what "the Director's copy" should hold rather than from what that specific file IS. **Durability consequence, named: the composed NA-0699/NA-0700 rows live ONLY in the packet until a governance touch lands them** |
| NA-0700 ceremony-payoff (SR-17 substrate, third datapoint) | drafting-side spend: 1 adversarial read + 3 amendments + a 4-seat census | caught PRE-COMMIT: 1 BLOCKER (a gate blind to what it guarded), 3 MAJOR (the corpus insufficient as byte-identity evidence; the edit set unable to deliver its own claim; the consumer census short on both flanks), and the redaction census that found `peer` verbatim at 10 of 14 labels. EXECUTION caught 3 more by expectations-written-first (probe-B same-binary, the C4 race, the piped fmt check) | — (data row) | **every Director miss this lane (5) was caught by a seat measuring instead of adopting; every seat miss (4) was caught by an expectation written before its check.** The two mechanisms hold at a third lane |
| NA-0717 exec | STOP_005 §2 wrote "squash/merge per house habit" | the standing rule is MERGE COMMITS ONLY; the Director caught it in the merged-resume packet | MISS (seat) | a merge-form stated as habit without measuring the rule; every later ask names the form explicitly |
| NA-0717 exec | sealed "all required checks settle green" on PR-A #1728 | `infra-literal-scan` (required) RED on ONE tier1:personal_email hit — the seat's own GH007 note wrote the operator's address into the as-built | MISS (seat); the NA-0684/0685 scan caught it on first exposure | the record duty needed the CLASS, never the literal (NA-0703); cured by the R275 amend; the catching instrument credited |
| NA-0717 exec | sealed needle "`Status:\s*READY` count = 0 in NEXT_ACTIONS.md" | count is 1 on main AND 1 after the edit (a historical comment at :71); the guard fails only >1 | MISS (wrong instrument — anchored scan vs unanchored guard) | suspect the instrument before the tree; the tree diverged nowhere (R276) |
| NA-0717 exec | first literal sweep needle `tebbens\|proton\|users.noreply` over the six records files | hits were ONLY sanctioned GH007-noreply literals in deep pre-existing history; the scan's own class read 0 | MISS (SR-21 — instrument wider than the claim) | narrow to the consumer's own needle before concluding |
| NA-0717 exec | 4b runtime "~105-110 min" (653's estimate; SR-15 MINOR-3 flagged it stale) | killed at 180 with 117/~130 green; the main-push repeat 127/~130, same kill; full suite later measured 206.5m | MISS — MINOR-3's "fail direction is safe" held EXACTLY | ceilings re-fitted from the kills (NA-0719); the ratchet filed (WF-0076) |
| NA-0717 arc | R268 adopted 653 §6's "NO PR admissible by ANY path while main is red" | FALSE at mechanism level — the docs door; BLOCKER-1, verified 3×; R268 RESCINDED | MISS (Director premise-adoption) | an exhaustiveness claim must include the conditional that decides whether the gate is INVOKED |
| NA-0717 arc | the SR-15 seating premise "the test module is Unix-gated (lib.rs:6)" | no crate-level Unix cfg gate exists; the boundary is de facto (NOTE-1); the directive never claimed it | MISS (seating premise) | a seating premise cannot propagate unmeasured |
| NA-0717 arc | STOP_001's ⭐ "A7 sealed before any log byte was read" | seat-blind only — NA-0715's banked copy of the same log was on-box (MAJOR-2) | MISS (evidence-weight framing) | C-2 never needed the garnish |
| NA-0717 arc | 653 Phase 3 sealed "goal-lint PASS"; the ruling packet resealed it for PR-B | goal_lint.py fails BOTH shapes (NEW-1); unsatisfiable as sealed | MISS ×3 chairs | the sealed-consumer lesson, instance 1; cured R272; WF-0075 |
| NA-0717 arc | 653 Phase 1 sealed fmt-clean JOINTLY with §4(b)'s exact bytes | rustfmt rewraps 4 of 6 sites (NEW-2); pair unsatisfiable | MISS (653's author; SR-15 + packet did not execute the consumer) | instance 2; cured R273 |
| NA-0717 arc | RECO §2 modeled the watchdog fit as "ceiling+20 ≤ 360"; R278 sealed that arithmetic (Director, authorship named); STOP_009's gate executed the sealed arithmetic after running only the consumer's EXTRACTION | the consumer's sizing adds a 60m queue margin (coverage 390) AND the platform kills hosted jobs at 360 regardless of declared timeout — the Director's R281 measurement that also refused the seat's 420/420 cure | MISS ×3 chairs (RECO's model · R278's arithmetic · STOP_009's half-consumer gate), instance 3 in ONE arc | **the rider (R281): THE CONSUMER INCLUDES THE PLATFORM CONTRACT — runner-class limits are part of any CI consumer, and no local execution reveals them** |
<!-- NA-0731 (D-1367) — THE RECORDS SWEEP, 2026-08-15. The rows below are the Director's own
SR-16 error rows for the 2026-08-14/15 working session, landed BYTE-VERBATIM from
`QSL_CARRY_FORWARD_PRE_SWEEP_2026-08-15.md` (whole-file sha256
`7dd6de8009adb1b7a7550ca31baec1dda9e8658627e26512ad81c71365de65ff`, 151 lines), lines 119-135,
extract sha256 `b42a23b2b9617e202c62801da763d8570275976d155ed3158664e8e0b74d301c`, 17 lines.
⚠ SHAPE DIVERGENCE, reported not tidied: these are a FOUR-column table (`# | the assertion |
the measurement | the instrument that caught it`) while the running table above is FIVE-column.
Reshaping them to fit would have broken the byte-verbatim requirement, so they land as their own
table with their own header. The bytes are unaltered; only the placement is a decision.
⚠⚠ THE CLOSING PARAGRAPH IS LANDED WITH THEM AND IS THE TRANSFERABLE PART. A successor who
inherits ten rulings without it inherits confidence where the record meant to leave caution.
⚠ Ten instances in ~30 hours, NONE of which reached main — the rows are evidence for a structural
property (seats measuring, the operator challenging), not a tally against a person. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 1 | "No PR is admissible by any path while main is red" (R268) | FALSE — the docs door; the claim missed the conditional deciding whether the gate is INVOKED | SR-15 cold read |
| 2 | "Press Update branch on #1725" | It had been CONFLICTING for two days and five merges; the button does not exist on a conflicted PR | the seat, at the edit |
| 3 | "This lane will not touch the gate file" (R289) | It must — the parser reads job keys, the wait takes check names | the drafting seat, first hour |
| 4 | "Prove the macOS arrangement by workflow_dispatch" | Impossible — dispatch only fires workflows present on the DEFAULT branch | SR-15 cold read |
| 5 | "Repointing `failure_check` is behaviour-preserving" | FALSE — the gate reads that check's JOB LOG, and an aggregate's log can never carry a test name | SR-15 cold read |
| 6 | "`qsc` trusts ONLY pinned CAs" | FALSE — roots compose ADDITIVELY; the comment naming `tls_built_in_root_certs(false)` exists to record that it is NOT called. ⚠ **`NEXT_ACTIONS.md:36204` already documented this correctly and was not consulted** | **the operator's challenge** — no automated gate covers a prose mechanism claim |
| 7 | "120 failures / 51 days" | 392 / 187. A 60-run API PAGE read as a DURATION | the drafting seat, by bisection |
| 8 | "Ten lines fewer" bound as a STOP constraint (R318) | Unreachable while executing the same ruling's other orders; the figure came from a read that measured only two of five changes | the executing seat |
| 9 | "The `ci-red` label" ordered without checking it existed | It did not; `gh issue create --label` fails the ENTIRE create on a missing label — literal compliance would have made the first real red record NOTHING | the executing seat |
| 10 | "Discretion is an explicit SR-15 trigger" (R288) | SR-15 lists three mechanical triggers; discretion is not among them. The read was validly ordered — the disposition power is the Director's — but "explicit" was belief | SR-15 cold read |
**THE PATTERN, which is the point: every one is the same failure — a claim adopted from a
fragment, a summary, another document, or memory, instead of measured at the moment of assertion.
Ten instances in ~30 hours by the chair whose recorded failure mode is exactly this. NONE reached
main. What stopped them was seats measuring and the operator challenging — a structural property,
not a personal one, and the single most valuable thing in this program.**

<!-- NA-0733 (D-1369) — SR-16 ROW 11, 2026-08-15, ORDERED AT R331.1.
⚠ LANDED BESIDE THE TEN ROWS ABOVE AND DELIBERATELY NOT INSIDE THEM. Lines 118-134 of this file
are the Director's byte-verbatim block (extract sha256
b42a23b2b9617e202c62801da763d8570275976d155ed3158664e8e0b74d301c, 17 lines, landed by D-1367);
they were re-hashed UNCHANGED as a gate of this lane. Inserting a row into that table would have
edited a landed byte-verbatim block and broken the digest D-1367's provenance sentence cites
(R327.2: corrections sit BESIDE a fence, never inside it).
⚠⚠ AND THE PLACEMENT IS NOT MERELY MECHANICAL, WHICH IS THE PART WORTH READING. The closing
paragraph above names the pattern of the ten: "a claim adopted from a fragment, a summary, another
document, or memory, instead of measured at the moment of assertion." THIS ROW IS NOT THAT. The
claim was never wrong -- it was never DELIVERED. Folding it into a table whose own closing
paragraph does not cover it would have made that paragraph false about its own contents, and would
have quietly widened a pattern statement that is the transferable half of the block.
⚠ Its sibling is R329.2's CORRECT FIGURE THAT EXPIRED, landed as B12 in ## D-1369. Together they
are the two shapes this arc produced that the original ten do not contain: a TRANSPORT failure and
a FRESHNESS failure. NEITHER IS A MEASUREMENT ERROR, and measuring at the moment of assertion
prevents neither -- which is exactly why they are recorded as distinct shapes rather than as an
eleventh instance of the same one.
Composed at STOP_NA0732_004 §2, on the ground that a row about a ruling which existed only in a
conversation must not itself be left only in a conversation. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 11 | "R329 was issued, therefore R329 is ruled" | FALSE as a claim about repo truth — measured at `6f6b72e9`, `R329` appears **ZERO** times tree-wide, and `### NA-0731`'s DONE line still read `no class declared` two hours after issuance; the ruling reached no seat and no file | the Director's own re-measurement at R331.1 — **second instance for this chair**, after R287 was found by the SR-24 cold read as a dangling citation |

**⚠ THE ROW ABOVE IS INDEPENDENTLY CONFIRMED, and the confirmation carries a distinction a sweep
would otherwise get wrong.** Measured at `6f6b72e9` by the executing seat as well as by the
Director, `R329`, `R330` AND `R331` each appear **ZERO** times tree-wide — but **only R329 is a
defect.** R330 and R331 were issued *after* #1748 merged and were routed forward by R331.5, so
their absence is by design; R329 was issued *before* #1748 existed and declared a result class it
should have carried. ⇒ ***absence is not by itself evidence of a records failure; the discriminator
is whether the ruling existed before an act that could have carried it.*** All three are landed by
`## D-1369`, and `### NA-0731`'s DONE line now carries the class R329.1 ruled.

<!-- NA-0736 (D-1371) — SR-16 ROWS 12-15, 2026-08-15, ORDERED AT R335 §1 and §4.
⚠ LANDED BESIDE THE TEN BYTE-VERBATIM ROWS AND THE NA-0733 ROW, AND DELIBERATELY NOT INSIDE EITHER.
Lines 118-134 of this file are the Director's byte-verbatim block (extract sha256
b42a23b2b9617e202c62801da763d8570275976d155ed3158664e8e0b74d301c, 17 lines, landed by D-1367); they
were RE-HASHED UNCHANGED as a gate of this lane, exactly as NA-0733 did. Inserting a row into that
table would break the digest D-1367's provenance sentence cites (R327.2: corrections sit BESIDE a
fence, never inside it).
⚠⚠ ROW 12 IS THE DIRECTOR'S OWN AND IS THE REASON THIS BLOCK EXISTS. It is not a measurement error
of the kind the ten rows describe -- nothing was measured wrongly. It is an INSTRUMENT error one
level up: the option set a ruling offers is itself an instrument, and an enumerated candidate list
that omits "neither" is narrower than the claim it invites. R335 §1 names it the SECOND instance of
that shape in two consecutive briefs, which is why the property is carried here rather than left as
an anecdote. ⚠ NO RULE IS MINTED (R305/D-2: a rule without an executable consumer decays).
⚠ ROW 15 IS RECORDED AS A HIT, NOT A MISS, ON THE DIRECTOR'S EXPLICIT ORDER (R335 §4) -- "that is
the house method working, and it is worth having on the record as a hit rather than only recording
misses." A ledger that records only failures teaches that measuring is a tax rather than a tool. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 12 | "Either ENG-0134 is BROADER than its filing states, or this is a sibling" (Director's brief §5) | **NEITHER** — measured, the pull did not abort and nothing was destroyed, so ENG-0134's mechanism does not fit; and ENG-0142's region sits inside a loop zero items never entered. The true answer was excluded by the option set | the seat, refusing the frame and saying so |
| 13 | "The user's message is sitting in bob's mailbox" (Director, repeated to the operator in chat) | FALSE — the retained items measure **4279 B** and **6436 B**, matching `handshake_send` **A1** and **B1** exactly; the user payloads were **17 B** and **15 B**. The cited 15250/23043 are the **JSON response-body file sizes**, not item sizes | the seat, parsing the artifact instead of reading its `ls -l` |
| 14 | "The `--mailbox` override has ZERO green coverage anywhere in the tree" (this lane, STOP 001 §6) | FALSE — **40 test files, 99 call sites**, with `recv_commit` and `qsp_unpack ok=true` assertions through it. The claim was quantified over *the tree* from a sweep of `scripts/demo/` and `.github/workflows/` alone — **an instrument narrower than its claim (SR-21)** | the seat, asking whether the Rust suite already covered it before letting "zero" stand |
| 15 | ⛳ **A HIT.** "`send_ab_1` is absent from the committed script, so the brief's `:372` is wrong" (this lane, first pass) | The brief was **RIGHT**: the script writes `"send_ab_${i}"` inside a `while` loop, so a literal needle returns 0. Re-measured from bytes, **all four** of the brief's line numbers are correct | the seat's own re-measurement, before the claim reached any record |

**⚠ THE SHAPE THESE FOUR ADD, which the ten above do not contain.** The ten are all *"a claim adopted
from a fragment, a summary, another document, or memory, instead of measured at the moment of
assertion."* **Rows 12 and 14 are a different failure: a claim whose INSTRUMENT was narrower than its
SCOPE** — an option set that excluded the true answer, and a two-directory sweep quantified over a
whole tree. Measuring at the moment of assertion does not prevent either; **checking that the
instrument's scope equals the claim's scope does.** ⚠ And row 12 is the Director's, row 14 the
seat's — **the same defect from both chairs in one lane**, which is the argument for treating it as
structural rather than as a lapse. Row 13 is a transport failure of the NA-0733 row-11 kind: the
figure was never measured, only repeated. **Row 15 is here to keep the ledger honest in the other
direction** — the method caught a *correct* record before a lane could "correct" it.

<!-- NA-0736 (D-1371) — SR-16 ROWS 16-18, 2026-08-15, ORDERED AT R336 §A, §B and §D.
⚠ LANDED BESIDE the sealed ten (lines 118-134, b42a23b2…d301c, re-hashed UNCHANGED again as a gate
of this amendment) and beside rows 11-15. Nothing above is edited.
⚠⚠ ROWS 16 AND 17 ARE BOTH THIS SEAT'S, AND ROW 16 IS THE FIRST ERROR IN THIS LANE THAT THE SEAT DID
NOT CATCH ITSELF. Rows 12 and 14 were caught in-lane; row 16 reached a PUSHED PR and was caught by
the Director. That distinction is the point of recording it: the in-lane catch rate is not 100%, and
a ledger that only shows caught errors would imply it is.
⚠ ROW 18 IS A HIT, ORDERED AT R336 §D. It is not decoration. The three failures it names were each
caught by the SAME mechanism -- an expectation written before the run -- and a ledger that records
only failures teaches that measuring is a tax rather than a tool. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 16 | "`ROADMAP.md:13`'s *No open P1 remains* is NOT SUPPORTED" (this lane, D-1371 PART 3 as first pushed) | **TOO WIDE.** That sentence reads "…and there is no known correctness gap **in the crypto core**", inside a paragraph opening "The cryptographic core is now correctness-complete:". ENG-0134/ENG-0142 are **transport**-layer ⇒ the finding does **not** falsify it. What is unsupported is the **ledger-wide** "zero open P1s" claim, and **only** that | ⚠ **the Director, at R336 §B — NOT caught in-lane, and it had already been pushed** |
| 17 | "Replacing §0's preimages with their shapes keeps the argument fully checkable, since the hashes carry it" (this lane, D-1371 PART 8, recommending option (a)) | **FALSE.** A reader given `bob-${run_tag}` **cannot** recompute `f4c89d20` — **a hash is not checkable without its preimage.** The option would have bought the APPEARANCE of a byte-verbatim landing while destroying the property that makes one worth doing | the Director, at R336 §A, applying R335 §1's own option-set property to the seat's option set |
| 18 | ⛳ **A HIT — three instrument failures, none of which reached a record.** (i) a uniqueness assert counting the bare token `NA-0736`, which the evidence-doc **path** also contains; (ii) a `- Severity: **P1**` count taken over the **whole file** where the claim was a **delta**; (iii) `classify_ci_scope.sh` invoked with two **SHAs** where it takes **paths** | Each was wrong in a way that would have shipped a false figure or a false CI-scope claim. **All three were caught by the same mechanism: the expected result was written down before the command ran** | the seat, in-lane, before any record — the method working |

**⚠ WHAT ROWS 16–18 ADD, taken together.** Rows 12 and 14 established that *an instrument narrower or
wider than its claim* is this program's live failure shape, from both chairs. **Row 16 shows the shape
surviving an entire lane's discipline and reaching a pushed PR** — caught only because the ruling
chair re-measured a claim it had not itself made. **Row 17 shows it one level up again:** the seat
supplied the Director an option set whose recommended branch rested on a false property, and it was
caught only because R335 §1's own rule — *a supplied option set is an INSTRUMENT* — was turned on the
seat that supplied it. ⚠⚠ **The rule generalises in both directions, and this lane is the proof: it
was minted against the Director's option set and it next caught the seat's.** Row 18 is the
counterweight — **the same discipline caught three of the seat's own errors before any of them
reached a record**, which is what the ledger exists to make visible.

<!-- NA-0737 (D-1372) — SR-16 ROWS 19-22, 2026-08-15, ORDERED AT §B and §D of the NA-0737 ruling.
⚠ LANDED BESIDE the sealed ten (lines 118-134, b42a23b2…d301c) and beside rows 11-18. Nothing above
is edited.
⚠⚠ ROW 19 IS A PRIOR DIRECTOR RULING'S PREMISE MEASURING FALSE -- not a seat error and not a brief
error, but a RULED ANSWER that the next run falsified. It is recorded because the alternative is a
successor lane executing it as written.
⚠⚠ ROW 20 IS THE DIRECTOR'S CHAIR AGAIN, AND IT IS A NEW SHAPE: not an option set and not a figure,
but a brief that CONTRADICTED ITS OWN ENUMERATION and then asked the seat for something the seat
structurally could not measure. The seat's STOP was the correct outcome.
⚠ ROW 21 IS THE SEAT'S, AND IT IS THE MOST DANGEROUS ARTIFACT IN THE SET: a preserved file whose
`ls -l` and sha256 both reported health over bytes that carried nothing.
⚠ ROW 22 IS A HIT. As with row 18, a ledger that records only failures teaches that measuring is a
tax rather than a tool. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 19 | "The assertion moves to AFTER the first message exchange, where `established` is genuinely reachable" (**R334.2**, the ruled repair for ENG-0191) | **FALSE AT ITS PREMISE.** With delivery repaired and working both directions, `status=established` occurs **0 times in 12/12** observations; at C5, after both directions have delivered, alice reads `established_recv_only` and bob `awaiting_peer_confirm`. **Moving the assertion just after `:388` would still fail.** NA-0735 called it unimplementable *because there was no successful exchange* — there now is one, and it still is | NA-0737's run — the first in which a receive SUCCEEDED, which is exactly the measurement NA-0735 named as its successor's first act |
| 20 | "File §9's `qsl-desktop` Slice-4 measurement as a ledger entry" (the NA-0737 brief §9, against its own §1) | **THE BRIEF CONTRADICTED ITSELF.** §1 enumerated **ONE file**; a ledger entry drags a queue block, a `STATE` move and an evidence doc. ⚠ And the figure was one the seat **structurally could not measure** — no desktop checkout, from a seat that same §1 bounded to one file in this repo. ⇒ **a brief's enumeration binds the brief's own later sections** | the seat, STOPPING rather than reconciling the contradiction — ruled CORRECT, the defect being the Director's, twice over |
| 21 | "The relay store is preserved: `cp` copied it and its sha256 is recorded" (this lane, first preservation pass) | **FALSE — the copy was a 4096-byte, COMPLETELY EMPTY database.** Every row was in the `-wal`; opening it returned *"no such table: messages"*. ⚠⚠ **Both `ls -l` and `sha256sum` reported a healthy preserved artifact.** A clean hash over the wrong bytes is indistinguishable from evidence until you open it | the seat, **OPENING THE COPY AND QUERYING IT** rather than trusting the hash — nothing else in the set would have found it. Cured with `VACUUM INTO` + a row-count assertion before the copy is trusted |
| 22 | ⛳ **A HIT — two further instrument defects, neither of which reached a record.** (i) the harness builder's prefix anchor matched a **tampered** step name (`recv_from_alice_TAMPERED_X`) and returned **rc 0**, so a drifted anchor would have been wrapped **silently** — the exact "silent skip" the order forbade; (ii) the relay's `route_key = hex(sha256(tok))` was compared against the client's `hex(sha512(tok)[..4])` | (i) caught by a **negative control asserted to differ first**, then cured with a word boundary and re-proven `rc 1` against three tamper classes; (ii) the "none of the four candidates" result **was itself the finding** — it exposed that `route_key` is a derivation, not the token | the seat, in-lane, before any record — **an expectation written before the run**, twice |

**⚠ WHAT ROWS 19–22 ADD.** Rows 12/14/16/17 established *instrument narrower or wider than its claim*
as this program's live failure shape, from both chairs. **Row 19 moves it up one more level again: a
RULING's premise, falsified by a later measurement** — and the correction runs upward, as the standing
rule requires. **Row 20 names a shape not previously recorded** — internal inconsistency *within a
single directive*, where the correct seat behaviour is to stop rather than to reconcile. **Row 21 is
the counterpart to row 17's lesson about hashes**: there, a hash without its preimage was not
checkable; here, a hash *with* its preimage was checkable and still worthless, because it was taken
over the wrong bytes. ⇒ **a sha256 attests to what you hashed, never to what you meant to hash.**

<!-- NA-0738 (D-1373) — SR-16 ROWS 23-28, 2026-08-16, ORDERED AT §5.5 of the NA-0738 brief.
⚠ LANDED BESIDE the sealed ten (lines 118-134, b42a23b2…d301c) and beside rows 11-22. Nothing above
is edited.
⚠⚠ ROWS 23 AND 24 ARE THE DIRECTOR'S CHAIR, and row 24 is the THIRD instance of the option-set
property (R335 §1, R336 §A, here) -- this time incomplete on TWO independent axes at once.
⛳ ROW 25 IS A HIT AND IT IS THE PROGRAM'S FIRST OF ITS KIND: the SR-15 practice caught a Director's
own reasoning BEFORE a decision was made, rather than a seat's after the fact.
⚠⚠ ROW 26 IS THE SEAT'S AND IT IS THE MOST DANGEROUS ENTRY IN THIS SET: a SEALED EXPECTATION whose
own decisive combination was defective, and which read mechanically would have declared a correct
theorem REFUTED. It is the NA-0715 property fired a second time -- the seal caught, not catching.
⚠ ROW 27 IS THE COLD READ'S, and the correction makes its finding stronger, not weaker.
⚠ ROW 28 IS THE SEAT'S INSTRUMENT PERTURBING THE SYSTEM IT OBSERVED. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 23 | "The assertion may be unreachable not because of WHERE IT SITS but because a second handshake sits mid-flow and **resets** what is being asserted" (the Director's ELIMINATION, carried in ENG-0191's NA-0737 amendment, tagged unmeasured) | **REFUTED, by its own two supports.** (i) **A reset cannot key a chain** — at C3 bob reads `send_ready=yes`, which by `hs_status_truth` branch 1 REQUIRES a keyed send chain, and bob's was unkeyed at C0/C1/C2. (ii) **ENG-0143, the cited support, says the session is FRESH** (*"fresh `nr == 0`"*); the surviving SCKA fossil is the exception it files as a bug, so it is affirmative evidence for REPLACEMENT. (iii) the grep found nothing because **there is no reset in any module** — the mechanism is an unconditional whole-session OVERWRITE, which no needle for "reset" can match. ⛳ The instinct pointed at the right line: `hs2` **bisects the round trip**, so no session ever sees both directions | the SR-15 adversarial cold read, from the Director's **own numbers**, before any option was ruled — then confirmed by NA-0738's A/B: 0/12 with `hs2` mid-flow, 3/12 with it relocated, in the identical environment |
| 24 | "The Director offered three and explicitly checked for a fourth" (ENG-0191's NA-0737 amendment) | **THE SET WAS INCOMPLETE ON TWO INDEPENDENT AXES.** (i) It is closed under delete / add / delete and contains **no MOVE** — while the defect is an **ORDERING** defect, so the answer was the one operation the set could not express **(d)**. (ii) It had **silently dropped one of the filing's own two originals**: ENG-0191 as filed offered *"assert a status true at that point … checking `peer_confirmed` / `send_ready` instead"* (a **RETARGET**); the amendment's (a) is *"**Drop** the assertion"* (a **DELETE**). **Different repairs, no note of the substitution** ⇒ the check for a fourth ran against a set that had already lost one **(e)** | the cold read, reading the ledger entry against its own amendment. ⚠ **THIRD instance of "a supplied option set is an INSTRUMENT and SR-21 governs it"** — minted at R335 §1 against a Director dichotomy, fired at R336 §A against a seat's own set, and here against a Director set incomplete two ways at once |
| 25 | ⛳ **A HIT, AND A FIRST FOR THIS PROGRAM'S SR-15 PRACTICE.** Everything in rows 23 and 24 — a refuted elimination, a doubly-incomplete option set, a lost option, an inert unit assertion nobody had looked at, and the one experiment that settles all of it — was produced by **one adversarial cold read, before any option was ruled** | The read's theorem predicted **all 12** of NA-0737's measured values with zero exceptions and then predicted NA-0738's X0–X4 **exactly**, including the transition (X3) nobody had ever observed. **A repair chosen without it would have been chosen on evidence that was an artefact of measurement selection** — only one arrangement had ever been run | the SR-15 cold read itself (`a5b7324e…b1b4`, 534 lines, sealed 444, sha verified BEFORE it was opened and re-verified unchanged after). ⛳ **Every prior SR-16 row records a measurement catching a seat; this one records the method catching the DIRECTOR's reasoning while it was still changeable** |
| 26 | ⚠⚠ "X0 reproduces AND X4 reads anything else ⇒ the cold read's theorem is **REFUTED**" (**this seat's own sealed decisive combination**, run 1) | **THE COMBINATION WAS DEFECTIVE.** X4 read `awaiting_peer_confirm` on alice — but **because alice never received**: `recv_from_bob` returned rc 1 with `qsp_env_decode_failed`. **The seal had no branch for its own antecedent failing**, so read mechanically it would have declared a theorem **REFUTED** that the very next run confirmed. It assumed the delivery it was measuring | the harness capturing **each step's own rc** (`obs_rc recv_from_bob "$rc"`) — an instrument choice, **not the seal's own correctness**. ⚠ NA-0715's property fired a second time: a sealed expectation earns its keep by **being catchable**, and this one was caught only because something independent recorded the antecedent. Cure, mechanical: **state the antecedent as its own branch** — "if the act the checkpoint is defined as following did not happen, the checkpoint is a NON-RESULT" |
| 27 | "ALL THREE consumers of `status=established` match by substring" (the cold read's §8.2 enumeration) | **SHORT BY ONE — there are FOUR.** `qsl/qsl-client/qsc/scripts/remote_soak.py:573` (`"…status=established" not in merged`) is a fourth substring consumer, and the `((a,b),(b,a))` loop applies it in both directions. ⛳ **The correction makes the finding STRONGER**: with **four of four** matching by substring, the claim *"`hs_status_truth`'s third branch has zero distinguishing coverage tree-wide"* holds over a larger set than the one it was drawn from | the filing seat re-measuring the census tree-wide instead of transcribing it — SR-21 again, and this time on the instrument that had just caught two others. Filed as **ENG-0194** with the corrected census and the CI-vs-operator distinction stated |
| 28 | (implicit) "adding six status checkpoints observes the run without changing it" (**this seat's harness**) | **FALSE — THE INSTRUMENT CAUSED THE FAILURE IT OBSERVED.** The harness adds **12 `qsc` invocations** (6 checkpoints × 2 peers), each a debug-build process with a passphrase-KDF vault unlock. Run 1's enqueue span measured **107 s** against a **60 s** relay visibility timeout (`PULL_LEASE_SECS_DEFAULT`, `store.rs:7`) ⇒ a never-acked hs1 handshake frame resurfaced at the head of alice's mailbox and aborted her receive. **The observer's cost was of the same order as the timeout it caused to expire** | the relay store's own `enqueued_at` / `leased_until` columns, read read-only, against the source constant. ⚠ It also exposed a **product** finding the instrument was not looking for: `handshake poll` never acks (0 / 0 / 1 `relay_ack`), and a single undecodable head-of-queue frame aborts the whole receive at `transport/mod.rs:1249` |

**⚠ WHAT ROWS 23–28 ADD.** Rows 12/14/16/17/27 are the same shape — *an instrument narrower or wider
than its claim* — now recorded from three chairs: seat, Director, and the cold read that audits both.
**Row 24 completes the option-set property's arc**: minted against a Director's dichotomy, fired
against a seat's own set, and now against a set incomplete on two axes at once — ⇒ **the property is
not about carelessness, it is about the shape of enumerating alternatives at all, and the cure is an
independent reader rather than more care.** **Row 26 is the counterpart to row 19**: there a RULING's
premise measured false; here a SEAL's own logic did — ⇒ **a sealed expectation can be wrong in its
CONDITIONS as well as in its VALUES, and only a second, independent record of the antecedent can tell
you which.** **Row 25 is the argument for the practice itself**: it is cheaper than any repair it
would decide between, and it is the first time this program can show it working *upward*.

<!-- NA-0739 (D-1374) — SR-16 ROWS 29-34, 2026-08-16, ORDERED AT §5 of the NA-0739 brief.
⚠ LANDED BESIDE the sealed ten (lines 118-134) and beside rows 11-22 and 23-28. Nothing above is edited.
⚠⚠ ROWS 29 AND 30 ARE THE DIRECTOR'S CHAIR, and row 29 is the one that mattered: an attribution that was
too NARROW rather than wrong, widened by the seat's measurement from one caller to three -- including one
that pulls the user's ORDINARY inbox under a command the user is required to run.
⚠ ROW 31 IS NA-0738's SEAT, already corrected in the open at its own STOP 003 and recorded here so the
correction survives in repo truth rather than only in a /srv stop-file.
⛳ ROWS 32 AND 33 ARE HITS. Row 33 is the second appearance of the property that an informative MISS can
be worth more than the hit: the seal failed, and reporting the failure produced the stronger finding.
⚠⚠ ROW 34 IS THIS LANE'S OWN, AND IT IS THE ENTRY THE LANE WOULD MOST LIKE NOT TO HAVE EARNED: the brief
required a negative control, the control FAILED, and enumerating the failure rather than refining the
needle exposed a search tool blind to 514 TRACKED FILES. SR-21 fired three times in one lane, all three
against the seat, and all three were caught by required controls rather than by care. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 29 | "The poison is deposited by **`handshake poll`**" (**R337 §B**, the ruling that identified the block as ENG-0142) | **TOO NARROW — and the record says so.** `relay_inbox_pull` contains **no ack call at all**, so it never acks **BY CONSTRUCTION** rather than merely in the runs observed, and the tree's OWN ratified census (NA-0688 C4 / D622, in the function's doc comment) names **THREE** production callers: `invite accept`, ⚠ **`invite finish` — pulling the user's ORDINARY inbox under a command the user is REQUIRED to run** — and `handshake poll`. ⚠ The bound is kept: undecodability is measured only for `handshake poll`. | Reading the function at source instead of adopting the ruling's attribution — the ruling itself ordered *verify, don't adopt*, and the verification returned a WIDER claim than the one it checked |
| 30 | ENG-0142's trigger is *"anyone who can post one **unprocessable** frame"* (**R337**, quoting the entry) | **A MISQUOTE, and it SHARPENS rather than weakens.** "unprocessable" is the wording of the entry's **heading** (`:3713`); its **trigger sentence** (`:3715`) reads **"unparseable"**. Both words are in the entry, in those two different places. It changes nothing — and the measured failure is at envelope **parsing** (`qsp_env_decode_failed`), which is the trigger sentence's word exactly | Diffing the quotation against the entry's own bytes rather than against its heading |
| 31 | "**PR #1753 still OPEN, unmerged**, head `e1026170…`, still green" and "Base main `62752adf` — verified UNMOVED" (**NA-0738 STOP 002's header**, the seat's own) | **BOTH FALSE WHEN WRITTEN.** #1753 merged 02:50:20Z; the header was written 02:57:12Z — **false by 6 min 52 s** — and main had already advanced to `64c60dc3`. Cause: **a correctly-measured figure carried across seven minutes into a sentence written later, instead of re-measured at the moment of assertion (R324.2)** | A final `git ls-remote` + `gh pr view` verification pass the seat **could have skipped**. ⚠ Nothing downstream depended on it; recorded because the record should be right even when the error was harmless |
| 32 | ⛳ **A HIT.** "A material difference in MECHANISM would outrank the ENG-0142 identification" (R337 §B's own test, handed to the seat to apply against itself) | **NO MECHANISM DIFFERENCE — verified clause for clause against the entry's filed text**, six clauses, all identical, with the retention bound independently confirmed from the relay's own advertised `retention.ttl_secs = 604800`. ⇒ do not file a sibling, do not widen ENG-0134, **amend** (WF-0029) | A clause table built from the entry's own text, with the two things verification ADDED — stale coordinates and a wider trigger — reported as additions rather than folded silently into agreement |
| 33 | ⛳ **A HIT, AND THE MISS BEAT THE HIT.** "Delete the head poison row and the blocked message ARRIVES" (**NA-0738's own sealed P4**) | **MISSED — `REJECT_S2_HDR_AUTH_FAIL`.** The blocked message was **already unreadable**, because a re-handshake had REPLACED the receiver's session while the mailbox was wedged. ⇒ ENG-0142's *"delayed until retention expires"* becomes **destroyed** — a **stronger** statement about impact and a **weaker** one about retrievability, ⚠ both halves stated, ⚠ marked an inference from a measured A/B and NOT source-proven, ⚠ with the clean demonstration explicitly NOT run | Reporting a sealed sub-check's failure instead of quietly re-scoping it — and an A/B the seat already owned, so the stronger claim cost no new experiment |
| 34 | ⚠⚠ **THIS LANE'S OWN, THREE TIMES.** "The id-freeness instrument measures what freeness means" (NA-0739, sweeping declaring forms as the brief required) | **THE REQUIRED NEGATIVE CONTROL FAILED — `SR-24` measured 5 files / 1 declaring against the brief's stated 6 / 0** — and enumerating rather than refining decomposed it into **two independent seat defects**: (i) an over-broad `^**SR-` alternative matching the **bold sentence opener** `**SR-24 itself is NOT yet adopted.**`, and (ii) ⚠⚠ **a recursive search that honours `.gitignore` and therefore CANNOT SEE 514 TRACKED FILES** — 483 under `docs/governance/evidence/`, 18 under `tests/`, 3 under `scripts/ci/` — returning **0** files for a glob where `git grep` returns **18**. A **third** instance followed in the same lane: the novelty claim *"0 files record the read half"* was first asserted on two narrow needles, then measured across all 100 `gitignore` mentions in 24 files | **THE CONTROL THE BRIEF REQUIRED, FAILING.** ⛳ Had the brief not demanded a negative control, this lane would have derived correct ids from a defective instrument **and filed a hazard entry while standing in it**. All three defects were caught by required controls, **not by care** |

**⚠ WHAT ROWS 29–34 ADD.** Rows 12/14/16/17/21/27 and now **34** are one shape — *an instrument narrower
or wider than its claim* — recorded from every chair the program has. **Row 34 moves it one level deeper
than any predecessor: not the instrument of a lane's finding, but the instrument of the RECORDS PROCEDURE
ITSELF**, the id-derivation step every lane runs; and it is the first entry where the failing measurement
was ordered by the directive **as a control** rather than discovered afterwards. ⇒ **the cure for this
shape is not more care, it is a control whose expected value is written down before it runs** — which is
what turned a silent 514-file blind spot into a filing. **Row 29 is the counterpart to row 24**: there a
Director's option SET was incomplete, here a Director's ATTRIBUTION was too narrow, and in both cases the
correction ran **upward** from seat to Director, as the standing rule requires. **Row 31 is row 21's
lesson in the time domain** — there a hash attested to the wrong bytes, here a measurement attested to
the wrong *moment*; ⇒ **a figure has a timestamp whether or not you write one down.** **Row 33 pairs with
row 26**: row 26 showed a seal can be wrong in its CONDITIONS, row 33 shows a seal can be wrong in its
VALUES and still be the most productive thing in the lane — ⇒ **the value of a sealed expectation does
not depend on its being right, only on its being reported.**

<!-- NA-0740 (D-1375) — SR-16 ROW 35, 2026-08-16. ⚠⚠ THE DIRECTOR'S CHAIR, and it is the OWED item
from the NA-0739 ruling's §C, banked at that lane's close and deferred to "the next records act" —
this one. LANDED BESIDE rows 29-34; nothing above is edited. The row itself is byte-verbatim from the
banked packet (sha256 a2cee958f9e077e03308557c3aaa1ac29f79fffeeecd898869dea55d6cb3f55a, 74 lines),
extracted programmatically and diffed back, never retyped.
⚠ IT BELONGS BESIDE ROWS 12, 14, 16, 17, 21, 27 AND 34 AS THE SAME SHAPE — an instrument narrower than
the claim hung on it — ⚠⚠ AND ITS CAUSE DIFFERS FROM ROW 34's: a GLOB, not an ignore rule. Two distinct
mechanisms producing one shape is the generalisable part, which is why the distinction is recorded
here rather than left for a reader to collapse. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 35 | "There are **THREE** consumers of `status=established` tree-wide" (**the Director's own census**, reported to the operator) | **SHORT BY ONE, AND THE INSTRUMENT IS WHY.** The search globbed only `*.rs` and `*.sh`, so `qsl/qsl-client/qsc/scripts/remote_soak.py` was **outside the glob entirely** — measured at `9d11e2bd`: the rs+sh glob returns **18** files for `established` where rs+sh+py returns **22**, so **4 Python files were invisible to it**, and `remote_soak.py:573` (`if "event=handshake_status status=established" not in merged:`) is the fourth consumer. ⚠ The cold read said three and the seat measured four (ENG-0194); **the Director's own "tree-wide" census would have missed it as well** | The seat's independent enumeration, which did not inherit the glob. ⚠ **Different cause from row 34 — a GLOB, not `.gitignore` — and the SAME SHAPE: an instrument narrower than the claim hung on it.** Filed from the Director's chair, at the Director's own instruction |

<!-- NA-0740 (D-1375) — SR-16 ROWS 36-40, 2026-08-16, ORDERED AT §5(d) of the NA-0740 brief.
⚠ LANDED BESIDE rows 29-34 and the Director's row 35; nothing above is edited.
⚠ ROWS 36-38 ARE THE DIRECTOR'S CHAIR AND ALL THREE ARE HITS. They are recorded because a ledger that
records only failures teaches that measuring is a tax rather than a tool — and because E2 in particular
was a READING of source offered as a prediction and it held exactly, which is the case for reading.
⚠⚠ ROWS 39 AND 40 ARE THE INSTRUCTIVE ONES. ROW 39 IS THE SEAT'S OWN: a sealed checkpoint whose ANTECEDENT silently
expired, which read mechanically would have reported a non-result as a measurement. -->

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 36 | **E1** (Director): `invite finish` leases the invite reply and never acks; post-expiry it is resident again; a subsequent `receive` hits it, and `qsp_env_decode_failed` → abort would make it **WEDGE CLASS** | **HIT, branch (ii).** Leased +54 s and unacked, resident after expiry, and `receive` returned `event=qsp_unpack code=qsp_env_decode_failed ok=false` → `event=error`, rc 1, out dir empty. ⚠ **One wording correction: the mailbox is the REDEEMER's, not "the inviter's"** — and the inviter is wedged too, by the A2 frame, so the expectation was right about the mechanism and short by one victim | Running the invite flow to completion on loopback and censusing the store after every command (routes / rows / unexplained = 0 throughout) |
| 37 | **E2** (Director): handshake frames open `51 48 53 4D` (`"QHSM"`); **the receive path never inspects it — zero occurrences in `transport/mod.rs`**; QSP envelopes do NOT open with QHSM; ⚠ **no prediction offered for the invite classes, deliberately** | **HIT, all three parts, and the abstention was the right call.** A1/B1/A2 all open `51 48 53 4D`; `git grep -c 'QHSM\|HS_MAGIC'` in `transport/mod.rs` returns **rc 1, ZERO**, against a **positive control of 3** in `handshake/mod.rs`; the QSP envelope opens `01 00 00 01`. The invite classes measured `01 01 01 …` and `01 02 02 …` — **no prediction existed and none was retrofitted** | A source read with a positive control on the same needle, plus frames pulled off the wire; ⛳ **the abstention is the transferable part — declining to predict a cell nobody had looked at is what let the measurement be believed** |
| 38 | **E4** (Director): with the head leased and unacked, a second pull returns the items behind it (`store.rs:722-723`'s WHERE clause) | **HIT.** Pull 1 (`max=1&ack=lease`) leased the head; an immediate pull 2 (`max=5&ack=lease`) returned items 2 and 3. ⇒ **the skip-and-leave-leased repair shape is not dead on arrival** | ⚠ **The CONTROL, not the result:** a third pull with everything leased returned **HTTP 204 / zero items**, which is the only thing separating "the relay honours leases and gave me the tail" from "the relay ignores leases entirely" |
| 39 | **The seat's own:** *"a `receive` fired IMMEDIATELY after the poll, while the lease is live, will read `recv_none`"* — a checkpoint in the first attempt at the delivery-delay arm | ⚠⚠ **NON-RESULT, NOT A MISS — AND THE DIFFERENCE IS THE WHOLE ROW.** The receive labelled *"immediate"* ran **97 s after the poll**, so the lease had already expired (`leased_until - now = -37 s`) and **the act the checkpoint was defined as following had not held**. Its `DELAY_OBSERVED=64 s` was **not** a measurement of the poll's delay but the gap between two of the seat's own receives. ⚠ Compounded by a second defect: the pair **could never have delivered**, because `qsp_unpack_channels_for_peer` (`lib.rs:2139-2157`) derives the channel from the **LOCAL contact label** and the seeded-session fallback keys on it, so asymmetric labels packed and unpacked on different channels (`qsp_hdr_auth_failed`). Rebuilt symmetrically and inside one script, the honest figure is **59 s** | ⛳ **The seal stating its ANTECEDENT as an explicit branch** — the NA-0738 cure, applied in a fresh instance and paying immediately. Read mechanically without that branch, a non-result would have been reported as a measurement. ⚠ **The committed test `na0688_c4_collateral_arms.rs:228-229` labels both sides `"peer"` and nothing says why the symmetry is load-bearing** |
| 40 | **The seat's drafted records** (STOP 001 §8.1; STOP 002 §C.1/§C.2): *"correction to the prior amendment's wording, which said 'the inviter's ordinary inbox'"* | **MISATTRIBUTED.** Measured at 9d11e2bd: "inviter" occurs **ZERO** times in the ENG-0142 entry; the landed amendment reads *"the user's ORDINARY inbox"*, which is CORRECT. The wording error under correction was the **NA-0740 brief's own E1** — the Director's chair. A drafted correction that indicts a correct record is itself the records defect this program hunts | The Director re-reading the landed entry before ruling (SR-01) — the verification discipline running in the OTHER direction for once: a seat's drafting caught by a Director's measurement |
| 41 | **The Director's own**, adopting the seat's census figure into the interim ruling as reason (i)/(iii) weight: *"Cost: ONE retarget"* / *"ZERO UNCLASSIFIED. Every row above carries exactly one class."* | **FALSE, and at least six-fold.** The SR-15 read measured **five test functions** breaking under the then-ruled option, **not one of them an §6 row**. The figure was inherited from the seat and made load-bearing without re-deriving it | The SR-15 adversarial cold read. ⚠ **The instrument was narrower than the claim hung on it — and this row is the FIRST time in this program that the shape is recorded against a RULING rather than a seat** |
| 42 | **The Director's own:** *"FIVE fail-closed SECURITY assertions"*, quoted verbatim from the tests | **FOUR.** `handshake_mvp.rs:2056` asserts a **`send`**, not a receive; a second row (`NA_0640_full_stack_e2e.rs:530`) has the same defect and was labelled *"terminal, pre-item"* | The read checking each assertion's **SUBJECT** rather than its wording. ⚠ **The quotations were all correct; verifying a quotation is not verifying a claim** |
| 43 | **The design block's own exit-semantics sentence** (the Director's): *"a receive that delivers what it can exits 0, with skip counts in markers"* — welding skip ⇒ exit-0 and never examined as a choice | **AN UNEXAMINED WELD.** A fourth option (continue-then-fail: skip the frame, deliver everything else, still exit non-zero) was **structurally excluded by the option set**, and was refuted only on its own arithmetic once someone wrote it down — not because it had ever been considered | The read constructing the option the set could not contain. ⚠ **Third instance of *a supplied option set is an INSTRUMENT and SR-21 governs it* — and the first inside a DESIGN rather than a directive or a filing** |
| 44 | **The seat's own** (§2(d)): *"every consumer of receive's exit code **and of the markers the loop emits**"*, claimed as ZERO UNCLASSIFIED | **THE INSTRUMENT COVERED ONLY THE FIRST HALF.** 499 marker rows across 96 files were counted and **never classified as tests**; the executable census keyed on *"asserts `qsc receive` exits NON-ZERO"*. Three of the five missed breakages assert `QSC_CONTACT_REQUEST action=created` — a **marker**, invisible to any rc-keyed needle | The read reading the CLAIM against the INSTRUMENT. ⚠ The seat had already widened this census once (13 → 28) after finding a miss **by reading rather than by the instrument**, and still did not widen it to the axis its own claim named |
| 45 | **The SR-15 read's own:** Option C *"dominates all three on the ruling's own criteria"*, at **zero** retargets | **REFUTED ON ITS OWN ARITHMETIC.** C requires `timeline_store.rs:181` (an InviteResp-class junk frame alone in the mailbox) to exit NON-ZERO while T6/E2 (InviteResp-class invite residue) exits ZERO — **the same class, opposite exits, no observable distinguisher.** Its zero-retarget claim is false on that row | The Director re-deriving the read's dominance claim instead of accepting it. ⛳ **Chairs checking chairs runs in every direction — seat→Director (rows 41-43), read→seat (row 44), Director→read (this row).** C's sticky-fail SHAPE was RECORDED for a future widening lane rather than discarded |
| 46 | **HIT — the ceremony itself.** SR-15 was triggered for this region on the theory that a transport-adjacent repair needs an adversarial read | **HIT, and the strongest instance yet: every one of the five errors above was caught BEFORE A SINGLE PRODUCT BYTE EXISTED.** Two ruled options were replaced, four findings were dissolved by bounding Legacy out, and the suite cost fell from *"one retarget"* (false) through *"seven assertions across six functions"* (true, under N) to **ZERO assertion retargets** (under N-PRIME) | The ceremony running to completion — formalization → interim ruling → cold read → final ruling → verification — with each stage's output sealed before the next consumed it |
| 47 | **HIT — a cheaper proof replaced an ordered act.** The interim ruling ORDERED `na0689_p3_a2_stranding.rs` classified **by execution** on three triggers | **DISCHARGED STATICALLY ON ALL THREE**, and the file is UNAFFECTED: the two receive results are bound to underscore-prefixed names never referenced; `grep -c recv_none` = **0**; the only `qsp_*` token outside doc comments is in a `///` comment | The read answering by reading what an execution was ordered for. ⛳ **The transferable part: an ordered measurement is not always the cheapest sound one — and a seat that says *"two of three triggers are UNMEASURED"* rather than guessing is what makes the cheaper proof safe to accept** |
| 48 | **The promotion seat's queue block:** `Status: MERGING (PR #1756)`, modelled on NA-0739 and NA-0740, which were born the same way | **WRONG FORM FOR THIS KIND OF LANE.** Those were **records lanes whose own PR was the deliverable** and which no seat would ever `qwork`; NA-0741 is a **promotion for a fresh seat**, and those are born bare `Status: READY` (`74037706`, `2f3edebc`, `53d36304`) and flipped to `MERGING (PR #N)` later by the working seat (`ac80489f`). `^Status: READY` measured **0** on main ⇒ `qwork` could not admit the lane | ⚠ **`qwork` itself, days after the PR merged green.** A precedent was reused without checking that the property making it correct — *this lane's own PR is the work* — still held. **Nothing in the copied blocks' text says which kind of lane they are** |
| 49 | **The Director's verification of the promotion cargo:** thirteen items confirmed PRESENT, each by its own needle, with negative controls | **PRESENCE IS NOT ADMISSION.** The cargo was all there and the lane was still unworkable: the queue gate reads **one field** and it held the wrong value. **SR-20 is the rule for exactly this — execute the consumer — and the queue gate WAS the consumer** | The failing `qwork` run. ⛳ **Both chairs verified the record and neither ran the thing that reads it**; the fix's own PR then executed the helper *before* opening, which is the corrected practice |
| 50 | **The FINAL directive's §6** (the Director's), justifying the skip marker's `id=` field: *"`item.id` is relay-assigned and already emitted unredacted by `recv_dup_skipped` (:527), `recv_item` (:1059) and `ack_replay_unrecoverable` (:1225-1229)"* | ⚠ **NOT UNREDACTED — MEASURED `id=<redacted>` ON EVERY EMISSION.** The marker layer redacts by VALUE SHAPE, not by key (`should_redact_value` → `looks_high_cardinality`: `len() >= 24` and contains a digit), and a relay item id is a UUID. The SAFETY conclusion is unaffected and is strictly stronger than claimed — but a CONSEQUENCE is lost: F-20's marker tax was recorded as *"every redelivery emits a fresh `recv_frame_skipped` carrying the SAME `id=`"*, and **an operator cannot observe that**, because every id renders identically. ⇒ the tax is real and its diagnostic is blind | The impl seat's FIRST EXECUTION of the marker. ⚠ **No census could have caught it:** the claim was about RENDERED output and every instrument aimed at it was reading SOURCE |
| 51 | **The impl seat's own**, written BEFORE the act when T7 measured 16 skips for 4 planted frames: *"with `pull_lease_secs=60` and everything else identical, T7 emits exactly 4 skips, `count=4`, delivers ≥1 and exits 0; if it still emits 16 the diagnosis is wrong and I re-diagnose rather than tune"* | **HIT, exactly.** `count=4`, delivery restored, rc 0 ⇒ the under-delivery was **lease expiry between rounds**, not a defect in §5.1's round condition | The refutation condition was stated before the run, so the pass is evidence rather than a tuned green. ⛳ The same measurement produced the lane's own new residual (§C of the evidence doc), which the directive does not name |
| 52 | **STOP 008 §2 (NA-0741's own):** *"the edit set is reproducible from these"*, written as the addendum that existed specifically to close evidence-completeness gaps | **FALSE AS SEALED.** `apply_edits.py:46` reads its `frameclass.rs` PAYLOAD from beside itself, and the payload was sealed nowhere. On a clean checkout the instrument dies at its FIRST ACT with `FileNotFoundError`. ⇒ **the addendum written to close completeness gaps shipped one of its own** — the instrument sealed, its payload not | ⚠ **The Director EXECUTING the sealed instrument (SR-20).** The seat verified the instrument's PRESENCE and never ran it from its sealed location. **Presence is not reproducibility** — the same shape as row 49's *presence is not admission*, one turn later and in that seat's own chair |
| 53 | **HIT — the instrument was fail-closed and exactly correct once fed.** It asserted every anchor unique, refused on a no-op, hash-guarded the three protected assertions, and regenerated all five landed files **byte-identically** | **THE FAILURE WAS COMPLETENESS, NOT CORRECTNESS**, and the distinction is the row: a wrong instrument that runs is far worse than a right instrument that cannot start, because the first fails silently and the second fails closed at its first act | The same execution. ⛳ **A fail-closed instrument converts an incompleteness into a loud stop rather than a wrong answer** |
| 54 | **NA-0742's own sealed directive:** the lane-1 protected region is `transport/mod.rs:1186-1250`, 4281 B, sha256 `6be034fe77f03006` — carried from lane 1 with *"re-derive rather than inherit"* attached | **THE LINE NUMBERS IDENTIFY THAT REGION ON NEITHER TREE.** At lane 1's own base `788c8de5` `:1186-1250` hashes `b22d305c`; at the landed main `c7aedec9`; the pinned content sits at **`:1232-1295`**, ~46 lines lower, displaced by lane 1's own insertion. **The sha reproduces exactly under BOTH slicing conventions — only the locator was wrong, and the 4281 figure is off by 4 because a byte count is an artefact of how the window was cut.** The same lane's `:521-1252` per-item-loop pin matches **neither construct's extent** either (brace-matched: `:527-1297`) | ⚠ **EXECUTING the check rather than restating it.** The first mismatch was 3 bytes and read like a boundary quibble; **refining was refused and the window was ENUMERATED** — exactly one window in the file carries that sha. ⇒ **a byte-unchanged pin must anchor on a line UNIQUE IN THE FILE plus a length plus the hash, and must state its slicing convention** |
| 55 | **[Director]** the blessed design block's PART 3 stated no mode gating, and the formalization brief inherited it | **A LEGACY AMPLIFICATION OF UP TO 16×–128×.** A mode-blind scan would enlarge `invite finish`'s pull from 1 to as many as 128 frames on a **delete-on-pull** server, turning a one-frame loss into a batch loss. Measured basis: `relay_inbox_pull` resolves the mode from `crate::resolve_ack_mode(None)` and the Legacy URL carries no `ack=lease` | ⚠ **The SR-15 cold read.** The defect was invisible to the chair that wrote BOTH the design and the brief — the same chair reviewing its own premise. ⇒ **lane 2 is re-ruled LEASE-ONLY END TO END** |
| 56 | **[Director]** the read brief's `LegacyCompat` pair cited as `:1035`/`:1130` | **A HEAD-LIMITED GREP** — an instrument narrower than the claim it was offered for, settled against the brief by the read | The read. ⚠ Same family as SR-21, and it fired in the Director's chair |
| 57 | **[Seat]** the consumer census's idiom 4, offered as the enumeration-before-replacement pass | **BLIND TO 17 OF 32 FILES.** It matched library symbols and could not see any test driving the commands through CLI argv pairs (`&["invite","finish",…]`) — `handshake_mvp.rs`, `receive_e2e.rs`, `same_host_client_to_client_e2e.rs` among them. The census of record is **32 files / 118 `#[test]`**; "52" is retired with its instrument named | The read's union instrument. ⚠ **The seat had DISCLOSED this exact single-line-needle blindness (67%) earlier in the same document — and did not turn it back on the one instrument the claim rested on** |
| 58 | **[Seat]** `record_seen_and_queue_ack` has *"exactly 10 call sites"* | **NINE.** `:620, :696, :774, :848, :921, :1054, :1132, :1228, :1433`; `:546` is a **COMMENT** and `:1451` the definition | ⚠ **The read.** The miscount was written **in the section drafting F-C's cure for counting mentions as constructs.** The reachability conclusion is unaffected |
| 59 | **HIT — [Read]** the sealed census instrument was **EXECUTED FROM ITS SEALED LOCATION** | **The payload cure paid on its FIRST successor.** NA-0741's *"an instrument's payloads seal with it — the test is not is-the-script-there but does-it-RUN-there"* was applied by the next reader, unprompted | The read's own method ⛳ |
| 60 | **HIT — [Read]** server parity (the pinned in-process server vs the production relay's Legacy DELETE) proven **three ways**, blob-identical | **A STRONGER CLAIM BOUNDARY THAN THE DIRECTIVE ASKED FOR, obtained for free.** The loopback result now transfers to production rather than being fenced to the test server | The read ⛳ |
| 61 | **[Directive §4]** *"Both names are PLANTED by this directive — the impl seat re-sweeps split by path class and **expects records-class hits, not zero**"* for `invite_scan_summary` and `producer_ack` | **ZERO files tree-wide for both**, measured at the impl base with the split-by-path-class sweep the directive ordered. The planting documents live in the **OPERATOR tree**; the sweep's scope is the **REPO**, and the promotion's landed records never name either marker ⇒ **WF-0087's plant hazard is scoped to the tree the sweep covers**, and "banked" is not the same event as "landed" | The ordered sweep itself, run with both controls: positive `recv_frame_skipped` returned **8 files** in exactly the recorded 6-records / 1-source / 1-test split, negative returned 0. ⇒ **a prediction about planting must name WHICH TREE it plants in** |
| 62 | **[Seat]** the CLI-argv-pair census instrument, written to find every test driving the three commands | **CORRECT ONLY BECAUSE IT WAS BUILT FROM THE BYTES.** The blindness control run beside it — the SAME needle applied line by line — sees **8 of 32 files, leaving 24 (75%) invisible**, because the prevailing argv form wraps each token onto its own line. The union census reproduces the ruled **32 files / 118 `#[test]`** exactly, `A−B=0`, `B−A=17`, unexplained 0 | ⛳ **A control the instrument ran against ITSELF.** This program had already recorded the single-line-needle shape at **67%** on `emit_marker`; measured here at **75%**, on a different construct, in the same lane that inherited the lesson. **Carry the control, not just the cure** |
| 63 | **[R345 §3(a)]** the a2_sig exit is driven *"through the existing `hs_rng_failure_forced("QSC.SIG.A2")` seam — **zero product bytes**"*, i.e. T8 is executable as ruled | **THE SEAM BUILD DOES NOT COMPILE AT MAIN.** `RUSTFLAGS='--cfg qsc_rng_failure_test_seam' cargo check -p qsc --lib` fails with **4 errors in `qsl/qsl-client/qsc/src/vault/mod.rs` (`:570 :578 :728 :733`)**, reproduced on a worktree of pristine main with **ZERO modified files** — so T8's ruled mechanism is not merely inert in the default gate (D-0883), it is **unbuildable**, and with it every `#[cfg(qsc_rng_failure_test_seam)]` arm across six existing test files | ⚠ **EXECUTING the ruled mechanism instead of writing to it.** No workflow builds that cfg, so nothing could ever have gone red; the only tell was trying to run it. **A seam nobody compiles is a seam nobody has** — filing PROPOSED, not minted |
| 64 | **[Seat]** the §3.8 protected-region pin checker, written specifically to enforce *"anchor on a line UNIQUE IN THE FILE, never on line numbers"* | **TWO OF ITS THREE ANCHORS COULD NOT BE LOCATED AT ALL** on the first run — `0 occurrences` — because the seat hardcoded a **plausible indentation** for `for item in items {` and `'pull: loop {` instead of measuring it (8 and 4 spaces, not 12 and 8). A pin that cannot be located is strictly worse than a stale one: it reports failure on a correct tree | ⚠ **The instrument written to cure the family fell into the family**, one lane after STOP 002 recorded it — the third such self-hit this program has logged. Cured by **ENUMERATING** (`l.strip() == needle` over every line) rather than refining, per SR-21 |
| 65 | **[Directive §6 + the lane-1 census]** *"THE RETARGET LIST — **TARGET ZERO** … nothing in §3 changes an existing assertion"*, enumerated over five idioms | **FALSE: `na0741_frame_class_dispatch.rs:889` FAILS on the repaired tree.** It asserts a completed invite leaves a `recv_frame_skipped class=invite_resp` in the redeemer's inbox — **the exact residue this lane removes at its source** — so the redeemer's receive now reports `recv_none`. ⚠ **The census measured the wrong property:** every idiom enumerated consumers of the COMMAND's outputs (`invite_finish=ok`, error codes, `--max`), and **none asked which tests assert that the RESIDUE EXISTS** | ⚠ **Executing the consumer's whole file rather than sweeping for its needles.** The sibling assertion at `:866` survives only because that arm never runs `handshake poll`. ⇒ **when a lane REMOVES a state, sweep the tests that ASSERT THAT STATE, not the ones that call the command.** Reported as a STOP; **nothing retargeted** |
| 66 | **[Directive §5 T5p]** *"A redelivered already-processed frame → decode-reject fall-through → `Ok(())`; **the retry's consume+ack lands `acked=1`**"* | **HALF RIGHT, AND THE WRONG HALF MATTERS.** The fall-through and `Ok(())` reproduce exactly; the ack does **not**: measured **0 producer-ack markers on the retry and 1 frame still resident**. An already-processed A2 no longer decodes into a consuming branch, so it reaches `hs_emit_decode_reject; continue`, which by design never acks ⇒ **the crash cost is NOT uniform across the three callers** — finish's retry re-consumes and its ack lands (`acked=1`, measured), the poll's cannot, so its frame is retired by **no retry at all** and ages out only on the relay's retention TTL | ⚠ **A fault proxy answering the ack route 500 — and NEVER 404**, since `transport/mod.rs:3167` maps NOT_FOUND to `LegacyComplete` and would have turned the injected failure into a success path. Both numbers are now pinned by the arm, so a successor that changes either has to say so |
| 67 | **HIT — [Directive §2(f)]** E3 restated in the INSTRUMENT's terms — *"the store has no `acked` column, so `rows = 0` and `unexplained = 0` is the measurable proposition"* | **DISCHARGED, and it discharges lane 1's owed E3(b), which died a MISS ON PREMISE for want of exactly this mapping.** Positive control after `invite redeem`: `routes=1 rows=1`, the A1 resident in the slot at `head8=01 01 01 0C`. After the full flow with acks landed: **`routes=0 rows=0 unexplained_routes=0 unexplained_rows=0`** | ⚠ The committed harness pins `StoreConfig::path = ":memory:"`, so **there was no SQLite file for the sealed `census.py` to open** — found by reading `StoreConfig::default()` rather than by a failed run. A clone-only harness stood the same real relay up file-backed through `qsl_server`'s PUBLIC API, changing no product byte and no committed harness. ⛳ **Mapping an expectation onto the instrument that must answer it is what makes it dischargeable** |
| 68 | **[Seat]** the PR-number placeholder fill, run as a whole-file replace because "the placeholder is mine" | **IT STAMPED `#1760` ONTO NA-0741's D-1377 TRACEABILITY ROW**, which carries an unfilled `PR #<N>` of its own — measured as 2 occurrences where the seat expected 1. Restored **byte-exactly** (another lane's landed record is not this lane's to edit) and re-applied **bracketed to the D-1379 row**, with the final diff measured at exactly two lines | ⚠ **The applier's own post-condition print, which reported `placeholders filled = 2`.** ⇒ **BRACKET-FIRST GOVERNS PLACEHOLDER FILLS EXACTLY AS IT GOVERNS LEDGER LANDINGS** — locate the enclosing record, then the anchor inside it; a needle that is unique *to your intent* is not unique *in the file* |
| 69 | **[Property]** the retarget census — five independent idioms enumerating consumers of `invite finish`'s outputs (`invite_finish=ok`, the error codes, `--max`, the marker names), concluding **TARGET ZERO** | **FALSE: one assertion breaks.** `na0741_frame_class_dispatch.rs:889` asserts a completed invite leaves a `recv_frame_skipped class=invite_resp` in the redeemer's inbox — **the very residue this lane removes at its source**. No idiom asked **which tests assert that the state EXISTS**; every one asked who calls the command | ⚠ **The FULL SUITE, which is the only instrument that ran the consumer's whole file.** ⇒ **WHEN A LANE REMOVES A STATE, SWEEP THE TESTS THAT ASSERT THAT STATE, NOT ONLY THE TESTS THAT CALL THE COMMAND.** A repair's blast radius includes every expectation that ENCODES the defect |
| 70 | **HIT — [Method]** the full-suite baseline, run to completion on the pristine tree **before any edit existed**, as required for attribution | **IT BECAME THE A/B THAT PROVED THE RETARGET'S CAUSE IN ONE COMPARISON.** The same test `both_mailboxes_of_a_completed_invite_receive_cleanly` **PASSES on the pristine baseline and FAILS on the repaired tree** — so the failure is this lane's change and not a pre-existing red, a flake, or an environment artefact, and no further investigation was needed to say so | ⛳ **The mandated baseline, used as a CONTROL rather than as a formality.** ⇒ **a before-run is not paperwork: it is the only artefact that can distinguish "I broke it" from "it was already broken", and it can only be produced BEFORE the edit** |
| 71 | **[Director]** the NA-0743 brief's §2(b) asserted, of six cited sites in `scripts/demo/qsc_remote_handshake_smoke.sh` (`:351-352`, `:353-355`, `:448`/`:449`/`:468`), that *"every one is STALE; re-derive by content, never adopt"* | **MEASURED FALSE — all six are CORRECT at `ae2047e6`**, with a **structural** proof rather than a coincidence: the only commit touching the file since `5201c275` is `fbf502c1` (NA-0737's ENG-0192 repair), whose diff is **+2/−2 IN PLACE** at `:375`/`:388` (`git diff -U0` → exactly `@@ -375 +375 @@` and `@@ -388 +388 @@`), so **no line number shifted anywhere in the file**. Independently corroborated by `IMPROVEMENT_LEDGER.md:4600`, which records the same sha and the same 482 lines | ⚠ **The very re-derivation the same sentence ordered.** ⇒ **asserted-without-measuring, this time about STALENESS ITSELF** — and the error runs in both directions: adopting a stale number, and propagating a false claim that a correct number is stale. **An in-place substitution changes bytes without moving anchors**, so "the file was touched" never implies "the anchors moved" |
| 72 | **HIT — [Method]** the re-derive-by-content discipline, applied to anchors a directive had already labelled stale | **IT PRODUCED A PROOF INSTEAD OF A SHRUG** — not merely "the numbers happen to hold" but *why* they must, from the intervening commit's own diff shape. ⛳ And in the same turn the `status=established` consumer census — 42 occurrence lines tree-wide with **no glob**, 7 executable, **7/7 classified** — **reproduced the tree's OWN ratified ENG-0194 table (`IMPROVEMENT_LEDGER.md:4820-4825`/`:4831`) cell for cell**: the same four bare-token substring consumers, the same three `established_recv_only` consumers ruled out | ⛳ **Two independent instruments returning one answer**, which is a different and stronger thing than one instrument returning an expected answer. ⚠ A blindness control fired in passing: the recursive `.gitignore`-honouring route returns **41** against `git grep`'s 42, blind to `docs/governance/evidence/NA-0737_as_built.md` |
| 73 | **[Director]** R348 §3 sealed **E7** with the clause *"X5 per E3"* — carrying the post-re-handshake pair NA-0738 measured into the `drop-reorder` scenario | **MISDRAWN AT THE SEAL, ruled so from the Director's chair at R351 §1.** NA-0738's pair was measured **n=3, all under `happy-path`**; `drop-reorder` had **never been run** by anyone. The clause transplanted a measurement across a boundary its evidence never crossed, and the arm it was sealed against duly missed it. ⛳ **The two LOAD-BEARING clauses of the same expectation — exit 0, and checkpoint equality at default knobs — both HIT**, so the repair is scenario-insensitive and the workflow's outcome is unaffected in both arms. ⚠ **This is the recorded property's THIRD instance: a sealed expectation wrong in its CONDITIONS rather than its values** (NA-0738's own X4 seal, NA-0742's retarget-target-zero, and now this). |
| 74 | **HIT — [Seat]** the implementing seat's handling of a miss it could have argued away | **IT REFUSED TO ARGUE THE MISS INTO A HIT.** The seat had a genuine case available — the checkpoint passed, the workflow gates on the exit code, X5 is recorded and never asserted — and it **still reported E7 as a MISS**, on the ground that the sealed text said *"X5 per E3"* and X5 was not per E3, then stopped for a ruling instead of proceeding. **The discipline holding under an incentive to bend it is the finding**, and the miss it preserved became **ENG-0198**. |
| 75 | **[Director]** the ruled §3.4 comment text, issued verbatim for the repaired test | **IT CARRIED TWO BASE-SCOPED LOCATORS AND WAS AMENDED SERIALLY, ON THE SEAT'S MEASUREMENTS.** A comment written for the OLD file's geometry was ruled verbatim into the NEW file **twice**: `:343-346` (amended at R349 §2) and *"the comment ten lines below"*, which **measured fifteen** in the repaired file (amended at R350 §1). ⚠ **The cure this lane applies to the script — content anchors, never counted offsets — reached its own ruled prose only under the seat's measurement.** |
| 76 | **HIT — [Method]** staging every edit and holding it UNAPPLIED until the baseline settled | **IT MADE BOTH AMENDMENTS FREE.** The locator defects were found while the edits were still staged, so correcting ruled text cost two payload rebuilds and nothing else — no commit to amend, no landed record to mark. ⛳ **The pattern of stopping before landing is what made the errors correctable at zero cost**, and the same property paid a third time when the sweep ordered at R350 §1 found three more locators in the seat's own prose before any of them landed. |
| 77 | **[Director]** the NA-0744 formalization brief's premises, consolidated | **FIVE MEASURED FALSE AT BASE `d484c065`, AND EVERY ONE CAUGHT BEFORE IT COULD LAND IN REPO TRUTH.** *"38 sites"* — no interpretation returns 38; the ledger's own figure is **28** (call sites 2, field keys 14, distinct vocabulary 35, return arms 39, token occurrences 28). *"the receive half is mute"* — it carries **43 emission sites / 39 marker names**, more than any push-half count; the truth is **sharper and narrower**: the **pull/ack HTTP transport boundary** is mute, 26 exits and zero markers. *"`mailbox_hash8` is the precedent"* — occurs **zero** times tree-wide; the field is `mailbox_hash` and **the spelling is load-bearing**, because `should_redact_value` (`output/mod.rs:325`) blanks any key containing `token`. *"loopback only for all runs"* — **impossible**: `qsc relay serve` has two routes and the invite-capable relay is a test-only dev-dependency, so no shell-runnable invite relay exists. *"the first green in recorded history"* — **four greens exist**; the true figure is the stronger one, 189 days and 201 consecutive failures. ⇒ **The Director's recorded failure shape, five-in-one-brief edition**, and the countermeasure that caught all five is the seat's **measure-don't-copy** discipline: a brief's figure is a claim, not an input. |
| 78 | **HIT — [Seat/Method]** the sealed expectation `P-A5`, written before any measurement — **REWORDED at R353 §9 to size the claim honestly** | **IT NAMED THE FAILURE AND ITS DISCRIMINATOR BEFORE THE FAILURE OCCURRED, AND WHAT IT CAUGHT WAS THIS SEAT'S OWN WORK.** P-A5 predicted that any id sweep run after the SR-14 banking would report the lane's own brief as evidence its ids were taken, and specified the cure in advance: split by **path class**, and treat the operator-tree count as **expected non-zero and not evidence of allocation**. It then happened in the strongest form — a **sealed** instrument, re-executed under SR-20 to prove it runs where it sits, printed an **affirmative `TAKEN`** about the **free** id `R352`. ⚠ **Sized honestly per the read: this is a correct RE-APPLICATION of WF-0087's already-filed cure, not a new discovery — two seat implementation defects (a name-based filter blind to the shared mutable pointer; a sweep run after its own banking) and one prediction that caught them.** The HIT is real and it is that size. Recorded as an amendment beside **WF-0087**; **no new countable id was minted** (R353 §9). |
| 79 | **[Director]** `E5` sealed on `items_count=0` as the ENG-0192 discriminator | **THE BASE MAKES THAT OBSERVABLE IMPOSSIBLE, AND IT WAS KNOWABLE WITHOUT RUNNING ANYTHING.** `qsl-server` `src/lib.rs:1209-1210` returns **204** whenever `outcome.items.is_empty()` — **identical at both revs that matter** (`131d63f4`, the `[dev-dependencies]` pin; `37ec8207`, the rev recorded [O] as running on the box) — and three server tests name it. The client maps 204 to `Ok(Vec::new())`, so the OK arm is reached **iff `items` is non-empty** and `items_count` is **≥ 1 by construction**. ⇒ **the sealed expectation's CONDITIONS failure class, FOURTH recorded instance** (after NA-0738, NA-0743's E7 X5 clause, and R351's misdrawn pair) — and the first in which the **Director's seal of another seat's draft** carried it. ⛳ **Read mechanically it would have PASSED by recording that its own subject never occurred.** Re-sealed on the strong form the lane had left unstated: `mailbox_hash` + `status_code=204` on the four non-receive pull callers, which publish nothing at all about where they looked. |
| 80 | **[Director]** ASK-3's *"REUSE the push dialect"*, issued as a scope | **IT IMPORTED A LAYER'S STATUS MAP ACROSS THE VERY BOUNDARY `D-1324` EXISTS TO KEEP.** Reused verbatim, the push status classifiers publish **false values at 403 / 400 / 204 / 404**, because the pull's arms (`:3122-3127`, `:3170-3174`) mean different things from the push's. **The tree's own comment states the rule being broken** — `relay_forbidden` / `access_forbidden` / `access_refused` differ *on purpose*, each site taking the form its own neighbours take. ⇒ **the vocabulary is adopted; the MAPPING is the layer's own**, and only the five `reqwest::Error`-based classifiers (layer-neutral) are reused. Caught by the cold read against the tree's own comment, not by the chair that wrote the scope. |
| 81 | **[Seat]** the eight emission sites, cited as placements at named lines | **THE COUNTS WERE ALL RIGHT AND ALMOST EVERY ANCHOR WAS WRONG — 3 of 13 exit rows correct, six of eight sites naming the wrong construct, and `A3`/`A4` TRANSPOSED ONTO EACH OTHER.** An implementation seat working from the table would have placed a **network-error diagnostic inside a parse-SUCCESS arm** and an `acked_count` line on the **NOT_FOUND** arm. The line numbers were never re-read from the pull/ack bytes. ⇒ **"a line number identifies a location, never a function"** — the rule is recorded at `transport/mod.rs:3069-3070`, **in the very file this lane instruments**, and NA-0742 was made to apply it one lane earlier. Cured by pinning every site to its **enclosing construct** and quoting its bytes; line numbers survive only as navigation aids, marked as such. |
| 82 | **[Seat]** *"the workflow file is DRAFTED VERBATIM as a lane artifact"* | **CLAIMED THREE TIMES AND DELIVERED AS PROSE.** The brief said it, the directive repeated it, the amendment restated it — and **no `.github` bytes existed anywhere**. ⚠ **Three chairs confirmed an artifact none of them held**: the Director ruled on it twice, the seat asserted it, and only an adversarial read that went looking for the file found there was none. ⇒ **a claim that a document EXISTS is checkable in one command, and is exactly the kind of claim confirmation cannot substitute for.** Cured: both artifacts now exist as bytes, are carried inside the directive itself, and are covered by a sealed arm (**E9**) that parses the yaml and asserts the roster name equals the workflow's `name:` field against a mutated-name control. |
| 83 | **HIT — [Read]** the cold read's own `RULING_*` delta, C8 | **THE HAZARD DEMONSTRATED BY ITS OWN INVESTIGATOR, AND CLEANLY CLASSIFIED.** Checking the R-id space, the read found the operator tree's `RULING_*` set had grown by exactly one file since the measurement it was auditing — **this lane's own banked ruling**, planted by the SR-14 first act that the same ruling ordered. The read **named it as its own delta and classified it OUT** rather than reporting a discrepancy. ⇒ **the WF-0087 plant hazard is now demonstrated in four chairs — the sweeping seat, the ruling, the sealed instrument, and the adversarial reader — which is the argument for the union-of-both-routes instrument being procedure rather than advice.** |
| 84 | **[Directive]** §3.1(d)'s *"REUSE only the five `reqwest::Error`-based classifiers — they are layer-neutral"*, issued as the correctness instruction for the send-error arm | **THE CLASSIFIERS ARE HANDED TEXT THAT CANNOT CONTAIN WHAT THEY TEST FOR, SO THE ARM E4 EXISTS FOR HAD NEVER FIRED.** Reused exactly as instructed, the dead-endpoint arm published `diagnostic_class=not_timeout` for a REFUSED CONNECTION and **E4 MISSED**. The cause is that `*_for_send_error` passes `err.to_string()`, while a `reqwest` connect failure keeps the operating system's reason in its **`source()` chain**. ⛳ **Measured on the PRE-LANE build that the PUSH half emits the identical wrong value** ⇒ the defect is INHERITED, not introduced by this lane, and it has been unreachable since NA-0554 because `relay_push_diagnostics.rs` only ever drives a LIVE fixture returning status codes. Filed **ENG-0199**; the pull half is cured by feeding the SAME pure `_from_parts` classifier the full chain. ⚠ The reuse instruction's own premise — that the classifiers work — **measures FALSE**, and only building the arm the directive ordered could reveal it. |
| 85 | **[Directive]** §3.2's drafted `marker_values_for_event`, carried as bytes and ordered landed | **`[^\n]` IS A BRACKET EXPRESSION OVER THE LITERAL `\` AND `n` IN ERE, NOT "ANY NON-NEWLINE".** Under `rg` the draft works; under the `grep -E` fallback the parent script deliberately keeps alive, it **truncates every marker line at its first `n`**. Measured: `grep -Eo 'event=x[^\n]*'` on `event=x op=pull noise` returns `event=x op=pull `. **Every field the helper harvests sits AFTER `op=`**, so on a runner without ripgrep it would have published `diagnostic_unavailable` for all of them — a vacuous pass with no red anywhere. Landed as `.*`, with the measurement inline. |
| 86 | **[Seat, inherited]** the three region pins recorded by the formalization seat, offered to the implementing seat as pins to verify against | **1 OF 3 EXACT AND 2 OFF BY ONE IN OPPOSITE DIRECTIONS, ON A FILE THAT NEVER MOVED.** `relay_inbox_pull_mode` reproduced exactly (3084-3129 / 1932 B / `5b3392af020dd08b`); `relay_inbox_ack` measured **3134-3176 / 1626 B**, not 3134-3175 / 1624 B; `producer_ack` measured **3202-3211 / 266 B**, not 3202-3212 / 267 B. `transport/mod.rs` was verified byte-identical since `d484c065`, so **the disagreement is instrument-versus-instrument, not tree-versus-tree** — the numbers were simply not reproducible by bracketing each fn to its enclosing construct. ⇒ *a pin is base-scoped AND instrument-scoped*; E8 used ONE instrument on BOTH sides, with a firing negative control. |
| 87 | **[Program]** `scripts/ci/preflight_qsc_impl.sh`'s `required_gates` list, read as the local pre-push gate set | **MAIN FAILS TWO OF THE THREE, AND NO WORKFLOW RUNS EITHER OF THEM.** Measured at `0d75a6a2`: `cargo fmt -p qsc -- --check` **rc 1 with 246 hunks** across the crate; `cargo clippy -p qsc --all-targets -- -D warnings` **rc 101 with 29 errors**; `git grep` finds both **only inside that script** and nowhere in `.github/`. ⇒ a seat adopting them as gates **reports failure on a correct tree**. The base-valid instrument is a DELTA: this lane measured `transport/mod.rs` **13 → 12** fmt hunks (0 introduced, 1 removed) and **0 of 29** clippy errors inside its changed line ranges. |
| 88 | **[Seat]** E3b's harness as first drafted — base build vs lane build, normalized by the parent's own id-strip list | **IT COULD NEVER HAVE PASSED, AND ITS OWN CONTROL SAID SO BEFORE IT WAS TRUSTED.** The draft ran `identity rotate` INSIDE each compared arm; a keypair is minted fresh per run, so `fp=` / `identity_kem_pk=` / `identity_sig_pk=` differed every time. The determinism control — two runs of the SAME binary — failed at byte 215, line 4. ⛳ Cured **at source** (provision once, clone the state, compare only the flow steps) rather than by widening the normalization, which would have silently outgrown the parent's inherited `:511-517` list. |
| 89 | **HIT — [Directive]** E5's premise that the four non-receive pull callers *"publish nothing at all about where they looked"* | **CONFIRMED, AND THE TRUE STATEMENT IS SHARPER THAN THE ONE SEALED.** Measured against a 204 relay on the pre-lane build, `handshake poll` **exits rc 0** and emits exactly one marker — `handshake_recv msg=none ok=true`. So it is not merely silent about the mailbox: **a poll of the WRONG mailbox is indistinguishable from a poll of an EMPTY one, at rc 0, under an `ok=true` marker.** The lane's line now answers it in place: `status_code=204 diagnostic_class=empty_mailbox mailbox_hash=<8 hex>`, with the caller's own output byte-unchanged. |
| 90 | **[Directive]** §1's authorized edit set, *"FIVE items, nothing else"* | **A NEW TEST BINARY ALSO REQUIRES A SHARD-MANIFEST ROW, AND THE EDIT SET DOES NOT NAME IT.** `scripts/ci/qsc_shard_check.py` fails closed when the committed manifest does not cover the test census exactly. Measured both polarities: pristine **rc 0** (census 134 / manifest 134); the new test present with the manifest untouched **rc 1**, `MISSING from manifest (present in tree): tests/relay_pull_diagnostics.rs` (census 135 / manifest 134); the row added **rc 0** (135 / 135). ⇒ the manifest edit is a **dependency of "New tests"**, not a sixth item, and without it CI goes red on a change that is otherwise correct. |
| 91 | **[Seat]** NA-0744's finding 3 — *"a new test binary REQUIRES a shard-manifest row"* — issued as a claim about **the manifest**, with a gate control executed both polarities | **THE INSTRUMENT WAS LINUX-MANIFEST-SCOPED WHILE THE CLAIM COVERED BOTH MANIFESTS, AND MAIN WENT RED FOR EXACTLY THE HALF IT DID NOT EXAMINE.** `scripts/ci/qsc_shard_check.py` takes a `--manifest` argument and the tree carries **two**: `QSC_SHARD_MANIFEST.txt` (Linux, K=12, doc shard 11) and `QSC_SHARD_MANIFEST_MACOS.txt` (macOS, K=5, doc shard 4). Every polarity of the control — pristine rc 0, missing-row rc 1, row-added rc 0 — was run against the **default** manifest only, so it proved the claim on one file and was reported as proving it on "the manifest". On main `726c3c8d` the macOS gate failed in 8 s: `census 135 targets / manifest 134 rows / missing 1`, naming `tests/relay_pull_diagnostics.rs`. ⇒ **a control that exercises one instance of a plural noun does not discharge a claim about the plural** — the same shape as *a census of a NAME is not a census of the PROPERTY*, and the tell was in my own sentence, which said "the manifest" while the tool takes an argument. |
| 92 | **[Director]** the property offered for the CI-migration lane — *"the macOS manifest gate fires only on PUSH, so a missing macOS row is invisible to every PR"* | **MEASURED FALSE, AND THE TRUTH IS WORSE.** The workflow's `on:` is `{pull_request: {}, push: {branches: [main]}, workflow_dispatch: {}}` — unrestricted on PRs — and `manifest_gate` carries **no `if:` at all**, so it runs on every PR. It **DID** run on PR #1767 and it **FAILED there**: run `32145489027`, job `95738284200`, 7 s, and on the earlier commit too (`d299f194`, run `32145161961`). ⇒ **a missing macOS row is not INVISIBLE to a PR — it is VISIBLE AND NON-BLOCKING.** Measured cause: main's required-status-check set has **15 contexts** and the only macOS member is `macos-qsc-qshield-build`; **`macos-qsc-shard-manifest-gate` is not among them**, so a red gate cannot block a merge. ⚠ **This changes the remedy the CI-migration lane inherits**: not *"make it run on PRs"* — it already does — but *"make it required, or make its failure block"*, which is branch protection and therefore the operator's. Sentinel issues **#1768** (`CI RED: macos-qsc-sharded-suite on main`) and **#1769** (`CI RED: public-ci on main`, propagated by design, the same shape D-1382 recorded). |
| 93 | **[Seat]** *"no check state is read or claimed"*, carried through STOP 011 and STOP 012 as a discipline | **I TURNED "NEVER CLAIM AN UNSETTLED STATE" INTO "NEVER LOOK AT ALL", AND THE SIGNAL WAS ALREADY RED.** The standing rule forbids *concluding* from a half-finished run; it does not forbid reading a run that has settled. `macos-qsc-shard-manifest-gate` was **red on the PR for ~37 minutes** (13:58:18Z → merge 14:35:55Z) and one `gh pr checks 1767` before the hand-off would have shown it. Instead the stop said *"let the checks settle"* and passed the whole question to the operator. ⇒ **the rule's subject is the CLAIM, not the LOOK** — a hand-off for merge should carry a settled read, and *"I did not look"* is not the same discipline as *"I did not overclaim"*. Companion to row 92: the check was non-blocking, so nothing else would have stopped it either. |
| 94 | **[Director]** the succession packet's *"ENG-0199 … P3"*, handed to the incoming Director as the entry's grade | **THE FILED HEADING READS `⚠ P2`, AND THE PACKET'S FIGURE WAS AN INFERENCE, NOT A READ.** Measured at `04340072`: the entry declares once as `### ENG-0199 — ⚠ P2 — …` and carries **no `- Severity:` bullet — no bullets at all** — so the heading is the only place the grade exists and reading it is one `git grep`. The packet reached `P3` by analogy with **ENG-0197 and ENG-0198, both measured `⚠ P3`**, both filed the same week — a neighbour argument plausible enough to read as measurement, and one that invokes the very severity ladder the operator later used to RULE the re-grade. Caught by the successor seat's first tree-check, before anything cited it; the operator then ruled `P3` (2026-08-18, provenance [O]) and it is landed BESIDE the heading by this act. | MISS | **an inference that later turns out RIGHT is still not a measurement** — at the moment it was asserted, the tree said P2. Say where a figure came from in the same sentence that states it; the outgoing Director's own correction is what put this row here |
| 95 | **[Seat]** STOP 012's self-containment audit, whose per-item YES/NO answers were offered as the audit's result | **TWO OF ITS OWN CHECKS WERE FALSE AND ONLY THE RE-CHECK CAUGHT THEM.** *"ENG-0199's filed text"* returned **YES on the stop's own prose about the defect**, not on the ledger entry — mention-vs-carriage, the trap STOP 006 had already recorded; *"the E1 seal's content"* returned YES **correctly**, its decisive combination being restated inline, so that one was never a gap. A third needle in the same audit found **zero `§` references in a document that discusses `§3.1(d)`, `§3.4` and `STOP 008 §4.1`**. ⇒ the audit's published answer was right only because it was **re-run**. | MISS (self-caught) | **re-run before reporting a gap, and re-check the PASSES too** — a false YES and a false NO are one defect wearing different signs — and **suspect the instrument before the tree**. Companion to the handoff-closure ratification (R355 §3) landed by this act |
| 96 | **[Seat, NA-0742]** the ENG-0142 re-grade of 2026-08-16, cited by this lane's own §7(b) bullet as the precedent to follow | **THE PRECEDENT'S SHAPE IS SOUND AND ITS EXECUTION SPLIT THE BULLET IT LANDED BESIDE.** Measured at `04340072`: commit `a1c6c969` (NA-0742, D-1378) inserted the re-grade bullet at `docs/ops/IMPROVEMENT_LEDGER.md:3720` — **between the first and second physical lines of the `- Severity:` bullet it annotates** — so ENG-0142 now reads *"…which has read `⚠ P1` since the"*, then the entire re-grade bullet, then *"entry was filed 2026-08-09."*. ENG-0134 carries the identical transcription bullet **unsplit** at `:3666-3668`, which is the only reason the break is visible. **The anchor existed twice and the landing anchored on a FRAGMENT of one of them.** ⚠ **NOT REPAIRED HERE** — ENG-0142's remainder is outside this lane's bounds (brief §10); recorded for the act that owns that entry. | MISS | **a content anchor must be a WHOLE unit of the thing you land beside** — a needle matching the first physical line of a wrapped bullet will insert into its middle, and markdown renders the result without complaint. This lane's own §7(b) landing therefore went bracket-first: the ENG-0199 heading read out of the file's own bytes, asserted unique, inserted after the heading's following blank line and never inside a bullet |
| 97 | **[Program]** `scripts/ci/preflight_governance.sh`'s queue gate — `rg 'Status:\s*READY' NEXT_ACTIONS.md`, read as the count of READY lanes and used as this program's one-READY invariant | **IT HAS NOT REPORTED THE TRUE NUMBER SINCE 2026-08-02, AND IT IS WRONG IN BOTH DIRECTIONS.** The needle is **unanchored**, so it also matches a MENTION inside a historical `<!-- prior: STATE… -->` comment at `NEXT_ACTIONS.md:92`, whose prose reads `` `Status: READY` becomes `DONE 2026-08-02` `` — landed by `b76016b5`, NA-0690's promotion. Measured across bases: on every settled main since (`9d11e2bd`, `4c59ffda`, `21597277`, `ae2047e6`, `d484c065`, `04340072`) it reads **1** while the true count is **0** — *permissive*; on a promotion tree (`ea0ee23e`, NA-0744's own) it reads **2** while the true count is **1** — *blocking*, and it failed **this lane's correct tree** the same way (`FAIL: READY_COUNT=2 (>1)`, rc 1). ⛳ At `4025eb44`, NA-0744's impl PR head, the **only** matching line is that comment, so a `READY_COUNT 1` read there counted the comment and not a lane. ⚠ **NOT REPAIRED HERE:** `scripts/ci/**` is outside this lane's edit set, and the comment is a historical record that must not be reworded to make a gate pass. ⚠ **Blast radius measured, not assumed:** `git grep preflight_governance -- .github/` returns **0** — **no workflow runs this script**, so nothing in CI was ever gated on it, and `public_safety_gate.py`'s queue proof is a **different** instrument that parses a structured `status` field rather than grepping prose. | MISS | **an unanchored needle counts mentions, and a gate that counts mentions is not a gate** — the same declaration-vs-mention defect this program has recorded in id sweeps, `Class:` censuses and self-containment audits, here sitting inside a CI script and passing for three weeks. **A count that is right can still be right for the wrong reason: read WHICH lines matched, never how many** |
| 98 | **[Seat, NA-0745]** STOP 001 §10's closing assertion that, after filling this lane's own PR-number placeholder, **"exactly one `PR #<N>` remains tree-wide"** | **FALSE AS STATED, AND THE FILL ITSELF WAS CORRECT.** Re-measured by the Director at the PR head **and** at base — identical, the act added none — **three lines match tree-wide**: `NEXT_ACTIONS.md:68`, NA-0744's genuine unfilled placeholder (the one the stop reported); `TRACEABILITY.md:1728`, NA-0741's genuine unfilled placeholder, **pre-existing** and already recorded at row 68; and `PREDICTION_LEDGER` row 68 itself, a **MENTION** quoting the token. The assert behind the sentence was `count(...) == 1` over **`NEXT_ACTIONS.md` alone**, while the sentence claimed **tree-wide** — an instrument narrower than its claim, SR-21's shape, in the stop's own closing sentence, the same class as rows 91 and 95. ⛳ The **fill** went bracket-first and was right; only the **claim about the fill** was wrong. Because it landed in no repo file it blocked nothing; disposed at **R357 §3**, and **both** placeholders are filled by NA-0746 (#1765 and #1758, each re-verified from `merged_at` at the edit). | MISS | ***bracket-first governs the CLAIM you make about a fill, not only the fill.*** State the needle's scope in the same sentence that states the figure — "tree-wide" is a promise about the instrument, not a flourish. ⚠ And this very row re-plants the token it counts: the honest form of the post-fill claim names its instrument, `git grep -F` over all tracked files, and is re-measured at the moment it is asserted |
| 99 | **[Director]** the NA-0745 formalization brief, whose **§6(d)** ordered the handoff-closure ratification ruled *"before any PR opens"* while its **§9** ordered the same stop to carry *"the PR number and head sha"* | **BOTH COULD NOT HOLD: A BRIEF IN TENSION WITH ITSELF**, recorded in the open by the Director at **R357 §4** as this chair's own drafting defect, the same family as row 20. The seat did not silently pick a reading: it **named the tension** and escaped by citing the **pre-existing R355 §3** as the ratifying authority, minting no ruling id and no standing rule, carrying the drafted text byte-verbatim with its source sha so the Director could rule from the artifact (SR-01), and leaving the **PR open, not merged** — so nothing reached repo truth ahead of the ruling. **ACCEPTED as the correct escape**, usurping nothing. ⛳ The same shape recurred immediately at NA-0746, where the successor brief sealed an arm (`A′ rc≠0`) that is **logically unsatisfiable** against its own companion arm, and two design constraints unsatisfiable as literally written — caught by the seat's measurement and confirmed by the SR-15 read. | MISS | **a directive's internal consistency is a CHECKABLE property, and checking it is the drafter's job before issuing and the seat's before executing.** Enumerate a brief's ordering obligations and its sealed arms against **each other**, not only against the tree. ⚠ When they conflict, **name the tension and take the reading that usurps nothing** — never reconcile it silently, because a silent reconciliation is the seat legislating |
| 100 | **[Director]** the NA-0746 formalization brief's sealed arm **E8**, whose control figures (`A′ rc 2 / B rc 2`) were carried across from a predecessor's record, and its constraints **C2** and the READY/MERGING **scope clause**, stated as satisfiable | **THE FIGURES DID NOT REPLAY AND TWO CONSTRAINTS WERE UNSATISFIABLE AS WRITTEN.** Re-measured at `9ac0870e` with perturbations COMMITTED: neither `rc 2` is producible — the count failure is **rc 1** and the hygiene-sentinel dirty-tree failure is also **rc 1**. Worse, E8 sealed *settled main COUNT 0 rc 0* **and** *A′ rc≠0* while A′ (the READY line removed) is **another count-0 state**, and the gate fails only above 1: **two count-0 states cannot yield different exit codes**, so the arm was **logically unsatisfiable**. Separately, **C2** required every check red on main to be settled-passing on the PR head while **`public-safety` is itself in main's red set** and cannot be green while it is the check being computed; and the **scope clause** had no source data at all — the gate's own `active_scope_paths()` returns **0 paths** for the incident's queue block, which carries no `Scope:` section. | MISS | **a sealed expectation's CONDITIONS are seal content, not preamble — and a figure adopted across a base is not a seal.** Re-measure every carried control at the base you are sealing for, and check a matrix's arms against **each other** for satisfiability before issuing. ⛳ The seat measured all four before building, which is the only reason the design was tested against a coherent seal at all |
| 101 | **[Seat, NA-0746]** the formalization's own sealed admit/refuse matrix and its census of the gate's bounded admission attempts | **THE MATRIX CARRIED ALL THREE RECORDED SEAL-FAILURE CLASSES AT ONCE, AND THE CENSUS WAS SHORT BY ONE LIVE VALIDATOR.** **VALUES** — E1's antecedent pinned the wrong variable. **CONDITIONS** — E4's second proof was asserted without an instrument to produce it. **OPTION SETS** — E7 and §6 sealed **two different counts** (21 vs 33) for the **same** command. And the admission-path census named **two** bounded paths where **three** exist: `validate_self_repair_bootstrap_pr` was wired the whole time, its root path set containing **both** the workflow and the gate file, eligible when main's `advisories` is red and the PR classifies `workflow_security`. The §3 C3 'price' paragraph therefore rested on a false premise. | MISS | **a census of admission attempts is taken from the DISPATCH SITE — the early-return and the attempts list — never from the profiles table.** The table enumerates *profiles*; the dispatcher enumerates *paths*, and they are not the same set. Same family as *a census of a NAME is not a census of the PROPERTY* |
| 102 | **[Read, SR-15]** the adversarial cold read commissioned on the generic red-main repair profile before any line of it was built | **A HIT — AND THE SECOND TIME THIS PRACTICE HAS OVERTURNED A DESIGN PRE-BUILD.** The read returned **7 BLOCKER / 13 MAJOR**; the Director ruled every blocker **independently fatal** and **REFUSED** the design outright. It did two things no chair had: it **constructed a measured false admission** against the proposed logic, and it **surfaced a wired-in third admission path** that the seat, the brief and the ruling had all missed. Cost: one formalization act. Avoided: a widened admission path through the program's most safety-critical gate, landed on the strength of a matrix whose own arms did not hold. | **HIT** | **an adversarial read pays for itself when it runs BEFORE the build, not after** — and the value is concentrated in what it does that the author structurally cannot: attack the design's *premises* rather than its execution. ⛳ Budget the read into any act that widens an admission path, and point it at the clause the author is proudest of |
| 103 | **[Director]** the v2 ruling + re-scoped directive of 2026-08-18, issued the same day row 99 landed the lesson *a brief's ordering obligations must be enumerated against each other before issuing* | **IT CARRIED TWO INTERNAL-CONSISTENCY DEFECTS OF ITS OWN.** (i) **§5 assigned a single `D-1385` to the impl PR**, which cannot hold against measured house practice — a promotion mints its **own** D-record and bumps the counter (`ea0ee23e` → `D-1382`; `4025eb44` → `D-1383`) — so following §5 literally would have left the promotion's own queue block citing a decision id with no record. (ii) **§5 and §3(e) assigned the §R4 rows to different PRs**, §5 placing *this ruling's records* in the promotion while §3(e) bundled those rows with `D-1385` in the impl act. **Both were caught by the seat MEASURING house practice rather than complying**, were flagged rather than silently reconciled, and were disposed at the flags ruling — (i) **AMENDED**, the Director recording the assignment as their own defect; (ii) accepted as the seat resolved it. | MISS | **the lesson a document teaches is not automatically applied to the document teaching it.** A ruling that lands a consistency rule is itself the next thing to check against that rule — and the check is cheap: enumerate every id the document allocates and every act it assigns, then read them against the tree's measured practice. ⛳ *Flagging a departure instead of silently complying is what produced the correction* |
| 104 | **[Director]** the NA-0747 formalization brief v2, whose §0 gave *"ENG-0195: 27 gateway commands"* and whose sealed arm A1 read *"27 functions (**ENG-0195's figure**)"* | **THE ATTRIBUTION WAS FLATTENED AND THE ENTRY SAYS 26.** The tree figure (27) was right; `ENG-0195` states **26** twice, and its own enumeration lists **27** names — so the brief fused a correct tree measurement with a record that contradicts it, and sealed the fusion as an expectation the executing seat would inherit rather than test. ⚠ The brief's *"a different count is a finding about ENG-0195, not a harness error"* was the right instruction and it fired — but only because the seat later opened the entry for an unrelated reason. | MISS | **a paraphrase of a record, placed inside a SEAL, converts a checkable claim into an inherited one.** A seal is the one place a citation must be quoted rather than summarised, because the seat is instructed not to question it. ⇒ when a brief attributes a figure to a named record, quote the record's own sentence beside it, or attribute the figure to the TREE and cite the record separately |
| 105 | **[Director]** the 2026-08-19 clearance of STOP 002, which verified at head `9209241e` and reported *"zero discrepancies of substance"*, including *"command census 27"* | **THE VERIFICATION CHECKED THE PARAPHRASE'S SUBJECT, NOT THE RECORD'S SENTENCE — the SR-01 shape.** `27` is true of the tree and was correctly confirmed; the clause actually at risk was *"re-measures **ENG-0195's figure** exactly"*, and confirming it required opening `IMPROVEMENT_LEDGER.md:4912`, which no arm of the verification did. The false clause was therefore cleared by a verification that was itself accurate about everything it examined. | MISS | **verifying a claim's OBJECT is not verifying the claim.** *"X re-measures Y's figure"* has two operands and the cheap one is the tree; the expensive one is the cited record. ⇒ for any clause of the form *record R says F*, the verification step is `grep R`, and an independent re-derivation of F does not substitute for it |
| 106 | **[Seat, NA-0747]** STOP 001, which re-measured the brief's mockup premise, its linkability premise and its folklore correction against the tree, and refuted all three | **THE ONE PREMISE NOT RE-MEASURED AGAINST THE RECORD WAS THE WRONG ONE.** Three premises about the TREE were tested and all three fell; the fourth — a premise about what a RECORD SAYS — was accepted from the brief's paraphrase and never checked, and it is the one that landed false in repo truth. The seat's own instrument was the discriminator it did not apply: the standing rule *a directive's enumeration is a description; measure against the record's own sentence.* | MISS | **a premise about a DOCUMENT is a premise, and it is cheaper to check than a premise about the tree.** Sorting the brief's claims into *about-the-tree* (measured, all refuted) and *about-a-record* (unmeasured, wrong) would have cost one `grep`. ⇒ enumerate a directive's citations as their own class and open each one, including the ones cited only in passing |
| 107 | **[Program]** R359 §6's order that the D-1388 records act carry an `ENG-0195` amendment recording the figure re-measured | **A HIT, AND THE STRONGEST ARGUMENT FOR ORDERING RECORDS AT ALL: THE ORDERED NEXT ACT FOUND THE DEFECT IN THE ACT BEFORE IT.** The amendment was ordered as routine bookkeeping — *record that the figure re-measures true*. Drafting it required opening the entry, and opening the entry is what exposed the 26-vs-27 contradiction, the phantom `vault_version_state` and the missing `ui_surface_changed`. Nothing else in the sequence would have opened it. | HIT | **an ordered record is an audit with a deadline.** A "confirm it still holds" record that nobody would otherwise write is precisely the act that re-reads a document under scrutiny. ⇒ when a lane cites a record it does not edit, order the amendment anyway; the cost is a paragraph and the yield is a re-read |
| 108 | **[Seat, NA-0747]** STOP 004, at the moment the merge had already landed the false clause and R359 §7 plus the clearance both authorized proceeding straight to the D-1388 PR | **A HIT — THE SEAT HELD RATHER THAN ENCODING ITS OWN RESOLUTION OF THE DIRECTOR'S FALSE PREMISE.** R359 §4 ordered the amendment to record *"figure and claim both re-measured TRUE"*; the figure had measured false. Proceeding was authorized, the resolution was obvious, and the seat had already written it down — and it still stopped, on the ground that the authorization **predated the finding**. The Director then superseded §4 in part rather than the seat silently departing from it. | HIT | **an authorization predating a finding does not cover it.** The test is not *am I permitted to proceed* but *does the permission know what I now know*. ⇒ when an ordered clause's premise measures false, the ruled instruction is the thing that stops, even when the corrective is unambiguous and the sequence says go |
| 109 | **[Seat, NA-0747]** STOP 003, sealed immutable at `21f4f86a`, whose §6 read *"Nothing has moved since STOP 002 … Main `bb7e6b9a`, unmoved"* — and the operator merged #1774 while it was being sealed | **A HIT ON THE CONVENTION, NOT ON THE PREDICTION: THE STALE PARAGRAPH WAS SUPERSEDED, NOT EDITED.** The sentence was true when written and false four minutes later. STOP 004 named the one paragraph it superseded and left the sealed file byte-unchanged — and STOP 003 had already discharged the case in advance, its §5 reading *"If it is already merged by the time this is read … Option A becomes the only path."* | HIT | **a stop is a point-in-time instrument, and its immutability is what makes a supersession legible.** Editing the stale line would have destroyed the evidence that the seat's ask was overtaken rather than wrong. ⇒ supersede by naming the paragraph; and where a stop's ask can be overtaken by an act in flight, discharge that branch inside the stop itself |
| 110 | **[Seat, NA-0748]** the pre-banking id sweep, written to `/srv/qbuild/operator/_na0748_stage/` and `tee`'d there, whose whole purpose was to establish that `NA-0748`, `D-1389`, `D-1390` and `ENG-0208` were unallocated | **THE INSTRUMENT PLANTED ALL FOUR OF THE IDS IT WAS MEASURING, IN THE SAME PATH CLASS IT WAS MEASURING THEM IN.** The operator-tree counts came back 10 / 12 / 9 / 10 — non-zero in every space, from a sweep run before a single record was written. Enumerated by file: **6 of 10, 5 of 12, 5 of 9 and 6 of 10 are the sweep's own script and its own captured output**; the remainder are NA-0747's stops and this lane's predecessor sweeps. **Zero are declarations.** | HIT | **freeness evidence is the PRE-artifact union sweep plus the repo count; any operator-tree count taken after your instrument exists is expected non-zero and is not evidence of allocation.** Split by PATH CLASS and classify DECLARATION vs MENTION before taking a maximum — a raw count of your own workspace refuses your own ids, with a plausible reason attached. WF-0087, now measured against the sweep script itself rather than against a banked record. |
| 111 | **[Seat, NA-0748]** the same sweep's SR-16 row-max arm, which reported `SR-16 max row: \| 1 \|` and would have made this lane's rows start at **2** | **THE NEEDLE WAS AIMED AT THE WRONG FILE.** SR-16 rows live in `docs/ops/PREDICTION_LEDGER.md`; the arm was pointed at `docs/ops/IMPROVEMENT_LEDGER.md`, matched the first `\| N \|` cell of an unrelated table there, and returned it with **exit 0 and no error anywhere**. Re-aimed: row max **109**, count **109**, positive control row 109 = 1, negative control row 110 = 0. | MISS | **an instrument that returns a number is not an instrument that measured your claim.** The tell was not an error — it was a figure implausible against a fact already in hand (rows 104–109 had just been read from another lane's record). ⇒ *carry one known value into every counting arm as its positive control*, so a wrong-file needle cannot come back quietly. Same family as the fixed-width window smaller than the thing it searched. |
| 112 | **[Director]** the brief's §0 warning that *"the plant classes apply, this brief mentions every candidate"*, with the cure of banking it before anything consumed it | **A HIT — AND THE PLANT THAT ACTUALLY MATTERED CAME FROM ONE LANE BACK, NOT FROM THE BRIEF.** The brief's own mentions were neutralised exactly as ordered (swept first, banked second). The live contamination was already **in repo truth**: `D-1389` and `D-1390` each return **1** tree-wide mention, both at `DECISIONS.md:43582`, inside **`D-1388`'s own IDS paragraph**, where they were written down as *that* lane's negative controls. A mention-counting sweep would have called both TAKEN and moved to `D-1391`. | HIT | **a predecessor's recorded sweep plants your candidates in the authoritative counter file itself.** The excluded-path list is not a property of your own workspace: the tree is a plant surface too, because a negative control is a record. ⇒ *derive on the DECLARING form, always; a mention count is never a freeness verdict, wherever it is taken.* |
| 113 | **[Director]** NA-0748's brief §1.8 and seal V1, both naming `src-tauri/Cargo.lock` as the lock the bump regenerates — carried unchallenged through the brief, the formalization stop and the clearance | **THE PATH HAS NEVER EXISTED — 0 commits touch it, against a positive control of 4 on the ROOT `Cargo.lock`.** Found by the SR-15 cold read (F-1), not by the chair that wrote it, not by the seat that executed it, and not by the clearance that verified it. The third zero-with-exit-0 of the lane: a pathspec that does not exist reports `0 files changed` and exits 0. | HIT (the read's) / MISS (the chair's) | **the chair whose job is catching phantom premises planted one, and a sealed expectation is not evidence that its subject exists.** ⇒ *name a path in a seal only after `git ls-tree` has returned it; the enumeration and the seal must be measured against the tree, not against each other.* |
| 114 | **[Seat, NA-0748]** the 16-item call-site table certifying `relay_server_info` at 1247 B byte-identical, and a public-surface taxonomy reporting 0 items removed / 4 added with its own "42 items" control | **BOTH INSTRUMENTS WERE WEAKER THAN THE CLAIMS THEY CARRIED.** F-2: the needle was a **prefix without a word boundary** and had certified `relay_server_info_from_parts`, 44 lines earlier; the real function is 1577 B (identical either way, so the verdict survived an instrument that could not have caught its own error). F-3: the taxonomy had **no CHANGED class at all** and conflated `pub(crate)` with `pub` — truth is **9 pub fn CHANGED**, and the seat's own control figure of 42 was itself the proof of the conflation (truly-`pub` = 22). | MISS | **a right answer reached by an instrument that could not have detected the wrong one is not evidence.** ⇒ *bound every needle at a word boundary, and require a taxonomy to carry the class that would falsify it — a partition with no CHANGED bucket cannot report a change.* |
| 115 | **[Read, SR-15 NA-0748]** the commission's ask for *"a third zero-with-exit-0"* in a lane that had already recorded two | **THE READ FOUND TWO MORE AND AFFIRMED THE CONCLUSION IT WAS SENT TO ATTACK.** It returned F-1 as a BLOCKER (the fictional path), F-2/F-3 as instrument defects inside the load-bearing table, F-12 (a seal quoting the superseded 152 where §1 had established 154) and F-13 (41 `qsc::` paths not reproducible; three independent regexes all give 40, so the 41 was a count asserted from reading a printed list) — every open closure closed clean, and the lane's substantive conclusion **affirmed on independent measurement**. | HIT | **the strongest confirmation available is an adversarial re-measurement that demolishes your instruments and leaves your conclusion standing.** ⇒ *commission the read against the load-bearing table itself, not against the finding — and count "the read found more than I asked for" as the commission working, not as an overrun.* |
| 116 | **[Seat, NA-0748]** STOP 002's own record that `R360` returned **0** content hits, written down as the lane's negative control | **WRITING DOWN THAT AN ID WAS FREE IS WHAT MADE THE NEXT SWEEP FIND IT.** Post-merge, `R360` reads **1** — at `docs/governance/evidence/NA-0748_as_built.md:67`, inside the seat's own ids table, recording `R360 = 0`. Route A (the banked `RULING_*R360*` filename, the declaring form) read 0 files against a positive control of 2 for `R359` ⇒ **FREE**, and the content hit classified as a MENTION. ⚠ The windowed needle `.{110}\bR360\b.{110}` returned **nothing** on a file that contains the token — the ugrep wrapper's bounded-repetition death — so a windowed grep returning empty is not evidence of absence here. | HIT (classified, not adopted) | **a negative control is a record, so the act of proving an id free consumes the evidence that proved it.** ⇒ *derive on the DECLARING form and classify every hit by file and path class; and never let a windowed grep's silence stand as absence — extract with `python3`.* |
| 117 | **[Seat, NA-0748]** the `Cargo.lock` census certifying **461 → 461** packages with **0 added / 0 removed / 0 version-changed** across the pin bump, offered as the proof that F-4's transitive-drift hazard did not materialise | **THE CENSUS'S DEDUP KEY WAS NOT THE THING IT COUNTED.** The Director's own count was **518 → 518**; both figures are true of what each measured — **518 is `[[package]]` ENTRIES**, the right unit, and **461 is DISTINCT NAMES**, with **47 names appearing twice** (`base64` at 0.21.7 *and* 0.22.1, `bitflags`, `der`, …). The seat's dict was keyed by NAME and kept only the last version, ⇒ **a version change inside a duplicated name would have been INVISIBLE to the very instrument certifying "0 version-changed."** Re-run on the `(name, version)` **multiset**: 0/0/0 — the verdict survives on an instrument that could have caught its own error. | MISS (the instrument) / HIT (the verdict) | **a census is only as strong as its key, and a key coarser than the property being asserted cannot falsify the assertion.** ⇒ *state the dedup key beside every census figure, and check that the key is at least as fine as the claim — "0 version-changed" demands a `(name, version)` key, never a name.* |
| 118 | **[Director]** design v1's `C2`, which stated the two-key concatenation `kem_pk ‖ sig_pk` was unambiguous and named its dependency — *"justified ONLY by §2(e)'s fixed lengths"* — as a challengeable clause | **THE DEPENDENCY WAS STATED AND NEVER MEASURED AGAINST THE CONSUMERS.** `§2(e)` proved the lengths fixed **in the wire path**; no off-wire code enforces them. `hex_decode` has no length bound and `contacts_add` checks none, so `SHA-512(DOMAIN‖0x00‖kem‖sig)` is **3,136-to-1 ambiguous** on two of three reachable entry paths — and the REQUIRED `formal-scka-model` was proving authentication under an injectivity premise the implementation did not satisfy. Found by the SR-15 read, pre-build, on paper. | MISS | **naming a premise as challengeable is not the same as challenging it.** A constraint that says *"this rests on X"* has done half the work; the other half is measuring X **at every consumer**, not at the one where it is obviously true. ⇒ *state the dependency AND the instrument that would falsify it, in the same sentence.* |
| 119 | **[Seat, NA-0749]** design v1's six sealed W-arms, every one of which read **HIT** on the tree carrying that ambiguity | **A SEAL MATRIX THAT PASSES ON A DEFECTIVE TREE IS EVIDENCE ABOUT THE MATRIX, NOT THE TREE.** Six axes were under-specified at once: the retirement sweep tested **symbol names**, not the **mechanism**; the pin arm had only accepting controls; the inventory came from three different universes; the voice branch silently extended to roles with no sealed value; the totality clause named no value; and the regression's prose described a sample as the full test. **Not one of them could have failed on the defect that mattered.** | MISS ×6 | **ask of every seal: what tree would make this MISS?** If the answer is *"one that differs in the thing I already changed"*, the seal tests the change, not the property. ⇒ *design each arm against the FAILURE, and prove it can fire before trusting the pass.* |
| 120 | **[Read, SR-15 NA-0749]** the commission to attack a construction two chairs had already sealed, with the sealed vector in hand | **THE READ BUILT AN EXECUTABLE CRYPTOGRAPHIC COLLISION ON THE SEAT'S OWN SEALED VALUES.** A distinct `(kem, sig)` pair reproducing the sealed FULL *and* the sealed VOICE — with an attacker-chosen signing key behind a byte-identical read-aloud value, against a design whose entire purpose is that the read-aloud value identifies the identity. It then falsified the required formal model's load-bearing premise, pinned two instrument universes exactly, and on the focused re-read found **two more defects the redesign had introduced in the surface it changed**. | HIT | **the strongest instrument this program has is an adversarial chair given the artefact and told to break it** — and the second-strongest is pointing that chair at the *delta* after a redesign, because a redesign is where new defects are. ⇒ *a post-blocker redesign gets a focused re-read by default (`R362` §5).* |
| 121 | **[Seat, NA-0749]** `W3`'s sealed baseline of **679** test attributes, which the corrected instrument reproduced **exactly** | ⛳ **THE FIGURE WAS RIGHT AND THE INSTRUMENT WAS WRONG — BY TWO ERRORS THAT CANCELLED.** The stated 3-line window **misses exactly one** attribute (`NA_0663_relay_tls_trust.rs:514`, a long `///` doc comment between `#[test]` and `fn`) and **over-counts by exactly one** elsewhere, arriving at 679 both ways. Replaced by an unbounded forward scan, which resolves **679 of 679 with 0 unresolved** — a completeness the windowed instrument could not even report. | HIT (self-caught) | **a figure that is right because two errors cancel is not a measured figure.** The tell is never the number; it is that the instrument cannot say how many inputs it FAILED to resolve. ⇒ *make every census report its unresolved count, and fix the instrument rather than keeping the lucky number.* |
| 122 | **[Seat, NA-0749 — OWED TO THAT LANE'S CLOSE, ordered at `R363` §1, landed by NA-0750 with predecessor attribution]** `W3`'s sealed baseline of **679** test attributes, offered as a measured figure | ⛳ **THE FIGURE WAS RIGHT AND THE INSTRUMENT WAS WRONG — BY TWO ERRORS THAT CANCELLED.** The stated 3-line window **missed exactly one** attribute and **over-counted by exactly one** elsewhere, arriving at 679 by cancellation. The correct instrument — for each own-line `#[test]`, scan **forward unbounded** to the next `fn` — resolves **679/679, 0 unresolved**, which the windowed one could not report | **HIT on the number, MISS on the instrument** | **A figure that is right because two errors cancel is not a measured figure.** Report what the instrument can RESOLVE, not only what it totals |
| 123 | **[Director/Seat, NA-0749 — OWED TO THAT LANE'S CLOSE, ordered at `R364` §0, landed by NA-0750 with predecessor attribution]** three of the Director's own verification checks flagged discrepancies against the seat's tree | ⚠ **ALL THREE WERE THE INSTRUMENT, NOT THE TREE.** The `QSCFP-` hit was arm A's **refused input**; the `from_be_bytes` hit was the **comment forbidding it by name**; and the routing needle **assumed the pre-unification symbol**, the unified `identity_fingerprint_single` being `R361`-authorized and correct | **MISS ×3 (Director's instruments)** | Recorded as the **seventh, eighth and ninth** instances of this chair's signature failure: **suspect the instrument before the tree**, and a needle built from a remembered symbol name goes stale the moment a lane unifies two functions |
| 124 | **[Director, NA-0750]** the brief's §3.8, ordering one line recording the production relay's `PULL_LEASE_SECS = 60` beside `ENG-0142` as though the packet-flagged unknown were still open | ⚠⚠ **THE FACT HAD BEEN IN REPO TRUTH SINCE 2026-08-16, INSIDE `ENG-0142`'s OWN ENTRY** — `IMPROVEMENT_LEDGER.md:3776`, NA-0740 / `D-1375`, with `[O]` provenance and an **explicit separation of the default from the running value** that the brief's phrasing collapses. Instruments named by the Director: **a 40-line head window on a 100+-line entry**, plus **adoption of the succession packet's stale `[P] UNKNOWN` flag** | **MISS (Director's, recorded as his own)** | **An instrument narrower than the claim it is offered for.** Before ordering a fact into a record, read the WHOLE entry — and treat a packet's own flag as a premise that can measure false, not as a finding. `R365` §2 struck the act entirely |
| 125 | **[Director, NA-0750]** the brief's §0, *"the retired published API's **ONE** desktop caller"* | ⚠ **MEASURED TWO**, both compile breaks — `src-tauri/src/commands.rs:145` **and** `src-tauri/tests/slice_a_flows.rs:57` — and the figure was **transcribed from a predecessor note rather than swept**. Proven at build time only by **staging**: `cargo check` aborted at the lib error and never compiled the test target (`slice_a_flows` = **0** occurrences in the whole log), so stage 1 was a **NON-RESULT** for site 2, not a miss | **MISS (Director's), caught by the seat before any edit** | **A count carried from a note is not a census.** And a red that cannot reach a site says nothing about it — the honest word is **NON-RESULT**, which is why the seal needed a branch for its own antecedent failing |
| 126 | **[Seat, NA-0750]** the inherited reading that adding a test **must** force a `scripts/ci/EXPECTED_TEST_INVENTORY.txt` re-pin — an edit **outside** the brief's §1 enumeration, the same shape as NA-0749's shard-manifest contingency | ⛳ **REFUTED FROM THE GATE'S OWN BYTES: `test_inventory.sh` is ASYMMETRIC.** `ADDED` prints *"NEW tests (allowed…)"*; **only `MISSING` exits 1**. Run bare on the committed tree it returned **rc 0**, printing the four new tests as allowed and then `PASS`. The standing note that a predecessor *"had to update it"* describes a **convenience re-pin, not a gate requirement** | **HIT (seat)** | **Re-measure an inherited premise from the instrument's own bytes before requesting scope.** The refutation is what kept §1's enumeration intact and cost one grep |
| 127 | **[Seat, NA-0750]** that `WF-0087`'s mention-plant would fire on this lane's id candidates, as it has on every lane since NA-0748 | ⚠⚠ **FIRED ON THREE OF FOUR.** `NA-0750` returned **2** tree mentions (`DECISIONS.md:43630`, `NA-0749_as_built.md:18`) plus **4** operator-tree files, and desktop `D-0031` returned **1** (`DECISIONS.md:1818`) — **every one a predecessor recording the id as its own negative control**. Only `D-1392` was clean on every route. All were **classified, not adopted**, and the sweep ran **BEFORE** the brief was banked | **HIT (seat)** | **Recording a sweep plants the ids it swept**, so a mention-counting sweep would have refused three of four with a plausible reason attached. Sweep before banking, enumerate every hit by file, and take the maximum **only over DECLARING forms** |
| 128 | **[Director, NA-0750]** the brief's `W2`, sealing that *"the **16-char** form appears nowhere on screen"* | ⚠ **THE DESCRIPTOR MEASURES FALSE AS A RENDERED LENGTH.** Measured at the old pin on a deterministic fixture, the retired code renders as **21 characters** — `4527-910E-41BB-92B4-V`, i.e. 16 Crockford payload characters plus four hyphens plus a check character. **16 is the PAYLOAD length.** A needle asserting 16 would have returned a false result in either direction | **MISS (Director's), caught before the seal was written** | **Seal a SHAPE, not a length, when the rendering adds separators.** The landed arm asserts `^[0-9A-Z]{4}(-[0-9A-Z]{4}){3}-[0-9A-Z]$` is absent from `document.body.innerText` |
| 129 | **[Seat, NA-0750]** `W3`'s negative control — a wrong passphrase must be refused, so that the correct-passphrase arm is not vacuous | ⚠ **THE FIRST ATTEMPT COULD NOT DISCRIMINATE.** Invoked with `--unlock-passphrase-file` (the vault-open source) where `--passphrase-file` (the credential under validation) was required, **both the correct and the wrong passphrase returned rc 1**; the control "passed" while proving nothing. Repaired and re-run, the arms differ — `vault_unlock ok=true state=unlocked` against `code=vault_locked` | **MISS then HIT (seat's own instrument)** | **A control that cannot discriminate is the finding, not the result it printed.** Assert that the two arms DIFFER before believing either — the same family as a no-op `.replace()` and a 444 fixture that never got modified |
| 130 | **[Seat, NA-0750]** that running the **full suite** (`cargo test`, rc 0, on the exact committed tree) plus the test-inventory gate discharged the desktop's CI obligation before pushing | ⚠⚠ **THE FIRST PUSH WENT RED ON `cargo fmt --all -- --check`.** CI's `rust` job runs **FOUR** gating steps — `fmt`, `test`, the inventory gate, `clippy --all-targets -- -D warnings` — enumerated from `.github/workflows/ci.yml`'s own bytes **after** the failure. **Two of the four had never been run locally.** The defect was real and entirely cosmetic: two line-length wraps rustfmt wanted in **this lane's own new `#[cfg(test)]` module**. Diagnosed by reading the **job log's step banner**, not the check name — `rust` FAILURE reads like "the suite failed", and the suite was green. Fixed by `cargo fmt --all`, confined to the file rustfmt named (+7/−2); all four steps then green in CI's own order, suite unchanged at 114/0/8 | **MISS (seat's own)** | **"The full suite" is not "what the job runs".** SR-05 governs the SUITE; it says nothing about the other steps in the same job. **Enumerate the job's steps from the workflow's own bytes and run all of them** — the cheapest possible check, skipped because "the suite is green" felt like completion. Same family as *a step's NAME is not its failure reason* |
| 131 | **[Director, NA-0751]** the brief's §2(b), *"the **NINE** wrapped functions"*, and the banked design's B(3) verb list *"list pending, create, accept, ignore, block, revoke"* | ⚠ **THE COUNT SLIP: the same sentence ENUMERATES FOURTEEN** — 4 contacts_request_* + 6 invite_* + qsp_status_tuple + handshake_status's fact source + 2 identity_*. And the verb list mixes CONTACT-REQUEST verbs (ignore, block — `contacts/mod.rs:1613`/`:1624`) into the INVITE surface while omitting redeem and finish, which are invite verbs | **MISS ×2 (Director's), caught by the seat before any edit** | **A directive's ENUMERATION is the record; its COUNT is a description of it.** The seat measured all fourteen and followed the enumeration, which `R367` §1 then ratified. ⇒ *when the two disagree, measure the list, not the number* |
| 132 | **[Seat, NA-0751]** that `established_recv_only` might be exposable as a handshake status without touching the forbidden `send_ready` | ⛳ **PREDICATE IDENTITY, PROVEN FROM BYTES:** `hs_send_ready_from_session` = `!(hs_zero32(send.ck_ec) \|\| hs_zero32(send.ck_pq))` (`handshake/mod.rs:1299-1301`) is TERM FOR TERM `qsp_send_ready_tuple`'s chain test (`protocol_state:114-118`). `established_recv_only` IS `send_ready == false` | **HIT (seat)** | **A rename is not a different fact.** Compare the PREDICATES, not the names — `W3` pins the WORD and would have passed a renamed one with room to spare |
| 133 | **[Seat, NA-0751]** `WF-0087`'s plant hazard as a MENTION problem, cured by classifying mentions | ⚠⚠ **A NEW DIRECTION, AND IT INVERTS: a TRUE DECLARATION IN THE WRONG NAMESPACE.** `- **ID:** D-0032` is declared at column 0 in the PROTOCOL repo's `DECISIONS.md:849` — a real decision of 2026-01-01 — in the DESKTOP's identical house form. A cross-repo grep returns a declaring form and refuses the desktop's next id with a plausible reason | **HIT (seat)** | **Classification cannot save you when the hit really is a declaration; the discriminator is the NAMESPACE.** ⇒ *a per-repo id space is measurable only inside that repo's own declaring file, and a maximum is stated with the FILE it was taken over* |
| 134 | **[Seat, NA-0751]** a needle reporting that NO qsc test drives the invite surface | ⚠ **THE NEEDLE WAS DEAD AND ITS OWN POSITIVE CONTROL SAID SO:** it demanded `(` immediately after the bare name, so `_at` variants never matched — and the control `vault_unlock(` ALSO returned **0**. Replaced by ENUMERATING every `invite_*(` and `vault_*(` symbol; the control then returned `vault_secret(` 89, `vault_json(` 36 | **HIT (seat), self-caught** | **A positive control that returns zero has told you about the INSTRUMENT, not the tree.** ⇒ *when a needle keeps missing, enumerate and classify — do not refine* (SR-21). Banked by the read as `C13` |
| 135 | **[Director, NA-0751]** `R369` §2's never-collapse rule — *"a locally-derived claim and a relay-reported claim never collapse into one variant"* — minted as a general rule with its instances named | ⚠⚠ **IT HAD COUNTEREXAMPLES INSIDE THE VERY TABLE THAT RATIFIED IT**, and one class is STRUCTURAL: FOUR consts are DUAL-PROVENANCE (`INVITE_MALFORMED`, `_NOT_FOUND`, `_CREATE_FAILED`, `_REVOKE_INVALID`), minted both locally and by the relay mapper, so **provenance is not a property of the const** and no variant split at const granularity can separate them | **MISS (Director's) — the FOURTH grouping-class instance this lane** | **Test a rule against the whole table before minting it, not against the cases that suggested it.** The rule survived, rescoped to bind at the tree's own `:99`/`:110` partition, with the four recorded as named exceptions (`ENG-0213`) |
| 136 | **[Director, NA-0751]** `R368` §2's ordering — test `vault_unlocked()` FIRST, before any qsp path | ⚠ **IT SHADOWS THREE REASONS UNLOCKING WILL NOT CURE.** `missing_home` (`:82`), `unsafe_parent` (`:85`) and `channel_invalid` (`:88`) are all decided BEFORE any vault secret is touched, and locked is the app's state at every cold start — so the banner would tell those users to unlock, they would, and only then learn the real reason | **MISS (Director's)** | **A true fact is not automatically the OPERATIVE one.** Reordered so the override applies to exactly one arm — locked ∧ `session_invalid` — which is also the only arm where the string route is genuinely unknowable |
| 137 | **[Director, NA-0751]** `R369` §4's `W10` — *"enumerate the gating steps of EVERY REQUIRED context in BOTH repos and run each locally"* | ⚠ **UNMEETABLE AS WRITTEN FOR TWO OF FIFTEEN.** `CodeQL` has ZERO producer bytes in the protocol's sixteen workflows and is a post-push GitHub-side analysis; `macos-qsc-qshield-build` gates on `runs-on: macos-latest`. **And the set was visible when the clause was ruled — `ENG-0208`'s own text lists both, and the ruling cited `ENG-0208` in the same section** | **MISS (Director's)** | **A generalization minted over a list that already contains its exceptions.** ⇒ *name the exceptions and what stands in their place, or a builder quietly reads "each" as "each I could"* |
| 138 | **[Director, NA-0751]** the read commission's reference to the *"compose-disabled 11/12"* mockups | ⚠ **MEASURED FROM THE MOCKUPS' OWN BYTES: BOTH CARRY AN ENABLED COMPOSER** (`mockup-11:145-147`, `mockup-12:134-136`; `grep -ic disabled` = 0 for both). The composer-disabled screens are elsewhere — `MOCKUP_channel_established_verify_banner.html` and `MOCKUP_channel_establish_FAILURE_STATES.html`, whose header states "the composer is NEVER enabled (fail closed, no downgrade path)" | **MISS (Director's), and THE CORRECTION STRENGTHENED THE RULING** | **A wrong citation can support a right conclusion, and finding that out makes the conclusion stronger, not weaker.** The read corrected it unprompted and said so |
| 139 | **[Seat, NA-0751]** STOP 005 §6's *"`st.gw` appears 21 times in commands.rs"* | ⚠ **MEASURED 18 on 18 lines. The 21 belongs to the `.gw` needle** (18 `st.gw` + 3 line-broken). **THE THIRD label-vs-needle mismatch this lane, in the document that discharged the first two** | **MISS (seat)** | **The instrument defect survived its own corrections.** ⇒ *state the needle beside every census figure, every time — a rule you have applied twice is not a rule you have internalised* |
| 140 | **[Read #2, NA-0751]** the commission to attack v2's error layer | ⛳ **THE INVERSION:** `StoreUnavailable` was wired to `CliError::Emitted`, which never carries a store failure, while the arm that DOES carry `contacts_store_unavailable` (**29 sites**) was wired to nothing — and `Other(&'static str)` could not have held `CliError::Code(String)` **by type**, without a leak | **HIT (read)** | **Follow the error VALUES to their producers, not the variant NAMES to their meanings.** The name that sounded right was attached to the arm that never fires |
| 141 | **[Read #2, NA-0751]** the same commission, on the const census | ⛳ **TWO `HANDSHAKE_ENVELOPE_*` CONSTS IN THE SAME TAXONOMY BLOCK** (`:135`/`:136`), reachable through `invite_accept` (`:1248`) and `invite_finish` (`:1355`) on RELAY-DELIVERED bytes — and they drive the one screen the ratified mockup marks *"DIFFERENT IN KIND: bug or attack"* | **HIT (read)** | **A census scoped to twenty stops at twenty.** ⇒ *scope a census to the BLOCK the tree defines, and prove the scope with a found-side control* |
| 142 | **[Read #2, NA-0751]** whether the typed error survives to the GUI | ⛳ **ALL EXISTING DESKTOP COMMANDS USE `String` AS THE ERROR CHANNEL, and no seal named the error DTO.** A builder following §6, `W4` and house style writes `.map_err(\|e\| format!("{e:?}"))`, every seal reports HIT, and the entire cure is undone one layer out, invisibly | **HIT (read)** | **A cure answered in WORD is not a cure answered in MECHANISM.** Ask of every fix: what is the boundary it must survive, and is there a seal ON that boundary? |
| 143 | **[Director, NA-0751]** `R370` §5's `RevokeOutcome` — that the Ok/Err POSITION could separate the dual-provenance revoke codes | ⚠⚠ **NOT CONSTRUCTIBLE. `invite_revoke` returns ONE FLAT VALUE**, its commit boundary is INSIDE it (`:919-920`), and `INVITE_REVOKE_INVALID` and `INVITE_NOT_FOUND` are minted BYTE-IDENTICALLY on both sides. Determining position needs an `invite/` edit (forbidden) or a racy second read | **MISS (Director's) — the FIFTH invented-structure refusal this lane** | **Position cannot carry provenance when the caller cannot determine position.** ⇒ the cure was COMPOSITION, not a type: the screen calls `invite_list()` and reads `state == Revoked` — three states from two calls the surface already had |
| 144 | **[Director, NA-0751]** `R370` §1's mapping of the bare `"vault_locked"` literal to `Locked`, adopted expressly *"so E6's window reports the true cause instead of Other"* | ⚠⚠ **THE STRING DOES NOT CARRY THAT CLAIM. An AEAD tag mismatch — a corrupt, truncated or tampered vault blob — is RELABELLED to `"vault_locked"` at `vault/mod.rs:965-973`**, and passphrase-source failures at `:927`/`:1419`/`:1449` do the same. **The tree refuses this collapse in EIGHT modules**, each translating to a scoped unavailability; v3 was the only layer taking it at face value | **MISS (Director's)** | **It replaced an honest residual with a CONFIDENTLY WRONG cause** — a user with a damaged vault told to unlock it. ⇒ *before mapping a string, read what its producers actually put in it; and when eight modules already refuse a collapse, that is the convention, not an oversight* |
| 145 | **[Seat, NA-0751]** STOP 005 §7's *"all 26 existing commands return `Result<_, String>` (E2's census, 26/26)"*, asserted twice | ⚠ **MEASURED 20 of 26.** SIX return no `Result` at all (`cli_vault_present`, `core_busy`, `settings_get`, `marker_stats`, `app_info`, `relay_config_get`), and a **27th** `#[tauri::command]` lives in `lib.rs` — which is what makes the brief's "27 commands" the true population | **MISS (seat)** | **A figure carried from a read is not a figure measured.** The read's ARGUMENT was untouched; the number was not. ⇒ *re-measure a borrowed figure before it lands in a record* — it now lands corrected in `ENG-0214` |
| 146 | **[Seat, NA-0751]** `W7(b)`'s found-side control, specified as the needle `^pub const [A-Z_]+: &str` and sealed at *"TWENTY-TWO today — measured, not assumed"* | ⚠⚠ **THE NEEDLE RETURNS 25 AND THE SEAL WAS RED ON DAY ONE**, over-capturing `DS_COMMIT`, `DS_SIG` and `INVITE_CODE_PREFIX` — none an error code. **And its own control (remove one key → RED) could not distinguish that failure from a real taxonomy change** | **MISS (seat), caught by the third read** | **A control that reds for two different reasons tells you nothing about which.** Cured by a VALUE-SHAPE needle (22, exact) rather than a line range, which would have let a const added outside the range escape entirely — reintroducing the very class it was built to catch |
| 147 | **[Read #3, NA-0751]** the commission to attack v3's four Director structures | ⛳ **THE SIX-PATH TRACE WITH AN IDENTICAL-VALUE PROOF.** It enumerated `invite_revoke`'s SIX error paths against the three the surface mapped, then showed two codes minted byte-identically on both sides of the commit — proving the structure unbuildable rather than merely arguing it | **HIT (read)** | **The strongest refutation is a construction, not an objection.** ⇒ *ask a read to show the mechanism that makes a design impossible, not to rate its plausibility* |
| 148 | **[Read #3, NA-0751]** the same commission, on the `"vault_locked"` mapping | ⛳ **AN EIGHT-MODULE CONVENTION CENSUS**, each site translating `"vault_missing"\|"vault_locked"` into a scoped code, with `msgqueue`'s own doc reading *"A LOCKED VAULT IS NOT AN ERROR HERE, IT IS A PAUSE"* | **HIT (read)** | **When the tree has already answered a design question eight times, the answer is the convention.** ⇒ *census the tree's HABIT before inventing a mapping* |
| 149 | **[Read #3, NA-0751]** v3's blanket prohibition of the seed-fallback env pair in facade tests | ⚠ **OVER-BROAD: 62 of 133 qsc test files set it** — it is the suite's standard idiom, forbidden PER LANE where real-crypto proof is the point. **And it is the ONLY route the tree demonstrates to two sealed states**, because a mock vault has no real store key | **MISS (seat/Director), corrected by the read** | **A prohibition inherited from one lane's reasoning can block the only demonstrated route in another.** ⇒ *scope a prohibition to the files whose seals depend on it, and say which* |
| 150 | **[Seat, NA-0751 — BUILD]** that the connect-reason totality file would drive `session_invalid` and read it back unchanged | ⚠ **IT RETURNED `VaultLocked`, AND THE FACADE WAS RIGHT.** That binary never unlocks anything, so `VAULT_UNLOCKED_THIS_RUN` (`lib.rs:190`) is default-FALSE and the override fired exactly as specified. ⛳ **The other five arms passed untouched in the same run** | **MISS (seat's test), and an unplanned HIT for the design** | **A test that fails because the product is correct is evidence about the test.** ⇒ and the five that passed are *independent* proof the override is scoped to one arm — *from a run that was not trying to prove it* |
| 151 | **[Seat, NA-0751 — BUILD]** that setting `set_vault_unlocked(true)` in-process would let the facade read a session a SUBPROCESS had written | ⚠ **`session_integrity_failed`. `qsp_session_store_key_load` (`protocol_state:168-181`) returns the REAL stored key when `vault::secret_get` succeeds and the TEST FALLBACK key only when it fails.** The subprocess carried `--unlock-passphrase-env` and wrote under the real key; setting the flag WITHOUT loading a passphrase took the fallback branch. Same config dir, two different keys | **MISS (seat)** | **SETTING THE UNLOCK FLAG IS NOT THE SAME AS UNLOCKING.** ⇒ *when two processes share a store, assert they share the KEY PATH, not just the directory* |
| 152 | **[Seat, NA-0751 — BUILD]** that `na0751_facade_locked_control.rs` was isolated, because `R371` §6 gave it ITS OWN BINARY and the file asserts `vault_unlocked() == false` as its first act | ⚠⚠ **THE FULL SUITE WENT RED: `COMMITTED_RC=101`.** Its pass-through arm expected `NoSession` for a locked vault with NO blob and got `VaultLocked`, with a `session_unsupported_version` marker proving it had READ one — the FABRICATED blob its SIBLING test had pointed the process at. Cargo runs a binary's tests in PARALLEL THREADS of one process, and all three write `QSC_CONFIG_DIR`, a process-global. **The own-binary rule isolates a file from other FILES, not from its own SIBLING TESTS** | **MISS (seat's test), caught ONLY by the full suite** | ⛳ **The diagnosis was TESTED, not asserted: `--test-threads=1` passes and three consecutive parallel runs in isolation ALSO pass — which does NOT refute the race, it shows the interleaving is rare on an idle machine.** ⇒ *a control that cannot be made to fail on demand is still unsound if it CAN fail; fix it deterministically rather than re-running until it is green.* And this is the case for SR-05 in one line: **subset-green would have shipped it** |
| 153 | **[Seat, NA-0751]** that a full-suite-green, all-gates-green build meant the protocol PR should open | ⛳ **IT DID NOT, AND THE SEAT SAID SO RATHER THAN THE DIRECTOR FINDING IT.** `W8` (fingerprint absence) and `W9` (pin/display identity) had **NO TEST AT ALL**; enumerating all 21 added tests against the surface showed **six of thirteen** public verbs never called, and `contact_list`/`contact_requests` called ONLY from the locked-control file asserting `Err(Locked)` — their success payloads never inspected. The suite was green because it never touched them | **HIT (seat), and the build did not catch it** | **A GREEN SUITE THAT NEVER TOUCHES A SEAL IS NOT EVIDENCE ABOUT THAT SEAL.** The cause was stated plainly: *tests were written for the seals being thought about, and the seal LIST was never re-read for coverage.* ⇒ *before declaring a build done, enumerate the SEAL SET and map each seal to the test that discharges it* |
| 154 | **[Seat, NA-0751]** the guard fix's mutation control, first attempted at **12 concurrent runs** and returning **0 failures** | ⚠⚠ **0/12 WOULD HAVE REFUTED A TRUE DIAGNOSIS.** Run at ~150 the same control returned **13 failures in 152 unguarded runs (~8.6%)** and **0 in 152 guarded**, with the failure modes naming both process-globals — a sibling's `QSC_CONFIG_DIR` and a sibling's `set_vault_unlocked(true)` | **HIT after a MISS-shaped first attempt** | **PROMOTED TO A PROPERTY (`D-1393`), not left as a row: A CONTROL THAT DOES NOT FIRE IS NOT EVIDENCE OF ABSENCE UNTIL IT HAS RUN ENOUGH TIMES TO SEE THE RATE IT HUNTS.** ⇒ *state the expected rate before running a probabilistic control, and size the run to it* |
| 155 | **[Director, NA-0751]** `W3` as sealed — *"`send_ready` occurs ZERO times in the facade module"* | ⚠ **THREE occurrences, all in the module's `//!` doc, all of them the module EXPLAINING that `send_ready` is deliberately not exposed.** The property is absolute (0 code lines, 0 pub items, 0 fields, 0 variants). The seal's letter contradicted the Director's OWN ordered discipline — `R368` §1 required exactly that explanation for `ENG-0206`'s closure | **MISS (Director's), and the seat altered NEITHER letter nor property** | **A SEAL THAT PINS A TOKEN COLLIDES WITH A RECORD THAT MUST NAME THE TOKEN.** ⇒ *seal the EXPOSURE, not the string; and when an instrument sweeps prose where the claim means code, say so and change nothing until it is ruled* |
| 156 | **[Director, NA-0751]** `R372` §1(c)'s W10 clause — the two written exceptions, *"and only those"* | ⚠ **MEASURED THREE.** `public-safety` is a PARTIAL third: 5 of its 14 steps run locally and **NINE are structurally CI-only** — two `actions/checkout`, two `needs.*.result` gates, two `public_safety_gate.py` steps needing `$GITHUB_REPOSITORY` and a live PR number, the main-red blocker, the push-only full-suite waiter, and a docs-only echo | **MISS (Director's), the same `D3` shape one layer down** | **A FIGURE RULED OVER A SET IS REFUTED BY THE SET'S OWN BYTES.** ⇒ *an exception COUNT is a measurement, not a ruling; enumerate the steps before writing "and only those"* |
| 157 | **[Director, NA-0751 GO]** the GO's *"7-scenario / 242-step baseline"* | ⚠⚠ **242 IS THE SIX-SCENARIO FIGURE.** `g_fingerprint_two_tier`'s own count appears in NO record, so comparing a seven-scenario run against 242 manufactures a false **+26** delta. Measured `g = 26` ⇒ the true seven-scenario baseline is **268**. The Director recorded this against his own chair in the NA-0751 close-out; it is transcribed here rather than re-argued, and INDEPENDENTLY re-derived by NA-0752 from the runner's own emission rules rather than adopted. | NA-0751's seven-scenario run, then NA-0752 modelling the runner's bytes (`liveness_pair` on launch #1 is the missing +2/scenario) and reproducing 242/26/268 exactly. |
| 158 | **[Seat, NA-0752]** SUPPLEMENT 001 §F6 — *"protocol main is RED at `9dcded4d` … the protocol PR MAY THEREFORE BE UNMERGEABLE"* | ⚠⚠ **FALSE WHEN WRITTEN DOWN, AND WRITTEN FROM AN INHERITED RECORD.** Re-measured at the SAME sha: **69 check-runs, ALL success, nothing pending**; the three settled green at 16:18Z, ~1h after NA-0751's STOP 013 recorded them red. STOP 013 was true when written. ⇒ **re-deriving a base sha and re-measuring that base's CHECK STATE are two different acts, and only the first was performed.** The `ENG-0200` condition does not apply to this lane. | The seat's own re-measurement, prompted by the standing rule that a recorded BLOCKER is base-scoped. ⚠ The legacy commit-STATUS endpoint reports `pending`/`total_count:0` for the same sha and is the WRONG API — check-runs is authoritative. |
| 159 | **[Seat, NA-0752]** that a world-writable **parent** directory drives `unsafe_parent` | ⚠ **IT DOES NOT.** Under `EnvOverride` — the branch `QSC_CONFIG_DIR` selects, and the one production takes — `check_parent_safe` (`:302-313`) sets `root = if path.is_dir() { path }`: it checks **the config dir ITSELF, never its ancestors**. The prediction was made from the reason's NAME instead of the function's bytes. | Driving the real states and getting `missing_seed` where `unsafe_parent` was predicted; re-driven correctly at 0777/0770 on the dir itself. |
| 160 | **[Seat, NA-0752]** a DefaultHome control proving `unsafe_parent` discriminates | ⚠ **THE CONTROL WAS VACUOUS AND ITS OWN CONTROL CAUGHT IT.** An all-0700 tree ALSO returned `unsafe_parent`, because that branch walks EVERY ancestor from `/` and `/tmp` is 1777; re-sited under `/srv/qbuild` (2775, group-writable) it fired again. ⇒ that branch cannot be driven to "safe" on this box, so it was DISCARDED as evidence and only the EnvOverride arms were used. | Running the negative control at all. Two arms returning the same value is the tell that neither is evidence. |
| 161 | **[Seat, NA-0752]** SUPPLEMENT 003 as the answer to *"is everything the Director needs in the stop file?"* | ⚠⚠ **19 OF 20 RULING-ESSENTIAL ELEMENTS WERE MISSING FROM IT.** And the deeper fault: STOP 001 plus three supplements had become a four-file *read-them-in-order* set — **the exact fragmentation the stop-file convention exists to prevent** — whose STOP 001 §10 still promised *"the Director needs only this file"*. ⇒ **A SUPPLEMENT CHAIN IS NOT A STOP FILE; past one correction, RE-ASSEMBLE.** Cured by `STOP_NA0752_002`, built by an assembler with round-trip diffs and 29/29 containment. | The operator asking the question twice. A containment test of the supplement ALONE against what a ruling requires. |
| 162 | **[Seat, NA-0752]** the *"BUILD-PHASE DEPENDENCY CLOSURE"* section as discharging the closure | ⚠⚠ **THAT VERY SECTION MISSED A DEPENDENCY.** The `ENG-0187` instance-2 SUBSTANCE was carried as a DESTINATION STRING, not as text — six of eight containment probes returned NO — and the closure had been asserted over `STOP 013` §7 having never opened **§5b**, where the content lives. ⇒ **COMPUTE THE CLOSURE AGAINST THE ORDERING DOCUMENT, NOT AGAINST YOUR OWN DRAFT OF IT:** the regex could not see `STOP 013 §5b` because the carried text never quoted it, so the needle had nothing to match. | A wider reference sweep including prose document references, run because the first sweep's shape looked too clean. *Suspect the instrument before the tree.* |
| 163 | **[Seat, NA-0752]** a needle held constant across two runs of the same containment audit | ⛳ **IT WAS NOT: `'SEND sub-process'` was weakened to `'send'` between runs**, which would have manufactured a pass. Caught before reporting; the ORIGINAL eight needles re-run verbatim returned **0 missing**, so the closure was genuine and the drift changed nothing. **Recorded anyway** — an instrument that drifts toward the answer you want is the failure whether or not it altered this result. | Re-reading my own two probe definitions side by side before reporting the second one's result. |
| 164 | **[Seat, NA-0752]** *"LATEST.md rewritten as pointer (41 lines)"* | ⚠⚠ **THE WRITE HAD FAILED WITH `Permission denied` AND MY OWN CONFIRMATION LINE LAUNDERED IT** — `wc -l` had read the OLD file. **A success message computed from the destination cannot tell you your bytes landed.** ⚠ And the first diagnosis was wrong too (the file measured 664 and writable, not 444). Cured by staging to a temp file and `cmp`-ing, with a tampered copy proving `cmp` discriminates. | Reading the error text instead of the success line printed after it. |
| 165 | **[Director, NA-0752 brief]** §1 item 8's *"the #1767 fast-fail context, 1m10s, too fast to reach the test"* | ⚠ **A FIGURE ORDERED WITHOUT ITS INSTRUMENT — `WF-0087`'s own shape.** #1767's failing CHECK-RUNS measure 4 s / 24 s / 4 s / 7 s, none near 1m10s and none a macOS shard job; `#1767` appears nowhere in STOP 013. The seat refused to land it and raised it as an ask. `R374` §5 supplied the instrument: it is a **workflow-RUN** duration from `gh run list`, a different object class — so the sweep was CORRECT and could not have found it. The clause lands as sourced CONTEXT with its provenance stated. **HIT: the refusal is the model behaviour.** | Sweeping every failing check-run across every commit on #1767 and computing durations, rather than accepting the figure. |
| 166 | **[Seat, NA-0752]** seal F3 as drafted — *"remove one name from a COPY of the pin ⇒ the gate goes RED naming it"* | ⚠ **RUN, IT RETURNED rc 0.** Removing a pinned name exercises the gate's **ADDED** direction, which `test_inventory.sh` treats as informational **by design** — an asymmetry this lane had itself measured and then written a control against the wrong arm of. Re-run in the **fatal** direction (a name in the pin absent from the tree) it returns **rc 1, "TESTS DISAPPEARED"**, naming the sentinel. ⇒ **a seal's control must be aimed at the arm that can actually fail.** | Running the control and reading its exit status instead of assuming the direction. |
| 167 | **[Seat, NA-0752]** the formalized directive's §F1 — five desktop files, and a 128-test re-pin | ⚠ **BOTH MEASURED WRONG, FROM ONE ROOT CAUSE.** §F1 constrained `gui_driver.rs` to *"the THREE-LINE wrapper only"*, leaving ruled seal **F1b** with **no file in the enumeration able to host it**. Resolved toward the ruled seal: the PR is **SIX** files (`design_polish.rs` is the sixth) and the re-pin is **129**, not 128. Both deltas reported rather than absorbed — dropping F1b would have silently discarded a ruled requirement. | Running the inventory gate after the build and reading 129 where the directive said 128. |
| 168 | **[Seat, NA-0752]** `read_tc` as the scenario's footer instrument, per the formalized §F3 | ⚠ **IT WOULD HAVE SHIPPED A RACE.** `read_tc` (runner `:343`) reads `textContent` ONCE with no poll, and `enterMain()` writes the footer after two `await`s. Substituted `read_text` (`:299`), which polls AND is visibility-coupled — same seal, no race, and it cannot pass while the footer is hidden. ⛳ A bonus the substitution buys: `index.html:156`'s static default is a DIFFERENT, shorter string, so a writer that never ran leaves it and the assertion FAILS. | Reading the two ops' implementations before choosing between them, rather than taking the directive's word — my own word, one document earlier. |
| 169 | **[Seat, NA-0752]** `f_h`'s emitted verdict-row count and the new harness total | ⛳ **HIT, SEALED BEFORE THE RUN: 28 rows, total 296.** Predicted from the runner's own emission rules (`note`/`teardown` 0, `countdown_commit` 2, `launch` 2 plus `liveness_pair`'s 2 on `n == 1`, `finish` +1) and confirmed EXACTLY by execution, with all seven prior scenarios reproducing 96/20/28/25/52/21/26 unchanged. | Modelling the emission rules from bytes and writing the prediction down before running the harness. |
| 170 | **[Director, NA-0753]** the governing design bank at `RBANK_flight_fixes_design_20260821.md` — *"sha256 9f43a3f9… (34 l / 2497 B); mismatch = STOP"* | ⚠ **MISMATCHED ON ALL THREE FIGURES**: `d85db39d…`, 33 lines, 2189 bytes. The declared bytes exist **nowhere on the box**. Refused by **GEOMETRY ALONE** — trailing newline, terminal sentence, prose line-length profile — which established "different revision, not truncated" **without reading a word of the body**, so nothing downstream was poisoned. `Birth == Modify`, mode 444, born 3m26s before the brief arrived ⇒ a sha computed over a draft that never landed; ruled a transport casualty and re-issued as v2. | Verifying the sha BEFORE reading, and answering the completeness question with file geometry instead of content when the content is the thing under suspicion. |
| 171 | **[Director, NA-0753]** §0 — *"a wipe becomes genuinely a wipe (**ENG-0048 closes**)"*, with §1 item 1 ordering the `settings.json` removal and §1 item 11 the ledger closure | ⚠ **ALREADY LANDED A MONTH EARLIER.** `ENG-0048` was RESOLVED at NA-0697 (`D-1337`, desktop `D-0024`, PR #24); BOTH vault-destroying sites already removed the file; and the existing seal asserts a data_dir listing **EQUALITY**, which is **STRONGER than the ordered X1** (`file_absent` checks one name). The NA-0750 §3.8 class, second instance, same chair. | Reading the ENTRY, not its `Status:` line — see row 172 for the mechanism. Measuring that a brief's headline deliverable is not already in the tree, BEFORE building it. |
| 172 | **[Seat, NA-0753]** — the mechanism behind row 171, recorded as its own row because it is transferable | ⚠ **UNDER `mark-don't-rewrite`, AN ITEM'S CLOSURE IS APPENDED, NOT SUBSTITUTED.** `IMPROVEMENT_LEDGER.md:2182` still reads `Status: open — filed 2026-07-19`; `:2183` declares it RESOLVED. Both lines are SUPPOSED to coexist — and that is exactly the trap: a `Status:`-shaped needle, the obvious one, reports a closed item as open. It reaches Directors, not just seats: this brief was drafted from it. Cured in the open by a clarity line appended beside the stale `Status:`. | Reading the whole entry to its next heading and treating the LAST-WRITTEN disposition as governing. |
| 173 | **[Director, NA-0753 addendum]** *"erase_all_impl removes settings.json … so the ghost address was RESTORED after removal, not left behind"*, with (a) loader fallback, (b) save shape and (c) erase arm as the measurement set | ⚠ **THE PREMISE MEASURED FALSE, AND (a)/(b)/(c) ALL MEASURING NEGATIVE IS THE CORROBORATION.** The loader has no `.tmp` fallback, `save` is write-tmp + atomic `fs::rename`, erase's removal IS `?`-checked — because all three are aimed at `erase_all_impl`, which is **not the path that fired**. The operator's *"erase-after-failed-unlocks"* is **the app's OWN NAME for a shipped control** (`index.html:298`, `<h3>Erase vault after failed attempts</h3>`), not a description of the manual ceremony. The ghost is **LEFT BEHIND**, not restored — a THIRD vault-destroying path (`ENG-0217`). | ⛳ **When a bug report's wording matches a shipped control's LABEL, suspect the control.** The naming match was found by grepping the UI for the operator's own words instead of mapping them onto a function. |
| 174 | **[Director, NA-0753]** design bank v2 item 2 — the grouped verification code is *"Presentation only; raw-30-digit value tests unchanged"* | ⚠ **NEITHER CLAUSE HELD.** `f_g:96` pins `/^[0-9]{30}$/` on the on-screen `textContent` and cannot survive a grouped render. Worse, the mockup's `<br>` split would have **INTRODUCED a defect**: `.verify-code` is nowrap + `overflow: hidden`, and `fitCode` releases the clip only when `scrollWidth > clientWidth` — a `<br>` halves each line's width, so the escape could never fire and the second line would clip **SILENTLY, with every seal still green**. Ruled to option (B), one text node; the mockup delta is enumerated rather than silent. | Sweeping what tests ASSERT rather than what the change looks like, and then reading the CSS the assertion guards instead of stopping at the test list. |
| 175 | **[Seat, NA-0753]** the eight prior scenarios after `f_g`'s strengthening — *"297, being the recorded 296 plus exactly one"* | ⛳ **HIT, EXACTLY: 297**, and it reconciles against the recorded baseline — `f_g` moves 26 → 27, every other scenario unchanged (96/20/28/25/52/21/28). | Predicting the delta from the emission rules and the recorded prior figure, then running the 8 BEFORE adding a ninth. |
| 176 | **[Seat, NA-0753]** the new lifecycle scenario `f_i` — *"49 steps, then 54 after the arm-sequence repair"* | ⚠ **MISS: 58 measured.** Diagnosed from the step NAMES rather than the number laundered — each launch emits **TWO** rows (`launchN_ready` AND `launchN_session`), not one (**+3** over three launches), and every scenario emits a terminal `isolation_bracket` row (**+1**). **Corrected model, stated for the successor:** launch #1 = 4 (ready + session + the liveness pair), each later launch = 2, `isolation_bracket` = 1 per scenario, `teardown` and `note` = 0, every other op = 1. Total across nine: **355**. | Dumping the actual step names from `verdict.jsonl` and reconciling against the model, instead of adjusting the prediction to fit. |
| 177 | **[Seat, NA-0753]** the harness would drive the wipe once the scenario armed it | ⚠ **THE ARM SILENTLY NEVER HAPPENED — TWICE OVER, from two different races.** (i) Arm is gated on an acknowledgement checkbox (`#wipe-ack`, `main.js:854`); without it the handler returns early and the click is a no-op that still reports `rc=0`. (ii) Opening the Server pane calls `refreshServerPane()`, which is **async**: the pane's `className` flips synchronously so a wait on it races, and the in-flight refresh then wiped both the typed field and the gate's message — measured EMPTY while a probe proved the message had been set. Cured by settling on `#relay-token-help` (written at the END of `refreshServerState()`) and by ASSERTING the armed state rather than assuming it. | Probing the running app for the actual state instead of reasoning about why the assertion failed — and, for the instrument itself: `read_text` polls but is VISIBILITY-COUPLED, `read_tc` reads a property but does not poll, and **`prop_eq` is the only op with both**. |
| 178 | **[Director, NA-0754]** S2(b), the brief's own load-bearing framing — the route to probe-with-field-values is *"existing command params, an added command-layer function … or IMPOSSIBLE without qsc bytes (= STOP with the measurement)"* | ⛳ **PARTIAL, and the residue was the cheap half.** Neither pole: **explicit non-empty values are reachable with ZERO qsc bytes**, because qsc consults the ENVIRONMENT before the vault in both secret chains — so an added command-layer function suffices. Only the **absence** arm ("test with NO token while one is stored") is impossible, since an empty env value is trimmed to `None` and falls through to the vault. | PARTIAL | *a binary framing can hide a third answer that is mostly the good pole; measure each rung separately rather than the question as posed.* `R379` §Q1 struck one bank sentence and the whole model became zero-qsc-bytes |
| 179 | **[Director, NA-0754]** S2(d) — *"if S2(c) finds cert-parsing needs a new dep, the check DEGRADES to exists+PEM-header with the boundary stated, and full-parse is FILED"* | ⚠ **THE CONTINGENCY WAS NEVER REACHED — it measured unnecessary.** A full, real PEM parse already runs inside `relay_http_client()` via `reqwest` (already a qsc dependency), and `relay_server_info` invokes it BEFORE opening a socket, so the CA rung validates offline at zero dependency cost. No degrade, no filing. | MISS (safe direction) | *a contingency that measures unnecessary is still a premise that measured false; record it rather than letting the unused branch imply the check was hard.* |
| 180 | **[Seat, NA-0754]** STOP 1 §11-A — the green path has no instrument, so Y2/Y3/Y5-accept and **Y1's own differ-control** are undrivable without a fixture relay | ⚠ **HALF WRONG, AND THE DIRECTOR FOUND BOTH DERIVATIONS THE SEAT HAD MISSED.** `R379` §Q4: (i) Y5's ACCEPTING arm is drivable **offline** — a real PEM plus an unreachable address yields `unreachable` rather than a CA code, and that classification IS the proof the CA rung passed; (ii) Y1's differ-control needs **no relay at all** — drive the stores through the trio commands and prove each observable MOVES, then prove the probe moves nothing. Both shipped. Only Y2/Y3's LIVE halves genuinely needed a relay, and they went to the operator's acceptance flight. | MISS | *"undrivable" was reasoned from the seal's WORDING, not from what the outcome taxonomy already distinguishes — the seat's own §4 measurement (the CA check runs before the socket) licensed the drive and it did not connect the two.* |
| 181 | **[Seat, NA-0754]** STOP 1 §11-C — `server_pane.rs` is REQUIRED because the suite goes red the moment `index.html` is edited | ⛳ **HIT, and it under-counted.** Six tests went red, not the one presence seal predicted — five pinned the SUPERSEDED model outright (`test_saves_first…`, the commit order, the prose-link removal affordance, the dirty-helper's Save branch, the failure-prose anchor) and a sixth was a SAFETY boundary whose needle a ternary had hidden. | HIT | *naming the file was right; the blast radius inside it was measured only at build time. A file that pins a model is red across ALL of it when the model is superseded, not at the one needle you can see from outside.* |
| 182 | **[Seat, NA-0754]** the six retired helper sentences would be absent from `main.js` after removal | ⚠ **FOUR OF SIX WERE STILL PRESENT — planted by the comment DOCUMENTING their removal.** Caught by the seat's own verification pass, not by a seal; the absence seal in `design_polish.rs` would have read green-when-red because a source-text pin cannot tell a comment from live copy. The same shape then bit twice more in the same build (the `secret_set` needle in an explanatory comment; the R-space's own classification sentences). | MISS | ⇒ **describe a retired construct, never spell it; the retired wording belongs in the records, which no seal reads.** Three instances in one lane. |
| 183 | **[Director, NA-0755]** the brief's S6 reference markup is the mockup's states 1-2 "VERBATIM FROM 142c1eb6" | ⚠⚠ **MISS — SIX DIVERGENCES**, found by extracting from the blob instead of reading the brief. Two are whole omitted elements, two are reworded sentences, one invents a sentence (*"house key"*) that appears nowhere in the mockup, and one changes a class name. The brief's OWN closing NOTE (*"the mockup's own bytes are the final authority… S2(h) extracts from THEM, never from this brief"*) is what made the miss safe. | MISS | **A reference copy of a ratified artifact is not the artifact.** Extract from the blob and ENUMERATE the delta; never resolve it by preference. The brief that anticipates its own transcription error is the one that survives it. |
| 184 | **[Director, NA-0755]** S1 item 4 — desktop invite wrappers needed "ONLY IF S2(b) measures a needed invite wrapper missing" | ⛳ **HIT, and the conditional resolved to NOT-NEEDED.** Census **40** = the expected figure exactly, and all six facade invite verbs were already registered at NA-0751. Item 4 STRUCK at `R380` §1; **no `.rs` product file touched at all**. | HIT | **A conditional that names the CLASS rather than the mechanism lets the enumeration SHRINK safely.** The brief's phrasing made "not needed" a legal outcome instead of a deviation to argue about. |
| 185 | **[Director, NA-0755]** S3 Z2 — "unreachable-relay create renders the reused vocabulary" (the relay pane's *Couldn't reach the relay*) | ⚠⚠ **MISS — THE ARM IS NOT DISTINGUISHABLE.** `relay_send_outcome_from_parts` returns the caller's own fallback for every non-TLS failure, so unreachable and refused both arrive as `relay_rejected` — **driven live, 4112 ms**. The reused copy would have asserted a cause the client cannot measure. Ruled to a both-provenances sentence at `R380` §2. | MISS | **Before reusing a vocabulary, check that the ARM CARRIES THE SAME INFORMATION.** Copy reuse is a claim about the error's provenance, not about its wording. |
| 186 | **[Seat, NA-0755]** STOP 1 §3 — a zero-dependency clipboard route exists in this webview | ⛳ **HIT, measured not recalled.** `isSecureContext: true`, `navigator.clipboard.writeText` **RESOLVES under a real click** at `core:default`. ⚠ And the measurement carried a constraint memory would not have: `readText` is **REJECTED**, so a seal may assert the CALL and never the CONTENT. `execCommand('copy')` returned **false even with a valid selection** — the opposite of the usual assumption. | HIT | **"Measure, do not recall" earns its keep on the NEGATIVE half.** The route's existence was guessable; the read/write asymmetry and the dead legacy fallback were not, and both changed what could be sealed. |
| 187 | **[Seat, NA-0755]** STOP 1 §6.1 — the baseline is 10 scenarios / 396 steps and will reproduce exactly | ⛳ **HIT to the step**, 10 passed / 0 failed, 580.62 s. ⚠ Two carried per-scenario figures were wrong in the same breath: `g`=**27** and `h`=**27**, not 26/28. The TOTAL was right because the two errors were not in the total. | HIT | **A correct total can sit on top of wrong parts.** Re-derive the decomposition from the producer's own rows, not from a remembered sum. |
| 188 | **[Seat, NA-0755]** STOP 1 §4.3 — the style.css complement is exactly the selectors the modal needs and lacks | ⚠ **PARTIAL.** The selector list was right, but the lane's own design system rejected two things the list did not cover: two modal buttons carried **no action tier**, and the scrim was an **inline `rgba`**. Both caught by the SHIPPED tests (`every_button_is_tiered_or_nav`, `colors_only_in_token_block`), not by the plan. | PARTIAL | **A "what's missing" census answers absence, not COMPLIANCE.** The complement told me what to add; only the house's own tests told me what shape it had to take — and a comment reasoning past a rule was the tell. |
| 189 | **[Seat, NA-0755]** the new seals are aimed at arms that can actually fail | ⚠ **PARTIAL — 8 of 10 controls were aimed correctly on the first try; TWO WERE NOT.** One tampered a **pre-existing** `.callout` elsewhere in the file (it replaced the first occurrence, not the modal's) and PASSED, and one carried a needle **missing a leading dot** and never ran. Both re-aimed and then RED. | PARTIAL | **A control proves nothing until you check WHICH occurrence it hit.** Replace-first is not replace-the-one-you-mean; and the needle-from-bytes rule applies to controls, not just to searches. |
| 190 | **[Seat, NA-0755]** a `git checkout -- <file>` safely reverts a counterfactual control | ⚠⚠ **MISS, AND IT DESTROYED WORK.** Nothing was committed yet, so `checkout` reverted to the **base commit** and wiped all three `ui/` files mid-control-run. Recovered by re-applying; the controls were then re-run with `cp`/restore. | MISS | **`git checkout --` restores to HEAD, not to "before my last edit".** Commit first, or save-and-restore the file's own bytes. A tamper harness must never use the VCS as its undo. |
| 191 | **[Director, NA-0755 v2]** the superseding order — *"the misroute is the one true defect"* | ⚠⚠ **MISS — A CAUSE NAMED FROM A SYMPTOM.** Measured at three layers and it **did not reproduce**: the engine's 11th sequential create returns `soft_cap_reached`; the desktop IPC delivers it intact; the operator's own ten-click gesture renders *"Too many live invites"*. The accused mapping was clean. Honest state: `vault_unavailable` was genuinely delivered, **provenance unknown**. | MISS | **A symptom names a surface, never a cause.** Reproduce at every layer between the report and the accusation before ruling one of them guilty — and when it does not reproduce, say so and make the arm diagnosable instead of picking a culprit. |
| 192 | **[Director/bank, NA-0755 v2]** the clipboard's user-activation window is *"~4 s"* | ⚠⚠ **MISS, AND IN THE DANGEROUS DIRECTION.** Measured: `writeText` after a plain `await` RESOLVES at 750 ms and **REJECTS at 1000 ms**. A create needs two network round-trips, so on that route "Activate & Copy" would have failed **every** time, not occasionally. ⛳ And the design was saved by a mechanism the bank never named: `ClipboardItem` built synchronously around a pending promise **RESOLVED at 4000 ms**. | MISS | **An unmeasured platform figure carried as known is worse than an unknown**, because it sizes a contingency that then guards the wrong thing. Measure the boundary, and probe for the mechanism that survives it. |
| 193 | **[Seat, NA-0755 v2]** STOP 005 — the desktop half is blocked by the pin and cannot compile | ⛳ **HIT.** `facade::invite_clear` = **0 occurrences** at `rev 9dcded4d`; `facade::invite_create` takes **3** params there, not 4; positive control — `invite_create` itself is present, so the needle works. Ruled at `R383`: option A, prohibition lifted scoped. | HIT | **A dependency pin is part of the blast radius.** A change that lands in a linked crate is not available to its consumer until the pin moves, and if the pin is on a forbidden-bytes list the sequencing is a ruling, not a build step. |
| 194 | **[Seat, NA-0755 v2]** the new engine seals are aimed at properties that can fail | ⚠ **PARTIAL — 6 of 8 first time; TWO WERE VACUOUS.** The lock-gate seal PASSED with the gate deleted (the store read fails with the same string — the read had predicted exactly this and I built a behavioural assertion anyway). And `#[serde(default)]` measured **DECORATIVE** on an `Option`: probed directly, a missing `Option<T>` deserialises to `None` without it, so that control could never fire. | PARTIAL | **A control that passes looks exactly like a working control.** When a read tells you a property is behaviourally invisible, believe it and seal the source; and probe an attribute's actual effect before sealing it — the dead control is kept at 444 because that is the only durable form of this lesson. |
| 195 | **[Director/sheet, NA-0755 v2]** the count/prose sweep names the sites the delta falsifies | ⚠⚠ **MISS BY ~5×.** The sheet named **2**; `R381` added ~**4**; the SR-15 read measured **45** — eleven stale prose sites, six red assertions, four files outside the ruled scope that **would not compile at all**, and two seals that go false **while staying green**. | MISS | **A blast radius is measured, never enumerated from the design.** The design surface and the change surface are different sets, and the gap is where the silent falsehoods live. |
| 196 | **[Director, NA-0756]** S0/item 10 — *"the first time anyone has ever exercised invite_redeem / invite_accept / invite_finish"* | ⚠⚠ **MISS.** `NA_0681_two_party_handshake.rs` has driven create→redeem→accept→finish GREEN against the same real in-process relay since NA-0681. ⛳ It was refuted by **ENUMERATE-AND-CLASSIFY**, not by a refined needle: a `name(` search returns `rc 1` in every test file because those drives run the CLI and assert on MARKERS. `R387` §S7 re-aimed rather than struck; the genuinely-first-ever set (facade layer, the tells firing, the not-yet outcome) was each measured `rc 1` before being claimed. | MISS | **A "first ever" is a measurement, not a roadmap inheritance.** And when a needle returns nothing, enumerate what DOES match before concluding absence — the drive was real and invisible to a call-site search. |
| 197 | **[Director, NA-0756]** S6's markup — `class="invite-surface"` is *"the v5 shared width class, reused"* | ⚠⚠ **MISS.** Zero occurrences in either UI file. The shipped class is `.modal`, whose `max-width: 500px` is the only one in the stylesheet. Built verbatim, all four surfaces would have carried a class styling **nothing** — the v4 geometry defect re-run. The brief's OWN item 3 said *"REUSING the v5 shared 500px class"*, so the enumeration contradicted its own reference markup. | MISS | **Reference markup must be extracted from the shipped bytes, never written from a model of them.** When two halves of one directive disagree, the one that names a PROPERTY beats the one that names a STRING. |
| 198 | **[Director, NA-0756]** S6 — *"SHIPPED ENTITY FORMS ARE THE PIN FORMS: `&#8212;` … `&#8230;` … `&#8217;`"* | ⚠⚠ **MISS.** All three occur **ZERO** times in `ui/index.html`. The file ships literal UTF-8 (39 em dashes) everywhere except Lane A's own invite block, which uses `&mdash;`/`&hellip;`/`&rsquo;`; `main.js` is literal-only (166 em dashes, 0 entities). `R387` §S5 voided the sentence and ruled `textContent`-level pins. | MISS | **A pin form is a fact about the file, not a house style you can assert.** And the strongest cure was to move the seal to a level where the question cannot arise — pin the rendered TEXT, not the source bytes, and the entity choice stops mattering. |
| 199 | **[Seat, NA-0756]** STOP 001 carried everything the Director needed to rule | ⚠⚠ **MISS, AND THE STOP WAS RE-ASSEMBLED.** It omitted the shipped copy vocabulary the brief's own S2(c) ordered reused — so the arm→copy mapping could not be ruled from the artifact at all. The re-audit then found `inviteErrorLine`: **14 of 35 arms already ruled**, 21 uncovered, 3 stating something false on a redeem, and both security tells already written and marked *"PREPARED FOR LANE B"*. | MISS | **A stop can be true in every line and still fail its job.** Audit it against what the RULING needs, not against what you happened to measure — and past one correction, RE-ASSEMBLE rather than chain a supplement. |
| 200 | **[Seat, NA-0756]** the ENG space's next free id is the one after the brief's enumeration | ⚠⚠ **MISS, AND THE NEGATIVE CONTROL IS WHAT CAUGHT IT.** I predicted `ENG-0232` FREE; the control measured it **PRESENT**. The space runs to **0234**, and the brief's item 13 named five of nine open rows. An adopted next-id would have COLLIDED with a live filing. | MISS | **A negative control that FAILS is the control working.** The value of running it is exactly the case where the prediction is wrong — and a directive's enumeration is a description of the tree, never the tree itself. |
| 201 | **[Seat, NA-0756]** the ordered retarget breaks exactly the three consumers the sweep found | ⚠ **PARTIAL — RUNNING FOUND A FOURTH.** `every_screen_transition_closes_the_invite_modal` went RED, and **not because its property broke**: its slice was a fixed **1400-byte window**, and one documented line pushed `show()`'s closing brace outside it, so the seal failed to FIND the body rather than failing to find the call. Window re-aimed at the function's actual end; seal STRENGTHENED to cover both overlays. | PARTIAL | **A source-text pin's REACH is part of its contract.** A fixed-size window makes a seal's success depend on how much comment a function carries — and the failure mode looks identical to a real regression, which is the expensive part. |
| 202 | **[Seat, NA-0756]** the new GUI scenario is green on its first run | ⚠ **MISS, and the red was worth more than a green.** One pinned string used a curly `’` where the JS ships an ASCII `'`. Everything else drove correctly — a real `invite_redeem` ran, `malformed` came back, the ruled copy rendered — so the failure isolated the needle exactly. Fixed by classifying every pin as JS-sourced (ASCII) or HTML-sourced (`&rsquo;` → U+2019). | MISS | **The needle must come from the target's BYTES even when you wrote the target.** Two houses live in one app — markup uses named entities, JS uses literals — and a pin has to know which house its string comes from. |
| 203 | **[Seat, NA-0756]** `malformed` is offline-drivable through the GUI, with no relay | ⛳ **HIT, driven live in the real webview.** It is decided at `invite/mod.rs:435-442`, before any socket opens and before the burn at `:1081`, so `Connect` → `invite_redeem` → the ruled copy is a genuine end-to-end round trip in a harness that has no relay at all. It is also the likeliest real failure in the flow — a truncated paste. | HIT | **Read the ORDER of a function's gates before deciding what a relay-less harness can reach.** The arms decided before the first socket are free coverage, and they are usually the ones users actually hit. |
| 204 | **[Seat, NA-0756]** the inventory grows 171 → 178 and the gate passes on growth | ⛳ **HIT to the number.** Seven added (six seals + one driver wrapper), zero removed, gate PASS before and after the deliberate re-pin. | HIT | **Predicting the exact figure before running the gate is what makes the run a CHECK rather than an observation.** A growth-only pin that is re-written without a predicted delta is a rubber stamp. |
| 205 | **[Seat, NA-0756]** `gh pr list`'s last column is the merge time | ⚠⚠ **MISS.** It is **`createdAt`**. The true merges are `2026-08-23T18:55:04Z` (#36) and `2026-08-23T04:31:35Z` (#1787), from the typed `mergedAt` field and cross-checked against the merge commits' own committer dates, which agree to the second. STOP 002 §1 carried the wrong figures; merged-ness and the bases were independently correct. | MISS | **Never read a positional column's meaning from its position.** The house already warns against `merge_commit_sha` for merged-ness; this is the adjacent trap one column over, and the cure is the same — use the TYPED field. |
| 206 | **[Seat, NA-0756]** the blast radius of adding ONE new qsc test file is the file itself | ⚠⚠ **MISS — CAUGHT BY CI, NOT BY ME.** `qsc-shard-manifest-gate`, its macOS twin and `qsc-sharded-suite` all went RED in **3-4 seconds**: *"MISSING from manifest (present in tree) … census 143 targets / manifest 142 rows / missing 1"*. A new test target must be registered in **both** shard manifests. NA-0755's own record names *"one new test and both shard manifests"* in the same breath — the precedent was there and I did not consult it. Rows added to the lightest shard on each platform; the repo's own checker passes on both, and a negative control removing my row proves the gate is live. | MISS | **A blast radius is measured from the CONSUMER's call set, and a FILE has consumers too.** I swept everything that consumed the retargeted *buttons* and never asked what consumes a new test *target*. The tell was available for free: the failing job ran in 3 seconds, which is a manifest gate, never a test run. |
| 207 | **[Seat, NA-0757]** the baseline at `f98af5cc` is **143 suites / 0 failed** | ⛳ **HIT, to the number.** `143 / 697 / 0 / 2`, rc 0, 3h12m. The derived pass count reconciles too: NA-0756's banked `694` + that file's own 3 = **697**. Two lanes, two machines-worth of elapsed time apart, arithmetic closing to the test. |
| 208 | **[Seat, NA-0757]** the two pre-committed edits compile with **no third source edit** | ⛳ **HIT.** `enforce_safe_parents` survives at `protocol_state:892` so its import stays used; the fully-qualified `crate::fs_store::ensure_dir_secure` needs no `use` line, which is also what keeps the edit to lines 981-982 exactly. |
| 209 | **[Seat, NA-0757]** the second `event=error` line the augmentation emits breaks **no existing test** | ⛳ **HIT.** 144/144 green including `rng_failure_behavior.rs:512`, which matches by `contains` and is therefore untouched (`R388` A6 dissolved on exactly this). |
| 210 | **[Seat, NA-0757]** arm B reads **`store_code=unsafe_parent_perms`** by equality, outer `code=` unchanged | ⛳ **HIT**, and its counterfactual fired: with the augmentation reverted the marker carries `code=handshake_session_store_failed` and **no `store_code`** at all. |
| 211 | **[Seat, NA-0757]** arm A's session dir is born **`0700` under umask 002**, finish `Ok(true)`, peer ACTIVE | ⛳ **HIT.** `session_store ok=true` → `handshake_complete` → `producer_ack`, mode asserted `== 0o700` BY EQUALITY. |
| 212 | **[Seat, NA-0757]** the post-edit suite is **144 suites / 700 passed / 0 failed** | ⛳ **HIT — after removing contamination of my own making.** The raw log read `145 / 703`: killing an earlier run killed **cargo but not its spawned test binary**, and the orphan appended `NA_0640`'s result into a log I had truncated and reused. Identified by structural count (144 `Running` lines vs 145 results, census 144) and settled by re-running both affected suites clean. ⇒ **kill the process GROUP, and never reuse a log path across a killed run.** |
| 213 | **[Seat, NA-0757]** `cargo fmt --check` and `clippy -D warnings` are clean on the first attempt | ⚠ **MISS, and the premise was about the REPO, not the repair.** `fmt` is **red at base: 309 hunks**, and neither `fmt` nor `clippy` is a CI gate in this repo (zero occurrences across all 16 workflows). The controllable property holds and is the honest claim: **this lane adds no fmt diff** (309 → 309; per-file `fs_store` 0→0, `protocol_state` 0→0, `handshake` 6→6, `na0756` 2→2 after fixing the two hunks **my** block introduced). A tree-wide `fmt` would have been a 309-file out-of-scope diff and was refused. |
| 214 | **[Seat, NA-0757]** registering the new target in BOTH shard manifests passes, and omitting either fails | ⛳ **HIT.** rc 0 / rc 0 (census 144 / manifest 144 on both platforms); negative control **rc 1** with `FAIL: MISSING from manifest` on **each** manifest. NA-0756 learned this from CI in 3-4 seconds; this lane paid it locally. |
| 215 | **[Seat, NA-0757]** the mode-explicit creation regresses no existing suite | ⛳ **HIT.** 144/144, including `fs_store_contract_na0217b`'s own `assert_eq!(mode(&cfg), 0o700)` — the contract test for the edited module. The recursive `.mode(0o700)` also reaches intermediate PARENTS the old code left at the umask; nothing observes it. |
| 216 | **[Seat, NA-0757]** *(not a prediction — a result recorded because `R389` B2 directs it)* the first counterfactual for seal arm A | ⚠⚠ **THE CONTROL PASSED AGAINST A REVERTED PRODUCT — and that is the lane's sharpest measurement.** Tampering the `DirBuilder` left arm A **green (rc 0)**, because `ensure_dir_secure` already chmods `0775 → 0700` one line after creation. Diagnosed to a **real property** rather than a harness error: the **`protocol_state` collapse carries the observable repair**; the `DirBuilder` closes an **unobservable window**. Re-aimed at the collapse → **rc 101, RED**. `D-1398`'s vacuous-seal property fired one lane later and was answered correctly — the negative-control doctrine working. |
| 217 | **[Seat/W, NA-0759]** the order file's sha256 matches its own bytes before a word of it is read | ⛳ **HIT.** `ae2544bf…` exact, 3967 B, 444 — verified against the file, not against the message quoting it |
| 218 | **[Seat/W, NA-0759]** protocol main is unmoved at `0b9d6967`, measured bare and unpiped at the NAMED github remote | ⛳ **HIT.** `0b9d6967948c2fcf799cb817aeee55d5095835aa refs/heads/main`. ⚠ `origin` in these seats is the local mirror; the token was never used |
| 219 | **[Seat/W, NA-0759]** the shard-1 log carries a `failures:` list and a stdout panic block rather than an abrupt timeout kill | ⛳ **HIT.** Both present; all **35** targets reported a `test result:` line, so nothing hung |
| 220 | **[Director/I, MACOSREG order S0]** *"the regression is OURS, introduced by one of three small edits"* — i.e. something in the diff reached na0742 | ⚠⚠ **MISS, AND THE REFUTATION IS STRUCTURAL.** The poll returns at `handshake:1727`, **201 and 440 lines above** the two edited sites; the test file is **byte-identical** at both mains (`5b136eee…`). Correlation adopted as cause from a one-sample comparison |
| 221 | **[Director/I, MACOSREG order]** the macOS manifest change moved na0742's shard membership | ⚠ **MISS.** na0742 is macOS shard **1** at both shas; NA-0757's new target went to shard **0**. Excluded by measurement rather than by assumption, which is why all 13 changed files were read and not only the three named |
| 222 | **[Seat/R, NA-0759]** the 2026-08-18 macOS main-red is the same `relay_inbox_pull_failed` class (`transport:3091-3093` records that code on that date) | ⚠⚠ **MISS — A FALSE TRAIL WITH TWO MATCHING FIELDS.** Run 32149318386 failed at `macos-qsc-shard-manifest-gate` with the shards **skipped**. Date matched, error code matched, and it still was not it. Corrected before it reached the stop |
| 223 | **[Seat/R, NA-0759]** the 3618 s wall indicates a hang | ⚠ **MISS / REFUTED BY THE CONTROL.** The prior **GREEN** run was **4033 s**; ratchet 67.0% vs 74.7% of the same 90 m ceiling. The red run's runner was ~10% **faster** across the ladder — the profile of a race, not a new defect |
| 224 | **[Director/I, MACOSREG order S2(d)]** the NA-0757 augmentation's marker appears on macOS, *"which is itself the finding"* | ⚠ **MISS.** The marker is `code=relay_inbox_pull_failed`; the augmentation emits `code=handshake_session_store_failed store_code=<wire name>`. It never fired — the store was never called |
| 225 | **[Seat/W, NA-0759]** on Linux the accepted socket is BLOCKING — `O_NONBLOCK` is not inherited from the listener — and if it is not, the platform-asymmetry claim dies | ⛳ **HIT.** listener `true` / accepted `false`, measured with `std::net::TcpListener` exactly as the fixture uses it |
| 226 | **[Seat/R, NA-0759]** `read_head`'s first read on a socket with no data yet returns `Err` | ⛳ **HIT, with a bonus the prediction did not contain:** a `SO_RCVTIMEO` expiry ALSO surfaces as `WouldBlock`, so the silent-drop path exists on both platforms and the inheritance question governs only how often it is taken |
| 227 | **[Seat/W, NA-0759]** the two mechanism arms differ — only the accepted socket's blocking mode varying | ⛳ **HIT, BOTH ARMS RUN.** blocking ⇒ client receives **56 bytes**; non-blocking ⇒ client receives **0 bytes**, reproducing `relay_inbox_pull_failed` *and* `faulted()==0` together. Probe sources banked so the result is re-runnable |
| 228 | **[Seat/R, NA-0759]** the colour escapes in a `gh run view --log` capture are real ESC bytes | ⚠ **MISS.** `od -c` says literal `^` `[` two-character text. A `\x1b` needle extracted **0 targets** and an empty extraction reads exactly like "nothing to report". Needle rebuilt from the bytes |
| 229 | **[Director/I, MACOSREG order S2(e)]** *"the macOS suite scope-skips on PRs"* | ⛳ **HIT, AND SHARPER THAN PREDICTED.** Not only the shards: the **named rollup check** skips too, and the run still concludes **`success`** in ~30 s. Filed as `ENG-0244` |
| 230 | **[Seat/R, NA-0759]** the ~24 green macOS push runs since t5p landed are 24 executions of t5p | ⚠⚠ **MISS — THE DENOMINATOR WAS HALF.** Only **11** actually ran `macos-qsc-shard-1`; 13 skipped the shards on a docs-only classification. **12 executions, 11 green, 1 red.** A run list counts RUNS, not EXECUTIONS. Both control arms run and they differ |
| 231 | **[Seat/R, NA-0759]** `ENG-0243` is free and the positive control fires | ⛳ **HIT.** `ENG-0243` **0** / `ENG-0242` **5** / `ENG-0299` **1**, the last being a MENTION — a prior lane's own negative control in prose, the plant hazard again |
| 232 | **[Director/I, NA-0760 order S1]** main already carries the NA-0759 merge | ⛳ **HIT.** `merge-base --is-ancestor` rc=0; main's tip **is** #1791's merge commit, merged 10:48:42 CDT — 25 min after the order was written. Negative control (reverse direction) rc=1, so the 0 is a measurement |
| 233 | **[Seat/R, NA-0760]** gnu.org's live AGPL text equals the audit's pin | ⛳ **HIT on all three figures at once** — 34,523 B / 661 lines / sha256 `0d96a4ff…abcb0`. The order made a mismatch a STOP; there was none |
| 234 | **[Seat/R, NA-0760]** LI-01's trio still ships sha `459cd3e0…`, 33,699 B / 651 l | ⛳ **HIT**, all three byte-identical to each other and to the record |
| 235 | **[Seat/R, NA-0760]** LI-02 is one line-wrap at 636-637, `cmp` byte 33,213 | ⛳ **HIT, exact.** `diff` = 6 lines; the shipped PR diff is 2+/2− |
| 236 | **[Director/I, NA-0758 census LI-01]** the closing appendix "is absent" | ⚠ **PARTIAL.** The heading and sample notice ARE present; the appendix's **final 132 words** are absent — incl. the §13 network-source pointer. The "~132 words" figure was exact; the word *absent* over-read. Word delta +134 confirms it |
| 237 | **[Seat/W, NA-0760]** the order's PR (1) enumeration passes `goal-lint` | ⚠⚠ **MISS — AND IT BECAME THE LANE'S RULING.** rc=1 on both arms: docs (`TRACEABILITY.md`), then tests. Three of the ordered manifests match `CORE_PATH_PATTERNS`, one named explicitly by the order. Measured three ways, then STOPPED BEFORE PUSHING rather than worked around; the Director ruled OPTION B and lifted the no-tests clause for one file |
| 238 | **[Seat/R, NA-0760]** the split shape (3 core manifests dropped) passes | ⛳ **HIT.** rc=0, *"OK: goal compliance checks passed"* — so the option menu offered to the chair was measured, not proposed |
| 239 | **[Seat/R, NA-0760]** every workspace crate reports AGPL-3.0-only post-edit | ⛳ **HIT.** `cargo metadata` rc=0, 4/4 members, zero exceptions — with the virtual-workspace root reported as an enumeration point rather than silently skipped |
| 240 | **[Seat/R, NA-0760]** LICENSE-only satellite PRs go green | ⛳ **HIT.** #40 5/5, #71 4/4, #46 4/4, #6 zero-checks-by-design (`.github` is unprotected and tracks no workflows — stated so a bare check list is not read as a missing gate) |
| 241 | **[Seat/R, NA-0760]** D-max is D-1400 across all four record forms | ⛳ **HIT**, and 0 open PRs at derivation time, so control 2 could not have disagreed |
| 242 | **[Director/I, NA-0760 order S2(5)]** "the post-p2 protocol NOTICE" establishes the form | ⚠ **PARTIAL.** The form is there and was copied, but **p2 never touched `NOTICE`** — it is the na-0110 file, one commit in its whole history, and its repo list still omitted two repos (LI-06 open). The qualifier implied a change that had not happened; the Director opened #1792 against it once flagged |
| 243 | **[Seat/R, NA-0760]** `gui-driver` settles in the 5-7 min norm | ⚠ **MISS.** ~20 min. Passed. Not a required check; it reached step 8 ("the six flows") promptly, so slow rather than hung |
| 244 | **[Seat/R, NA-0760]** the local mirrors are usable as a base | ⚠⚠ **MISS, all four stale** — protocol **65 commits** behind. Caught in the seat's own output when two diff bases disagreed **4-vs-1** on file count in the attachments seat. Every branch was cut from `github/main`, so nothing shipped wrong |
| 245 | **[Seat/W, NA-0760]** the Option B shape passes `goal-lint` on the real PR, not just locally | ⛳ **HIT.** `goal-lint` **pass** on #1793, with the three core manifests correctly listed as core changes — so the fix works through CI's own entry point, not only through a synthesised payload |
| 246 | **[Seat/W, NA-0760]** the red arm fails and names the flipped crate | ⛳ **HIT, exact wording predicted.** *"1 of 4 workspace crate(s) disagree: qshield-cli: declares \\"MIT\\""*, panic at `:107`, exit 101 |
| 247 | **[Seat/W, NA-0760]** removing the shard row reddens the gate on BOTH platforms | ⛳ **HIT.** rc=1 both, *"MISSING from manifest (present in tree)"*; restore → rc=0 both |
| 248 | **[Seat/R, NA-0760]** main is unmoved at push time | ⚠ **MISS.** Moved `962fec09` → `74ec043b` (your #1792) between the gate runs and the push. Caught by re-deriving at the NAMED remote immediately before pushing rather than trusting the fetch from ten minutes earlier |
| 249 | **[Seat/W, NA-0760]** the conflict set with #1792 is EMPTY | ⛳ **HIT**, predicted by intersecting the changed-file sets BEFORE merging; git agreed — clean merge, one file, +2 lines |
| 250 | **[Seat/R, NA-0760]** a `public-safety` failure on this diff would be content-related | ⚠⚠ **MISS, and the safe direction.** Step 9 PASSED; the failure is step 15 blocking on an unfinished main. The same LICENSE bytes had already passed `public-safety` on three satellite PRs, which was the tell I should have weighted first |
| 251 | **[Seat/R, NA-0760]** this PR gets zero macOS test coverage (`ENG-0244`) | ⛳ **HIT.** Both macOS checks report `skipping`, and neither is required — a third confirming instance, obtained for free |
| 252 | **[Seat/R, NA-0760]** my two-line reading of step 15 was the whole diagnosis | ⚠⚠ **MISS — SELF-CAUGHT WHILE ASSEMBLING THIS STOP.** Eleven ERROR/NOTE lines exist, including a **403** that makes the gate assert a falsehood about branch protection. My first extraction anchored on the step's display name and returned **empty**, and I quoted a narrow grep instead of reading the block. **An empty extraction reads exactly like "nothing to report."** Corrected at §5 |

**⚠ NA-0756's 27 RESERVED ROWS, LANDED AT LAST — RENUMBERED `207-233` → `253-279`, a uniform **+46**, transcribed from the Lane B stops of record (`STOP_NA0756_006_20260824T202939Z.md` §11.1 carries `207-226`; `STOP_NA0756_008_20260825T004748Z.md` carries `227-233`, whose seven rows are **byte-identical** to STOP 007's, so the choice of stop is immaterial and was proven so by `cmp`). The original numbers collided: while these rows waited to land, `207-233` were consumed by NA-0757, NA-0759 and NA-0760 — the collision NA-0760's ruling anticipated when it held this transcription for its own later landing. **ONLY THE NUMBER CELL CHANGED**, proven by stripping the number cell from both the source rows and the landed rows and comparing the remainders for byte-identity. ⚠ These rows keep the Lane B stops' own four-column shape (`prediction | measured | score`) rather than being reworded into the three-column form the recent rows use: renumbering was ordered, rewording was not.**

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 253 | the two existing Lane B commits carry the house noreply identity | they do — AND the check exposed that the seat's local config has NO `[user]`, so the NEXT commit would have used the private address | HIT (+ unforecast finding) |
| 254 | baseline `cargo fmt --check` rc 0 | rc 0 | HIT |
| 255 | baseline `cargo test` = 166 pass / 0 fail / 12 ignored / 178 total | **165 / 0 / 13 / 178** | **PARTIAL** |
| 256 | baseline inventory enumerates 178, pin 178, PASS | exact | HIT |
| 257 | baseline `clippy -D warnings` rc 0 | rc 0 | HIT |
| 258 | baseline `gui_driver` 12 pass / 0 fail | exact | HIT |
| 259 | the exact v1 button tag form occurs exactly 1× per row in `index.html` | 1 and 1 | HIT |
| 260 | the chooser slice holds 1 `btnrow` today ⇒ the negative pin WOULD fire at the v1 head | 1 | HIT |
| 261 | the new CSS satisfies all four design-system disciplines (predicted by re-implementing them in python first) | `design_system` 6/6; the replicas matched the real tests exactly | HIT |
| 262 | `json.dumps(indent=1, ensure_ascii=True)+"\n"` round-trips the scenario BYTE-identically | True | HIT |
| 263 | post-change `cargo test` = 166 / 0 / 13, 179 total | exact | HIT |
| 264 | post-change inventory 179, pin 179, PASS | exact | HIT |
| 265 | post-change `clippy` rc 0 | rc 0 | HIT |
| 266 | post-change GUI = 12 pass / **547 steps**, `f_l` 64 → 74 verdicts (derived: +18 json steps of which 8 are `note` ops) | **547 / 74, exact** | HIT |
| 267 | trailers 0 on the new commit; positive control returns 1 | exact | HIT |
| 268 | the push is a fast-forward on an EXISTING branch, with no "Create a pull request" hint | exact | HIT |
| 269 | C1-C5 each go red on the assertion each NAMES | all five red, five DISTINCT source lines, each the named assertion | HIT |
| 270 | G1 (rows re-wrapped side-by-side) → geometry probe RED while the copy pins stay GREEN | exact: `rows:4`, all four copy pins PASS | HIT |
| 271 | G2 (rows inline at 48%, child count UNCHANGED) → `full:false, stacked:false`, `rows` still 5 | exact | HIT |
| 272 | all five CI checks settle green | 5 pass / 0 fail | HIT |
| 273 | github main = `1ca40bc0` (the Director's figure) AND this seat's `origin/main` is STALE and unequal to it | both true — mirror at `11e8e17c` | HIT |
| 274 | the two sides' changed-file sets intersect in exactly `DECISIONS.md`, so it is the ONLY file that can conflict | exact — 1 conflicted file | HIT |
| 275 | merged-head `cargo test` = 166 / 0 / 13 at 179, UNCHANGED by the pin move | exact | HIT |
| 276 | inventory 179 / pin 179 | exact | HIT |
| 277 | fmt rc 0 and clippy rc 0 on the merged head | both rc 0 | HIT |
| 278 | GUI 12 pass / **547 steps** at the NEW pin (unmeasured by #38) | exact | HIT |
| 279 | all five CI checks settle green | 5 / 0 | HIT |

**⚠ NA-0762 — THE LIVENESS RUNWAY'S LANE ONE, RE-SCOPED TO A RECORDS LANE (`D-1403`). Rows 280-290 were written and measured at the premises phase and at the edit. Rows 291-294 are SR-22 FIRST PASS: the assertion is registered here with its measurement slot deliberately EMPTY, because those figures cannot be measured until the PR is open; they are filled in a second commit on this branch and reported in STOP 002. A slot left empty is the discipline working, not an omission.**

| # | the assertion | the measurement | the instrument that caught it |
|---|---|---|---|
| 280 | **[Seat/R, NA-0762]** carried state had protocol main at `35c80c00` with #1794 OPEN; predicted BEFORE running that main had MOVED and that the brief's base `c859fcb9` would prove to be #1794's own merge commit | ⛳ **HIT, both halves.** `c859fcb9` is main's tip AND `mergeCommit.oid` of #1794, `mergedAt` non-null | bare unpiped `ls-remote` at the NAMED remote + `gh pr view`, predicted aloud first |
| 281 | **[Seat/W, NA-0762]** the brief's sec-0 premise — the three pull-only commands *"never ack what they consume (by construction, measured)"* | ⚠⚠ **MISS, and the lane's whole reason.** FIVE guarded producer-ack call sites, Lease-gated, shipped **2026-08-17** at NA-0742 / `D-1379` / PR #1760 | a census of ack sites in product source, run because premise (b) ordered re-measurement *from bytes* rather than adoption |
| 282 | **[Seat/R, NA-0762]** STOP 001's own figure of **"six"** producer-ack call sites | ⚠⚠ **MISS — the measured number is FIVE.** The six was STOP 001's own S4 **pull**-site table row-count read as an **ack**-site count; its sixth row is the Legacy arm, whose ack cell already read *"none, correctly"* | the order carried FIVE, the seat carried SIX, and the disagreement was **measured** rather than reconciled by preference — caught before it reached any record |
| 283 | **[Seat/W, NA-0762]** the `ENG-0142` entry carries no lane-2-built amendment | ⛳ **HIT.** Needle `LANE 2 IS BUILT` = 0 in the entry region, against a **positive control** `LANE 1 IS BUILT` = 1 proving the needle method works | grep with a positive control, because an absence measured by an instrument that cannot return presence is not evidence |
| 284 | **[Seat/W, NA-0762]** both riding one-liners are still owed at the edit | ⛳ **HIT, both.** `D-1401`'s Class field still read *"to be amended when it is declared"*; `(excerpt)` = 0 in the `D-1400` region | re-measured at the edit rather than inherited from STOP 001 — a debt is base-scoped |
| 285 | **[Seat/W, NA-0762]** `D-1400`'s fence is an excerpt, so the whole-file sha above it cannot be checked against it | ⛳ **HIT, exact.** Fence content = **67 lines**; the citation immediately above declares the source **84 lines / 6098 B** | located the fence **by content**, per the order, never by the reported line number |
| 286 | **[Seat/W, NA-0762]** the `qsc_rng_failure_test_seam` still does not compile at this newer base | ⛳ **HIT.** **4 compile errors** in `vault/mod.rs` (`:570`, `:578`, `:728`, `:733`), reproducing NA-0742's own §12(D) with the identical command — which also confirmed the invocation was the house idiom rather than a bad instrument | `RUSTFLAGS='--cfg qsc_rng_failure_test_seam'`; blame dated the break to `ba1411491`, **2026-07-15**, a month BEFORE NA-0742 |
| 287 | **[Seat/W, NA-0762]** the brief's own instruments are green at this base | ⛳ **HIT.** `na0742_invite_finish_scan_producer_acks` — **12 passed / 0 failed / 0 ignored**, 751.02 s | a full targeted re-run, because "the tests exist" is not "the property holds" |
| 288 | **[Seat/W, NA-0762]** the `ENG-0142` amendment can be landed **beside-only**, editing nothing above it | ⛳ **HIT.** That file's diff is **7 insertions / 0 deletions** | `git diff -U0` deletion count, asserted as a gate on the edit rather than inspected after |
| 289 | **[Seat/W, NA-0762]** exactly ONE anchored `^Status: READY` survives the flip, for `preflight_governance.sh` | ⛳ **HIT.** count = **1**, and it is NA-0762's own | anchored `grep -c`, run immediately after the flip |
| 290 | **[Seat/W, NA-0762]** the class transcription is byte-exact against the close-out's own bytes | ⛳ **HIT.** Both sides shown and grep-equal, with a **tampered needle** confirming the comparison discriminates | the NA-0761 precedent's own method, reused because the order named the property that made it work |
| 291 | **[Seat/W, NA-0762]** `goal-lint` passes on the real PR — this lane touches no core path, and the `Goals:` line is in the PR **body** | ⛳ **HIT.** `goal-lint` **SUCCESS** on #1795 — one of the 15 required contexts, resolved **by name**. | the `Goals:` line was placed in the PR **body**, never as a trailing line of the commit message, because a trailing `Goals:` line is parsed as a **git trailer** |
| 292 | **[Seat/W, NA-0762]** CI classifies this PR **docs-only** | ⛳ **HIT, and EXECUTED rather than inferred from job names.** `bash scripts/ci/classify_ci_scope.sh` on the PR's own changed-file set (fetched from the API, not from the local diff) → **`docs_only=true`**, `workflow_security=false`, `runtime_critical=false`, **`scope_class=docs_only`**. | the tree's own classifier, run as the consumer (SR-20), with a **negative control**: appending one `qsc/src/transport/mod.rs` path flips it to `docs_only=false` / `scope_class=runtime_critical`, so the true is discriminating rather than vacuous |
| 293 | **[Seat/W, NA-0762]** every required check settles green, with APPEAR-then-SETTLE observed rather than a first reading trusted | ⛳ **HIT.** **34 SUCCESS / 0 FAILURE / 10 SKIPPED** of 44; **all 15 required contexts green BY NAME, 0 absent-or-not-green** — including `public-safety` (which blocked NA-0760) and `CodeQL`. | APPEAR-then-SETTLE observed rather than a first reading trusted: 42→44 registered, then 5→1→1→1→1→0 still running across the settle poll. ⚠ `CodeQL` was resolved **as its own CheckRun**, distinct from its three green `Analyze (…)` feeders, because a green Analyze is not a green CodeQL gate |
| 294 | **[Seat/W, NA-0762]** no check reddens on the `ENG-0142` amendment's unicode (the entry already carries ⚠/⛳ and em dashes) | ⛳ **HIT.** Zero failures of any kind; the amendment's ⚠/⛳ and em dashes reddened nothing. | the full rollup tally, read after settle — the entry already carried the same character classes, so this was a low-risk prediction and is recorded as one |
| 295 | **[Seat/W, NA-0763]** the two committed `"why":"surface_open"` expects in `f_l_invite_redeem.json` go red the moment a tick exists, because the scan's no-relay short-circuit still marks | ⚠ **MISS, and the correction matters.** Measured with the marker separation REMOVED, `na0756_gui_l_invite_redeem` **still passes**: that scenario configures no relay, so `R10`'s gate stops the tick from running there at all. The reasoning was right about `redeemMark` and wrong about reachability — the gate is an EARLIER protection than the one being reasoned about. ⇒ both protections are real and which is load-bearing depends on the scenario. |
| 296 | **[Seat/W, NA-0763]** the SR-18 census at STOP 001 found every committed assertion this lane would break | ⚠ **MISS.** The census was scoped to tests pinning the scan MARKERS, per the brief's I4 wording, and therefore missed two other classes the full suite caught at once: `design_polish.rs` pins both trigger CALL SITES **and** the whole-file `setInterval` count by exact string, and two tests build `AppSettings` with **exhaustive struct literals** that a new field breaks (E0063). ⇒ a census of "tests pinning X" is not a census of "tests this change breaks". |
| 297 | **[Seat/W, NA-0763]** the tempo seam can ride `AppSettings` behind `#[serde(skip)]`, satisfying R4 while still reaching the UI | ⚠ **MISS, caught by the lane's own passing test before it shipped.** `skip` omits a field from **serialization**, and Tauri's IPC serializes with the same impl — so the value could never have reached the UI. The very assertion proving it could not reach the FILE also proved it could not reach the WEBVIEW. A probe printed `{"autolock_minutes":60}` and settled it; the seam moved to a `Serialize`-only DTO with no save path, which is structurally stronger. |
| 298 | **[Seat/W, NA-0763]** the I3 single-flight delta is exactly 1 refusal | ⚠ **MISS TWICE, then made deterministic.** First run measured **60** — the running tick being refused, which is the guard working but not a deterministic arm. With the tick's gate closed it measured **2**, because a tick's scan was still IN FLIGHT and both calls were refused. Only after WAITING FOR QUIESCENCE (`relayScanBusy` falling) does it read exactly 1. ⇒ closing a gate stops new work; it does not abort work already running. |
| 299 | **[Seat/W, NA-0763]** I1's red arm fires: with the timer disabled, zero tick-sourced runs | ⛳ **HIT, RUN not asserted.** `poll_exec I1_tick_fires` → `TIMEOUT after 20 polls, last=0`. |
| 300 | **[Seat/W, NA-0763]** I2's red arm fires: with the lock gate flipped open, the locked window shows ticks | ⛳ **HIT, RUN.** The post-lock gate check read `true` where `false` was expected, at exactly the predicted step. |
| 301 | **[Seat/W, NA-0763]** I4's red arm fires: with the marker separation removed, the shared slot reads `tick` | ⛳ **HIT, RUN.** Measured `{"value":"tick"}` against the expected `unlock`. |
| 302 | **[Seat/W, NA-0763]** the `R4` seal's red arm fires: dropping `#[serde(skip)]` puts the seam in the saved file | ⛳ **HIT, and MORE SENSITIVE THAN PREDICTED.** Predicted one failing arm; measured **two** — the seal itself and `settings_key_allowlist`, which catches the same break independently, the file reading `{"autolock_minutes":60,"tick_override_ms":137}`. |
| 303 | **[Seat/W, NA-0763]** the measured edit set stays within SR-15's line of five source files | ⛳ **HIT.** Four: `ui/main.js`, `ui/index.html`, `src-tauri/src/settings.rs`, `src-tauri/src/commands.rs`. No lock or crypto adjacency; nothing retired. |
| 304 | **[Seat/W, NA-0763]** `goal-lint`'s core-path trap does not fire on the desktop PR, though `src-tauri/src/**` contains a bare `src` segment and the repo has no `TRACEABILITY.md` | ⛳ **HIT, measured rather than hoped.** The desktop repo does not carry `goal_lint` at all — zero hits repo-wide — and its single `ci.yml` runs fmt/test/inventory/clippy plus the scan jobs. The NA-0760 shape was checked for and is absent. |
| 305 | **[Seat/W, NA-0763]** the full desktop suite is green at the head, all gui-driver flows included | ⛳ **HIT.** **182 passed / 0 failed**; all **13** flows green in 162.04 s; `fmt`, `clippy -D warnings` and `test_inventory` all rc=0. Baseline at the base was 166/0 before the lane's arms were added. |
| 306 | **[Seat/W, NA-0763]** the new instrument will actually gate the PR rather than reporting a vacuous green | ⛳ **HIT on the structural half, measured at STOP 001:** the `gui-driver` job has **no `if:` condition**, no path filter, and the workflow triggers on both `push: [main]` and `pull_request:`, running every `#[ignore]` row. ⚠ The CI half is adjudicated on the real PR at STOP 2, not here. |
| 307 | **[Seat/W, NA-0763]** the brief's *"bounded 8 pulls × 16 frames"* is not the desktop's finish-scan bound, because the GUI passes `max: 1` | ⚠ **MISS — RETRACTED, and the brief was right.** `finish_scan_select_invite_resp` computes `max.clamp(FINISH_SCAN_TOTAL_MIN=16, FINISH_SCAN_TOTAL_MAX=128)` and stops at `FINISH_SCAN_PULLS_MAX=8`, so `max: 1` is clamped UP to exactly 16 and the figure is **exact**. The seat compared a call-site ARGUMENT against a bound computed AFTER clamping, and explicitly declined to chase it into `qsc` — precisely where the answer was. ⇒ *declining to look is itself a measurement decision, and it can be the wrong one.* |
| 308 | **[Seat/W, NA-0763]** the tick makes an invite complete end to end with zero human nudges, so `E1` is reachable on a two-desktop flight | ⚠ **NOT AS STATED, and found while deriving the flight card rather than on the rig.** The chain is Bob `invite_redeem` → **Alice `invite_accept`** → Bob `invite_finish`. The tick automates **Bob's finish only**; `invite_accept` is registered as a desktop command and invoked from `ui/` **zero** times, reachable only from the `qsc` CLI. ⇒ on two desktops Alice's accept must be driven from the CLI or the chain stalls and the tick spins finding nothing. Pre-existing, not repaired here, and carried in the flight card so the flight adjudicates `E1` against what the code can actually do. |
| 309 | **[Seat/W, NA-0763]** the harness green at the head means the tick is correct; CI will agree | ⚠⚠ **MISS, AND THE MOST VALUABLE ONE.** Green **twice** locally, **RED on CI** — `enterMain` hung. Cause: `relayScan` re-read the pending slot **in a loop**, so whenever a **beat is shorter than a scan**, the timer refills it faster than the loop drains it and the loop never terminates. Cured structurally (at most ONE rerun, slot cleared — a rerun BIT). ⛳ Now proven at an **adversarial 30 ms seam** where the old code hung deterministically, AND at the committed 250 ms. ⇒ *a slower box is a different instrument; and "never stacks" must be bounded by construction, not by how fast the timer happens to be.* |
| 310 | **[Seat/W, NA-0763]** the `I2` locked-window arm measures ticks WHILE LOCKED | ⚠ **MISS at an adversarial tempo, corrected.** At 30 ms it read a delta of **2** — correctly: ~2 beats fit between *asking* to lock and the screen actually changing, and the vault is not locked in that gap. The arm sampled its baseline before the lock LANDED. It now samples after `scr-unlock` is visible. ⇒ *an arm that passes only at the tempo you chose is not measuring what its name says.* |
| 311 | **[Seat/W, NA-0763]** a red-arm run through `runner.py` measures the same thing a run through `gui_driver.rs` does | ⚠⚠ **MISS, TWICE, AND IT COST TWO FALSE DIAGNOSES.** `gui_driver.rs` is what sets `QSLD_TICK_MS`; invoked directly, `runner.py` gives the app the shipped INSTANT tempo (a 15-25 s beat), so `I1`'s bounded window sees ~1 tick and the run fails `I1_tick_fires TIMEOUT, last=1`. That was read once as a red arm's result and once as a baseline flake — both invalid. ⇒ *an instrument invoked wrongly reports a RESULT, not an error.* Cured by asserting `tickOverrideMs !== null` immediately after launch, so the run dies in seconds naming its own cause. |
| 312 | **[Seat/W, NA-0763]** a tamper that compiles is a valid red arm | ⚠ **MISS.** One tamper produced `if (false) const next = …` — a JS syntax error — and the run failed at the launch-time liveness check with `measured=''` (the page never rendered). It "failed", which looked like the arm firing, and proved nothing. `node --check` had caught it, but its output was suppressed behind `&& echo OK` so the absent OK went unnoticed. ⇒ *a red arm must be shown to fail FOR ITS OWN REASON; check the tamper parses before believing the red.* |
| 313 | **[Seat/W, NA-0763]** piping a build through `head` is harmless | ⚠ **MISS.** `cargo build \| head -2` closes the pipe early and can kill the build, so an arm ran against a STALE binary and reported the PREVIOUS arm's failure verbatim. ⇒ *never truncate the output of a build whose product you are about to measure.* |
| 314 | **[Seat/W, NA-0764]** the brief's approval gate has a backend state to render | ⚠⚠ **MISS, AND IT RE-SCOPED THE LANE.** There is none: `Redeemed` is written by the creator's OWN accept and refused as an input by that same function; the slot is pulled at exactly ONE site inside it; the relay exposes no non-consuming read. You cannot check without accepting. Measured at the premises phase, before any edit |
| 315 | **[Cold read, NA-0764]** `missing_seed` is a structural fault and `no_session` is the only defensible "connecting" member (C-16) | ⚠⚠ **MISS — BACKWARDS AGAINST THE CONSUMER.** The desktop never sets `QSC_QSP_SEED`, so `no_session` is UNREACHABLE and every not-yet-connected contact answers `missing_seed`. Shipping C-16's reading puts "This connection has a storage problem" on the commonest state in the app and leaves Connecting… with no member. `D-0033` had ruled it once already |
| 316 | **[Cold read, NA-0764]** F4's refresh path is unmeasured for re-entrancy (C-12) | ⛳⛳ **HIT, AND THE HARNESS THEN REPRODUCED IT.** Planted rows were asserted present and were gone two steps later when an in-flight refresh resolved on top of them. A stale refresh could publish over a newer one. Cured with a generation counter checked at EVERY publication point |
| 317 | **[Seat/W, NA-0764]** neutralising `refreshContacts` stops an in-flight refresh | ⚠ **MISS.** Reassigning the binding cannot stop a call already executing; the first fix changed nothing and the same row failed identically. The cure had to be in the product, not the test |
| 318 | **[Seat/W, NA-0764]** dropping `#[serde(default)]` reddens the legacy-blob arm | ⚠ **MISS — A RED ARM CLAIMED AND NOT HELD.** It was run and NOTHING went red: serde already defaults a missing `Option<T>` to `None`. The arm the test actually has is its vacuity guard (`skip_serializing_if`), which fires with an exact red set of one. Corrected in the open by its own author |
| 319 | **[Seat/W, NA-0764]** a bare push can plant a frame in an invite slot | ⚠ **MISS, AND THE REFUSAL IS A SECURITY PROPERTY.** 403 `ERR_INVITE_TICKET_INVALID` — an invite slot is capability-gated ("Reachable ONLY for a route the invite system created"), so the tick's every-beat empty accepts cannot be poisoned by a third party. The relay was right and the test was wrong |
| 320 | **[Seat/W, NA-0764]** `R1`'s `alias := the invite's own label` always has a label | ⚠ **MISS.** The mint's own caption calls the label OPTIONAL, so a blessed GUI flow produces invites with no alias to provision under. Skipped and TALLIED in `marks.unlabelled` so the gap is observable; disposition carried to the operator |
| 321 | **[Seat/W, NA-0764]** `D-1404`'s "44 tauri commands" is correct | ⚠ **MISS.** It registers **42**, and did at that record's own base — measured two agreeing ways, with the instrument validated against a known-different answer (39 at an older base) and six candidate instruments enumerated, none yielding 44. The conclusion it supported is undisturbed; the count was the evidence, not the finding |
| 322 | **[Seat/W, NA-0764]** a JSON round-trip edit keeps a scenario's diff minimal | ⚠ **MISS.** Re-serialising `f_h` reformatted the whole file: 206 changed lines for a 2-step addition. Restored to a string insertion, +2/−0, and re-run green — the same minimal-diff discipline the shard-manifest edit had already needed |
| 323 | **[Director, NA-0765]** `B5` — the invite-code input is a one-line box and must be made multi-line | ⚠⚠ **MISS, AND IT IS THE DIRECTOR'S OWN, RECORDED BY HIM AT THE RULING'S SEC 4.** `#redeem-code` was ALREADY a `<textarea>` at `min-height: 5.5rem` — **88px of box against the blessed layout's 86px** — and an element census found **exactly ONE textarea in the whole app**, that one. Cause: a **mockup-VERSION delta (v1 → v2)** transcribed into a brief as an APP defect, without measuring the app. Same family as the NA-0764 miss: a claim about the tree that was never checked against the tree. ⇒ *when a mockup is versioned, one of its "fixes" may be a fix to the MOCKUP — measure the app, never the mockup's changelog.* |
| 324 | **[Director, NA-0765]** the brief's sec 6(d) filing has a home inside the authorized edit set | ⚠ **MISS.** The filing declares an `ENG-` id, and those live in `docs/ops/IMPROVEMENT_LEDGER.md`, which sec 2's enumeration did not name — while the `ENG-0246` precedent (NA-0761, `b21135f0`) touched exactly sec 2's five files **plus** that ledger. The seat STOPPED and asked instead of widening the set itself, and the ruling added the file. **The third instance of a brief contradicting its own enumeration on this chair's lineage**, and the STOP-and-ask is the HIT. |
| 325 | **[Director, NA-0765]** an abbreviated digest is enough to identify an input | ⚠ **MISS, SECOND INSTANCE.** The brief cited the v5 mockup as `cc7691db...62f8`, leaving **52 of 64 digits uncomparable against the brief**. The seat routed around it by verifying v5 against its own NA-0764 **444 bank** and recording the full digest from an immutable artifact. ⇒ *full digests always; the cure is mechanical.* |
| 326 | **[Seat/W, NA-0765]** the blessed Block/Unblock controls can be built at this pin | ⚠⚠ **MISS, AND IT RE-SCOPED THE LANE — MEASURED BEFORE ANY EDIT.** `contacts_block`/`contacts_unblock` exist at `contacts/mod.rs:1541`/`:1551` and are **NOT IN THE FACADE** (0 hits each; positive control `contact_set_display_name` = 1). The one reachable verb, `contact_request_block`, is **one-way** and writes `primary.state = "REVOKED"`, which nothing exposed restores — so the layout's *"restores the connection you already had"* would be FALSE. Ruled `Option A`: defer. ⇒ *a control whose blessed copy is measurably untrue does not ship.* |
| 327 | **[Seat/W, NA-0765]** `A3` is deliverable inside the brief's `ui/`-only edit set | ⚠ **MISS.** `facade::contact_set_display_name` shipped at NA-0764 and was **never reachable** — absent from `generate_handler!`, and `ui/main.js` invokes 35 commands of which none is a rename. Without widening by two desktop `.rs` files, `A3` would have landed at ZERO. Ruled `R-2 = widen`; commands 42 → 43; product source 5, so SR-15 still does not trigger. |
| 328 | **[Seat/W, NA-0765]** STOP 001's census named every pin the repairs move | ⚠ **MISS, BY THREE, AND FOUND BY RE-CENSUSING.** The stop reported FOUR re-aims; the true set is **SEVEN**. The extra three — `f_l`'s own `#btn-invite-open` click, the second `#redeem-name-hint` read, and the hint pin sharing a test with the caption pin — appeared only when the census was re-run over the ACTUAL change list rather than over the stop's summary of it. ⇒ *re-census the change list, not the description of the change list.* |
| 329 | **[Seat/W, NA-0765]** a seal that bans a phrase can safely explain itself in prose | ⚠⚠ **MISS, TWICE MORE IN ONE LANE (`ENG-0235`'s family).** A draft assertion banned two English words in a paragraph containing both — caught in review. Then a stylesheet comment explaining a REMOVED class named the class, and the test caught it for real. ⇒ *describe the retired thing, never spell it; and where a needle must exist, ASSEMBLE it at runtime from halves that never sit adjacent in any file.* This is a hazard that survives being known about. |
| 330 | **[Seat/W, NA-0765]** `I2`'s subject row can be any planted contact | ⚠ **MISS, caught by the instrument on its first run.** The planted rig gives **only `dana`** a fingerprint, so every other row draws two sections rather than three and renders its bare alias rather than a display name. The first draft asserted a name the plant never had. ⇒ *read the fixture's own data before predicting what it renders.* The correction was made against the plant, not guessed a second time. |
| 331 | **[Seat/W, NA-0765]** the predicted red set names the test that will fail | ⛳ **PARTIAL — the substance held and the LABEL did not.** Phase 0 predicted "the design_polish test holding :2559 + :2563 goes red, and nothing else"; exactly one test failed and it was that one — but the name written down, `na0756_v2_the_code_entry_copy_is_the_ruled_wording`, is not its name (`na0756_the_redeem_copy_ships_in_its_blessed_form`). ⇒ *a red set is a set of NAMES; copy them from the tree, not from memory of what they probably are.* |
| 332 | **[Seat/W, NA-0765]** `I3`'s structural census must be built by this lane | ⛳ **HIT for the tree, MISS for the plan.** It was **already in main WITH its can-fail proof**, so the ruling's "committed and red-capable BEFORE Rename is wired" held by construction. The lane did not manufacture a second census; it ran a control proving the inherited one guards the **NEW** call — swapping the label into the key's argument position moved the count to 1. ⇒ *an inherited seal proves nothing about code that did not exist when it was written; aim it at the new call.* |
| 333 | **[Director, NA-0764]** verifying Lane C's PR by STRING presence establishes that the blessed ELEMENTS shipped | ⚠⚠ **MISS, AND IT IS WHY NA-0765 EXISTS.** The claim was element presence; the check was string presence, so a detail pane missing its Rename control, its Block control and its whole section structure **passed review**. This is the *instrument-narrower-than-the-claim* family, and the **second instance inside NA-0764 alone**. ⛳ The artifact that made it invisible was **DEAD CSS**: a divider class declared once and used ZERO times — styling for a row that was never built, which a string scan cannot see and an element count can. ⇒ **the cure is I2's shape: assert the ELEMENT.** NA-0765 lands it as `find_count` rows over the rendered DOM plus a renderer-structure seal, and control C-3 proves that row can go red. |
| 334 | **[Property, NA-0764 → NA-0765]** a lane may safely mint its own term for a thing the surface already names | ⚠⚠ **MISS, RECORDED AS A PROPERTY RATHER THAN AN INCIDENT.** Lane C called the contact's code by a new name while the shipped Settings > Identity pane had said **"Verification code"** since before that lane existed. The cost is not the word — it is that **a second vocabulary forces every later screen to choose**, and the two drift apart silently because nothing compares them. Measured at repair time: the invented term stood in **6 places** (3 user-visible strings, 3 source comments) and the app's own word in 2. ⇒ **THE PROPERTY: before minting a term for a thing, SWEEP THE SHIPPED SURFACE for what that thing is already called; a lane that invents a second name creates a second vocabulary.** Sealed by `na0765_the_verification_code_naming_is_singular`, whose needle is ASSEMBLED at runtime so the seal cannot fire on its own prose. |
| 335 | **[Director, NA-0764/0765 close-out]** a mockup may add a CONTROL and the control will be buildable | ⚠⚠ **MISS, and it is the Director's own, carried forward from the close-out's SR-25 sec 4.** The blessed layout drew **Block**; nothing checked whether Block was BUILDABLE until NA-0765's repair lane measured that the only reachable verb is one-way and destructive, making the blessed caption *"unblocking restores the connection you already had"* measurably FALSE. Cost: one deferred control, one round of rework. ⇒ **STANDING PRACTICE, minted from this miss: when a mockup adds a CONTROL, PRICE ITS VERB AGAINST THE FACADE BEFORE THE BLESS, not after.** | the NA-0765 seat's facade census (0 hits for the honest pair, positive control = 1) |
| 336 | **[Director, NA-0764]** verifying a PR by STRING presence establishes that the blessed ELEMENTS shipped | ⚠⚠ **MISS — transcribed from the close-out's sec 4(i), and it EARNED ITS KEEP INSIDE THE VERY STOP THAT TRANSCRIBED IT.** NA-0766's element census caught `btn-invite-back` rendering as the fresh-mint label: a search for the Back word would have mis-filed it under item 3, and a search for the button's label returns **ZERO** hits across the test surfaces because that label ships entity-encoded. ⇒ **verify by ELEMENT, never by string.** | NA-0766's per-view control census (id + class + rendered label) |
| 337 | **[Director, NA-0764/0765]** a truncated sha in a relay is good enough to work from | ⚠ **MISS, twice, and mechanical.** 52 of 64 digits are uncomparable, so a seat cannot verify what it was handed. Both seats routed around it correctly; the fix is the Director's. ⇒ **FULL DIGESTS IN EVERY RELAY.** NA-0766's kickoff carried all 64 for all three inputs and every one verified with a negative control on the comparator. | the receiving seats' own verification steps |
| 338 | **[Director, NA-0766 brief]** the owed "require a name at invite creation" item lives in `docs/ops/IMPROVEMENT_LEDGER.md`, so the brief may order "the ledger entry" amended | ⚠⚠ **MISS — THE WRONG-ADDRESS PREMISE, AND IT IS THE FOURTH INSTANCE IN SIX LANES.** Measured with controls: `require a name` = 0 and `skip-and-count` = 0 in that ledger, with `block/unblock` = 1 as the positive control on the same needle; the item lives ONCE in `DECISIONS.md` (sentence case) and ONCE in `NEXT_ACTIONS.md` (**UPPERCASE**, which is why a case-sensitive census finds it in one file and not the other). **Cause, named by the Director in the open:** the destination was written from the item's ROLE — *"it's a backlog item, backlog items live in the ledger"* — instead of being measured. ⇒ **a brief ordering an amendment to an existing record must carry that record's MEASURED coordinates, or say plainly that the seat must locate it first.** | the NA-0766 seat's six-needle census with a positive control, re-measured independently by the Director |
| 339 | **[Seat, NA-0766]** a baseline run may be piped through `tail` and still be reportable | ⚠ **MISS, SELF-REPORTED, AND THE THIRD INSTANCE BY THIS SEAT'S OWN COUNT.** The exit status survived only because `PIPESTATUS[0]` was read; the TEST COUNTS were truncated away and unmeasurable. Re-run unpiped by redirect: 181 passed / 0 failed / 15 ignored. ⇒ **never pipe the check that gates you** — and *recording a lesson does not install it; only an instrument does.* | the seat's own re-read of the captured output |
| 340 | **[Seat, NA-0766]** a copy seal asserting a control's label is PRESENT can still catch that control's removal | ⚠⚠⚠ **MISS, AND IT HAD BEEN UNFALSIFIABLE FOR TWO LANES.** `design_polish.rs` asserted the retired mid-mint cancel control's label was present and was *"the ONLY post-activate action"*. v4 removed the control; the assertion kept passing because `html_says` collapses whitespace but does NOT strip HTML comments, and the label's only remaining occurrence was **inside the comment that recorded the removal**. Its counterpart 700 lines below asserted the OPPOSITE and also passed, because ITS needles were on the shipped forms. **Two green seals contradicting each other, invisible because one could no longer fail.** Proven both arms (raw → present; comments stripped → absent) with positive and negative controls, and confirmed independently by the Director. ⇒ **A COMMENT THAT DOCUMENTS A REMOVAL RE-PLANTS THE REMOVED THING'S NEEDLE.** Any seal built on bare-word presence can be held green by the explanation of its own subject's deletion; needle on the SHIPPED FORM. | `na0766_a_comment_cannot_satisfy_a_copy_seal`, whose can-fail arm fired on exactly that shape |
| 341 | **[Seat, NA-0766]** knowing the re-planting hazard is enough to avoid committing it | ⚠⚠ **MISS, IN THE SAME LANE, BY THE SEAT THAT HAD JUST DIAGNOSED IT.** A self-check over the lane's own first draft found **TEN** re-plantings written while documenting these very removals — retired ids and labels spelled inside the comments explaining their retirement. All ten corrected to DESCRIBE rather than spell. ⇒ *the diagnosis is not the cure; only a running check is.* | a comment-stripping self-check run over the lane's own diff, with a positive control proving real markup survives stripping |
| 342 | **[Seat, NA-0766 Phase 0]** "hide one element pre-activation" is a valid can-fail arm for the no-transform instrument | ⚠ **MISS, AND THE INSTRUMENT WAS INNOCENT.** `I4` measures a DELTA across activation, so an element hidden in BOTH states is invisible to it BY CONSTRUCTION — the perturbation went red at a later row and `I4` itself passed. The corrected arm makes ACTIVATION ITSELF move something, and `I4` then fired at exactly the compare row, naming the moved element set. ⇒ **a can-fail arm must perturb the PROPERTY the instrument measures, not merely the screen it looks at.** | Phase 0's own verdict log, read row by row rather than trusted at the file level |
| 343 | **[Director, NA-0766 ruling Q4]** the tenth-invite boundary is reachable by the gui-driver, so the instrument can prove the cap suppression there | ⚠ **MISS, and it is an environment bound rather than an error.** Reaching that boundary needs TEN live invites, hence a successful `invite_create`, hence a relay — and the desktop harness has **no fixture relay** (`ENG-0226`, open). The runtime arm proves the window does not move with the cap latched; the claim it cannot make is sealed STRUCTURALLY instead, by a seal whose can-fail arm is the retired defect itself (the toggle put back into the refresh) and which fired on it. ⇒ **when an ordered instrument cannot reach its boundary, bring a different instrument and SAY SO — never a weaker version of the same one wearing the ordered name.** | `na0766_the_cap_line_is_decided_once_at_open` + Phase 0 control C6 |
| 344 | **[Seat, NA-0766 STOP 1]** the SR-18 census named every test the change would move | ⚠ **MISS BY ONE, and the stop had already called itself a floor.** Seven seals went red; six were named at STOP 1 and `the_invite_code_box_wraps_and_is_selectable` was not — it pins the minted class in the markup, which item 6 moves to runtime. ⇒ *a census of a change list is not a census of the diff;* re-census against the real diff, which is what found it. | the full suite, run after the first edit rather than trusted |
| 345 | **[Director, NA-0767 brief sec 5, H1 PRIMARY]** the inviter's session is never built because completing it requires a handshake POLL and no shipped trigger performs one | ⛳⛳ **HIT, confirmed twice over.** Measured from the engine's own emission sites (the responder stores at the A2 branch `handshake/mod.rs:2167` and nowhere else; her A1 branch stores only a pending record) AND on the rig: a single `handshake poll` outside the tick's lease window completes her (`handshake_recv msg=A2 ok=true` → `session_store ok=true` → `handshake_complete role=responder peer_confirmed=yes`). Independently re-measured by the Director at the same bases with zero discrepancies | HIT | the Director's primary hypothesis was right about the mechanism, and it is the mechanism his own NA-0763 deviation removed |
| 346 | **[Director, NA-0767 brief sec 5, H2]** the inviter's accept builds a partial session that a later act discards or fails to persist | ⚠ **MISS — storage is innocent.** The pending record persists correctly and is still loadable at X5: `handshake_pending peer=diana present=true role=responder key=handshake.pending.self.diana state=present`. Nothing is discarded; the second half is simply never performed | MISS | "not persisted" and "never performed" present identically from outside — the pending record's own marker is what separates them |
| 347 | **[Director, NA-0767 brief sec 5, H3]** the session exists and the GUI reads it wrongly — a display defect | ⚠ **MISS, and the GUI is exonerated.** `qsp_status_tuple` and `handshake_status` branch on the SAME `qsp_session_load` call, so "Connecting…" is the honest rendering of a genuinely absent session | MISS | ⛳ the Director's own hedge — *"the seat should confirm rather than inherit that reasoning"* — was the right instruction: confirming it surfaced that the CLI's `no_session` and the desktop's `missing_seed` are ONE fact seen through `QSC_QSP_SEED` |
| 348 | **[Director, NA-0767 brief sec 4, X5 THE DECIDING TEST]** running `handshake poll` on the inviter decides between H1/H2/H3, and if it completes her "the repair is small and known" | ⚠⚠ **MISS ON BOTH CLAUSES, AND THIS IS THE LANE'S RESULT.** X5 as ORDERED — a poll after the X4 tick beat — completed NOTHING and returned rc 0 (`handshake_recv msg=none ok=true`), because the tick's own finish-scan had leased the A2 and declined it. The identical poll with no preceding tick completed her immediately. And completion yields `established_recv_only / send_ready=no / chainkey_unset` — a RECEIVE-ONLY inviter | MISS | ⛳ **the ordered test was confounded by a mechanism in none of the three hypotheses — and the confound IS the finding.** Filed `ENG-0251`; the design of record is amended for it |
| 349 | **[Seat, NA-0767 SEAL 001]** P0-P4 — the six-checkpoint two-peer table, predicted from source BEFORE any run | ⛳ **HIT, 5 of 5 rows**, including the X4 marker verbatim (`invite_scan_summary scanned=1 pulls=1 truncated=false selected=none classes=handshake`) and the operator's field line reproduced exactly at X2 (`status=no_session peer_confirmed=no send_ready=no send_ready_reason=no_session pinned=true`) | HIT | predicting the MARKER TEXT, not merely the outcome, is what made X4 checkable rather than interpretable |
| 350 | **[Seat, NA-0767 SEAL 001 P5b]** when the poll does complete the inviter, her status reads `awaiting_peer_confirm` | ⚠ **MISS — it reads `established_recv_only`, with `send_ready=no send_ready_reason=chainkey_unset`** | MISS | ⛳⛳ **THE EQUALITY RULE VINDICATED ON A REAL MEASURED VALUE, not a hypothetical:** `established` is a PREFIX of `established_recv_only`, so a `contains` test would have called the inviter HEALTHY when she cannot send |
| 351 | **[Seat, NA-0767 SEAL 003]** the A2 is unavailable to a poll for the large majority of wall-clock time under the shipped cadence; `available_fraction < 0.35` | ⛳ **HIT, by a wide margin: 11/150 samples = 0.073.** `leased_until` stepped 1787859146 → 1787859211 → 1787859277 (65 s, 66 s); the row was never consumed and never acked across the whole 150 s window | HIT | ⚠ measured at a FIXED 20 s beat so the sampler is deterministic; the shipped beat is jittered 15-25 s, so the field window is wider than one number — the bound is stated, not hidden |
| 352 | **[Seat, NA-0767]** needles can be built from a remembered API shape rather than from the target's bytes | ⚠⚠ **MISS FIVE TIMES IN ONE LANE, SELF-REPORTED.** (i) `--recipient-label` passed to the CLI — a DESKTOP parameter name; **it cost an entire flow run** (X0 rc=2). (ii) an `awk` terminator using `\s`, unsupported by this awk ⇒ ran to EOF. (iii) `git grep -l 'generate_handler!'` selected the file that only MENTIONS it. (iv) a `fn classify` needle matching nothing, leaving a line variable empty so `awk` printed from line 1. (v) a carried source excerpt whose range stopped four lines short of the `role=responder` marker | MISS | ⛳ the fifth was caught by the seat's OWN presence-assertion over the artefact it had just built (`occurrences=0` where 1 was required) ⇒ **assert over the artefact, not over the intention for it** |
| 353 | **[Seat, NA-0767]** `git ls-remote` for a ref that cannot exist reports the absence through its EXIT CODE (predicted rc 2) | ⚠ **MISS — it exits 0** with EMPTY output. The discriminator is the BYTE COUNT, not the exit status | MISS | a check keyed on that rc alone would be vacuous; the lane's own measurement used the CONTENT, so the base derivation stands |
| 354 | **[Seat, NA-0767]** a two-arm control whose arms differ in exactly one thing can name a cause the enumeration did not contain | ⛳⛳ **HIT, and it is the lane's central instrument.** Arm A (tick beat, then poll) → `handshake_recv msg=none ok=true`, rc 0, inviter unchanged. Arm B (no tick, identical poll) → completes her. Arms differed ONLY in the preceding tick ⇒ the lease is the cause, by control rather than by inference | HIT | the Director's three sealed hypotheses could not contain this; the control did not need them to |
| 355 | **[Seat, NA-0767]** a defect this severe would leave no trace in the shipped observability | ⛳ **MISS in the useful direction — the shipped marker stream ALREADY reports it.** Beats that find the frame emit `invite_scan_summary … selected=none classes=handshake`; beats inside the lease emit an EMPTY class list. The inviter's own app says "I can see a handshake frame and I will not use it" roughly once a minute | MISS (favourable) | ⇒ **the data existed and no SURFACE consumed it.** Recorded so the successor treats surfacing as distinct from instrumenting |
| 356 | **[Director, NA-0767 ruling sec 1 — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR]** a deviation from a banked design may be recorded by its consequence rather than named as a deviation | ⚠⚠⚠ **MISS, and it is the bug's origin, self-reported by the Director.** The banked design of record (`DESIGN_delivery_ladder_metronome_v2_20260825.md`, sha256 `aba8e2a5f8c388d1c7ac850c7b94790365cc9749e92e1a40b63ff22d056b8c59`) sec 1.1 reads, verbatim: *"performs the bounded scan (finish-scan + handshake-poll classes now; message classes when messaging ships)"*. NA-0763 ruling `R1` installed **the finish-scan class ONLY**, and recorded the consequence as *"rung 1's tick does not carry handshake rotation"* — true, narrow, and it concealed the actual consequence: **the inviter's session is never built.** The deviation read as a scoping choice and survived three lanes unexamined | MISS | ⇒ **A DIRECTOR WHO DEVIATES FROM A BANKED DESIGN MUST SAY SO IN THE RULING, NAME THE DEVIATION AS A DEVIATION, AND STATE WHAT THE DESIGN EXPECTED.** Filed against the Director's chair at his own instruction |
| 357 | **[Director, NA-0767 ruling sec 1 — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR]** the three sealed hypotheses H1/H2/H3 span the space of causes for the inviter's failure | ⚠⚠ **MISS — none of the three contained the lease confound**, which is what actually defeated the ordered deciding test. **Fourth instance of an option set narrower than its problem on this lineage**, self-reported by the Director | MISS | ⇒ an enumeration offered as a decision space must say what it does NOT cover; the seat's two-arm control found what the enumeration could not contain |
| 358 | **[Director, NA-0767 ruling sec 0]** the banked design of record's sha256 abbreviates to `aba8e2a5...f088` | ⚠ **MISS ON THE TAIL — measured `aba8e2a5f8c388d1c7ac850c7b94790365cc9749e92e1a40b63ff22d056b8c59`.** `f088` occurs nowhere in the real digest, and no file under `/srv/qbuild/operator/` hashes to a value ending `f088`. Head8, filename and the quoted sec-1.1 sentence all match, so the DOCUMENT is correctly identified and only the abbreviation is wrong. ⛳ `AMENDMENT_1_delivery_ladder_20260827.md` carries the FULL digest and it matches this measurement exactly | MISS | ⇒ **an abbreviated sha is a citation, and a citation is checkable — check it.** The full digest is used in every record this lane lands, and the defective abbreviation is not propagated |
| 359 | **[Director, NA-0768 ruling sec 2 — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR]** the resolver's total-length check (`D3'-a`) CLOSES the destructive path, and is therefore MANDATORY rather than optional — ruling verbatim: *"D3'-a IS NOT AN OPTION, IT IS PART OF D3'"* | ⚠⚠⚠ **MISS, AND THE CLAIM WAS RULED MANDATORY ON THE WRONG BASIS.** Measured at protocol `503dcdad` by NA-0769 as a third instrument: **six** (reason, gate) classes can clear an addressed pending record, and **exactly one of them is length-shaped**. The ungated one — `REJECT_QSC_HS_CONTEXT_MISMATCH` at `handshake/mod.rs:1778`/`:2066` — is reached by a frame that **PASSES** a total-length check, because its length is exactly `9 + block_len + payload_len` by construction; it has no `is_explicit()` gate and fires before any decapsulation, transcript MAC or signature. ⇒ a total-length check closes **one of six** and not the one that needs closing. ⚠ The reads reported four; the brief carried four; **the seat was instructed to enumerate rather than adopt, and enumerating gave six** | MISS | ⇒ **A SAFETY ARGUMENT MUST NAME THE SET IT CLOSES OVER, AND THE SET MUST BE ENUMERATED FROM THE BYTES BEFORE THE ARGUMENT IS RULED MANDATORY.** "Closes the path" is not a measurement until the paths are counted. Filed against the Director's chair at his own instruction; the defect is `ENG-0252` |
| 360 | **[Director, NA-0768 amended design — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR]** a design that retires `invite_finish` and replaces the per-contact scan with a class-dispatching beat needs no `AckMode` clause | ⚠⚠ **MISS — and it was measured against a shipped gate whose own comment calls it *"mechanical, not decorative"*.** `invite/mod.rs:1416-1422` states that under `AckMode::Legacy` the relay DELETES ON PULL and that a scan there converts a one-frame loss into a **16x–128x** amplification; the code branches accordingly at `:1423-1442`. Measured, `invite_finish:1423` is the **only** site in the invite flow that branches the **pull shape** on ack mode (`handshake/mod.rs:1718-1719` branches only the ack; `invite/mod.rs:1384` gates only a `debug_assert`) ⇒ **unregistering that verb removes the flow's only pull-shape branch**, and no clause of `D1'`–`D7'` mentions `AckMode`. ⛳ **The SECOND cold read found this; the Director did not, and neither did the first read** | MISS | ⇒ **A DESIGN THAT DELETES A VERB INHERITS EVERY GUARD THAT VERB WAS CARRYING, AND THE GUARDS MUST BE ENUMERATED BEFORE THE DELETION IS DESIGNED — a comment calling itself load-bearing is the cheapest possible warning and it was in the file.** The defect is `ENG-0253`; the same asymmetry, ungated, is already shipped elsewhere as `ENG-0254` |
| 361 | **[Method, NA-0768 — THE HIT, RECORDED BECAUSE A LEDGER OF ONLY MISSES DRIFTS]** an SR-15 cold read of an already-approved design is ceremony that will find nothing a ruling has not already found | ⛳⛳ **HIT, TWICE, BY TWO INDEPENDENT INSTRUMENTS, BEFORE ANY BYTE MOVED.** Read 1 found `F2` — the destructive clear on a decode failure, and that the guard actually holding was a hardcoded constant three modules away from the route depending on it. Read 2 found `G1` (the clear is reachable with **no** gate and **no** crypto, on a frame that passes the very check ruled mandatory to prevent it) and `G2` (the missing `AckMode` arm). **Both reads were of a Director-APPROVED design, and both found a mail-destroying defect in it.** ⇒ the practice paid for itself twice on one lane, and the two hazards were pre-existing — the reads did not merely check the lane, they found defects in shipped code the lane happened to route through | HIT | ⇒ **THE COLD READ EARNS ITS COST ON APPROVED WORK, NOT ONLY ON DRAFTS — approval is exactly the state in which nobody is still looking.** And the corollary this lane adds: **a THIRD instrument over the same bytes still found four disagreements** (two wrong line cites, one wrong stated mechanism, and four-vs-six), so re-measurement is not redundant even after two competent passes |
| 362 | **[Director, NA-0770 `RULING_005` sec 3(d) — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR, ENTERED BY THE DIRECTOR'S OWN ORDER]** the disposition of the two-armed `q4a`/`q4b` collateral arms is one of exactly two things: the arms are RETIRED with the mode, or their treatment halves survive as non-contrastive leftovers | ⚠⚠ **MISS — AND THE RECORDED FAMILY IS "AN OPTION SET OF TWO THAT EXCLUDES THE CORRECT ANSWER".** A third option existed and neither arm of the set contained it: the negative capability can be REBUILT MODE-FREE, because under lease a pulled-but-unacked item is held INVISIBLE until expiry. Probing the same plant twice — once inside the lease window, where it is genuinely unrecoverable, and once after expiry, where it must return — restores exactly the property the legacy control provided, using only shipped behaviour and no test-only seam. The arms are neither retired nor leftovers; they are reconstructed and remain contrastive | the seat, asked to re-measure the ruled scope from bytes rather than adopt the ruling's enumeration. ⚠ The option set was not wrong about its two members — both were real; it was wrong to be CLOSED. A set offered as exhaustive is itself a claim, and this ledger now carries three instances of that family |
| 363 | **[Seat, NA-0770 — THE HIT, RECORDED BECAUSE A LEDGER OF ONLY MISSES DRIFTS]** the stop file about to be banked (STOP 004) is complete; the self-containment audit is a formality at this point, the sixth asking of the same question in one lane | ⛳⛳ **HIT — THE AUDIT FAILED ITS OWN AUTHOR'S FILE, ON FIVE COUNTS.** Documents quoted at 1% and 2% of their length were CITED where the reasoning depended on them; 26 enumerated members were named NOWHERE in the stop, living only in a scratchpad script — *a proof whose premises are not in the document is an assertion*; three losses were abbreviated past the point of use; ten product files were unlisted; the Lane B digest was absent. The file was re-assembled whole as STOP 005 rather than supplemented, on the NA-0752 precedent that a supplement chain is not a stop file | the mechanical audit, run over the finished bytes rather than recalled — which is precisely the difference `SR-26` was minted to make binding. ⚠⚠ **THE SIXTH ASKING STILL FOUND FIVE GAPS, ONE ON AN ITEM THE GOVERNING BRIEF ORDERS.** Any argument that the audit becomes unnecessary through practice is refuted by its own last run |
| 364 | **[Seat, NA-0770 — THE HIT]** a near-trigger reached under momentum can be resolved by judgment, since the eleventh product file would only carry one small enum variant | ⛳ **HIT — MEASURED INSTEAD, AND THE FILE WAS NOT NEEDED.** The retired-key refusal appeared to require a new `ErrorCode` variant in `model/mod.rs`, an ELEVENTH file against an authorised set of ten. Before reaching for it the seat read `CliError::code`'s signature and measured that it takes `impl Into<String>`, so the refusal is expressible with the existing type. The edit set stayed at ten | reading the constructor's signature from bytes before designing against a remembered shape. ⚠ The trigger was armed against *product growth*; this is the case where measuring first is what kept it from firing, rather than the case where it fired |
| 365 | **[Method, NA-0770 — THE MISS THAT THE ARMED TRIGGER CAUGHT]** the test census's per-site disposition classes are settled; `file_transfer_mvp.rs:901` is D-REAIM-MECH — a mechanical flag drop with every assertion unchanged | ⚠⚠ **MISS — THE SITE CARRIES `assert!(!second.status.success())`, WHICH CANNOT SURVIVE UNDER LEASE.** Under the retired mode the second receive failed; under lease the frame is redelivered and the command exits 0, so the assertion is not merely unproven but FALSE. The class was wrong, not the line number. Re-classified D-NAMED-LOSS as `L1c`; counts moved D-REAIM-MECH 26→25, D-NAMED-LOSS 10→11, named losses 6→7 | the seat STOPPED at the contested disposition rather than reclassifying under momentum, swept for siblings first (finding none — `timeline_store.rs` has zero ack-mode selectors, and the other three negative-exit asserts belong to tests that never select the mode), and reported. ⚠ **THIS IS THE THIRD INSTANCE OF THE F-1 FAMILY IN THIS LANE**: a census that counts the right sites and mis-reads what one of them ASSERTS. A sweep measures what tests ASSERT, not what the code DOES |
| 366 | **[Seat, NA-0770 — THE MISS, AND THE TELL WAS VISIBLE FOR A FULL CYCLE]** the sharded baseline launcher is correct; each shard is running the targets the manifest assigns it | ⚠⚠ **MISS — EVERY SHARD WAS RUNNING ALL 147 TARGETS, TWELVE-WIDE ON SIX CORES.** `qsc_shard_check.py --emit-args` writes two `scope examined:` headers to **stderr** and the args to **stdout**; the launcher suppressed stderr and then applied `tail -n +3` to the single remaining line, yielding EMPTY. An empty arg list makes cargo select everything. Seven spurious failures followed, every one of them contention | the shard logs' own arithmetic, read at last: every shard reported "6 targets, 1 FAILED" — **including the shard assigned exactly ONE target**. ⚠ The tell was printed for a full cycle before it was read. Cured by taking stdout directly plus an explicit non-empty assertion that aborts the shard with `rc=97` rather than silently running the suite |
| 367 | **[Seat, NA-0770 — THE MISS THE COMPILER DID NOT CATCH ALONE]** removing a CLI flag from a test helper is complete once the flag no longer reaches the command line | ⚠⚠ **MISS — REMOVING A FLAG LEAVES THREE KINDS OF RESIDUE AND ONLY TWO ARE COMPILER-VISIBLE.** The emitted args were removed, but `na0741_frame_class_dispatch.rs`'s `receive_args` kept an `ack_mode` parameter it no longer used, and four `let mut args` bindings kept a mutability that existed only to `push` the flag. Worse, the DEAD parameter was still being passed `"legacy"` by a caller whose assertions then required a legacy-only outcome — so that arm would have run under **lease** and failed at runtime, having compiled cleanly | two instruments agreeing: a full `cargo test --no-run` over every target (which flags the unused parameter and the four needless `mut`s) and an independent static sweep for helpers taking a mode parameter their body never reads. ⚠ **NEITHER INSTRUMENT SEES THE CALLER STILL PASSING A NOW-IGNORED LITERAL** — that one is visible only to a census of the literals themselves, which is why the flag sweep and the value sweep are two separate passes |
| 368 | **[Seat, NA-0770 — THE HIT THAT CORRECTED THE SEAT'S OWN RECORD]** the tombstone is verified by the tests and the source: the reader carries the operator's raw value into its marker, so a stale `ack_mode` key is visible rather than inert | ⛳⛳ **HIT ON THE MECHANISM, MISS ON THE RENDERING — AND THE RECORD WAS WRONG BEFORE THE CODE WAS.** All four behavioural predictions held (third state reported; `config set` refuses BY KEY so `lease` is refused exactly as `legacy` is; `--ack-mode` no longer parses; the file is left byte-unchanged). But the emitted line read `value=<redacted> … file=<redacted>`: under the DEFAULT output policy those two fields are redacted and render only under `--reveal`. The value **is** carried; whether the operator SEES it is a policy question, and the as-built and `D-1411` both said "visible" without saying "under which policy" | running the shipped binary and **reading the actual emitted line**, which is the discipline `na0742_invite_finish_scan_producer_acks.rs`'s own header prescribes: *"A CLAIM ABOUT RENDERED OUTPUT CANNOT BE CHECKED BY ANY SOURCE CENSUS."* ⚠ **NO PRODUCT BYTE WAS CHANGED IN RESPONSE** — the redaction is a standing safety property and loosening it under momentum, inside a lane about ack modes, is exactly the unscoped change the armed trigger exists to prevent. The wording was corrected instead; the finding is named for the Director |
| 369 | **[Seat, NA-0770 — THE SECOND MISS IN THE SAME DIRECTION, WHICH IS WHY IT IS A PATTERN AND NOT A TYPO]** the tombstone's notice is emitted at startup, so any operator carrying a stale `ack_mode` key learns of it | ⚠⚠ **MISS — IT IS NOT A STARTUP NOTICE.** `announce_retired_ack_mode_key()` is called at `transport/mod.rs:247`, on the **receive path**, at the exact point where the mode used to be resolved. Measured: a `receive` with the retired key present dies at the **vault gate** (`code=vault_locked op=receive reason=explicit_unlock_required`) UPSTREAM of the announce site, and the control arm with no retired key emits the identical line — so an operator with a locked vault never reaches the notice, and no other command emits it at all | running the shipped binary in **both arms** and comparing, rather than reading the call site and inferring reachability. ⚠⚠ **THE PATTERN IS THE FINDING, NOT THE LINE:** this and row 368 are the same error twice — both claimed **more operator-visible behaviour than the program delivers**, and both were invisible to every source census this lane ran. A source census can prove a call EXISTS; only running the thing proves it is REACHED and RENDERED. ⚠ The site was NOT moved in response: it fires where the key would have mattered, and relocating it under end-of-lane momentum is precisely what the armed trigger forbids |
| 370 | **[Seat, NA-0770 — THE HIT THAT ONLY A SLOWER MACHINE COULD SCORE]** the lease-invisibility reconstruction is proven by a local full-suite green: `na0688_c4_collateral_arms` passed 3/3 on a contended 6-core box, which is the contention case the widened 8s/20000ms constants were chosen for | ⛳⛳ **HIT ON THE DESIGN, MISS ON THE NUMBER — AND THE DESIGN CAUGHT THE NUMBER.** CI's 2-core runner measured the in-lease probe at **8.915s against the 8s lease**, and the arm's SELF-ASSERTING PRECONDITION refused with its remedy attached: *"PRECONDITION UNMET, NOT A RESULT … Widen TEST_PULL_LEASE_SECS; do not delete this probe."* It did **not** report a false negative-capability result, which is precisely what it was built to prevent. Fixed by giving THAT FILE ALONE 45s/60000ms — it is the only one that does work INSIDE the window; the other seven merely wait a lease out and all seven passed CI unchanged | a slower machine. ⚠⚠ **A LOCAL 6-CORE RUN CANNOT REPRODUCE THIS AND NEVER WILL** — the local suite went green on the exact committed tree, twice, with this arm passing both times. The instrument that found it was CI, and the only reason the finding is legible rather than a mystery failure is that the arm asserts its own precondition instead of assuming it. ⚠ The fix BREAKS a parity comment I wrote earlier in the same lane ("if you change one, change all"); that comment was corrected in all seven files rather than left to lie, because a parity claim that is false is worse than no claim |
| 371 | **[Seat, NA-0770 — A PROCESS MISS, NOT A MEASUREMENT ONE]** a CI failure on my own code at an open PR is mine to diagnose, fix, verify and push, since the fix is small, measured, and obviously correct | ⚠⚠ **MISS. THE SHAPE IS: STOP WITH THE LOG -> PROPOSE THE FIX -> PUSH AFTER THE RULING.** I diagnosed `qsc-shard-10`, widened `na0688`'s lease, verified 3/3 locally, committed and pushed — with no ruling. The operator's messages in that window were about the build box; **he did not bless the push and the record does not pretend he did.** The stop that followed was the right half; the push that preceded it was not | the Director, at the STOP-009 ruling. ⚠ The fix was ACCEPTED on measurement, which is exactly why this row exists: **a correct fix pushed out of order is still out of order**, and the ruling is precisely where an INCORRECT one would have been caught. The lane's armed trigger covered new product files and contested dispositions; it did not name "CI went red", and momentum filled the gap |
| 372 | **[Director, `ORDER_ladder_climb_sequencing_20260828.md` sec 4, P1 — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR, ENTERED BY HIS OWN CLOSE-OUT]** *"Lane A's PR verifies against `CHECKLIST_NA0770_laneA_PR_20260828.md` (sha256 `accfc0a7a03a17c43b97400d43bd9fd961144834f4668659ad84970b9588f39e`) with zero returns."* | ⚠ **MISS — ONE RETURN, AND IT WAS RECORDS ONLY.** The return carried three items: the missing `### NA-0770` queue block, the prior lane's flip, and `SR-26`'s hole. **THE BUILD TOOK ZERO RETURNS.** Scored a miss rather than a partial for two stated reasons: the prediction said *"the PR"*, not *"the build"*; and the checklist itself omitted the block birth — see row 373 | MISS | scored by the Director at `CLOSEOUT_NA0770_laneA_20260829.md` sec 5 (sha256 `2153d5b1…2b21c`), carried into repo truth by NA-0771 (`D-1412`) because the close-out lives in the operator area and a prediction scored only there is a row a successor cannot check. ⚠ The distinction the row preserves is the useful one: **a records return and a build return are not the same evidence about a specification** |
| 373 | **[Director, `CHECKLIST_NA0770_laneA_PR_20260828.md` — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR, HIS OWN INSTRUMENT]** the pre-PR checklist enumerates what the promotion must land, so a PR verifying clean against it has landed its records | ⚠⚠ **MISS — THE CHECKLIST OMITTED THE ONE ITEM THAT WAS MISSING.** It listed the `STATE:` line and the prior lane's flip **but not the birth of the lane's own `### NA-0770` block**, so the instrument could verify clean while the block did not exist. ⇒ **the one return this lane took was partly the Director's instrument's gap, not only the seat's.** ⚠ AND THE MECHANICAL GATE COULD NOT HAVE CAUGHT IT EITHER: `ENG-0230` is OPEN and records that the `STATE:` header must be gated on **NAMING** the newest declared block rather than on **counting** `READY` lines — so `NA-0770` advanced `STATE` to `READY=NA-0770` while no `### NA-0770` block existed **and nothing failed** | MISS | self-reported by the Director at `CLOSEOUT_NA0770_laneA_20260829.md` sec 5(b), carried into repo truth by NA-0771 (`D-1412`). **THE LESSON IS `SR-09`'S, APPLIED TO A CHECKLIST: an enumeration is a description of the record, never the record** — and a checklist that omits an item is an enumeration that measures clean on a document missing exactly that item. A LIVE instance of `ENG-0230`, which remains open |
| 374 | **[Seat, NA-0771 STOP 004 S12]** the second cold read finds at least one reachable site or interaction this specification still misses, and it is on the INITIATOR role | ⛳⛳ **HIT, and the read scored it to the seat.** The second read found `:1856` reachable for an ExplicitV2 initiator pending — the initiator mirror of the `:2101` site the seat had measured and called BLOCKING-grade. ⇒ the seat named the blind spot in writing and did not look there | HIT | ⚠⚠ **KNOWING WHERE YOU HAVE NOT LOOKED IS NOT LOOKING THERE.** A scored prediction is not a substitute for the measurement it predicts |
| 375 | **[Seat, NA-0771 STOP 004 sec 7.3 / F-G]** the `continue` and the deletions INTERACT: turning `:1785` into a `continue` would make `:1856` reachable if the deletion set did not include it — *"the sharpest thing the cold read bought"* | ⚠⚠⚠ **MISS — THE MECHANISM IS IMPOSSIBLE.** `hs_contexts_match` has two true arms, both requiring the same variant, so passing it FORCES `resp.is_explicit() == pending.is_explicit()`. `:1856` sits below `:1782` and is reachable only by a frame that passed it ⇒ it fires iff the INITIATOR'S OWN pending is explicit, with or without the `continue`. **7.3 and F-G are WITHDRAWN** | MISS | ⚠⚠ **REDUCE A STRUCTURAL ARGUMENT TO THE PREDICATE IT RESTS ON.** Four lines of a `match` refuted a finding banked as the lane's sharpest. The seat argued from control flow and never checked the algebra the control flow ran on |
| 376 | **[Seat, NA-0771 STOP 004 secs 0/4/9.1]** candidate F removes the head-of-line block — *"GONE RATHER THAN PRICED"*, *"there is NO block to bound"*, *"B2 COMPLETES"* | ⚠⚠ **MISS ABOVE A THRESHOLD THE SPECIFICATION NEVER STATED.** Measured on three trees at N=1 and N=4 against `--max 4`: F completes at N=1 and does NOT at N=4. The `continue` moves the attacker's price from ONE frame to `--max`; above it the state is `ENG-0198`'s. ⚠ The arm could not see it — it pushed ONE poison frame, so it could not reach the regime by construction | MISS | ⚠⚠⚠ **WHEN AN INSTRUMENT IS RETURNED, ASK WHAT ELSE IT HOLDS CONSTANT.** A prior ruling returned this arm for using the wrong SERVER; the seat replaced the server and left the BUDGET at one frame — the same SR-21 defect, at the same finding, one variable over |
| 377 | **[Seat, NA-0771 STOP 004 sec 20]** the eight carried documents are carried IN FULL, and the `sha256` and `size` printed beside each describe the bytes carried | ⚠⚠⚠ **MISS. THE STAMPING PASS CORRUPTED TWO OF THEM, +50 BYTES EACH**, writing STOP 004's own digest over each carried file's `@@SELFDIGEST@@` literal — and **the seal verified anyway**, because the self-digest construction normalises every 64-hex run and is blind to that class by design. The self-containment audit could not see it either: it STRIPS embeds, which the stamper WRITES into | MISS | ⚠⚠⚠ **A SEAL THAT MASKS A CLASS OF BYTES IS BLIND TO EDITS IN THAT CLASS** — and everything a governance lane carries that is load-bearing IS hex. Cured as `SR-26` **(a'')**. ⚠ The seat built BOTH halves of the asymmetry, adding the embed-stripping itself one stop earlier as a cure for the section map |
| 378 | **[Seat, NA-0771 STOP 005]** the fix for a blindness is sound once the mechanism is understood | ⚠⚠ **MISS — THE CURE'S OWN FIRST INSTRUMENT HAD THE SAME BLINDNESS, FOR THE SAME TWO FILES.** A `[^-]+` fence label skipped exactly the two hyphenated "SR-15" documents B-3 is about, so the stamper would have written into them and the carry check would not have looked. Caught before sealing ONLY because the builder printed the carry COUNT: six where eight were specified | MISS | ⚠⚠ **TEST A FIX AGAINST THE INSTANCE THAT REVEALED THE DEFECT**, and **make an instrument print its CARDINALITY, not just its verdict**. A carry check reporting "all verified" while seeing six of eight is the same shape as the seal it was written to replace |
| 379 | **[Director, NA-0771 — SR-16 ROW AGAINST THE DIRECTOR'S CHAIR, entered by his own ruling]** a stop whose seal reproduces has been verified | ⚠⚠ **MISS, self-reported.** The Director ordered a second cold read on STOP 004 without checking its carried copies, and reproduced the stop's seal — which is precisely the instrument that cannot see the corruption. ⚠ AND A SECOND ROW: the ruling and both first-read files named `start_qsl_server_with_pull_lease`, a function that does not exist; the symbol is `start_qsl_server_with_store` (`tests/common/mod.rs:512`). The seat measured it and corrected the chair | MISS | **VERIFYING A SEAL IS NOT VERIFYING A DOCUMENT.** The seal proves the bytes are the bytes it was taken over; it says nothing about bytes it was designed to normalise |
| 380 | **[Director, `ORDER_ladder_climb_sequencing_20260828.md` sec 4 `P2`]** the `ENG-0252` repair *"touches at most TWO product files and one cold read suffices"* | ⚠ **HALF HIT, HALF MISS, scored by the Director at `CLOSEOUT_NA0771_20260829.md` sec 5.** Product files: **ONE** — hit, and with margin. Cold reads: **TWO** were needed — miss ⛳ **and both were RIGHT TO RUN**: the first refuted the specification's reachability census (read, not run) and the second caught a root-cause claim stated twice in sealed prose | PARTIAL | ⇒ **THE FILE-COUNT HALF AND THE CEREMONY HALF OF A PREDICTION ARE INDEPENDENT AND MUST BE SCORED SEPARATELY.** A small edit set is not evidence that one read suffices; the second read's value was measured, not assumed, and a prediction that bundles scope with ceremony cannot be falsified on either |
| 381 | **[Director, NA-0771 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR, `F-E`]** the fixture helper the build order names, `start_qsl_server_with_pull_lease`, exists | ⚠ **MISS.** The symbol does not exist; it is `start_qsl_server_with_store` (`tests/common/mod.rs:512`). Accepted by the Director as a row against his own chair | MISS | ⇒ **A DIRECTIVE THAT NAMES A SYMBOL NAMES IT FROM THE TREE, NOT FROM MEMORY.** An order citing a function that does not compile costs the seat a lookup at exactly the moment it is following instructions literally — and a seat that "corrects" it silently has deviated from the order without saying so |
| 382 | **[Director, NA-0771 kickoff — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR]** the red arm's environment needs no statement; the instrument will be measured on the semantics the property is claimed for | ⚠⚠ **MISS.** The first red arm ran on a **delete-on-pull mock** — the exact semantics `D-1411` had removed from the client **the day before** — so it could not price a lease-relay consequence at all. ⚠ The kickoff never said *"measure on lease semantics"*, and the Director who wrote it had just closed the lane that made lease the only mode | MISS | ⇒ **`PR-6`: THE TEST RELAY'S SEMANTICS MUST EQUAL THE DEPLOYED RELAY'S FOR THE PROPERTY CLAIMED.** This is `SR-20`'s extension — the emitting step's ENVIRONMENT is part of the artifact's identity — arriving on a FIXTURE rather than on a CI job, and the author most able to name the environment was the one least likely to notice it needed naming |
| 383 | **[Director, NA-0771 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR, self-reported]** a stop whose seal reproduces has been verified | ⚠⚠ **MISS, self-reported and corrected in the same act.** Verifying `STOP_NA0771_006`'s seal, the Director's first check read the stamped value **from an 8-line window** and compared it against an **empty string**, reported **MISMATCH** — and had to correct itself. **The file was sound**; the instrument was not | MISS | ⇒ **A VERIFIER THAT REACHES FOR A VERDICT BEFORE ITS OWN EXTRACTION IS PROVEN WILL MANUFACTURE A FALSE ALARM AGAINST TRUE WORK** — the same failure class the constitution already records for a command handed to a reader with an unverified expected value, now on the CHECKING side. An extraction that yields an empty string is an instrument failure, never a comparison result |
| 384 | **[Seat, NA-0771 `STOP_NA0771_005` sec 16 and `STOP_NA0771_006` sec 10 — the seat's own rows, scored by the Director at close-out sec 5]** `S8` (holds), `S15` (the suite moves only `na_0313` and the new arms), `S16`, `S17` (no reachability correction survives into the impl — the INVARIANT, not reachability, selects the edit set) | ⛳ **`S8` HOLDS · `S15` HIT · `S17` HIT · `S16` NOT YET SCOREABLE**, and the close-out says so rather than scoring it anyway | HIT (3 of 3 scoreable) | ⇒ **A SEAT THAT WRITES ITS OWN PREDICTIONS BEFORE THE BUILD SCORES THREE OF THREE, AND THE UNSCOREABLE ONE IS DECLARED UNSCOREABLE RATHER THAN QUIETLY GRADED.** ⚠ `S17` is the load-bearing one: it says the edit set was selected by the INVARIANT rather than by the reachability census — which is why the census being wrong twice did not corrupt the final diff |
| 385 | **[Director, `ORDER_ladder_climb_sequencing_20260828.md` sec 4 `P3` — ARMED BY THIS PROMOTION, NOT YET SCOREABLE]** the inviter-repair menu will measure a **REAL defect** in the *"do not be clever"* entry on the **mixed-role axis**: two independent fetchers of one mailbox lease each other's frames when one inviter-role and one redeemer-role contact are pending at once. (The Director's source reading, *"recorded as a prediction, not adopted as a premise"*) | **NOT YET MEASURED.** `NA-0768`'s STOP 001 (the menu) is the instrument that scores it, and the kickoff orders the axis measured rather than reasoned — *"The Director's reading says yes ...; measure it"* | — | ⇒ **RECORDED NOW SO THE MENU CANNOT QUIETLY INHERIT THE PREDICTION AS A PREMISE.** `R4` of the same order binds: this chair's reasoning enters as a QUESTION for the seat to measure, never as a premise, and a menu that reports the mixed-role result without having RUN it would be the `PR-4` failure (reachability is a run) repeating on a new axis |
| 386 | **[Director, `RULING_NA0768_006` sec 2 and `RULING_NA0768_007` D3 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR, THE SAME ERROR TWICE]** the caller's own alias is the ASSERTED peer, so a mismatch on it is a real security event and only the OTHER candidates need silencing | ⚠⚠ **REFUTED BY THE SEAT'S OWN ARM (`S5`).** `finishScanClass` LOOPS `contact_list`, so every contact takes its turn as the caller; carving the caller's alias out re-creates `identity_mismatch` with two fingerprints on every beat for every contact — the exact condition `RULING_006` sec 2(a) forbids. The line is **ASSERTION vs ITERATION**, and `RULING_008` sec 1 WITHDRAWS both clauses. The seat built the carve-out, measured it, and threw it away before shipping. |
| 387 | **[Director, `RULING_NA0768_007` D3 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR]** the cost of silencing the iterated candidate is bounded: contact Y's A1 is re-offered under Y's OWN finish call in the same beat, where the alarm fires — *"delayed by one iteration, not lost"* | ⚠⚠ **MEASURED FALSE.** `invite_finish` has two offer sites: the fan-out (`:1549`, SPECULATIVE) and the asserted site (`:1637`, `Provided`) — and the asserted one consumes **only the selected invite-RESP envelope**, which a bare A1 can never reach. Y's own call offers Y's A1 speculatively and the gate is silent. **The alarm is not delayed; it is not announced by any automatic path.** `RULING_008` sec 2 withdraws the bound and sec 3 accepts the cost on other grounds. ⇒ `PR-15`. |
| 388 | **[Seat, `STOP_NA0768_008` sec 9 and sec 16 — the seat's own row, found by the Director at `RULING_008` sec 4]** `R4-B3` is cured: the arms carry assertions and `RED-ARMED`/`PRINTS ONLY` tags, census `s2` 7 / `s4` 6 / `s5` 4 | ⚠⚠ **TRUE OF THE TREE, FALSE OF THE CARRY.** `C7` carried the PRE-repair arm file: **0** tags, `s2` **0**, `s4` **1**. The stop that cured `R4-B3` carried the file disproving it — `B2`'s class again. Cause: the carry was inherited from the predecessor's carry set and never RE-EXTRACTED after the re-run. Closed by this PR rather than by a re-bank: the arm file ships, and the census can be run on the bytes GitHub carries. ⇒ `WF-0092`'s remit. |
| 389 | **[Seat, NA-0768 impl — SELF-REPORTED, and the instrument caught it]** a suite run that exits 0 has passed | ⚠⚠⚠ **FALSE, AND IT NEARLY SHIPPED.** The run was piped through `tail`, so the shell reported `tail`'s status: **exit 0 for a suite that returned FAILED, 4 passed 1 failed.** Caught only because `PR-7`'s census reads the `test result:` line rather than the exit code. Re-run unpiped: **101**. ⇒ **never pipe the check that gates you**, and read the RESULT LINE, not the status. |
| 390 | **[Director, `RULING_NA0773_001` / the NA-0773 kickoff sec 0 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR, entered by his own ruling]** the finish line is *"THE 27 HAVE RUN AND THEIR RESULT IS RECORDED"* | ⚠⚠ **MISS, self-reported and corrected in `RULING_NA0773_002` sec 2.** The finish line was **built on 27, and 27 was never measured** — it is the count of gated `#[test]` fns, of which **8 assert nothing** and, after the cold read, only **16** pin the property the severity argument is about. Restated to **"the 19 have run and the Err-path cleanup behaviour is recorded"**. ⇒ **a figure adopted into a finish line is a figure that must be measured first.** |
| 391 | **[Director, NA-0773 — `SR-16` ROW AGAINST THE DIRECTOR'S CHAIR, entered by his own ruling]** `STOP_NA0773_002` sec 6.3's table of the 19 describes what each test asserts | ⚠⚠ **MISS, and the cold read caught what two chairs did not.** Row 12 reads *"RNG fail signing A2 → `handshake_reject` + `sig_sign_failed`; no `msg=A2`, no `handshake_complete`"* — true as far as it goes, and **omitting the two assertions that pin a SURVIVING partial write**. A Director reading that row alongside `E-1` concludes 19 tests pin *"no half-written session"*; **one pins the reverse.** The omission was **visible in the seat's own artifact** and neither the seat nor the Director saw it; **the read did**, and it became `E-4` / `ENG-0267`. |
| 392 | **[Seat, NA-0773 `STOP_NA0773_002` sec 4 / `N-1`]** the break is `ba141149` (NA-0646 PR-B, 2026-07-15), **not** the NA-0649 / `6a93012c` / 2026-07-16 hypothesis its own governing input carried | ⛳⛳ **HIT, and the seat refuted the hypothesis it was handed.** Bisect over 1498 commits, 11 steps, **zero skips**, perfectly monotonic; the bracket exact because `ba141149` has one parent. ⛳ **Independently confirmed by the SR-15 read from three commits' blobs**, by a different instrument. ⇒ **a last-touched fact answers who edited a line, never who broke it.** |
| 393 | **[Seat, NA-0773 `STOP_NA0773_002` `N-2`]** 8 of the 27 gated tests assert nothing — pure `println!`, zero call sites | ⛳ **HIT, re-derived exactly by the cold read** (0 asserts, 0 non-`println!` statements, 0 call sites each, against a positive control of 4 and 3 for the corpus's two real helpers). ⛳ **And it went further than the seat's claim:** the species is **repo-wide — 13 such tests across 939**, and **five of them are NOT behind the seam and pass green in every suite run the project performs.** Filed `ENG-0268`. |
| 394 | **[Seat, NA-0773 `HOLD_NA0773_001` `E-2`, raised as a SUSPICION and deliberately left unmeasured so as not to bias the commissioned read]** the seam is label-gated, so randomness drawn outside a labelled wrapper is dark **by construction** | ⛳⛳ **HIT, and the discipline is the point.** The read measured it: **13 shipped draws the seam cannot reach** — 8 in `qsc` including **the vault master key** at `vault/mod.rs:1476`, 5 in `quantumshield_refimpl`, which carries the seam token **zero times** — plus **5 of 22 wrapped labels never forced**. Filed `ENG-0266`. ⇒ **raising a suspicion as a suspicion and leaving the measurement to the independent instrument is what made the answer worth having.** |
| 395 | **[Read, `FINDINGS_SR15_NA0773_20260830T155905Z.md` `F-1` / `E-3`]** the seam cannot reach a real RNG failure **at any site, wrapped or not** | ⛳⛳ **HIT, STOP-class, and it is the finding of the lane.** From three measurements and one crate source: `try_fill_bytes` **0** tree-wide · `rand_core 0.6.4`'s `OsRng::fill_bytes` **panics** · `assert_no_secret_output`, reached by **10 of the 19**, asserts `"panicked"` is **absent**. ⇒ **the suite asserts the absence of exactly the signature a real RNG failure produces.** Filed `ENG-0265`; property `PR-18`. ⚠ Scored as a HIT on its **measured** half; its panic-consequence half is **reasoned, not observed**, and the read said so itself. |
| 396 | **[Read, NA-0773 — the escalation id derivation]** `E-3` is free | ⛳ **HIT, with `WF-0087` caught live.** Declaring form (`**E-N --`) swept over the whole operator tree: `E-1`=3, `E-2`=1, `E-3`=**0** ⇒ free; positive controls `E-1`/`E-2` ≥1; fresh negative control `E-91`=0/0. ⚠⚠ **A MENTION count returns `E-3` in 14 files / 26 occurrences and would have reported it TAKEN** — all 26 being prior lanes' unrelated lane-local evidence labels, predating `PR-17` and belonging to a different id space. **Classified, not counted.** |
| 397 | **[Seat, NA-0773 `STOP_NA0773_002` sec 7.4 / `n3`, the specification's largest declared uncertainty]** the one line is **necessary**; whether it is **sufficient** — whether the nine test targets compile after 46 days of drift — is UNKNOWN and unreachable until the library compiles | ⛳ **HIT on the uncertainty, and it resolved in the specification's favour.** Measured on the impl commit: `cargo check -p qsc --tests` with the cfg → **exit 0, 0 errors** (2 unrelated warnings). ⇒ **the one line is necessary AND sufficient**, and `n3` is discharged by the build itself, exactly as `RULING_NA0773_002` sec 3 anticipated. |
| 398 | **[Seat, NA-0773 — the run, ARMED BEFORE IT WAS RUN]** the originating report named three outcomes: (i) all pass, (ii) tests drifted, (iii) *"the PRODUCT does not in fact fail safe — a real defect, found by the arms built to find it"* | ⚠⚠⚠ **OUTCOME (iii), ON EXACTLY ONE OF THE 27.** 27 specified, **27 executed, 26 passed, 1 FAILED**, cardinality reconciled by unique target path. `t8_the_a2_sig_failure_exit_emits_no_producer_ack` failed with **both antecedents passing**: the session is committed, `handshake_reject reason=sig_sign_failed` is emitted, and the frame is **acked anyway**. Contention ruled out under a rule written **before** the re-run (*fails alone = real*): re-run alone, single-threaded, **FAILED again**. Filed `ENG-0269`. ⇒ **the arm was written in NA-0742, could never run, and found a real defect the first time it did.** |
| 399 | **[Director, NA-0768 close-out sec 4 — the forward prediction, scored at this lane's close]** flows `g` and `k` do not red on the next ten `gui-driver` runs on `main` | ⏳ **OPEN, 1 of 10 counted.** Main's push run `33407819974` at `0a908099` (desktop `#49`'s merge): green, 14/14 flows, 0 fail rows. ⚠ **A WEAK INSTANCE, AND SAID SO:** every `#settings-code` precondition satisfied on its FIRST poll, so that run **did not race** — it is consistent with the prediction without testing it. ⇒ `ENG-0279`. |
| 400 | **[Director, `RULING_NA0768_017` sec 2 — the CLASS-SHAPED SUCCESSOR, added because the instance-scoped original was the Director's own `SR-21` miss]** no scenario reads `#settings-code` without the populated precondition, and no `gui-driver` run on `main` reds at that step shape in the next ten runs | ⏳ **OPEN.** First half MEASURABLE NOW and TRUE at `0a908099`: a census over all 14 scenarios found exactly two readers (`f_g`, `f_i`) and both carry the precondition. Second half: 1 of 10, as row 399. ⚠ **THE CLASS IS WIDER THAN THE PREDICTION:** only `#settings-code` has been swept; every other element written behind an `await` and asserted without a precondition is the same defect unfound. ⇒ `ENG-0279`. |
| 401 | **[Seat, NA-0774 — the brief's sec 3, tested rather than assumed]** ONE CSS rule (`overflow-y` on the rows container) fixes the contacts scroll | ⚠⚠ **MEASURED FALSE, BY THE SEAT, BEFORE PUBLISHING A CURE.** The one-rule arm was driven: the declaration **applied** (computed `overflow-y` `visible` → `auto`) and the defect was **unchanged** — rows 1410px with `scrollHeight == clientHeight`, pane 1496px in a 673px window. `.list-pane` is a **grid item** whose default `min-height: auto` refuses to shrink below content, so it grew and handed the rows container a 1410px box to "scroll" in. Two rules ship. ⇒ **a cure that lands is not a cure that works; drive the arm.** |
| 402 | **[Seat, NA-0774 — the brief's sec 3 assertions (1) and (3)]** the scroll test's heading-in-viewport and window-height-unchanged assertions carry the red arm | ⚠⚠ **MEASURED FALSE, AND BOTH ARE KEPT ANYWAY, RELABELLED.** In the RED state the heading was already in view (`headTop` 17, `headBottom` 41 — the pane grows downward, the header sits above it) and `window.innerHeight` was unchanged (no resize on `scr-main`). They are **no-regression guards**, not discriminators. The red is carried by `scrollHeight > clientHeight` plus an assertion this lane ADDED — the pane must fit the window — which measured 1496 against 673. ⇒ **an assertion that cannot fail in the red state proves nothing; say which one carries the arm.** |
| 403 | **[Seat, NA-0774 — `E3`'s two-arm requirement, caught before the red arm was run]** the tick test's positive control can sit after the tick assertion | ⚠ **FALSE, and the test was reordered rather than the expectation weakened.** The harness aborts at the first failed row, so a control placed after the tick assertion **never executes in the red arm** — and `E3` requires it to transition in BOTH arms. Moved before the tick arm; in the red run it reads `PASS / "user-call-shows-indicator"` while the tick arm reads `FAIL / "UNHID 2 time(s) under a tick"`. ⇒ **step order is load-bearing when the runner aborts on first failure.** |
| 404 | **[Seat, NA-0768 STOP 016 sec 3f — SELF-REPORTED, caught by the seat's own re-measurement]** `public-safety` and `infra-literal-scan` are both REQUIRED status contexts | ⚠⚠ **FALSE. Measured at the live gate: `["rust","advisories","infra-literal-scan"]`.** `public-safety` is NOT required; `gui-driver` is not either. The set was taken from a COMMENT INSIDE `ci.yml` — in the very act whose order (`3b`) existed because two prior seats had reproduced CI from a description. **A file cannot state its own branch protection.** Chasing the error produced `ENG-0280`, a required leak-scanner documented as advisory. ⇒ **read the gate, never the description of the gate; and the set is base-scoped.** |
| 405 | **[Director, NA-0773 close-out — the banked forward prediction, SCORED AT THIS LANE'S CLOSE, clause by clause with no charity]** "an ORDERING change inside handshake/mod.rs — no ack until a witnessed push, or push before commit — at most ~30 lines, SR-15 fires, t8 flips green under the same narrowed assertion with no test edit." **CLAUSE 1 PARTIAL** (there IS an ordering change, but the ordering was already correct and the real defect was a RETURN CONTRACT across a boundary). **CLAUSE 2 PARTIAL AND WRONG AS A RULE** (the ack does wait, but on a TYPE at the caller, and "witnessed push" mis-describes `:2250`, a Consumed exit that pushes nothing). **CLAUSE 3 HIT** (push before commit — exact). **CLAUSE 4 MISS, AND IT GREW AT EVERY ACT**: 83 product-code lines = 2.8x, 267 code lines added in src/ = 8.9x, 553 changed src/ lines = 18.4x. **CLAUSE 5 HIT** (SR-15 fired twice and both reads broke something real). **CLAUSE 6 HIT** (t8 green, test file untouched). |
| 406 | **[Seat, NA-0775 — SELF-REPORTED, and the failure mode is the point]** the seat's own size estimates were ~86, then ~120, then ~138, against a measured **553** changed src/ lines. **FOUR estimates, every one LOW, each re-pricing moving toward the truth and none reaching it** — because each counted the code being added and not the 214 comment lines being written about it. A prediction that misses is one thing; an estimate that stays serially optimistic under repeated correction is another. |
| 407 | **[Seat, NA-0775 sec G1b — SELF-REPORTED: a sealed falsifier that FIRED and was scored HIT anyway]** the sealed expectation read "if instead the tree already holds locks across network calls, my objection is weaker and I must say so". It does — `transport/mod.rs:3748` acquires and `:4005` pushes with no drop between — and the seat scored it a HIT. Caught by cold read #2's `G-1`, not by the seat. **RE-SCORED A MISS.** A sealed expectation is worth only what re-reading it AT SCORING TIME is worth. |
| 408 | **[Seat, NA-0775 — the act-4 risk enumeration, and what it did NOT catch]** STOP 002 named SIX at-risk `invite_finish=ok` assertions and predicted a red among them would be "the lie being removed". **ALL SIX PASSED.** The red came from `t5f`, the seventh thing in the same file, which no enumeration had named and neither cold read had been aimed at. **An enumeration of risks is a description of what you already suspect** — the transferable rule is to run the whole neighbourhood, not the enumerated risk. |
| 409 | **[Seat, NA-0777 `R18` — the first UNATTENDED backup run on the new roots]** five expectations sealed before the timer fired: the unit runs and deactivates successfully; a `daily-20260903T02*` snapshot exists; the fourteen-night-old blocker prunes with zero `cannot remove`; both status files read `outcome=OK` naming the new snapshot; the failed-unit list is 0 | all five HIT, measured from journalctl and the platter, with two clauses supplied by the operator's `systemctl` paste; the timer fired 02:33:02 and the unit finished 02:38:43 | HIT ×5 | E-1 CLOSED. A hand run proves the script; only the timer run proves the unit path |
| 410 | **[Seat, NA-0777 — the seat's OWN sealed expectation for the first run with the new backup roots]** the snapshot contains `state/operator/relay/LATEST.md` and the `lanes/` root, and growth is on the order of `state/`'s 437 M, **not** the tree's 8.7 G; near 8 G is a MISS meaning an exclude did not hold | HIT on all three: the relay file present at the size it had when the timer fired, `lanes/` present with its single entry, new-tree bytes in the snapshot **437 M** against 437 M on disk, `cache/` and `mirrors/` absent | HIT ×3 | an additive two-line patch, proven on its first unattended run, with the falsifier written first |
| 411 | **[Seat, NA-0777 gate arm (i) — the ordered four arms]** a permitted command succeeds; `sudo` is denied by the hook; an `Edit` under `.github/` is denied by permissions; an `Edit` outside it succeeds | all four HIT, and arm 3's denial **prevented** the write (the file stayed byte-identical to its HEAD blob) rather than merely reporting | HIT ×4 | the arm passed as written |
| 412 | **[Seat, NA-0777 `E-21` — the PROPERTY behind arm 3, which the record had been treating as proven by it]** "the `.github` deny protects checkouts" | **FALSIFIED** over 14 arms: `Write` allowed 4/4 including a clobber of the exact path `Edit` had just been denied; `Edit` allowed 2/8, the SAME edit of the SAME file allowed once and denied on re-run. Four hypotheses raised and all four falsified; the predicting rule is **unknown** and the stop says so | MISS, SEAT'S — free, self-caught | **an arm passing is not its property holding.** The record stopped calling the deny a boundary |
| 413 | **[Seat, NA-0777 `E-22` — SR-20]** the merged settings candidate is installable as banked | **FALSIFIED at the operator's keyboard**: the platform rejects the `Write(path)` deny form at startup, so the banked pair would not load. The candidate had been validated by **parsing** it (jq) and by a key census — instruments about the FILE — and **never by starting the reader**. Sixteen arms had been run on the hook, by executing the hook; zero on the settings file, by starting a session | MISS, SEAT'S — free | *the consumer is executed, never assumed.* And the fix nearly adopted the consumer's own stated reason unmeasured — measured, that reason is false on this build |
| 414 | **[Director, NA-0777 `R33`/`R67` — ONE shape, two instances: presence confirmed where completeness was owed]** rule 4 confirmed "implemented"; S4 confirmed "DONE" | **FALSIFIED both times.** Rule 4 was implemented in a working tree and **never committed**, so a fresh clone would not have had it; "S4 DONE" was true of one row of ten in the directive's own enumeration | MISS ×2, DIRECTOR'S | *a claim that an apparatus change is implemented names the COMMIT; a multi-row act's gate is its ENUMERATION, not its first row* |
| 415 | **[Both chairs, NA-0777 `E-30`/`E-33` — "zero old-tree references in the apparatus"]** asserted at S3 | **FALSIFIED TWICE.** True of **three** files, not the apparatus (Director's scope). The seat's replacement census was a **top-level glob** that never descended into two subdirectories: it reported 22 across 10 when the recursive figure is 41 across 12 | MISS ×2 | the file written to correct a scope error had a scope error. *Instrument scope = claim scope* |
| 416 | **[Seat, NA-0777 `E-34` — a completeness claim written before the last write it described]** STOP 008: two things diverge after S4's gate "and nothing else" | **FALSIFIED** by the seat's own `diff -r` minutes later: three diverged, the third being the stop itself, written after the sentence was composed. Amended | MISS, SEAT'S — free | ADOPTED: **a whole-set assertion is re-run after the LAST write of the turn**, and the figure in the record is that run's |
| 417 | **[Seat, NA-0777 — the same shape again, one turn later]** R63's zero-reference census printed 0 and a commit was pushed on it | **FALSIFIED**: the census ran **before** the tree-root `CLAUDE.md` was written, and that file named the old root once. Caught by reading the **remote's** bytes back, not by the local check that had just passed | MISS, SEAT'S — free | two instances in two turns is a pattern, not an accident. It is why row 416's practice was adopted rather than merely noted |
| 418 | **[Seat, NA-0777 — the freeze's sealed mode strings]** the frozen root would read `dr-xr-x---` / `dr-xr-s---` | **FALSIFIED on the cosmetic clause**, HIT on substance: both writes were refused and the probe file did not exist, but the modes are `dr-x--S---` / `dr-xr-sr-x`. The seat predicted from an **assumed 0700** when it had measured 2700 setgid itself and written that fact into its own prior stop | MISS, SEAT'S — free, operator-caught | *a predicted string that could have been COPIED from a measurement is never retyped from a model* |
| 419 | **[Seat, NA-0777 `E-31` — the ordered PATH change]** putting the tree's `bin/` first in `~/.bashrc` makes the bare word resolve to the new tree | **FALSIFIED.** The operator's shells are LOGIN shells; `~/.profile` sources `~/.bashrc` and **then** prepends `~/.local/bin`, so the ordered line could never win. It resolves correctly only because the operator **moved the old wrapper aside** — by removal, not by ordering. The seat had measured in `bash -ic`, which never runs `~/.profile` | MISS, SEAT'S — free, operator-diagnosed | ADOPTED: **every claim about the operator's shell is measured in `bash -lic`**, and both forms are shown where they could differ. SR-20 one level out: the consumer was executed, but not the one the operator uses |
| 420 | **[Seat, NA-0777 `E-36` — a premise the seat was HANDED and measured anyway]** `R79`: the loose `~/work/build-*.sh` are not copied because they are byte-identical to the tracked copies | **FALSIFIED** — and made false by the seat's **own** edit two turns earlier. Measured further: they are byte-identical to the tracked copies **as of the commit before** that edit, i.e. a superseded older copy. Outcome unchanged (do not copy); reason replaced | MISS (inherited premise), self-caught | measuring a premise you were handed, including one you made false yourself, is the practice. Copying them would have re-imported old-tree paths |
| 421 | **[Seat, NA-0777 `E-37` — the hook's own header, checked against the vendor documentation as `R48(5)` ordered]** "Exit 0 allows the call" | **FALSIFIED**: the documentation says exit 0 "means the hook has no decision to report … **staying silent doesn't approve it**". Under `bypassPermissions` the two are observationally identical, which is exactly why the wrong belief survived. A second fact from the same page bounds the fix: **a timed-out hook does not block** | MISS (inherited), R48(5) STOP | fail-loud covers a guard that CANNOT RUN; it cannot cover one that runs TOO SLOWLY. Both now in the shipped header |
| 422 | **[Seat, NA-0777 `E-39` — the probe runner]** the runner reports the candidates' verdicts | **FALSIFIED**: it mapped exit 2 to DENY and **everything else to ALLOW**, so when a path broke and every probe returned 127 it scored `command not found` as *permitted*. A runner written to prove a fail-open fix was itself failing open | MISS, SEAT'S — free | caught because seven probes reported MISS at once, an impossible result. `WF-0103`'s third datapoint |
| 423 | **[Seat, NA-0777 `E-40` — the revision build]** "rev3 parses clean", with four probe results printed beneath it | **FALSIFIED**: the build copied a **444** file, `cp` preserved the mode, python wrote nothing, and the four probes were measuring the **previous** revision under a heading naming the new one — with the traceback in the same output | MISS, SEAT'S — free | caught because the four values were identical to the baseline just printed: *an edit that changes nothing is not an edit*. The build now asserts it DIFFERS from its input |
| 424 | **[Seat, NA-0777 — the hook harness, deliberately falsified before its green was believed]** the 89-arm matrix can go RED | **HIT**: the pre-revision candidate scores **61/89** on the same arms and misses **exactly** the arms the two findings files predicted, no more and no fewer | HIT | a matrix that passed on both candidates would prove the arms blind. This is what makes 89/89 evidence rather than decoration |
| 425 | **[S5 read, NA-0777 — the maintenance retarget]** the retargeted nightly job would do something | **FALSIFIED before install**: the job selects by `CACHEDIR.TAG` under the work root and by `NA*` under tmp and **refuses any candidate under the cache root** — where rule 2 puts every build directory. Pointed at the new tree it is a **permanent no-op**. And `S5-13` named the unstated consequence: a root-run `rm -rf` given write access inside `$HOME` for the first time | MISS, SURVEY/PROPOSAL'S — caught pre-install by a cold read | ADOPTED: *a job is designed for the tree it runs in; a retarget across a change of layout rules is a REDESIGN, and the cold read is ordered on the SHAPE of a change, not only on its diff* |
| 426 | **[Seat, NA-0777 — the A2 invariant, held all lane]** the deployed maintenance script stays byte-identical to the spine's copy | **HIT**, re-measured at every act including after the withdrawal: `cmp` rc=0 throughout. The withdrawal is what kept it true — the proposal's own §11.1 would have forked it | HIT | the cheapest way to keep two copies identical is to not change one of them |
