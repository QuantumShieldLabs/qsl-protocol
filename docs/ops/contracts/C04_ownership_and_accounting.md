C04 -- OWNERSHIP AND ACCOUNTING -- FINAL (ACCEPTED WITH NAMED FIXES: CONTRACT_ACCEPTED_WITH_NAMED_FIXES)

==============================================================================================================
FIXES (draft -> FINAL), keyed to RULING_NA0783_C04_ACCEPT_2026-09-24 (sha256
c99b476ad0d3236528dcd5438bc5207cbc46061a6368e8d295d305e67307842e)
==============================================================================================================
Status: C04 ACCEPTED (contract) WITH NAMED FIXES; result class CONTRACT_ACCEPTED_WITH_NAMED_FIXES. The Director's
ruling, on the SR-15 read (SR15_C04_FINDINGS.md sha256
55313e4d00ec224fe0d051837b0f5fed455a02e30b3d65d64a0064b71d6de0a2, fable/xhigh, recommendation ACCEPT WITH NAMED FIXES
F1-F9; X1 and X2 MAJOR; no BLOCKER), takes F1-F9 as the read words them and rules E1-E7 below; nothing else in the
draft changes. Director-verified at #1831 ffc8fc52: transport/mod.rs:3657-3659 maps device_revoked to FailPermanent
after seal; msgqueue/mod.rs:1012-1019 -> retire_packed :1039-1048 -> RelayMessageSender::commit transport:3748-3764
calls state.accepted(raw) and timeline_project_message(peer,"out",...). X2 is REAL in the candidate (not on main). The
body below is the draft (C04_DRAFT.md sha256 dfb30a877855e23bbb24ca186dc10f980323ddeb961e015d483987f51303d679) with these
applied. The word PROPOSED in the body is retained verbatim and now reads: the accepted contract value, NOT YET
ALLOCATED; C04 allocates no wire or local-schema identifier, and every code marked NEW stays a PROPOSED spelling
registered through DOC-SCL-002 by the implementing PR (C01 O9). F01 stays ACTIVE (C05 next, then the C06 boundary).
Every fix was verified at the source before it was written (C = #1831 ffc8fc52; M = qsl-protocol main 1fbaa814; the
serde map code at the version locked by ffc8fc52:Cargo.lock).
| Key | Fix | Where | Source verified |
|---|---|---|---|
| F1 (X1) | T6 gains one DERIVED row, the ordinary-material L2 ceiling B_V - H_W - n x L06 (n = 1: 14,552,962; n = 2: 12,852,996; n = 8: 2,653,200 = L07); one maximum wire <= 262,145 L2 (all-255), about 234,000 for random ciphertext; ONE session's R04 L0 ceiling (64 wires) is 16,777,280 L2 > B_V, so R04's 4 MiB is unreachable in the vault as encoded and the aggregate is the binding ordinary limit. S10 and V601 cite the row. C4-O7 extended to the vault byte fields (Flight.wire, Disposition.receipt, Application.body, Key) with the 4x versus 4/3x arithmetic, OPEN, C01 A13 / F04 territory -- the encoding decision is NOT made here. The R11 feasibility note (32 KiB x 64 = 2 MiB fits beside two sessions) | T6 L29 (new), L23; T2 S10; T8 V601; T9 C4-O7 | vault:95 (secrets BTreeMap<String,String>: one JSON string per secret); delivery:217, :228, :236 (Vec<u8> fields); vault:2015-2016 -> protocol_state:1493-1498 (check_aggregate on the L2 length); protocol_state:1410-1411; delivery:174, :1126-1127; arithmetic re-done |
| F2 (X2, E3) | Q10 extended with the timeline Sent projection (transport:3762 -> timeline:462-466) and the receive abort (transport:545 via :4162); E3 MAJOR, reachability stated (a relay whose max_body_bytes is below the sealed wire; LOCAL device_revoked after seal, transport:3657-3659); V709 gains the three assertions; F07's named symbols gain RelayMessageSender::classify and RelayMessageSender::commit; a revoked device after seal is routed to K07 (C05 meaning), never to a terminal row | T4 Q01, Q10; T8 symbols, V709; ESCALATIONS E3 | transport:3606-3618 (classify: 413 TooLarge -> Fail), :2106, :3634, :3657-3659, :3748-3764, :4158-4162, :4184-4193, :540-550; timeline:462-466, :485; msgqueue:1012-1019, :1039-1048, :1065-1079, :641; contacts:324, :729 |
| F3 (X3) | L08 reads LIFETIME at C; L06-L08 read DERIVED (on HYPOTHESIS inputs L02-L05); S11's count reads "bounded by the closure discipline (window 16, <= 3 contexts), not by R04/R05 alone; F06 counts it"; K07 states that the close removes the PeerReserve AND the peer secret in ONE paired commit | T6 L06-L08; T2 S11; T5 K07 | vault:1992, :2001-2003 (either alone refused at reopen); no peers.remove/retain/clear on CapacityOwner at C |
| F4 (X4) | T9's LOCK-REGION line: V03 is the unbounded first read, under the lock at protocol_state:1315 on the directional_update_inner path; lockless at timeline:375, owed_receipts:76, transport:4187, msgqueue:641 | T9 LOCK-REGION line | protocol_state:1314-1316; vault:326-334 (secret_get takes no lock); timeline:375; owed_receipts:76; transport:4187-4188; msgqueue:641 |
| F5 (X5) | C4-O11 MEASURED (library source): serde_core 1.0.228 de/impls.rs:1542-1543 inserts every entry, last wins, no error; V406 RED at C by library source | T9 C4-O11; T8 V406 | ffc8fc52:Cargo.lock (serde 1.0.228, serde_core 1.0.228, serde_json 1.0.149); serde_core-1.0.228/src/de/impls.rs:1536-1547 (map_impl visit_map) |
| F6 (X6) | An SR-18 CENSUS LINE closes T9, listing the observables C04 remaps | T9 (new line) | msgqueue:46-49; transport:1109-1131, :3720-3724; msgqueue:1012-1019, :2112; lib.rs:54; main.rs:891 |
| F7 (X7) | X03 and E1 name the invitation candidate (C02 R6 / step 10 commit) as the THIRD op_id holder | T7 X03; ESCALATIONS E1 | C03 AM-8 C6 (a committed B1 or A2 retried with the same op_id and bytes); C02 T2 R6 and step 10 |
| F8 (X8) | Q07 gains the missing-row rule (reconstructed as a PREPARED row from the Flight before any send; a receipt for a Flight with no row still retires the Flight and its completed id); vector V713 | T4 Q07; T8 V713 (new) | delivery:885-893 (project); msgqueue:2099-2102 (Ok(false) for a missing row), :2118-2120 (directional_queue_missing); delivery:735 (COMPLETION_CAPACITY) |
| F9 (X9) | V07 and V410 count the +28 once (S15's MAXPACK already includes it); R03's release column adds "(lazily, at the next control admission or paired commit of that session)" | T3 V07; T8 V410; T1 R03 | msgqueue:390-412 (+28); delivery:1166-1168, :1271-1272 (prune_refs, retire_epoch_witnesses run inside the next admission or reconcile) |
| E1 | + X7: a C03 amendment -- op_id holders are THREE: Flight (PREPARED commit), Disposition (receive commit), invitation candidate (C02 R6 / step 10 commit); the queue row carries a copy for application sends. C4-O6 closed | ESCALATIONS E1; T7 X03; T9 C4-O6 | C03 AMENDMENTS AM-17 |
| E2 | the P28 quota (64 x 256 KiB) is NOT carried; R11 sits inside B_V; N_FC and N_INV HYPOTHESIS until F10 measures | ESCALATIONS E2; T9 C4-O5 | P28 handshake/mod.rs:202-206 |
| E3 | + X2: ENG-0357 filed (the #1831 defect, both routes; consequences: false Sent, Flight replayed on every receive, receive abort on repeated 413, undiscardable row); repair named for F07; NOT repaired here | ESCALATIONS E3 | as F2 |
| E4 | C4-O15 KEEP (the per-commit margins are part of INV; V402 the proof) | ESCALATIONS E4; T9 C4-O15 | vault:1144 -> :1999 -> delivery:1362-1369 -> :1119 |
| E5 | accepted (the formula fixed here; 64 / 4 MiB / 60,000 HYPOTHESIS for F07) | ESCALATIONS E5 | lib.rs:54; main.rs:891; delivery:174 |
| E6 | a C02 amendment -- R11 quota full at step 10 -> DEFER (the AM-3 disposition), processed when a record retires (V1002); code spelling via DOC-SCL-002 as C01 O9 | ESCALATIONS E6 | C02 AMENDMENTS AM-10 |
| E7 | BOTH -- a C03 amendment raising the T3 C1 floor to v2.max_body_bytes >= MAX_WIRE (65,536) AND Q11 kept as the seal-time guard; a 413 after sealing leaves the row PREPARED; C4-O17 closes | ESCALATIONS E7; T4 Q11; T9 C4-O17 | core:18 (MAX_WIRE 65,536); C03 AMENDMENTS AM-18 |
Not taken, named: the SR-15 read's section 7 items outside F1-F9 (vectors for the R06 projection-credit refusal, the
K04 remote QueueFull replay, the two-process race for the last queue slot, the Q06 pre-seal FAILED removal) and its
V402 wording nit ("the smallest possible ordinary write") -- the ruling names none of them. Also changed, as
consequences of the ruling only: the title line, this FIXES section, the RULED clauses of ESCALATIONS E1-E7, the state
cells of T7 X03 and T9 C4-O5, C4-O6, C4-O15 and C4-O17, the ESCALATIONS header and the closing END line. The owed
C02/C03 items R-1..R-8 are carried by the C02 and C03 AMENDMENTS appended with this contract, not by this text.

PLAN: QSL-solution-plan rev3 d9016e53d2ab46c32b7a7cb060ea70dc79421617e9b054848b464ba7a5518275 (amended A4), card F01,
sub-assignment C04. Lane NA-0783 (D-1425..D-1430). Drafted 2026-09-24 by the executor seat (opus/high). Status of every
row: PROPOSED; ACCEPTED only by the Director's ruling after the SR-15 read (fable/xhigh; lock regions and capacity
invariants). Scope, THE PLAN row C04 verbatim: "Dimensions and owners of each reservation; maximum serializer inputs;
admission versus reopen validation; global queue peak/temp/terminal rules; control-progress and retirement rules; limits
derived or explicitly still hypotheses." It is the contract F04-F07 build against; F06 completes its proof.
Not in scope (each dependency is an OPEN cell in T9): C05 dispatcher, ACK dispositions, finite budgets, history policy
and DTO names; C06 exact file bytes (C04 fixes file RESERVATION DIMENSIONS and their owners only, THE PLAN F01 framing);
C07 rollback. The owed C02/C03 amendment items R-1..R-8 are not drafted here.
Fits C01 (docs/ops/contracts/C01_versions_and_boundaries.md at main, AM-1..AM-4): C04 allocates NO wire or local-schema
identifier; every code below marked NEW is a PROPOSED spelling registered through DOC-SCL-002 by the implementing PR
(C01 O9); refusal before effects is kept at every admission point; every store named here lives in the successor
directory (C01 O8 L1-L6); C04 adds NO top-level VaultPayload field (C01 O13: the C07 lineage fields stay reserved), all
owner fields below live inside the owner secret whose key strings stay OPEN (C01 O5, A15); the queue rules apply to the
msgqueue_v2 family (C01 row 19, A17). Fits C02 and C03 as merged (with their AMENDMENTS): T7 answers every cell that
names C04.
Sources measured (read-only; the drafting mission's MEASURE files -- MEASURE_candidate.md sha256
ad39be615cfb11a05974594afb9fe1439c901eba3f9719e1dcddcf6e580f8762, measure_main_readers.md sha256
9c31f409630501512508405f38101c635b70cef33646405286d5bac553f55f91, measure_p28_firstconn.md sha256
e6d256dbd2728fa7bf29b0f6bd32b16bf6381a2cd5cbb9fcea4aa105f101d4ad -- listed in its REPORT.md sha256
6142365dae35538877c7bedd0ffff01b406c7ee7fea7fe1b35e52a9b479d36d0; zero unclassified): M =
qsl-protocol main 1fbaa814 (#1841 merge); C = #1831 ffc8fc52 (the preserved directional candidate); P28 = #1828 e29a07df.
File:line is qsl/qsl-client/qsc/src/<file>:<line> at C unless prefixed M: or P28:. Every DIRECTIONAL ownership structure
is ABSENT at M (git grep count 0 for CapacityOwner, PeerReserve, OwnerEntry, SessionControlReserve,
REVIEW_AGGREGATE_CANDIDATE, commit_directional_pair, directional_intent, response_pending); M contributes only the legacy
queue, vault and side stores (so R01-R06 below are ABSENT at M; R08-R10 and R14 exist there in legacy form). Nothing
was built or run; every behaviour stated as "today" is a source reading.

NOTATION. B_V = the vault aggregate budget, 16,777,216 (protocol_state:1410). H_W = the global write headroom, 524,288
(:1411). Byte LAYERS, never mixed in one sum: L0 raw payload bytes; L1 inner JSON text (serde_json; a Vec<u8> or
[u8;N] is a JSON number array, at most 4n+1 characters for n >= 1 bytes: "255" plus a comma per element, two
brackets); L2 outer JSON (an inner JSON text embedded as a JSON string: at most 2 x L1 + 2, since only '"' and '\' are
re-escaped and serde_json emits no raw control character; number arrays contain neither, so they pass 1:1); L3
encrypted bytes (vault: L2 + the 16-byte tag, framed by the 53-byte header; read cap B_V + 53 + 16, vault:44, :1922; queue row: L1 + 12 nonce + 16 tag = L1 +
28, msgqueue:390-412, measured +28 in P4); L4 on-disk bytes including the replacement temporary. INV(s) = the
committed-state invariant every authoritative writer establishes; ADM(s, op) = INV of the post-state plus op's declared
futures. "Paired commit" = one authenticated vault replacement under lock_store_exclusive (fs_store:375-400, one
exclusive flock per store directory, reentrant).

==============================================================================================================
T1. RESERVATION DIMENSIONS AND OWNERS  (LR = LOCK-REGION row: SR-15 sensitive)
==============================================================================================================
| # | Reservation | Owner (authority, THE PLAN sec 1) | Dimensions (layer) | Charge point | Release point | Lock / commit |
|---|---|---|---|---|---|---|
| R01 LR | Vault aggregate | CapacityOwner (paired owner) protocol_state:1413-1419 | actual L2 VaultPayload + remaining promises + H_W <= B_V (check_aggregate :1493-1500) | EVERY vault writer, after its mutation and before its write (vault: secret_set :350, secret_set_with_passphrase :407, session_set :494, persist_session_with_ownership :544/:555, write_directional_payload :2033, commit_directional_pair :2102, project_owned_secret :2143, create_directional_pair :2180) | none: a promise becomes actual bytes in the commit that materializes it | lock_store_exclusive; the one vault replacement |
| R02 LR | Session reserve (one per established session) | PeerReserve inside CapacityOwner protocol_state:1420-1432 | inner future = CTRL_B - retained control L1 + core future (CORE_B - actual core L1) (delivery:1343-1345); outer future = 2 x (CORE_B + CTRL_B) - retained reserved L2 + 2 x 524,288 (the literal, numerically 2 x H_W; :1349-1353; the factor 2 on the envelope covers outer JSON escaping, :1346-1348). TOTAL per session, retained plus future, is the constant 2 x (288,920 + 36,775) + 2 x 524,288 = 1,699,966 (DERIVED, T6 L06), plus the 20-digit funding of its u64 scalars (remaining_vault_bytes protocol_state:1470-1492) | create_directional_pair (vault:2151-2183), in the SAME commit that creates the session state, before completion is announced (THE PLAN sec 4); refreshed at every paired commit (vault:2093) | PROPOSED: only by an explicit session-close transition (T5 K07), after every flight and disposition of that session is retired or failed honestly. TODAY: no release exists (no CapacityOwner.peers removal at C) | LR; paired commit |
| R03 LR | Control credit (per session) | SessionControlReserve inside PeerReserve protocol_state:1459-1468 | COUNT: 2 epoch witnesses (recv_epoch_state 154 = 2 x 77 B) x 18 refs = 36 refs (control_refs 1620 = 36 x 45 B; protocol_state:1594 refuses the 19th ref of one epoch); 1 outstanding send request (45 B); BYTES: CTRL_B (L1) = 36,775; receipt future (36 - retained refs) x 113 (L0) (delivery:1122); maintenance future 2,287 (L0 wire) while no maintenance flight exists (:1125) | already funded by R02 at session creation; each authenticated control is admitted by admit_epoch_control / retain_control / record_request inside the staged transition (protocol_state:1572-1669) | ref: prune_refs when its disposition is retired (protocol_state:1557-1569); witness: retire_epoch_witnesses when the epoch leaves recv (:1635-1644); request: confirm_request (:1670-1678); refs and witnesses are released lazily, at the next control admission or paired commit of that session (delivery:1166-1168, :1271-1272) (F9) | LR; paired commit |
| R04 LR | Ordinary in-flight (per session) | Transaction (paired peer state) delivery:238-264 | COUNT: <= 64 application flights, <= 1 maintenance flight, send window 16 slots per epoch (:597, :600, :617, :629); BYTES: sum(flight wire L0) + maintenance future + 65,536 <= 4 MiB (:1126-1127) | prepare_padded (:580-715): the Flight (exact wire) is sealed and committed = PREPARED (I03, I04) | ONLY an authenticated exact receipt (:744) or an explicit session close (K07). NEVER relay acceptance, a push failure, a queue state, expiry or cancellation (D08) | LR; paired commit |
| R05 LR | Receive admission (per session) | Transaction | COUNT: <= 64 unprojected events, <= 3 receipt contexts (:482, :485, :768); BYTES: sum(disposition receipts) + sum(event bodies) + receipt future + 65,536 <= 4 MiB (:1123-1127) | receive_inner (:722-851) | disposition: the peer's closure promise (:554-556); event: project_received (:927) | LR; paired commit |
| R06 LR | Owned projection credit (per owned operation) | OwnerEntry protocol_state:1433-1451 in CapacityOwner | projection = 2 x the largest singleton timeline record at L2 (timeline:1015-1032); len 60,000 for a send (delivery:1225), the body length for a receive (:1220) | reconcile_owner in the same paired commit that seals the flight or admits the receive (:1212-1245) | converted to actual bytes by project_owned_secret (vault:2114-2147; growth <= credit); the entry is removed when projected AND closed AND charge 0 (delivery:1267-1268) | LR; paired commit / its own vault replacement |
| R07 | History quota (NEW dimension) | CapacityOwner (a history field; spelling OPEN) | COUNT H_N and BYTES H_B (L2) of retained timeline entries. TODAY: no bound; one TimelineEntry per message is kept forever and only its projection credit is charged (timeline:441-507), so history eventually fills B_V and every admission then fails as directional_aggregate_waiting | the paired commit that creates the owned operation charges one history unit; refused before the queue row is created (T4 Q03) with history_full (NEW) | only a user-visible history retirement (policy C05, C4-O3); a protocol retirement never frees history (THE PLAN sec 4) | paired commit |
| R08 LR | Queue slot (global) | the encrypted queue row before PREPARED (THE PLAN sec 1 authority table) | COUNT of rows in the counted pool P = {QUEUED, PREPARED-not-yet-relay-accepted} across ALL contacts <= Q_S; per peer <= Q_P (fairness, T4 Q08) | enqueue, under the one exclusive lock, before any directory or file is created (T4 Q03) | when the row leaves P: relay acceptance of its exact wire (row reduced to a SENT tombstone, T4 Q06) or a pre-seal terminal FAILED | LR; the row's write_atomic |
| R09 LR | Queue bytes (global) | as R08 | sum over P of MAXPACK(r) + TEMP_Q <= Q_B (L3; T4 Q02); MAXPACK is charged at ENQUEUE, so post-pack growth is pre-funded | as R08 | as R08 | LR |
| R10 LR | Replacement temporaries | fs_store::write_atomic (fs_store:223-254) under the writer's lock | vault: one full new file <= B_V + 69 beside the old one (L4 peak 2 x (B_V + 69)); queue: one new row <= max MAXPACK (TEMP_Q, inside R09); a crashed process's temporary (another pid) is never removed today (only the current pid's name is removed, fs_store:240) | the write | rename; PROPOSED: stale temporaries of this store are removed under the exclusive lock at store open, before any admission (T4 Q05) | LR |
| R11 LR | Invitation operation records (client; C02 OC5, C03 OC3) | the invitation operation / lifecycle capsule (THE PLAN sec 1), stored in the successor vault | COUNT: per (self, peer identity): <= 1 outgoing attempt + <= 1 responder candidate per generation + <= 1 superseded receipt (C02 T3, T4 X-d); globally <= N_FC live records and <= N_INV Active/Creating invites. BYTES (L2, each charged against R01): the operation record (C03 T5 L1: op_id, payload <= 639 B, profile, S, T, H_S, H_T), the saved RESULT (C03 EP6), the delivery receipt (<= 256 B), the exact A1 envelope (<= 9,668 B), the outgoing candidate secrets, the responder candidate with its exact B1 (<= 11,729 B) and HsReceipt, the exact A2 (<= 3,380 + L B), the inviter's InviteRecord (R_slot, bearer_secret, C(self)) | a RESERVATION of the record's maximum retained set is committed with the record itself: inviter at C03 AM-12 I0 (before EP5); redeemer at C03 T5 L1 (before any network, after C02 R3); responder candidate at C02 T2 step 10. No capacity is checked by a preflight that releases the lock before the network call (P28 does: invite preflight P28:871-886) | secrets zeroized in the commit that makes the record terminal (C02 T3 F5c; C03 AM-10); public replay data and the reservation released at that side's horizon (C03 T6, AM-2, AM-16), then the record is removed | LR; paired commit |
| R12 | File incoming reservation (DIMENSIONS only) | FileJob in CapacityOwner (THE PLAN sec 6) | full encrypted bulk spool for the whole file (L4; <= 4,194,304 plaintext + per-chunk AEAD overhead x <= 256 chunks); progress bitmap (<= 256 bits); 2 in-flight protocol slots; manifest and completion receipts; publication temporary on the publication filesystem (L4, <= file size); discard/receipt drain allowance; the fixed recovery horizon record | the atomic commit that admits the authenticated manifest (receiver order, THE PLAN sec 6); a lack of capacity DEFERS before any acceptance ACK or NDR1 (I07) | after owned-artifact cleanup AND explicit transfer of remaining protocol liabilities to the session (R02-R05) (THE PLAN sec 6) | paired commit; bytes and formats C06 |
| R13 | File outgoing reservation (DIMENSIONS only) | StagePreparing ticket, then FileJob, in CapacityOwner | private temporary identity; outgoing slot; peak disk for the encrypted snapshot/spool (L4); 2 in-flight protocol slots; retirement credit for <= 2 committed chunk flights plus manifest/completion | the StagePreparing commit, BEFORE bulk source I/O | after receipts/closure of every committed file flight and cleanup; cancellation never erases committed flights (I11) | paired commit; bytes C06 |
| R14 | Legacy side stores | owed_receipts_v1 (owed_receipts:46-93, <= 256 per peer, TTL 604,800 s), relay dedup, quarantine | per C01 CENSUS; disposition OPEN (C01 O6) | -- | -- | C01 O6 |
Owner rule (D03): exactly ONE durable owner per reservation above; no second database, no counter file that can drift
from the rows it counts (the queue count is derived from the rows under the lock, T4 Q04). Local funding never implies
remote admission (I05, D07): the peer's relay mailbox depth is shared by ordinary and control frames and is not a
local reservation (C4-O9).

==============================================================================================================
T2. MAXIMUM SERIALIZER INPUTS  (each bound names its layer; OPEN = the constant F06/F07/F10 must compute with the real serializer)
==============================================================================================================
| # | Structure | Maximum legal input | Bound and layer | Status / instrument |
|---|---|---|---|---|
| S01 | NDE1 wire (sent; stored in Flight.wire and in the packed row) | <= 65,536 B (core:18; enforced on build :250, :258 and parse :309) | L0 65,536 | MEASURED (enforced) |
| S02 | NDI2 typed body (sealed in NDE1) | padded size <= 60,000; payload <= 60,000; id <= 64; <= 3 closures, always reserved in padding (delivery:287-298, :349-370, :411-412) | L0 60,000 | MEASURED (enforced) |
| S03 | NDR1 receipt | exactly 113 B (delivery:10-11) | L0 113 | MEASURED (format) |
| S04 | Flight | wire <= 65,536; id <= 64; closure_proof exactly 184 chars (:1076-1115) | L1 <= JSON(wire) 262,145 + fixed fields; OPEN constant | OPEN: F06 computes FLIGHT_MAX with the real serializer at a 65,536-byte all-255 wire |
| S05 | Disposition | hash 32, receipt 113, response_pending (required, T3 V04) | L1 constant; OPEN value | OPEN (F06) |
| S06 | Application (event) | body <= 60,000 | L1 <= 4 x 60,000 + 1 + id + names | OPEN constant (F06) |
| S07 | Core (+ Epoch, LocalTarget) | 2 recv epochs, 1 send epoch, <= 16 skipped keys, 1 local and 1 peer target, last_out <= 65,536 (core:15-18) | L1 CORE_B = 288,920 is a "conditional paper bound" (delivery:1283-1286) | HYPOTHESIS (F06: serializer calculator at maximum widths) |
| S08 | SessionControlReserve | fixed-width base64 fields (24 + 60 + 208 + 2,160 chars) + two u64 | L1 constant | DERIVED shape; value OPEN (F06) |
| S09 | Retained control set | 36 dispositions + the reserve + 1 maintenance flight + send holes x 11 (control_retained_encoded :1177-1198) | L1 CTRL_B = 36,775 | HYPOTHESIS (literal "Hypothesis under review", vault:2165-2166) -- F06 |
| S10 | Transaction (per peer) | the union of S04-S09 under R04/R05 | L1 <= MAX_RECORD 16 MiB (checked before parse on the string, delivery:466); in the vault the ordinary part is bounded by the aggregate, the ordinary-material L2 ceiling of T6 L29 shared by every session (R04's 4 MiB of L0 wire is unreachable in the vault as encoded) (F1) | MEASURED (enforced); the ordinary part is actual bytes under R01 |
| S11 | CapacityOwner | one PeerReserve per session, one OwnerEntry per unretired owned operation; each u64 is funded to 20 digits (remaining_vault_bytes protocol_state:1470-1492) | L2 (inside VaultPayload) | OPEN per-entry constant (F06); count bounded by the closure discipline (window 16, <= 3 contexts), not by R04/R05 alone; F06 counts it (an entry outlives its Flight and its completed id until closed) (F3) |
| S12 | TimelineStore | one entry per message (R07) | L2; per-entry bound = directional_projection_bound (runtime serializer) | DERIVED at runtime; H_N/H_B OPEN (C4-O3) |
| S13 | VaultPayload | everything above plus unowned secrets | L2 <= B_V - promises - H_W; L3 file <= B_V + 53 + 16 (read cap vault:1922) | MEASURED (enforced on commit paths only, T3 V03) |
| S14 | QueuedIntent | 267-276 B measured (P4) | L1 <= 1,024 (decode cap delivery:327) | MEASURED |
| S15 | Queue row v2 (C01 A17) | body <= 60,000; packed: + wire <= the maximum wire for the row's padding size + 41-B marker + channel + wire hash | L3 MAXPACK(r) = 28 + L1 of the row with ciphertext at the maximum wire length for r.padding.size and every mutable field at maximum width. MEASURED UNPACKED (P4): 1,215 / 16,865 / 240,497 B for bodies 2 / 3,913 / 59,817 (all 0xFF for the last) | OPEN: F07 computes MAXPACK(p) for every padding size p with the real serializer; the byte-field encoding of v2 is C4-O7 |
| S16 | Invitation records (R11) | per C02 T1a/T1b/T1c maxima and C03 EP6/EP7 | L2; per-record bound OPEN | OPEN (F10). P28's records store byte fields as JSON number arrays (P28 handshake/mod.rs:202-206 store, 256 KiB per record) |
| S17 | Replacement temporary | one full copy of the file being replaced | L4 | DERIVED (R10) |
| S18 | File NIF payload, binary file row, FileJob, spool | C06 | C06 | OPEN (C06/F14) |
Rule S-R1: a budget sums values of ONE layer only; raw payload, JSON expansion, encrypted row and replacement temporary
are never exchanged for each other (THE PLAN Values row). Rule S-R2: every constant in this table that is OPEN or
HYPOTHESIS is computed by the named card with the production serializer at maximum-width scalars and all-255 byte
arrays, and fails its card if it exceeds the budget; a passing scenario is not the bound (THE PLAN sec 4).

==============================================================================================================
T3. ADMISSION VERSUS REOPEN VALIDATION
==============================================================================================================
Rules. V-R1 (I06): reopen checks INV exactly; admission checks ADM(s, op). Reopen never charges an operation margin that
is not part of INV. At C the per-commit headroom H_W (R01) and the per-peer 65,536 reserve with 2 x H_W (R02, R04, R05)
are established by EVERY writer (refresh_reservation -> check_reserved_cost -> bounds(65536), delivery:1119, vault:2093)
and are therefore part of INV, not a new-admission margin; reopen re-running them (decrypt_payload -> check_directional_
aggregate -> verify_reservation, vault:1144, :1999) is PROPOSED KEPT, and a maximum-valid state built at INV's exact
boundary must reopen (V402). The reading is the Director's (C4-O15). V-R2 (I03): a refusal at admission precedes every
external release (wire, receipt, ACK, plaintext projection, success status) and leaves the prior committed state
byte-identical; a reopen refusal is a local fault: it writes nothing, resets nothing, migrates nothing, and is never
counted by the attempt guard (C01 O7). V-R3 (I06): every reader bounds the read on the open handle before allocating
(metadata length, then take(cap+1)), as read_directional_runtime does (vault:1917-1935). V-R4 (F04): successor
persisted structs carry deny_unknown_fields at EVERY level, no serde(default) and no default-to-empty on parse error;
duplicate keys are refused at every map level (C01 row 15).
| # | Reader | Refused at write (admission) | Refused at reopen (PROPOSED) | Today at C (measured) | F04/F07 instrument |
|---|---|---|---|---|---|
| V01 | VaultPayload (vault:89-96, :1114-1146) | R01 over budget -> directional_aggregate_waiting | version, profile, mode (C01 row 16), unknown field, duplicate key at any level, INV of every owner and peer | top level: deny_unknown_fields, unique secrets; nested maps: no detector. M: VaultPayload {version, secrets} (M:vault:87-90), version never compared, duplicate secret keys last-wins (C01 S1) | V406 |
| V02 | CapacityOwner family (protocol_state:1413-1468) | owner writers only (vault:2022 refuses generic writes to owned keys) | as V01; PeerReserve.generation == Transaction.generation == control.generation; entry.sid == peer sid; ticket canonical (vault:1988-2014) | these checks EXIST (vault:1995-2013); deny_unknown_fields on all five structs | V407 (pin) |
| V03 | Vault file read (every path) | -- | size <= B_V + 53 + 16 before allocation on EVERY read path | BOUNDED only in read_directional_runtime (vault:1917-1935); UNBOUNDED fs::read in load_vault_runtime (vault:1049), which secret_get uses and which is the FIRST read of every directional_update_inner (protocol_state:1316 -> :1258-1261); UNBOUNDED at vault:530 and :1943. M: every vault read unbounded (the drafting mission's measure_main_readers.md, sha256 9c31f409..., named in full in the Sources line) | V401 |
| V04 | Transaction / Disposition (delivery:464-475) | R04/R05 at prepare and receive; INV at every commit | MAX_RECORD before parse, profile, bounds, Disposition.response_pending REQUIRED | response_pending DEFAULTS TO TRUE when absent (delivery:223, :229-230) | V403 |
| V05 | Core / Epoch / LocalTarget (core:121-199) | enforced on receive (core:688, :651, :725, :760) | recv.len() <= MAX_EPOCHS, skip_count <= MAX_SKIP, local/peer <= MAX_TARGETS, unknown fields refused | NO deny_unknown_fields; none of the bounds is checked at decode | V408 |
| V06 | QueuedIntent (delivery:326-334) | encode at enqueue | 1,024 cap, profile, kind, id, body hash, padding | as proposed | pin |
| V07 | Queue row (msgqueue:445-467) | T4 Q03 (slots, bytes, history) | read bounded by MAXPACK_MAX before allocation (S15's MAXPACK already includes the +28; F9); record version 2 checked (C01 A17); deny_unknown_fields; no serde(default); msg_id/seq echo | UNBOUNDED fs::read (:450); version never checked; NO deny_unknown_fields; serde(default) on ciphertext, next_state, channel, directional_wire_hash, directional_intent (:203-216) | V404, V405, V410 |
| V08 | Timeline (timeline:374-384) | R06/R07 | deny_unknown_fields; no serde(default); count <= H_N | serde(default) on target_device_id, content_commitment, state, status (:21-28); next_ts 0 -> 1 repair (:380-382) | V412 |
| V09 | owed_receipts (owed_receipts:75-89) | -- | exact version; parse error refuses and keeps the bytes (C01 T2 G) | parse error -> EMPTY store, next save overwrites (:85); version never compared; identical at M | V411 (disposition C01 O6) |
| V10 | Invitation records (R11) | R11 reservation at I0 / L1 / step 10 | exact version; bounded read; secrets absent from terminal records | ABSENT at C and M as quota'd records; P28 counts active records only and keeps terminal records forever (P28 handshake/mod.rs:318-319, :386-393, :419-426, :582-589). M and C: the legacy HandshakePending, one vault secret per pair, no bound, its key-material fields serde(default) to empty (M:handshake:161-190) | V413 |
| V11 | SessionControlReserve (protocol_state:1522-1553) | every mutator validates first | canonical base64, zero-filled free slots, unique witnesses and slots | EXISTS | pin |
| V12 | carrier proof (delivery:1095-1115) | encode | exact length, canonical re-encode | EXISTS | pin |

==============================================================================================================
T4. GLOBAL QUEUE RULES  (the queue row is the authority only BEFORE PREPARED; after it the Flight is, THE PLAN sec 1)
==============================================================================================================
| # | Rule |
|---|---|
| Q01 | STATES. QUEUED (immutable intent + padding choice, the row is the authority) -> PREPARED (Flight sealed and committed in the vault; the row is a projection holding a copy of the exact wire) -> SENT (relay accepted those exact bytes; the row is reduced to a tombstone: msg_id, seq, peer contact id, state, wire hash; no body, no ciphertext) -> DELIVERED (exact authenticated receipt durably applied) -> RETIRED. FAILED is reachable ONLY from QUEUED (nothing sealed). After PREPARED the only terminal exits are DELIVERED and an explicit session close (T5 K07); a push failure of a sealed wire leaves the row PREPARED and retriable with the same bytes (I04, D08). A sealed wire the relay will never accept (413 from a relay whose max_body_bytes is below the wire) therefore stays PREPARED until session close; Q11 prevents sealing such a wire. A contact's device revoked after seal (device_revoked, transport:3657-3659) is routed to K07's session close with C05's user meaning, never to a terminal row (F2) |
| Q02 | PEAK. The counted pool P = rows in QUEUED or PREPARED. Admission requires |P| + 1 <= Q_S, |P_peer| + 1 <= Q_P, and sum over P plus the candidate of MAXPACK(r) + TEMP_Q <= Q_B, with TEMP_Q = max MAXPACK over P and the candidate (one replacement temporary: every row write holds the exclusive lock, R10). MAXPACK is computed from the candidate's padding size, fixed in its intent at enqueue, so post-pack growth never exceeds what was admitted (V703) |
| Q03 LR | ADMISSION ORDER, under ONE hold of lock_store_exclusive: stale-temporary sweep (Q05) -> count P and its bytes -> history unit (R07) -> refuse or write. Refusals: msgqueue_queue_full (slots), msgqueue_queue_bytes_full (bytes), history_full (NEW); each BEFORE ensure_dir_secure or write_atomic creates a directory, file or temporary, so the store tree is byte-identical after a refusal (THE PLAN sec 4 "refuse before creating another accepted intent"). The refusal is visible to the user; it replaces the documented "enqueue never refuses" soft-warning invariant (msgqueue:46-48, transport:1121-1122), whose comments F07 corrects |
| Q04 | COUNTING COST. The count is derived from the rows on disk under the lock (no second source of truth). Rows on disk are bounded by construction: |P| <= Q_S, SENT tombstones <= 64 per session (R04), DELIVERED rows retire in the commit that completes their projection (Q06), so a count never scans an unlimited lifetime (THE PLAN sec 4). Whether a persisted counter may replace the scan is C4-O16 |
| Q05 LR | TEMPORARIES. The only temporary a queue write creates is the write_atomic temporary of the row being replaced (charged as TEMP_Q). A temporary left by a crashed process (another pid; fs_store:240 removes only the current pid's name) is removed under the exclusive lock at queue open before any admission; until removed it is charged at its on-disk size (conservative over-accounting, never over-admission) |
| Q06 | TERMINAL RETIREMENT. A DELIVERED row is removed in the commit that makes its timeline projection durable and after its Flight retired; a SENT tombstone is removed when its Flight retires. Pre-seal FAILED rows are removed after their user-visible notice is projected. A row whose Flight is live is NEVER removed or made terminal to reclaim room (THE PLAN sec 4, F07). History is R07, not a queue row |
| Q07 | EXACT RETRY. The wire and its push identity are fixed in the PREPARED commit and live in the Flight (T7 X03); the row carries copies. At restart a committed Flight wins over a stale row: an unpacked row whose Flight exists re-reads the same wire (today delivery:587-591), never reseals. A Flight whose row is MISSING at restart is reconstructed as a PREPARED row from the Flight (id, wire hash, padding from the intent stored with the Flight) before any send; a receipt for a Flight with no row still retires the Flight and its completed id (the row is a projection, THE PLAN sec 1). Today a missing row keeps its id in completed (msgqueue:2101 returns Ok(false) to Transaction::project, delivery:885-893), and at 64 undrained completions the next authenticated receipt is refused COMPLETION_CAPACITY (delivery:735) (F8; V713) |
| Q08 | FAIRNESS. One peer cannot hold all of P: Q_P < Q_S (values C4-O1); finite per-peer drain budgets are C05's (C4-O10) |
| Q09 | CONTROLS NEVER USE THE QUEUE. Receipts, maintenance, advertisements and closures are replayed from the vault (pending() delivery:566-568), never enqueued (measured: enqueue_padded_at has two production callers, both application sends, transport:1104, :1106). P full never blocks an owed control (V707) |
| Q10 | TODAY (C), MEASURED BY READING: no slot, byte or history check on enqueue (msgqueue:569-607); rows are never retired for a directional peer (remove refuses, :641); every drain and summary loads every row of every contact (:904-906, :1101-1103); post-pack growth is uncharged (mark_packed :243-250); retire_packed on Fail/FailPermanent calls MessageSender::commit (:1039-1048), which for the relay sender runs Transaction::accepted (transport:3748-3764): a relay-REFUSED sealed flight is recorded as relay-accepted and the row becomes terminal FAILED while the Flight stays live and is replayed on every receive (transport:4184-4193); the same commit projects the message as SENT in the timeline (transport:3762 -> timeline:462-466) although no relay accepted it, and on a repeated 413 the replay's Err (transport:4162) aborts the receive at transport:545, before directional_flush, on every receive for that peer until session close; the row cannot be discarded (discard_at msgqueue:1065-1079 runs retire_packed again, then remove refuses, :641). Two routes reach it: a relay 413 below MAX_WIRE, and LOCAL device_revoked after seal (transport:3657-3659, FailPermanent), no relay needed; under the local route the replay push succeeds at the relay, so the peer receipts a frame the sender's queue calls FAILED-PERMANENT (F2). Q01 and Q06 forbid that (ESCALATION E3). M, the same absence: enqueue_at (M:msgqueue:550-581, sole caller M:transport:1923) never refuses; rows leave the disk only by the named discard (M:msgqueue:598, :1019) or a vault-destroy wipe; no production path sets DELIVERED; the seen_inbound sidecar is production code at M (M:transport:1115, :1176), rewritten whole per receipt-carrying message and never pruned (test-only at C, msgqueue:709-790) |
| Q11 | SEAL ONLY WHAT THE RELAY CAN TAKE. PREPARED admission refuses, before sealing, a wire longer than the target relay's advertised v2.max_body_bytes (C03 T3 EP10), with the row left QUEUED and a visible reason (NEW code). Today C03 C1 (with AM-6) requires only v2.max_body_bytes >= 12,288, while an ordinary NDE1 wire may reach 65,536 (MAX_WIRE, core:18): a relay advertising any value in 12,288..65,535 passes C1 and then refuses a sealed wire with 413 after PREPARED (ESCALATION E7). RULED (E7): C03 AMENDMENTS AM-18 raises the C1 floor to v2.max_body_bytes >= MAX_WIRE (65,536); Q11 is KEPT as the seal-time guard against a relay reconfigured after C1 ran; a 413 after sealing leaves the row PREPARED (Q01) |

==============================================================================================================
T5. CONTROL PROGRESS AND RETIREMENT
==============================================================================================================
| # | Rule |
|---|---|
| K01 | OWED CONTROLS (C, measured): the receipt of every admitted frame (Disposition, replayed while response_pending, delivery:566-568, :757); ONE maintenance flight at a time (id "", NDI2 kind 5, body [0] request / [1] boundary / [2] closure; next_control :866-884); the target advertisement (core typed kind 2); the closure promises carried by every ordinary frame (promises :506-514) |
| K02 | ALWAYS FUNDED. Each owed control is funded when its obligation is created: receipts by R03's receipt future; the maintenance flight by the maintenance future held apart from ordinary flights; retained control bytes inside the constant session envelope (retained R plus remaining C - R always equals C, delivery:1346-1350). An owed control therefore never waits on ordinary BYTE or COUNT credit. It can still WAIT on three shared limits: the per-epoch send window of 16, which non-boundary controls share with the ordinary flights of that epoch (delivery:609-619); the 3 receipt contexts; and the single maintenance slot (deferral without mutation: control_before_receive :858-865). Each is released only by the peer's receipts and closures. F06 must show that no schedule turns these waits into a cycle, for example a window full of ordinary flights waiting on receipts that wait on this side's closure (V601 window-full arm; C4-O2) |
| K03 | ORDINARY WORK CANNOT TAKE CONTROL OR RECOVERY CREDIT (I05). Ordinary flights: sum(wire) + maintenance future + 65,536 <= 4 MiB (delivery:1126-1127); ordinary receives: raw + receipt future + 65,536 <= 4 MiB (:1126); generic writers: refused on owned keys and charged only against unpromised aggregate bytes (vault:2018-2030, :2016); timeline: only its own entry's credit (vault:2132); invitation records: their own R11 reservation. MEASURED as structure; the encoded sufficiency is F06's (V601, V606) |
| K04 | DEFERRAL IS NOT FAILURE. SEND_WINDOW, RECEIPT_CONTEXT_CAPACITY, TRANSACTION_CAPACITY and MAINTENANCE_CAPACITY defer a control without consuming a target, slot, promise or demand (delivery:855-865); a remote QueueFull leaves the committed response pending (transport:4156-4160); neither is a permanent invalidity (I07) |
| K05 | RETIREMENT CONDITIONS. Flight: its exact authenticated receipt (delivery:744). Disposition: the peer's closure promise covering its slot (:554-556). Receive epoch: a final closure (:558). Send epoch: a confirmed final carrier (:530). Epoch witness: only when its epoch leaves recv (protocol_state:1635-1644). Control ref: when its disposition is retired (protocol_state:1557-1569). Owner entry: projected AND closed AND zero charge (delivery:1267-1268). Session reserve: only K07 |
| K06 LR | EPOCH / GENERATION RETIREMENT AND KEY CLEANUP (I05, I08). directional_core owns epoch keys. The staged transition that removes an epoch from core.recv, ends core.send, or consumes a target zeroizes that epoch's ec, pq, hk, adv and every skipped key in the same transition; every staged clone that is dropped (Transaction::receive clones the whole state, delivery:717; *self = next, core:763) is zeroized on drop. TODAY: the removal at core:665 drops hk, adv, ec, pq unzeroized; :734-735 zeroize ec and pq only; Core and Epoch have no Drop (LocalTarget :171-175 and ReceiptContext delivery:24-28 do). Generations: owner, peer and control generations advance by exactly one per paired commit (vault:2088-2092, delivery:1270) and a stale generation refuses (vault:2075-2076); invitation generations per C02 AM-3. F05 implements; F06 relies |
| K07 | SESSION CLOSE (the unilateral escape, THE PLAN sec 6). An explicit close fails every outstanding flight and pending attempt honestly, stops new seals, and releases R02-R05 and any R12/R13 of that session only in the commit that records the close; a permanently absent peer otherwise leaves bounded protocol debt. The close removes the PeerReserve AND the peer secret in ONE paired commit (vault:1992, :2001-2003 refuse either alone at reopen) (F3). ABSENT at C (C4-O4) |
| K08 | QUIESCENCE. After input stops and the necessary traffic is delivered, next_control returns None on both sides within a bounded number of steps; receipts are never receipted (an NDR1 frame creates no disposition: receive_inner returns at :749). F06 proves it by reduced-state schedule exploration replayed through production callers (V607) |
| K09 | THE 36-CONTROL BOUND IS A HYPOTHESIS (THE PLAN Values). Its induction obligations: two receiving epochs, span 16, at most two useful advertisements per epoch, persistent witnesses, one maintenance flight, useful closure effects (THE PLAN sec 4). The 19th-ref refusal (protocol_state:1594) and the 36-slot array are admissible only once F06 proves no legitimate transition needs more; if one does, the bound is re-derived, never enforced by refusing a legitimate transition (THE PLAN F06) |

==============================================================================================================
T6. LIMITS  (DERIVED = arithmetic on stated inputs; MEASURED = source constant or instrument named; HYPOTHESIS = the F-card that must prove it)
==============================================================================================================
| # | Limit | Value | Status | Derivation / instrument / card |
|---|---|---|---|---|
| L01 | B_V vault aggregate | 16,777,216 (L2) | HYPOTHESIS (candidate ceiling to preserve during proof; never raised as a repair) | protocol_state:1410 "NOT an operating-limit decision"; THE PLAN Values |
| L02 | H_W global write headroom | 524,288 | HYPOTHESIS | :1411; F06 |
| L03 | per-session write headroom | 2 x 524,288 | HYPOTHESIS | delivery:1353; F06 |
| L04 | CORE_B | 288,920 (L1) | HYPOTHESIS | delivery:1283-1293 "conditional paper bound"; F06 V604 |
| L05 | CTRL_B | 36,775 (L1) | HYPOTHESIS | vault:2165-2166, delivery:1341; F06 V603 |
| L06 | per-session reserve | 1,699,966 | DERIVED (on HYPOTHESIS inputs L02-L05) (F3) | 2 x (L04 + L05) + L03 = 651,390 + 1,048,576 |
| L07 | left after 8 sessions | 2,653,200 | DERIVED (on HYPOTHESIS inputs L02-L05) (F3) | L01 - 8 x L06 - L02 = 16,777,216 - 13,599,728 - 524,288 (THE PLAN "about 2.65 MB"); before owner-entry widths, generic secrets, history and invitation records |
| L08 | sessions at zero ordinary material | 9 (953,234 B left), LIFETIME at C (no release exists, R02/K07; C4-O4) | DERIVED (on HYPOTHESIS inputs L02-L05) (F3) | floor((L01 - L02) / L06) |
| L09 | retained controls | 36 = 2 witnesses x 18 refs | MEASURED structure; HYPOTHESIS sufficiency | protocol_state:1467, :1529, :1594; F06 |
| L10 | maintenance future | 2,287 (L0) | HYPOTHESIS | delivery:1125 (literal, not derived in source); F06 |
| L11 | NDR1 | 113 | MEASURED | delivery:10-11 |
| L12 | MAX_WIRE | 65,536 | MEASURED (enforced) | core:18 |
| L13 | payload / padded size | 60,000 | MEASURED (enforced) | delivery:289, :296, :388 |
| L14 | per session: 64 application flights, 1 maintenance flight, 64 events, 64 completed, 3 receipt contexts, window 16, MAX_BYTES 4 MiB, MAX_RECORD 16 MiB | as stated | MEASURED (enforced); HYPOTHESIS sufficiency | delivery:173-175, :481-505, :617; F06 |
| L15 | MAX_SKIP 16, MAX_EPOCHS 2, MAX_TARGETS 1 | as stated | MEASURED (enforced on receive only, T3 V05) | core:15-17 |
| L16 | Q_S global queue slots | 64 | HYPOTHESIS | THE PLAN Values; MAX_QUEUE_LEN lib.rs:54 (M :62) bounds only `qsc util queue` (main.rs:891), never the persisted queue; F07 V701 |
| L17 | Q_B global queue bytes | 4,194,304 (L3, incl. TEMP_Q) | HYPOTHESIS | THE PLAN Values; F07 V702 (MAXPACK(60,000) decides how many maximum rows fit) |
| L18 | Q_P per-peer share | OPEN (< Q_S) | HYPOTHESIS | C4-O1; F07 V706 |
| L19 | QueuedIntent cap | 1,024 | MEASURED | delivery:327 |
| L20 | row overhead | +28 (L1 -> L3) | MEASURED | P4 (NA-0783 formalization); msgqueue:390-412 |
| L21 | JSON expansion | Vec<u8>: <= 4n+1 (L1); outer string <= 2 x L1 + 2 (L2) | DERIVED | serde_json number arrays; string escaping of '"' and '\' only |
| L22 | first-connection store (P28) | 64 records x 256 KiB = 16 MiB | MEASURED at P28 (P28 handshake/mod.rs:202-206); INFEASIBLE as is inside L01 beside even one session reserve | DERIVED: 16,777,216 + 1,699,966 > L01 (ESCALATION E2) |
| L23 | successor N_FC, N_INV, per-record bound | OPEN | HYPOTHESIS | R11; F10 V1001-V1004. Feasibility note (F1): a per-record bound of 32 KiB x N_FC 64 = 2 MiB would fit beside two sessions (L29, n = 2) |
| L24 | envelope maxima | A1 9,668; B1 11,729; ENV_MAX 12,288 | DERIVED in C02 T1b | C02 |
| L25 | H_REC, CLOCK_SLACK | 259,200 s; 3,600 s | PROPOSED in C03 (C3-O2, C3-O3) | C03 |
| L26 | file: chunk 16,384; <= 256 chunks; 1..4,194,304 B; 2 in-flight slots per job; 24 h horizon | as stated | HYPOTHESIS (C06/F14) | THE PLAN Values |
| L27 | history quota H_N, H_B | OPEN | HYPOTHESIS | R07; C4-O3; F07 V708 |
| L28 | owed_receipts | 256 per peer, TTL 604,800 s | MEASURED (legacy) | owed_receipts:50, :56; C01 O6 |
| L29 | ordinary-material L2 ceiling (F1) | B_V - H_W - n x L06 (n = 1: 14,552,962; n = 2: 12,852,996; n = 8: 2,653,200 = L07); one maximum wire <= 262,145 L2 (all-255) and about 234,000 for random ciphertext; ONE session's R04 L0 ceiling (64 wires) is 16,777,280 L2 > B_V, so R04's 4 MiB is unreachable in the vault and the aggregate is the binding ordinary limit | DERIVED | L01 - L02 - n x L06; L21 at n = 65,536 (4n+1); byte fields are JSON number arrays (delivery:217, :228, :236) inside one JSON string per secret (vault:95), passing 1:1 into L2, which check_aggregate measures (vault:2015-2016 -> protocol_state:1493-1498). Every retained ordinary Disposition, unprojected event body, OwnerEntry, the timeline history (R07) and every invitation record (R11) draw on the same ceiling; the encoding is C4-O7 |

==============================================================================================================
T7. CROSS-CONTRACT CELLS
==============================================================================================================
| # | Cell | Answer here (PROPOSED) | State |
|---|---|---|---|
| X01 | C02 T8 OC5: count and byte quotas of first-connection records (P28 64 x 256 KiB candidate) | R11: records live in the successor vault and are charged against B_V; per-pair counts from C02 T3/T4; a reservation of each record's maximum retained set commits WITH the record (redeemer at C03 L1 before any network; responder at C02 step 10); global N_FC and the per-record bound are HYPOTHESIS for F10; the P28 candidate as is cannot fit (L22, E2). At step 10 a full quota DEFERS the authenticated A1 (I07; THE PLAN sec 3 table) (E6) | CLOSED IN DIRECTION; values OPEN (C4-O5) |
| X02 | C03 T9 OC3: "client byte/count quotas stay C04 (C02 OC5)" | = X01 plus the inviter's InviteRecord and the redeemer's operation record, RESULT bytes, receipt and exact A1; released at each side's horizon (C03 T6, AM-2, AM-16) | CLOSED IN DIRECTION; values OPEN (C4-O5) |
| X03 | C03 T1 row 10: push op_id "16 bytes CSPRNG per queued item, fixed when the item is committed (C04/C05 own the queue row)" | the op_id of EVERY v2 push is minted in the paired commit that fixes the pushed bytes and is stored with them: application and maintenance flights in the Flight (PREPARED commit), receipts in the Disposition (receive commit), handshake replies (B1, A2) in the invitation candidate (C02 R6 / step 10 commit), which has neither a row nor a Flight (F7); the queue row carries a copy for application sends. Receipts and controls have no queue row (Q09), so "per queued item" does not reach them (E1). Byte effect: + 16 B raw per Flight and Disposition, inside S04/S05 (F06) | RULED (E1 + X7): C03 AMENDMENTS AM-17, THREE holders; C4-O6 closed |
| X04 | C01 O5 "C04 owner schema" | C04 fixes the owner schema DIMENSIONS (T1 R01-R13) and the owner of each; key strings and the contact-id format stay C01 O5 | O5 stays OPEN |
| X05 | C01 row 15: duplicate keys refused at every map level (F04) | T3 V-R4, V01, V406 | carried |
| X06 | C01 row 19 / A17 queue family | T4 applies to msgqueue_v2; record version 2 checked at reopen (T3 V07); the byte-field encoding of v2 is C4-O7 | carried |
| X07 | C01 O13 / A24 reserved C07 fields | C04 adds no VaultPayload top-level field; when F02 allocates the lineage fields their bytes are actual bytes under R01 and must keep every committed state inside INV | OPEN with C07 (C4-O12) |
| X08 | C01 O8 location rule | every store and temporary C04 names is inside the successor directory; nothing is charged to or written in the old directory | carried |
| X09 | C01 T2 G (owed_receipts, dedup silent resets) | T3 V09 refuses; disposition of the stores stays C01 O6 | O6 OPEN |
| X10 | C02 T3 "Remembered" | R11 per-pair counts | carried |
| X11 | C05 (ACK dispositions, finite budgets, fairness, history policy, DTO) | Q08, R07, K04 name the dependencies | OPEN (C4-O3, C4-O9, C4-O10) |
| X12 | C06 (file bytes, combined resource target) | R12/R13 dimensions and owners only | OPEN (C4-O13) |

==============================================================================================================
T8. VECTORS AND INSTRUMENTS SPECIFIED (NOT GENERATED; NOTHING RUN)
==============================================================================================================
BASE (SR-19(a) "compiles and runs at base"): a vector's base is the base of its delta symbol. A symbol that exists at C
(#1831 ffc8fc52) has base = the F03 integration head that carries it (named in the F-card directive); the vector is RED
there wherever the expected behaviour changes. A symbol that exists nowhere yet has base = the F-card commit that
introduces it with today's behaviour (no refusal it does not already make), so the vector compiles and runs RED there and
GREEN on the change. Every refusal vector also asserts: prior committed bytes unchanged (vault, owner, peer, queue tree
listing and bytes), no wire, no receipt, no ACK, no attempt-guard count (C01 O7). SR-19 delta symbols (existing at C
unless marked NEW): LVR = vault::load_vault_runtime_with_passphrase; RDR = vault::read_directional_runtime; CDA =
vault::check_directional_aggregate; CAG = protocol_state::CapacityOwner::check_aggregate; TXD = directional_delivery::
Transaction::decode; DSP = the serde attribute on Disposition.response_pending (delivery:229); CRV = directional_
delivery::Transaction::check_reserved_cost; RSF = Transaction::reservation_futures; CRE = Transaction::control_retained_
encoded; CCF = Transaction::core_context_future; NXC = Transaction::next_control; RTC = SessionControlReserve::retain_
control; CPR = vault::create_directional_pair; EPA = msgqueue::enqueue_padded_at; RDRC = msgqueue::read_record; QMS =
the serde attributes of msgqueue::QueuedMessage; MPK = msgqueue::QueuedMessage::mark_packed; RTP = msgqueue::
retire_packed; RMC = transport::RelayMessageSender::classify; RMM = transport::RelayMessageSender::commit (F2);
WAT = fs_store::write_atomic; CRX = directional_core::Core::receive; NEW: MXP = the MAXPACK calculator
(F07), QSW = the stale-temporary sweep (F07), HQ = the history-quota check (F07), SCL = the session-close transition
(F06/F16), CDV = Core decode validation (F04), DUP = nested duplicate-key detectors (F04), FCQ = the R11 reservation
(F10).
| Id | Card | Rule | Discriminating case | Expected | Delta | Base |
|---|---|---|---|---|---|---|
| V401 | F04 | V03 | vault file of B_V + 69 + 1 bytes opened through secret_get (the first read of directional_update_inner) | refused before allocation (the read cap), nothing written | LVR | F03 head (RED: fs::read unbounded, vault:1049) |
| V402 | F04 | V-R1 / I06 | committed state built with actual + promised + H_W = B_V exactly; reopen; then one ordinary byte more | reopen OK (maximum-valid state reopenable); the +1 write refused directional_aggregate_waiting before effects, prior bytes kept | CDA, CAG | F03 head |
| V403 | F04 | V04 | an encoded Transaction whose one Disposition lacks response_pending; control: the unmodified string | TRANSACTION_TAMPERED; control decodes | DSP | F03 head (RED: defaults to true, formalization P5 measured the decode arm) |
| V404 | F04 | V07 | a v2 row lacking directional_intent; lacking ciphertext while its Flight is sealed | refused (row version/shape code, O9) | QMS | F03 head (RED: serde(default)) |
| V405 | F04 | V07 | a row whose record version is 1 or 3 | refused | RDRC | F03 head (RED: never checked) |
| V406 | F04 | V-R4 | a duplicated key in CapacityOwner.peers, .entries, Transaction.flights, .dispositions, Core.recv | refused at reopen | DUP (NEW) | F04 commit introducing DUP (RED at C by library source: serde_core 1.0.228 map_impl is last-wins, F5) |
| V407 | F04 | V02 | peer.generation != transaction.generation; entry.sid != peer.sid; non-canonical ticket | directional_stale_generation / directional_owner_binding (pin of existing checks) | CDA | F03 head (GREEN pin) |
| V408 | F04 | V05 | a persisted Core with 3 recv epochs; with 17 skipped keys; with an unknown field | refused at reopen | CDV (NEW) | F04 commit introducing CDV |
| V409 | F04 | V-R2 | truncation of the vault, the owner JSON, a peer Transaction and a row at every field boundary | refused; bytes unchanged; guard not counted | TXD, RDRC | F03 head |
| V410 | F04 | V07 | a row file of MAXPACK_MAX + 1 bytes (F9) | refused before allocation | RDRC | F03 head (RED: fs::read unbounded, msgqueue:450) |
| V411 | F04 | V09 | an unparseable owed_receipts secret | refused, bytes kept | owed_receipts::load | F03 head (RED: reset to empty, owed_receipts:85) -- if C01 O6 keeps the store |
| V412 | F04 | V08 | a timeline entry lacking state | refused | timeline_store_load | F03 head (RED: serde(default)) |
| V413 | F10 | V10, R11 | a terminal first-connection record re-opened | no secret field present; public replay data only until the horizon | FCQ (NEW) | F10 commit introducing FCQ |
| V501 | F05 | K06 | receive the terminal frame of an epoch (core:663-665 path) and a boundary retiring the previous epoch (:733-737); inspect the retired epoch's key buffers via a non-shipping zeroize observer | every retired key zeroized; staged clones zeroized on drop | CRX | F03 head (RED by reading) |
| V601 | F06 | K02, K03 | two peers; ordinary saturation (64 application flights to peer A, or the aggregate filled by ordinary material to the T6 L29 ceiling first: at n = 2 about 49 to 55 maximum wires across both peers, F1); then an owed receipt and a maintenance closure for peer B. Window-full arm: 16 unreceipted ordinary flights in peer A's current epoch, then A's closure carrier is owed | both prepared and committed; ordinary sends refused (APPLICATION_CAPACITY / directional_aggregate_waiting); window arm: the carrier defers without mutation and commits once A's receipts arrive, with no cycle | CRV, RSF, NXC | F03 head |
| V602 | F06 | K09 | reduced-state schedule exploration over two epochs, span 16, adverts, lost/duplicate receipts; every schedule reaching a 19th ref in one epoch replayed through production callers | no legitimate schedule is refused; if one is, the bound is re-derived (card fails) | RTC | F03 head |
| V603 | F06 | S09, L05 | the retained control set at maximum (36 refs, maximum-width scalars, maintenance flight at maximum wire, all send holes) serialized by the production serializer | <= CTRL_B, else CTRL_B is disproved | CRE | F03 head |
| V604 | F06 | S07, L04 | Core at maximum (3 contexts, 16 skipped keys, targets, last_out 65,536 of 0xFF, maximum-width counters) | core_context_future >= 0, else CORE_B is disproved | CCF | F03 head |
| V605 | F06 | R02 | session creation with the aggregate 1 byte short of one session reserve | refused before completion is announced; no peer state, no owner change | CPR | F03 head |
| V606 | F06 | K03 | fill the aggregate with an unrelated generic secret to the limit; then an owed control | the generic write refused at the limit; the control commits | CDA | F03 head |
| V607 | F06 | K08 | two peers exchange N messages with crossed sends, then stop; drive both | next_control None on both within a bounded step count; no receipt of a receipt | NXC | F03 head |
| V608 | F06 | K05 | delayed old-epoch frames; duplicate frames; a lost receipt then its exact replay | duplicates get the saved receipt; no new disposition; old epochs retire | receive_inner | F03 head |
| V609 | F06 | V-R1 | after each of V601-V608, reopen | reopens; INV holds | CDA | F03 head |
| V610 | F06 | K07 | explicit session close with outstanding flights, dispositions and projections | outstanding work failed honestly; R02-R05 released in the close commit; nothing released earlier | SCL (NEW) | F06/F16 commit introducing SCL |
| V701 | F07 | Q02, Q03 | Q_S QUEUED rows alternating two contacts; the next to a THIRD contact | msgqueue_queue_full; store tree byte-identical, no new directory (the formalization's red test, sha256 44f89185722e56f72a1f2a9c45e3ee635d3c57e905c74c44dd469076c6edd554, RED at C) | EPA | F03 head |
| V702 | F07 | Q02 | rows at padding 60,000 until the next would exceed Q_B under MAXPACK | msgqueue_queue_bytes_full before slot Q_S, only when held + MAXPACK + TEMP_Q > Q_B; tree byte-identical | EPA, MXP | F07 commit introducing MXP |
| V703 | F07 | Q02 | pack a maximum row (padding 60,000, 0xFF body, boundary wire) | on-disk row <= its admitted MAXPACK | MPK, MXP | F07 commit introducing MXP |
| V704 | F07 | Q05, R10 | process cut after the row temporary is written and before rename; restart | the old row intact; the stale temporary removed under the lock before admission (or charged); no over-admission | WAT, QSW | F07 commit introducing QSW |
| V705 | F07 | Q04, Q06 | 200 sequential sends to one peer with receipts applied | every send admitted; P never exceeds Q_S; rows on disk <= Q_S + 64 tombstones | EPA | F03 head (RED: rows never retired) |
| V706 | F07 | Q08 | peer A silent (no receipts) holding Q_P rows; peer B sends | peer B admitted | EPA | F03 head |
| V707 | F07 | Q09, K02 | P full; the peer's receipts and a maintenance closure are owed | receive still pushes the receipts and the closure | NXC, pending | F03 head |
| V708 | F07 | R07, Q03 | history at H_N; a new send | history_full before any row, directory or temporary | HQ (NEW) | F07 commit introducing HQ |
| V709 | F07 | Q01, Q06 | a sealed row whose push is answered 413 (FAIL) and one answered with the permanent class | the row stays PREPARED and retriable with the same bytes; the Flight is NOT marked accepted; nothing terminal; no timeline entry and no Sent state; the next receive for that peer succeeds and pushes its controls; arm: the contact's device revoked while the packed row is in a Retry class -> routed to K07's session close (C05 meaning), never a terminal row (F2) | RTP, RMC, RMM | F03 head (RED by reading: retire_packed -> commit -> accepted; timeline Sent; transport:545 abort) |
| V710 | F07 | Q06 | a DELIVERED row whose Flight retired; a SENT row whose Flight is live | the first removed; the second kept | EPA, retirement (NEW) | F07 commit introducing retirement |
| V711 | F07 | Q07, X03 | cut between the PREPARED vault commit and the row save; restart; push | the same wire and the same op_id as the Flight's | EPA, prepare_padded | F03 head (op_id: the F08-client commit introducing it) |
| V712 | F07 | Q11 | the relay advertises v2.max_body_bytes 20,000; a QUEUED row at padding 60,000 | PREPARED refused before sealing; the row stays QUEUED with the visible reason; no Flight | the PREPARED admission check (NEW) | the F07 commit introducing it |
| V713 | F07 | Q07 (F8) | delete the row of a PREPARED flight (fixture); restart; receive its receipt | Flight retired, completed drained, row reconstructed or its absence tolerated; no COMPLETION_CAPACITY | EPA, prepare_padded, Transaction::project | the F07 commit introducing the reconstruction |
| V1001 | F10 | R11 | a new redeem with N_FC live records | refused before any network or durable write (no relay claim, no burned invite) | FCQ (NEW) | F10 commit introducing FCQ |
| V1002 | F10 | R11, E6 | an authenticated A1 at the inviter with the quota full | DEFER: retained un-ACKed, no write; processed when a record retires | FCQ | F10 commit |
| V1003 | F10 | R11 | two concurrent redeems racing for the last unit | exactly one reserves (the reservation commits under the lock before the network); the other refuses before network | FCQ | F10 commit |
| V1004 | F10 | R11 | records at each side's horizon | secrets zeroized at terminal; public data and reservation released at the horizon; record removed | FCQ | F10 commit |

==============================================================================================================
T9. OPEN CELLS (each with the cards it blocks). LR = LOCK-REGION (SR-15 sensitive)
==============================================================================================================
| Id | Question | Depends on | Blocks |
|---|---|---|---|
| C4-O1 | Values Q_S, Q_B, Q_P (HYPOTHESIS 64, 4 MiB, OPEN) and whether MAXPACK(60,000) leaves a useful count of maximum rows | F07 measurement | F07 |
| C4-O2 LR | CORE_B, CTRL_B, the maintenance future, the 36-control bound, H_W and the 2 x H_W per-session headroom | F06 (serializer calculator + schedule exploration) | F06, F07, F13 acceptance |
| C4-O3 | History quota H_N/H_B and the user-visible retirement policy | C05 (history policy) | F07, F13 |
| C4-O4 LR | The session-close transition that releases R02-R05 (absent at C) | F06/F16 design; C05 for its user meaning | F06, F16 |
| C4-O5 LR | N_FC, N_INV and the per-record byte bound of R11; P28's 64 x 256 KiB cannot be kept (E2). RULED (E2): the P28 quota is NOT carried; R11 sits inside B_V; N_FC and N_INV HYPOTHESIS until F10 measures | F10 measurement | F10 |
| C4-O6 | op_id ownership for every v2 push (Flight / Disposition / invitation candidate), a C03 T1 row 10 amendment (E1). CLOSED: RULED (E1 + X7), C03 AMENDMENTS AM-17 | Director: RULED | F08 (client), F10, F11 |
| C4-O7 | Byte-field encoding of the v2 queue row AND of the vault's Transaction and Owner byte fields (Flight.wire, Disposition.receipt, Application.body, every Key): JSON number arrays today (<= 4n+1 L1 per n bytes, passing 1:1 into L2); base64 would cut the expansion from 4x to 4/3x. A persisted-format decision, C01 A13 / F04 territory; OPEN; the encoding decision is NOT made here (T6 L29) (F1) | Director / F04 / F07 | F04, F06, F07 (MAXPACK) |
| C4-O8 LR | The disk budget: vault peak 2 x (B_V + 69), Q_B, stale temporaries, received outputs; joint with the C06 combined resource target | C06 | F07, F14 |
| C4-O9 | The peer's relay mailbox depth is shared by ordinary and control frames (remote capacity; one mailbox per peer is C03 C3-O13) | C05, C03 | F11 |
| C4-O10 | Finite per-peer drain and fairness budgets | C05 | F11 |
| C4-O11 | serde behaviour on duplicate keys inside nested BTreeMaps: MEASURED (library source): serde_core 1.0.228 de/impls.rs:1542-1543 inserts every entry, last wins, no error; version locked by ffc8fc52:Cargo.lock (F5). CLOSED; only F04's detector (DUP, V406) remains | -- | F04 |
| C4-O12 | Bytes of the C07 lineage fields inside R01 | C07 / F02 | F02, F04, F05 |
| C4-O13 | File reservation values (R12, R13) | C06 | F14 |
| C4-O14 | Disposition of owed_receipts, relay dedup and quarantine on the successor path | C01 O6 | F04 |
| C4-O15 | Whether INV keeps the per-commit margins (H_W, 65,536, 2 x H_W) as today (PROPOSED keep, T3 V-R1) (E4). RULED (E4): KEEP; V402 the proof | Director: RULED | F04 |
| C4-O16 | Whether a persisted queue counter may replace the bounded scan (a second source of truth) | Director / F07 | F07 |
| C4-O17 | The relay body floor vs MAX_WIRE: raise the C03 C1 floor to 65,536, or keep 12,288 and rely on Q11's refusal before sealing (E7). CLOSED: RULED (E7) BOTH -- C03 AMENDMENTS AM-18 raises the floor to 65,536 AND Q11 stays the seal-time guard | Director: RULED | F07, F08 (client), F11 |
LOCK-REGION rows (the SR-15 read's focus): T1 R01-R05, R08-R11; T3 V-R1 with V03 (the unbounded first read, under the
lock at protocol_state:1315 on the directional_update_inner path; lockless at timeline:375, owed_receipts:76,
transport:4187, msgqueue:641) (F4); T4 Q03, Q05; T5 K06; T9 C4-O2, C4-O4, C4-O5, C4-O8.
SR-18 CENSUS LINE (F6): F07 (queue), F04 (readers) and F06 (session close) remap observables and each owes the census
at drafting: the documented "enqueue never refuses" invariant O1 and its markers (msgqueue:46-49
QUEUE_WARN_THRESHOLD; transport:1109-1131 msgqueue_enqueued and msgqueue_backlog_warning); the MsgState vocabulary
(FAILED reachable from a packed row today, msgqueue:1012-1019; DELIVERED set at msgqueue:2112); the NEW codes
msgqueue_queue_full, msgqueue_queue_bytes_full, history_full and Q11's; `qsc util queue`'s MAX_QUEUE_LEN (lib.rs:54;
main.rs:891) if it is retired or re-pointed; and the delivery-state marker "accepted_by_relay" (transport:3720-3724),
whose meaning Q01 fixes.

==============================================================================================================
ESCALATIONS RAISED WITH THE DRAFT (named; each RULED by RULING_NA0783_C04_ACCEPT_2026-09-24, clause appended)
==============================================================================================================
| Id | Conflict | Between | Draft's position (a proposal only) |
|---|---|---|---|
| E1 | C03 T1 row 10 fixes a push op_id "per queued item ... (C04/C05 own the queue row)"; receipts and controls are v2 pushes with no queue row (Q09), and the exact bytes are fixed by the vault Flight/Disposition, not by the row | C03 / tree C | X03: the op_id lives with the record that fixes the bytes; a C03 amendment (C4-O6). RULED (E1 + X7): a C03 amendment -- op_id holders are THREE: Flight (PREPARED commit), Disposition (receive commit), invitation candidate (C02 R6 / step 10 commit); the queue row carries a copy for application sends (C03 AMENDMENTS AM-17) |
| E2 | P28's first-connection store (64 x 256 KiB = 16 MiB, P28 handshake/mod.rs:202-206), named by C02 OC5 as the candidate quota, equals the whole vault aggregate B_V of THE PLAN Values; inside one vault it cannot coexist with even one session reserve | tree P28 / THE PLAN Values / C02 OC5 | X01: charge R11 inside B_V with a per-record bound from the successor envelope maxima; values HYPOTHESIS (F10). RULED (E2): the P28 quota (64 x 256 KiB) is NOT carried; R11 sits inside B_V; N_FC and N_INV HYPOTHESIS until F10 measures |
| E3 | At C a relay-refused push of a SEALED flight (FAIL / FAIL_PERMANENT) runs retire_packed -> MessageSender::commit -> Transaction::accepted, recording relay acceptance that did not happen, and makes the row terminal FAILED while the Flight stays live and is replayed on every receive | tree C / THE PLAN D07, D08, sec 4 ("never delete a live exact-retry obligation"), I04 | T4 Q01, Q06: after PREPARED only DELIVERED or session close is terminal; F07 changes retire_packed for directional rows (V709). Source reading, NOT RUN. RULED (E3 + X2): MAJOR. Reachable (a) on any relay whose max_body_bytes is below the sealed wire (not the rig's configured 65,536, not the qsl-server source default 1,048,576), (b) LOCALLY when a contact's device is revoked while a packed row retries (transport:3657-3659), no relay needed. Consequences: a false Sent in the timeline, the Flight replayed on every receive, the receive aborted on a repeated 413, an undiscardable row. Filed as ENG-0357; repair named for F07 (RelayMessageSender::classify and commit, besides retire_packed: a 413 on a sealed row stays PREPARED; device_revoked after seal routes to the K07 session close); NOT repaired here (F2) |
| E4 | THE PLAN sec 4: "Validate stored state at its allowed limit without incorrectly charging another new-admission margin merely to reopen it"; C re-runs the full admission predicate at every decrypt (check_reserved_cost bounds(65536); the aggregate with H_W) | THE PLAN / tree C | T3 V-R1: the margins are part of INV because every writer establishes them, so reopen charges no ANOTHER margin; the Director rules the reading (C4-O15); V402 decides. RULED (E4): C4-O15 KEEP |
| E5 | THE PLAN Values: "64 queue slots / 4-MiB queue ... Candidate limits to freeze with exact accounting in C04/C06"; a design-only seat can fix the accounting (MAXPACK, T4 Q02) but cannot measure it, and THE PLAN sec 4 keeps 64/4 MiB "only if this exact accounting proves them feasible" | THE PLAN Values / the directive ("limits derived or explicitly still hypotheses") | T6 L16, L17: the formula is fixed here, the values stay HYPOTHESIS for F07; nothing is presented as frozen. RULED (E5): accepted |
| E6 | C02 T2 step 10 (occupancy) has no arm for a full first-connection quota; I07 and THE PLAN sec 3 table ("valid authenticated new work awaiting local admission capacity: defer") require a deferral, not a refusal that would burn the invite | C02 / THE PLAN I07 | X01, V1002: DEFER, retained un-ACKed, no write; a C02 amendment names the code and the DEFER arm (the mechanics stay C05's). RULED (E6): a C02 amendment -- R11 quota full at step 10 -> DEFER (the AM-3 disposition), processed when a record retires (V1002); code spelling via DOC-SCL-002 as C01 O9 (C02 AMENDMENTS AM-10) |
| E7 | C03 T3 C1 with AM-6 accepts a relay whose v2.max_body_bytes is anything >= 12,288 (the B1 envelope floor); an ordinary NDE1 wire may be 65,536 bytes (core:18). On such a relay a padded message is SEALED (PREPARED, committed protocol debt, D08) and only then refused with 413, and by T4 Q01 it can leave PREPARED only by session close | C03 / tree C / THE PLAN D08 | T4 Q11: refuse before sealing a wire longer than the advertised max_body_bytes; or the Director raises the C1 floor to 65,536 (C4-O17). The deployed rig's configured 65,536 equals MAX_WIRE, so the rig is not affected (C03 E1). RULED (E7): BOTH -- a C03 amendment raising the T3 C1 floor to v2.max_body_bytes >= MAX_WIRE (65,536) (C03 AMENDMENTS AM-18) AND Q11 kept as the seal-time guard; a 413 after sealing leaves the row PREPARED; C4-O17 closes |

END OF C04 FINAL

==============================================================================================================
AMENDMENTS (appended by D-1433; the text above is C04 FINAL as merged by D-1431 and is NOT rewritten)
==============================================================================================================
Ruled by RULING_NA0783_C05_ACCEPT_2026-09-24 (sha256 c135b67538405af40cab8410c3970b2bbf97d7e8b7dd6757dcb2aaa916ae02ea) at the C05
acceptance (docs/ops/contracts/C05_dispatcher_and_gui.md), on the operator's answer Q5 (banked verbatim in
RBANK_C05_operator_answers_2026-09-24, sha256 6e5e8bd43ae8f3f83ff207b87a5e660da3930c7fdeabde2fe67f75baa6bd219c, and its
addendum A1, sha256 848a1c9ebda18c6beb8a8db83c286825f40687a4049201fa440072886625b256): the ruling's words, "PLUS a per-peer
history share H_P (a C04 amendment, value HYPOTHESIS) so one contact cannot fill the global quota R07". Each amendment
names its source and the cell it amends; where an amendment and the text above disagree, the amendment governs.
PROPOSED keeps the meaning the FIXES section gives it.
| Id | Amends | Amendment |
|---|---|---|
| AM-1 (C05 Q5; per-peer history share) | T1 R07; T4 Q03; T6 L27; T9 C4-O3 | R07 gains a per-peer share H_P: the history units of ONE conversation never exceed H_P, with H_P < H_N (Q08's Q_P < Q_S, applied to history), so one contact -- a flooding peer included -- cannot fill the global quota. An arrival that would exceed that peer's share is DEFER-C (C05 T2 reason C3: un-ACKed, no receipt), as for the global quota; a submit that would exceed it refuses history_full before any row, directory or temporary (Q03's order, the history unit checked against R07 and H_P). Value HYPOTHESIS: L27 gains H_P (C4-O3; C05 C5-O7), measured with H_N and H_B at F07 |
| AM-2 (C05 Q5; retention interplay) | T1 R07 (the release column); T4 Q06; T9 C4-O3 | The user-visible history retirement R07 names is fixed in policy by C05 T6 HI4 (Q5): a user setting "keep messages for" -- 1 week / 1 month / 1 year / Forever, default 1 month -- with expired history deleted automatically; that expiry IS a user-visible history retirement and frees R07 and H_P units. An entry whose message is still undelivered (C05 M1-M3) or closed_undelivered (M6) is NEVER retired by expiry, so expiry never removes the user's view of a live obligation and never touches a queue row or a Flight (Q06: a row whose Flight is live is never removed; history is R07, not a queue row). A protocol retirement still never frees history (THE PLAN sec 4). What deleting a conversation does to its still-pending messages is OPEN for F13 (the operator; C05 C5-O20). C4-O3's policy half closes; its values stay OPEN (C05 C5-O7) |

END OF C04 AMENDMENTS
