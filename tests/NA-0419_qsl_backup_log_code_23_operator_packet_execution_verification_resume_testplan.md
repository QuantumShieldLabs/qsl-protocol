Status: Supporting
Owner: QSL governance
Last-Updated: 2026-06-05

# NA-0419 QSL Backup Log Code 23 Operator Packet Execution Verification Resume Testplan

Goals: G4

## Objective

Validate that NA-0419 accepts the NA-0418 operator packet apply/verify markers
only when packet-local result files and live read-only state support them, then
classifies the scheduled-backup residual and selects the exact NA-0420
successor without running backup, restore, sudo, qwork, qstart, qresume, or
generated packet scripts.

## Protected Invariants

- qwork proof files are read and verified without rerunning qwork.
- Queue starts with exactly one READY item: NA-0419.
- D-0825 exists once; D-0826 is absent at start.
- Operator markers are not sufficient without live support.
- qsl-backup remains unchanged.
- Rollback evidence is preserved.
- Codex does not mutate the rollback subtree.
- Codex does not run sudo, backup, restore, or generated packet scripts.
- Backup status and backup plan files remain read-only.
- Same-host continuity caveats remain explicit.
- No public overclaim is introduced.

## Allowed Scope

Allowed qsl-protocol mutation paths:

- `docs/governance/evidence/NA-0419_qsl_backup_log_code_23_operator_packet_execution_verification_resume.md`
- `tests/NA-0419_qsl_backup_log_code_23_operator_packet_execution_verification_resume_testplan.md`
- `DECISIONS.md`
- `TRACEABILITY.md`
- `docs/ops/ROLLING_OPERATIONS_JOURNAL.md`

Allowed local proof output root:

- `/srv/qbuild/tmp/NA0419_operator_packet_verification_<timestamp>/`

Allowed read-only local paths:

- qwork proof files under `/srv/qbuild/work/NA-0419/.qwork/`
- the NA-0418 packet and `operator_result` directories
- the NA-0407 rollback directory
- `/backup/qsl/logs`
- `/backup/qsl/manifests`
- `/usr/local/sbin/qsl-backup`
- `/home/victor/work/qsl/codex/ops/backup/QSL_BACKUP_STATUS.md`
- `/home/victor/work/qsl/codex/QSL_BACKUP_PLAN.md`

## Forbidden Scope

- Running qwork, qstart, or qresume.
- Running sudo.
- Running generated packet scripts.
- Running backup.
- Running restore.
- Mutating `/usr/local/sbin/qsl-backup`.
- Mutating `/backup/qsl`.
- Mutating the NA-0407 rollback subtree.
- Mutating backup status or backup plan files.
- Mutating systemd units, timers, fstab, source lists, retention, or backup
  scripts.
- Mutating qwork/qstart/qresume/qshell.
- Mutating runtime, crypto, dependency, workflow, qsl-server, qsl-attachments,
  qshield runtime, website, public docs, README, or START_HERE.
- Creating durable Director State Index output.
- Creating public technical paper content or public readiness claims.

## qwork Proof Gate

Required checks:

- `.kv` proof exists and has `startup_result=OK`, lane `NA-0419`, repo
  `qsl-protocol`, clean-state fields, READY_COUNT `1`, queue top READY
  `NA-0419`, and requested lane status `READY`.
- JSON proof parses and mirrors the required `.kv` fields.
- live `HEAD` and `origin/main` match the proof after fetch.
- PR #1106 is MERGED with merge commit `5cc99eaa4a0b`.

Stop if any proof is missing, stale, malformed, or inconsistent.

## Queue and Decision Gate

Required checks:

- `python3 scripts/ci/qsl_evidence_helper.py queue`
- `python3 scripts/ci/qsl_evidence_helper.py decisions`
- direct count proof for D-0824, D-0825, D-0826

Acceptance:

- READY_COUNT `1`;
- READY NA-0419;
- NA-0418 DONE;
- latest decision D-0825;
- D-0824 once;
- D-0825 once;
- D-0826 absent at start;
- duplicate decision count zero.

## Operator Marker and Result Gate

Required markers:

- `NA0418_OPERATOR_PACKET_APPLY_OK`
- `NA0418_OPERATOR_PACKET_VERIFY_OK`
- `NA0418_OPERATOR_PACKET_NO_BACKUP_EXECUTED`
- `NA0418_OPERATOR_PACKET_NO_RESTORE_EXECUTED`
- `NA0418_OPERATOR_PACKET_QSL_BACKUP_UNCHANGED`
- `NA0418_OPERATOR_PACKET_ROLLBACK_EVIDENCE_PRESERVED`

Acceptance:

- all markers are present in prior directive history;
- `operator_result` exists;
- pre-action metadata records rollback directory `root:root` mode `2700`;
- post-action metadata records rollback directory `root:root` mode `2755`;
- pre/post qsl-backup checksums match;
- rollback checksum evidence exists;
- rollback file content is not printed;
- reviewed output has no secret-looking content.

## Live State Gate

Required checks:

- stat rollback directory;
- stat expected rollback file if readable without privilege;
- checksum expected rollback file if readable without privilege;
- verify qsl-backup checksum still matches expected value;
- verify exact Codex ops source inclusion count remains `1`;
- verify no packet script, backup, or restore process is running;
- verify no newer log/manifest was generated by Codex during evidence
  collection.

Acceptance:

- rollback directory is `root:root` mode `2755`;
- readable rollback file checksum matches operator-result checksum;
- qsl-backup is unchanged;
- Codex ops source inclusion count is exactly `1`;
- zero process matches are accepted as valid zero-match proof.

## Latest Scheduled Log/Manifest Gate

Required checks:

- identify latest scheduled log;
- identify latest scheduled manifest;
- compare log/manifest mtimes to operator action time;
- count latest log occurrences of rsync error, code 23, Permission denied,
  NA0407, rollback, and the exact rollback directory reference;
- count latest manifest Codex ops source entries.

Classification rules:

- no newer scheduled backup exists after operator action:
  `CODE23_REMEDIATION_APPLIED_PENDING_SCHEDULED_BACKUP_PROOF`;
- newer scheduled backup exists and code 23 cleared:
  `CODE23_REMEDIATION_VERIFIED_CLEAN_SCHEDULED_LOG`;
- newer scheduled backup exists and code 23 persists with same path:
  `CODE23_REMEDIATION_APPLIED_BUT_WARNING_PERSISTS`;
- newer scheduled backup exists and new failure appears:
  `CODE23_REMEDIATION_NEW_BACKUP_FAILURE_FOUND`.

## Status/Plan Gate

Inspect read-only:

- `/home/victor/work/qsl/codex/ops/backup/QSL_BACKUP_STATUS.md`
- `/home/victor/work/qsl/codex/QSL_BACKUP_PLAN.md`

Acceptance:

- if scheduled proof is pending, no status/plan mutation is made;
- if clean scheduled proof exists, a future status/plan refresh may be
  recommended only in a later exact-scope lane;
- if warning persists, caveats remain preserved.

## Validation Bundle

Required commands before PR:

```bash
git diff --check
python3 scripts/ci/qsl_evidence_helper.py queue
python3 scripts/ci/qsl_evidence_helper.py decisions
cargo audit --deny warnings
cargo tree -i rustls-webpki --locked
cargo tree -i pqcrypto-mlkem --locked || true
cargo tree -i pqcrypto-traits --locked || true
cargo tree -i pqcrypto-internals --locked || true
cargo fmt --check
cargo +stable test -p qsc --locked --test send_commit -- --test-threads=1
python3 formal/model_qsc_handshake_suite_id_bounded.py
python3 formal/run_model_checks.py
```

Additional local gates:

- scope guard confirms only the five allowed qsl-protocol paths changed;
- link-check reports no missing links;
- leak scan reports no secret findings;
- overclaim scan reports no positive public overclaim;
- PR body preflight and goal-lint pass.

## Expected Result

D-0826 records accepted operator-packet execution verification with live support,
classification
`CODE23_REMEDIATION_APPLIED_PENDING_SCHEDULED_BACKUP_PROOF`, and selected
successor
`NA-0420 -- QSL Backup Log Code 23 Post-Remediation Scheduled Backup Verification Plan`.
