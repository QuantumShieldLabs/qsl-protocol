Status: Supporting
Owner: QSL governance
Last-Updated: 2026-06-21

# NA-0516 Build-to-Inspiron remote qsc client-to-client E2EE implementation testplan

## Purpose

Validate the NA-0516 implementation attempt, including D409 residue cleanup, hardened path handling, retained qsc rechecks, local qsc provenance, isolated runtime setup, transport failure classification, cleanup proof, and release-claim boundaries.

## Scope guard

Allowed changed paths:

- `docs/governance/evidence/NA-0516_qsl_build_to_inspiron_remote_qsc_client_to_client_e2ee_implementation_harness.md`
- `tests/NA-0516_qsl_build_to_inspiron_remote_qsc_client_to_client_e2ee_implementation_testplan.md`
- `DECISIONS.md`
- `TRACEABILITY.md`
- `docs/ops/ROLLING_OPERATIONS_JOURNAL.md`

No qsc source/test/fuzz/Cargo, workflow/script/helper/dependency, corpus/vector/input, formal/refimpl/service/public/backup, qsl-server, qsl-attachments, qshield, or qshield-cli path may change.

## Required proof checks

- qwork proof files exist, are not regenerated by Codex, and match live pre-fetch HEAD/origin-main.
- READY_COUNT is 1 and READY is NA-0516 at startup.
- NA-0514 and NA-0515 are DONE.
- D-1019 and D-1020 exist once.
- D-1021 is absent before patch and exists once after patch.
- D409 response exists and is consumed.
- D409 local sensitive runtime cleanup passes.
- D409 literal-dollar-HOME remote cleanup passes.
- No literal-dollar-HOME residue remains after cleanup.
- Hardened manifest resolves remote home as `/home/qslcodex`.
- Manifest rejects literal `$HOME`, `..`, and executable remote paths outside the resolved E2EE root or retained qsc path.
- Remote boundary recheck passes.
- Retained qsc owner/hash/help recheck passes.
- Local qsc source/Cargo diff since retained source commit is empty.
- Local qsc build/hash/size/help proof is recorded.
- Isolated local and remote runtime roots are used.
- Synthetic messages only are used.
- qsl-server and qsl-attachments are not used.
- Result classification is exactly `REMOTE_E2EE_TRANSPORT_FAILURE`.
- Cleanup deletes the retry remote E2EE root and local sensitive runtime root.
- Runtime output scan finds no private key blocks, synthetic passphrase output, API token-style output, or raw route token output outside manifest proof.

## Transport failure proof

Expected evidence for this run:

- Reverse-forward attempt fails before handshake/send/receive with remote port forwarding failure.
- Bounded recovery uses remote qsc relay under the E2EE root plus SSH local forwarding.
- Local forwarding fails with administrative prohibition before successful send/receive.
- No custom proxy, qsl-server, qsl-attachments, package install, remote source checkout/build, or public relay exposure is introduced.
- The successor targets transport remediation before any renewed E2EE attempt.

## Deferred positive / negative proof

Because transport failed, these are intentionally not claimed:

- Build-to-Inspiron send/receive exact message match.
- Inspiron-to-Build reply exact message match.
- Wrong-mailbox negative/no-mutation execution.

The successor must restore these as required validation once transport is authorized.

## Static validation

Run and record:

- `git diff --check`
- exact five-path scope guard.
- link-check.
- leak-scan.
- overclaim scan.
- classifier.
- PR body preflight.
- goal-lint.
- marker proof for NA-0516 evidence.
- private key block and token/password/passphrase material scans on checked-in evidence.
- proof that local sensitive runtime was deleted.
- proof that remote E2EE root was deleted.
- proof that D409 literal-dollar-HOME residue was deleted.

## Required local validation

Run and record:

- `cargo test -p qsc --locked --test same_host_client_to_client_e2e -- --test-threads=1 --nocapture`
- `cargo test -p qsc --locked --test receive_e2e -- --test-threads=1 --nocapture`
- `cargo test -p qsc --locked --test key_lifecycle_zeroization_expansion -- --test-threads=1 --nocapture`
- `cargo test -p qsc --locked --test secret_material_diagnostic_boundary -- --test-threads=1 --nocapture`
- `cargo test -p qsc --locked --test handshake_provider_error_no_mutation -- --test-threads=1 --nocapture`
- `python3 scripts/audit/validate_binding_fuzz_corpus_no_secrets.py --format json --path qsl/qsl-client/qsc/fuzz/corpus/qsc_binding_semantics`
- `python3 scripts/audit/validate_binding_fuzz_corpus_no_secrets.py --format json --path qsl/qsl-client/qsc/fuzz/corpus`
- `python3 formal/run_model_checks.py`
- `cargo audit --deny warnings`
- `cargo audit --deny warnings --file qsl/qsl-client/qsc/fuzz/Cargo.lock`
- `cargo fmt --check`
- `sh -n scripts/ci/qsc_adversarial.sh`
- `bash -n scripts/ci/qsc_adversarial.sh`

## Required markers

- `NA0516_REMOTE_HOME_RESOLVED_OK`
- `NA0516_NO_LITERAL_DOLLAR_HOME_PATHS_OK`
- `NA0516_REMOTE_PATH_CONTAINMENT_OK`
- `NA0516_QSL_SERVER_NOT_USED_OK`
- `NA0516_QSL_ATTACHMENTS_NOT_USED_OK`
- `NA0516_D409_REMOTE_BAD_ROOT_CLEANED_OK`
- `NA0516_D409_LITERAL_HOME_PARENT_ABSENT_OK`
- `NA0516_D409_RETAINED_QSC_UNTOUCHED_OK`
- `REMOTE_BOUNDARY_RECHECK_OK`
- `REMOTE_E2EE_ROOT_CLEANED_OK`
- `LOCAL_SENSITIVE_RUNTIME_CLEANED_OK`
- `NO_SECRET_OUTPUT_REVIEW_OK`
- `REMOTE_E2EE_TRANSPORT_FAILURE`

## Post-fix hardening review

1. Correctness under stress: the lane failed closed when SSH forwarding was unavailable and did not invent an unapproved proxy.
2. Minimality: repository changes are governance/testplan/traceability/journal only; no qsc runtime code changed.
3. Maintainability: the follow-up is a transport authorization problem, not hidden in runtime code or CI changes.
4. Coverage quality: validation distinguishes cleanup/path-hardening success from E2EE transport failure.
5. Cross-lane stability: qsc source/tests/workflows/dependencies are untouched, preserving macOS/Linux behavior for existing gates.
